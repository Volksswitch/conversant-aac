# AI-Driven AAC — Project Context for Claude

## Product Name
The product is named **Conversant AAC** (decided June 18 2026). The name captures the product's core mission: transforming communication into conversation. "Conversant" carries both meanings simultaneously — fluent in conversation, and *in* conversation. The project/working directory continues to be called "AI-driven AAC" for continuity; the product-facing name in the app and documents is Conversant AAC. The product is free and open source.

## About Ken (the user)
Ken Hackbarth is President and Founder of Volksswitch.org. He has a three-decade background as a systems engineer at AT&T Bell Laboratories. He is **not** a developer or AI/AAC researcher — he is a product manager and systems thinker. He defines requirements, makes feature decisions, and recruits beta testers. Claude Code is the developer.

Published author: "Revolutionizing Augmentative and Alternative Communication with Generative Artificial Intelligence," ATOB Volume 18, Spring 2024 (pp. 100–123). This paper is the foundational vision document for this project and is stored as `ATOB_V18_Hackbarth.pdf` in this folder.

---

## Working Guidelines for Claude Code

**Verify UI details against source before writing about them.** When producing any document (manual, overview, spec, etc.) that describes specific UI elements — button labels, icons, layout positions, control names — read the relevant source files (`index.html`, `ui.js`, `styles.css`, etc.) first. Do not infer or assume UI details from convention or common patterns. Invented UI details (e.g., assuming a gear icon exists for Settings when the button has no icon) are silent errors that reach users. If source files are not read first, the document must not assert specific UI details — hedge or omit them instead.

---

## Project Vision
Build an AI-driven AAC system that gives non-speaking individuals access to **real-time conversation** — currently impossible with existing AAC devices. The core problem is not words-per-minute; it is the human aversion to awkward silence (~4 seconds). AAC users are locked into near-real-time or batch communication, which marginalizes them and restricts them to transactional rather than interactional communication.

**Mechanism:** Generative AI presents response options within seconds of a communication partner's utterance. The user selects one; the device speaks it. The system speaks *as* the user, not *for* them.

---

## Target User (Initial)
Literate, non-speaking individual with cerebral palsy (CP), 16+, using **direct select** (touch/pointer) as the access method. Vision is to expand to wider range of ages, literacy levels, and accessibility profiles over time.

---

## Architecture Components (from the paper)
1. **Situational Awareness** — GPS, face/voice recognition, calendar
2. **NLP In** — Speech-to-text
3. **Statement/Response Generation** — LLM (the linchpin)
4. **Statement/Response Shaping/Tuning** — worldview model, relationship modeling, conversation history
5. **NLP Out** — Text-to-speech

---

## Build Decisions (confirmed)

**Platform:** Static web app served from GitHub Pages (or similar). User points browser at a URL. App runs entirely in-browser. No backend server.

**Target hardware:** The File System Access API (FSA) is not supported on iOS or Android, and Apple does not make a tablet-class Mac. Therefore the device must be a Windows tablet running Microsoft Edge or Google Chrome. The Microsoft Surface is the primary candidate.

**Local data storage:** File System Access API — the app is granted permission to read/write a local folder on the user's machine. All worldview data, conversation history, and profile data stays on-device. No data sent to any server except the LLM API.

**Cross-device transfer:** Later feature — user manually exports a data package and moves it via iCloud, Google Drive, or OneDrive.

**AI vendor:** Claude API (Anthropic) first. Multi-vendor support added later. User creates their own Anthropic account and supplies their own API key. The Claude API is pay-as-you-go, billed per token — there is **no ongoing free tier for API access** (the free tier on the consumer claude.ai app does not apply to the developer API). The user pays only for the conversations they actually have. API key stored locally.

> **Vendor choice is a USER decision — do not architect around Anthropic only. (Ken, June 14 2026.)** Anthropic is the *first* implementation, not the *only* one. The design must keep the LLM provider swappable: which vendor (Anthropic, OpenAI, Google, a local model, …) and which model tier is ultimately the user's choice. **Architectural requirement going forward:** isolate all provider-specific concerns behind a provider-agnostic interface (a generation call that takes conversation + profile/system text and returns `{classification, options, missing_facts}`), so adding a vendor is a new adapter, not a rewrite. *Current Anthropic-specific touchpoints to abstract when we add the second vendor* (all in `app/js/llm.js`): the `api.anthropic.com/v1/messages` endpoint, the `x-api-key` / `anthropic-version` / `anthropic-dangerous-direct-browser-access` headers, the request body shape (`system` + `messages` with `user`/`assistant` roles), the model id string, and the `usage.input_tokens`/`output_tokens` response fields. Settings currently labels the field "Claude API Key" and the key validation assumes `sk-ant-…`; both become vendor-dependent. Pricing is per-vendor/per-model (`app/data/pricing.json` is currently single-rate). Keep this list current as the abstraction is built.

> **Future setting — quality-driven model tier (upgrade/downgrade). (Ken, June 14 2026.)** Add a user-facing control to move the model up or down a tier (e.g., within Anthropic: Haiku ↔ Sonnet ↔ Opus) based on the user's *perception of response-option quality* vs. cost. Today the model is hardcoded to `claude-sonnet-4-6` (chosen for the user-pays-per-token cost balance). This setting makes the intelligence ↔ cost tradeoff the user's call, surfaced in plain terms ("responses not good enough? try a stronger model — it costs more per conversation"). Pairs naturally with the multi-vendor abstraction above (tier is a per-vendor concept). Not built yet.

**Speech-to-text transcript validation:** Displaying the transcript of what the partner said is **critical** — not optional. The user must be able to confirm the system heard correctly before selecting a response.

**End-of-utterance detection (continuous partner capture):** A silence period (the "Optional Responses silence period" setting) is a *checkpoint, not a stop*. Each time the partner pauses for that period, the speech collected so far is sent to the AI for response options and a placeholder is spoken; recording continues. Resumed speech is appended and the combined utterance is re-sent after the next pause, so each checkpoint yields a more complete option set. Recording stops only when the user selects a response, at which point the transcript is cleaned once and the exchange is stored. A "latest-wins" generation token discards superseded in-flight requests, and a persistent "Please repeat what you said." control discards a garbled capture and keeps listening. An **auto-resume-listening** setting (Settings → General; default **off**) turns partner recording back on automatically after a response is spoken, so the user need not tap Start Listening between exchanges. (Implemented June 2026; see `Continuous-Partner-Capture.docx` and Architecture Overview §9. **Refined in v0.2.32 (see Further Design Thoughts #4):** Start Listening never fires at startup, must be invoked manually at least once per session, and auto-resume is gated behind that first manual invocation (`manualListenArmed`), re-armed only after another manual Start once listening is manually turned off. This is the Phase 1 realization of the COMPLETE/INCOMPLETE/CONTINUING classifier and filler ladder above.)

**Voice/TTS:** Browser built-in TTS acceptable for MVP. Voice banking (personalized/cloned voice) is a valuable future feature.

**Communication partner:** Needs nothing. Entirely unaware of system internals. AR glasses are a future hardware form factor.

---

## Response Option Display

Response options shown to the user may appear in **compressed form** — a few words representing a fuller thought — to speed up reading. The system may also read the compressed hint aloud. The **trigger for the auditory hint is a user preference**: options include auto-playback as each option is highlighted in sequence, or on first tap (before a double-tap confirms selection). Additional triggers may be supported based on user feedback.

The user normally hears the **fully expanded response only when it is spoken** after selection. Whether the expanded text is displayed or read before confirmation may become a configurable option based on user feedback.

Position 1 in the options list is the AI's best guess. There is no special visual treatment to distinguish it — the design goal is simply that the first option is the best option, with that accuracy improving over time.

---

## Worldview Model
- Minimum viable version: a carefully selected "getting to know you" questionnaire (~100 questions)
- Questions are completed entirely at the user's own pace; none are required
- The system provides meaningful functionality even with zero worldview questions answered
- Info gathering must be **chunked** — physically draining for CP users
- Many real-time exchanges need zero worldview context ("Hi, how are you?" needs no personalization)
- RAG will reduce token overhead as the profile grows over time
- Designing the specific questionnaire content is part of the work ahead

---

## Rollout Sequence

**Phase 1 — Core Conversation Loop (Proof of Concept)**
STT → display transcript → placeholder utterance auto-spoken → LLM generates response options → user selects → TTS speaks full response. No worldview shaping, no situational awareness.

Key Phase 1 features:
- **3 response options** by default (configurable)
- **Conversation context:** rolling window of the current session only
- **Placeholder/filler utterances** (e.g., "Just a second…", "Let me think about that") automatically generated and spoken while the user is selecting; customization supported
- **Conversation initiation:** via a set of canned conversation starters (pre-configured phrases the user can select to open a conversation); later refined by situational awareness in Phase 2
- **Compressed display / expanded speech** for response options (see above)
- **Identity / personalization — now the worldview profile (was the interim name + "About You" seed):** The June-2026 stopgap (Settings → General "Your Name" / "About You" fields injected into the prompt) has been **removed in worldview Build Step 4**. Personalization now comes entirely from the worldview questionnaire ("About Me"), injected via `worldview.buildBlock()`. The no-bracket safety rule still always applies (the prompt forbids `[Name]`/`[city]` and tells the model to phrase around any unknown detail), so the app degrades gracefully with an empty profile. See the **Worldview Questionnaire (June 2026)** section.
- **Offline review/feedback mode:** deferred — too complex even in rudimentary form
- **Response option UI layout:** requires a design discussion on how much of the screen is allocated to traditional AAC vocabulary vs. AI-facilitated response options; ultimately user-configurable

**Phase 2 — Situational Awareness**
Add location context (GPS), communication partner recognition (face/voice), and calendar integration. Canned conversation starters become contextually suggested based on location and partner identity.

**Phase 3 — Worldview Shaping and Tuning**
Integrate the worldview model. Responses shaped to reflect the individual user's personality and goals. RAG employed to manage profile size. Offline feedback/review mode introduced here.

---

## Advanced Future Capability: Streaming Response Generation
The long-term vision is for the system to begin generating and refining response options *while the communication partner is still speaking* — using partial STT transcripts as they arrive. Options would be continuously updated and narrowed as the utterance becomes clearer. This eliminates virtually all post-utterance latency. This capability requires significant design thought and is not targeted for Phase 1 or 2.

---

## Strategic Context
Prior attempts at AI-driven AAC have been shelved when funding ran out. Ken's architecture avoids this by:
- **No server infrastructure** the project must pay for
- **User-funded AI** — users bring their own API key, pay only for what they use

Ken has screenshots of and has presented live demos of a prior team's shelved product. The goal is to share this architecture with those developers as a more sustainable path forward.

**First deliverable:** A high-level architecture document suitable for sharing with those prior developers — not code.

---

## Conversation Analysis Design Layer (June 2026)

Ken collected Conversation Analysis (CA) literature and had it synthesized into `CA-Concepts-for-AAC-Architecture.docx` (on Ken's Desktop). Three design documents in this folder map those concepts to the system. **All project decisions are recorded here in CLAUDE.md** (Ken's preference — this folder syncs across machines; Claude's machine-local memory does not).

**Document convention (Ken, June 14 2026): all project documents use American English spelling** (e.g. "color", "center", "personalized", "emphasized" — not "colour", "centre", "personalised", "emphasised"). Applies to every generated document.

**Document convention (Ken, June 15 2026): every generated document's footer includes a "Page m of n" page number** (current page of total — e.g. "Page 3 of 12"), in addition to the standard "Volksswitch.org | June 2026 | For internal use" line. In docx-js use `PageNumber.CURRENT` and `PageNumber.TOTAL_PAGES`. Applies to every generated document going forward.

**Documents** (all Word, styled like Ken's papers — Arial, "Volksswitch.org | June 2026 | For internal use" footer; figures `ui-fig1..7.png` alongside, generated with Python/Pillow, regenerable on request):
1. `Conversation-Engine-Design.docx` — CA concepts → programmatic flow
2. `UI-Design.docx` — presentation layer and dynamic UI
3. `Configuration-Model.docx` — user-owned settings registry and experimentation loop

### Settled decisions — Conversation Engine

- **Sequence stack, not turn alternation**: conversation state is a stack of open sequences (adjacency pairs); repair sequences nest on top; the original obligation is restored automatically when they resolve.
- **Four-slot move palette**: PREFERRED / DISPREFERRED (hedge + account) / INITIATIVE / REPAIR — structurally distinct conversational moves, positionally stable for motor automaticity. (Refines the earlier "3 response options" default: the palette is typed by move, not just counted; option count remains configurable.)
- **Five modes**: LISTENING, RESPONDING, REPAIR-OF-SELF, INITIATING, PRE-CLOSING/CLOSING.
- **Access method independent of conversation logic**: engine emits a typed, prioritized palette descriptor; renderers (direct select now; scanning/eye gaze later) present it. Consistent with the direct-select initial target while keeping the expansion path clean.
- **System infers mode, user overrides** via persistent buttons (default set: Say again / Hold on / Pardon? / Wind down).
- **Single combined LLM call** classifies the partner's action (FPP type) AND generates options; classification is emitted first in structured output so it is inspectable. LLM stateless; app injects state.
- **Filler ladder** (refines the placeholder mechanism and the old 4 s initial delay): rung 1 acknowledgment token ≤1 s after end-of-utterance (no LLM — this is what makes transcript validation affordable), rung 2 projection filler ~2.5 s covering the confirm window, rung 3 periodic re-fill. CA: silence becomes meaningful past ~1 s.
- **Backchannels/continuers deferred**; end-of-utterance classification schema reserves COMPLETE | INCOMPLETE | CONTINUING so they slot in later without rework.
- **Highest-leverage near-term item**: REPAIR-OF-SELF mode — partner says "What?" → re-speak / rephrase / expand the user's last utterance.
- **Mode vs. floor are distinct dimensions; LISTENING is the system's *receptive posture*, not "the partner owns the floor" (refinement, Ken, June 15 2026, v0.3.2).** Ken's catch: the diagnostic panel showed `Mode: LISTENING` before any capture, and "partner has the floor" is wrong for the at-rest case. Two things were conflated. **`mode`** = what the *system* is doing (LISTENING / RESPONDING / REPAIR-OF-SELF / INITIATING / PRE-CLOSING-CLOSING). **`floor`** = whose turn it is — **open** (at rest / between turns, up for grabs), **partner** (partner producing or owed the next turn), **self** (user's turn to act). LISTENING spans *both* floor=partner (partner mid-utterance) and floor=open (startup, or the beat after a response) — so floor cannot be read off the mode, and the design doc's gloss of LISTENING as *"partner is speaking"* only covers the partner-mid-utterance case. Also: because the persistent overrides (Start conversation / Say again / Hold on / Pardon? / Wind down / "Please repeat" / the composer) are available in **every** mode, the user can seize the floor at any time — floor=partner is a *default expectation*, not an exclusive lock. **Built:** `floor` is a first-class field on the engine `ConversationState` + snapshot (`engine.FLOOR = {OPEN, PARTNER, SELF}`), set at each transition (partner capture → partner; INCOMPLETE → partner; COMPLETE → self; repair-initiator → self; selectMove/completeRepairOfSelf → open; pardon → partner; windDown/initiate → self), and shown as its own **Floor** row in the degenerate panel (open=grey, partner=orange, self=blue). The earlier **Capture (mic)** row (v0.3.1) stays — it's an I/O state, also distinct from both mode and floor (a silent-but-listening mic is still floor=open). **Mode shows "STANDBY" when genuinely at rest (v0.3.3, Ken).** Ken's follow-up: "LISTENING" reads wrong when the mic is off. Rejected relabeling Mode off the mic directly (re-couples the dimensions we just split) and the word "Holding" (collides with *floor-holding* — the filler ladder + the "Hold on" button). Instead the renderer shows **STANDBY** in the Mode cell only when the engine is *truly idle* — `capture off AND floor open AND empty stack AND no palette` — otherwise the engine's mode verbatim. **Presentation-only:** the engine's internal mode stays `MODE.LISTENING` (no sixth mode, engine untouched). Because the label now depends on both the snapshot *and* the mic state, `ui.js` keeps the last snapshot + a `capturing` flag and re-renders the Mode cell from `renderModeCell()` whenever either changes (so it flips STANDBY↔LISTENING the instant the mic toggles, not only on the next snapshot). *(Open question deferred: floor transitions are best-effort heuristics in the engine; whether the renderer should ever override them, and how lapses are handled, is unspecified.)*
- **No transcript-confirmation gate — generation fires on the silence period, not on a confirm tap (DECIDED Ken, June 15 2026).** The Conversation-Engine-Design.docx §6 line *"the generation window starts only after the user confirms (or corrects) the transcript"* — the CA-purist intersubjectivity gate — is **relaxed to a configurable default, off.** Generation starts after `n` seconds of partner silence (the existing "Optional Responses silence period" setting), with **no** explicit transcript-confirm step, to shorten the user's time-to-respond. This is not a new departure: (a) the design doc already lists **"confirmation gates"** among the *defaults of the standard profile, not fixed rules* — it is **not** an invariant (the invariants are repair path / floor-holding / closing / free composition / visibility of system speech / revertibility); and (b) the shipped **Continuous Partner Capture** already generates on each silence checkpoint with no confirm tap. **What we keep (these are what make "no gate" safe):** (1) the **transcript still displays** (CLAUDE.md "critical, not optional") — the user *sees* what was heard, just isn't required to tap-confirm; (2) the **REPAIR / "Pardon?" slot + persistent "Please repeat what you said" control** is the recovery for STT errors — the old gate caught errors *before* generation, the relaxed model catches them *after* (cheaper in time; repair is an invariant); (3) the auto-confirm-above-confidence-threshold knob was already the escape hatch — this effectively sets it to "always." The three-state transcript display may survive as **informational** (heard / generating / ready) but no longer **blocks** generation. **Setting:** "Require transcript confirmation before generating" — Profile-tier, default **off**, for the rare CA-purist user. **Docs to update (deferred):** `Conversation-Engine-Design.docx` §6 and `UI-Design.docx` transcript-gate section must stop asserting the gate as mandatory.

### Settled decisions — UI

- Four screen regions: transcript (with mode chip), move palette, persistent controls, composer access. Geometry never reflows on mode change — "the cards changed," never "the screen changed."
- Move card anatomy: glanceable hint (primary reading target; names the action, not a truncation), full TTS text, slot badge, format tag, latency dot (filled = instant, hollow = generation round-trip).
- **Triple coding**: slot identity = position + color + text badge; never color alone. Defaults: green preferred, amber dispreferred, blue initiative, purple repair.
- Transcript is a three-state *display*: amber UNCONFIRMED → blue CONFIRMED·GENERATING → green OPTIONS READY. **It no longer gates generation by default** — per the Conversation-Engine decision above (Ken, June 15 2026), generation fires on the silence period, not on a confirm tap; the states are informational. The blocking gate (never generate from an unconfirmed transcript) survives only as a Profile-tier setting, default off.
- **Nothing is spoken on the user's behalf invisibly** — every filler/token is displayed as it plays.
- **Palette updates queue until a selection boundary**: options never change under a user mid-selection (mandatory for future scanning/dwell renderers).
- Engine→UI contract is a versioned palette-descriptor JSON with `priority` and `droppable` per move; renderers drop/page by priority, never by move type.

### Configurability philosophy (overarching)

- **As many decisions as possible are user-determined and configurable.** This population is made up of unique individuals; the user experiments with a flow and adjusts from experience. Behavioral statements in the design docs are *defaults of the standard profile*, not rules.
- **Invariants are guaranteed capabilities, not fixed behaviors**: a repair path, a floor-holding path, a closing path, free composition, visibility of system speech, and revertibility must always exist; how they manifest is configurable. Config validation enforces "at least one path" rules at save time, with explanation — never silent correction.
- Three settings tiers by *when* a change is safe — Quick (mid-conversation) / Profile (between conversations) / Setup (rare, may be supporter-assisted) — all user-owned; nothing locked away from the user.
- Experimentation loop: named profiles (bindable to Practice Mode scenarios), automatic change journal, session review metrics (descriptive, local, private — inform the user, never score them), guided trials, one-tap revert. Revertibility is the one absolute.
- **Confirm before destroying significant work (standing rule, Ken, June 15 2026).** *Every* action that can wipe away significant user work — clearing/restarting answers, deleting people or relationships, bulk resets, overwriting a saved profile — MUST include an explicit validation step before it runs. This is a build checklist item, not an afterthought: when adding any clear/reset/delete/overwrite control, wire its confirmation in the same change. (Trivial, easily-redone actions — a single-field edit/skip with Undo, resetting the usage-token counter — don't require it; the bar is "significant work.")
  - **The confirmation must NOT be the native `window.confirm()` (Ken, June 15 2026, v0.2.30).** Native confirm renders a generic dialog indistinguishable from routine browser/OS prompts — notably the File System Access folder-permission popup — so the user dismisses it on autopilot and a destructive action doesn't read as dangerous. Use the **`confirmDanger()`** modal in [confirm-dialog.js](app/js/confirm-dialog.js) instead: a deliberately distinct red danger card (⚠ mark, red title + red confirm button, `.danger-dialog` CSS), Cancel focused by default (a stray Enter cancels), `showModal()` top-layer so it sits above the full-screen About Me overlay. Returns `Promise<boolean>`; use `if (!(await confirmDanger({title, body, confirmLabel, cancelLabel}))) return;`.
  - *Audit June 15 2026:* About Me **Restart** (`onRestart`) and **Remove person** route through `confirmDanger`; the only un-confirmed destructive action is the Settings usage-counter reset, which is below the bar.

---

## Conversation Engine — Phase 1 build behind a degenerate UI (June 15 2026), v0.3.0

The Conversation Engine from `Conversation-Engine-Design.docx` is **built and verified in preview** — the **full Phase-1 mode set** (Ken's call), rendered through a deliberately throwaway **degenerate diagnostic UI** (Ken: "hold off on the full UI; show engine state + simplistic options"). The real four-region triple-coded Presentation Layer (`UI-Design.docx`) is **not** built yet and is a separate later job.

**New module `app/js/engine.js` — the CA core, no DOM (design §2 strict one-way knowledge).** Holds `ConversationState` (§3): `sequenceStack` (what is "owed", innermost last), `register`, `phase`, `lastUserUtterance`, `lastPartnerUtterance`, `mode`, `lastClassification`, `palette`. Exposes: `reset`, `getSnapshot` (everything the renderer shows), `buildRequestContext` (§9.1 block for the LLM), `partnerSpeaking`, `ingestClassification(result, text)` (the heart — updates mode/stack/palette), `selectMove`, `completeRepairOfSelf`, and the four overrides `pardon`/`windDown`/`initiate`(+`MODE` enum). Five modes (§5): LISTENING, RESPONDING, REPAIR_OF_SELF, INITIATING, PRE_CLOSING_CLOSING. Four-slot palette (§4) PREFERRED/DISPREFERRED/INITIATIVE/REPAIR with stable ordinal priority; plus repair-of-self ops (respeak/rephrase/expand) and static OPENER/CLOSER palettes (inline now, user-owned later per the Configuration Model). **Stack ops verified:** partner FPP pushes; user SPP pops the innermost partner sequence; `pardon()` pushes a USER `REPAIR` sequence and the partner's next COMPLETE re-speak pops it and re-activates the FPP beneath (§3/§7.1); INCOMPLETE/CONTINUING → stay LISTENING, no palette (the false-TRP guard, §8); `is_repair_initiator` → REPAIR_OF_SELF on `lastUserUtterance` (§7.2, the highest-leverage item).

**`app/js/llm.js` — extended to the §9 contract.** `generateResponses(history, context)` now requests the full structured object — `partner_action`, `turn_status` (COMPLETE|INCOMPLETE|**CONTINUING reserved**, treated as INCOMPLETE for now §8), `is_repair_initiator`, and typed `moves[]` (the four slots, with the §9.3 obligations: DISPREFERRED = preface+declination+account never a bare "No"; INITIATIVE varies grammatical format; REPAIR open-class vs restricted by confidence) — classification emitted **first**. Injects the engine's request context. Added `repairSelf(lastUserUtterance, op, history)` for the rephrase/expand round-trips (respeak needs no LLM). `parseGeneration` returns `{classification, moves, missingFacts}` and still tolerates a legacy bare array / `{options}` by mapping onto the slots. `max_tokens` 500→700. **Model unchanged (`claude-sonnet-4-6`).**

**Filler ladder — `app/js/placeholders.js` rebuilt to the §6 three rungs** (same `start()`/`stop()` signature): rung 1 acknowledgment token ~0.8 s (inline `ACK_TOKENS`, no LLM/fetch — fires inside ~1 s, replacing the old 4 s initial delay), rung 2 projection filler ~2.5 s (from `placeholders.json`), rung 3 periodic re-fill every `subsequentDelay`, never the same phrase twice consecutively. *(The Settings "Initial Placeholder Statement Delay" control is now effectively unused by the ladder — rung 1/2 timings are fixed constants; left in the UI for now, a candidate to retire.)*

**Degenerate UI** (`index.html` + `ui.js` additions, `styles.css`): an **engine-state panel** (monospace) showing Mode / Phase / Partner action / Turn status / Repair initiator / Sequence stack / Last user said; **moves as plain `[SLOT] text` buttons** (`ui.showMoves`) — no cards, color coding, latency dots, or compressed-hint styling; and the **four persistent override buttons** Say again / Hold on / Pardon? / Wind down, plus a Start-conversation (initiate) button. The transcript still displays but **does not gate generation** (per the no-confirmation-gate decision above — generation fires on the silence period). `app.js` wires silence-period → `engine.partnerSpeaking` → `generateOptions` (→ `engine.ingestClassification` → render) and selection → `handleMoveSelected`/`handleRepairOfSelf` → `commitExchange`. The old single-list `showResponseOptions`/`handleResponseSelected` are gone.

CACHE_VERSION/APP_VERSION → **`0.3.0`** (first minor bump past 0.2.x — the engine is a substantial new layer); `engine.js` added to the sw precache. **Verified in preview** (no API key needed — fetch stubbed for the LLM path): app boots clean, no console errors; INITIATING shows openers, Wind down shows closers, selecting an opener sets `lastUserUtterance` + returns to LISTENING; the stubbed RESPONDING round-trip parses classification + four priority-ordered slots and pushes the INVITATION FPP; INCOMPLETE stays LISTENING with no palette; repair-initiator → REPAIR_OF_SELF (respeak instant / rephrase+expand round-trip); SPP pops the FPP; the Pardon→re-speak cycle pushes then resolves the repair sequence. *Not yet exercised against the live Claude API or real STT/TTS — needs Ken's key + mic on the tablet (the silence-period → engine path and the filler ladder timing specifically).*

### Test-suite Pass 1 — filler timing + TTS-echo feedback loop (Ken, June 16 2026), v0.3.4

Two defects Ken found running the live conversation-engine test suite, with **Ken's chosen approach** for the second:

1. **Placeholders started/repeated too soon — now driven by the Settings values.** The §6 filler ladder used fixed constants (rung 1 at 0.8 s, rung 2 at +1.7 s) and ignored the user's two Settings: "Initial Placeholder Statement Delay" (`initialDelay`, default 4 s) and "Subsequent Placeholder Statement Delay" (`subsequentDelay`, default 10 s) — exactly the controls whose hint says they govern this. Rewrote `placeholders.js`:
   - **First placeholder lands `initialDelay` seconds after the partner *finishes* speaking** (matching the Settings hint). Because the silence checkpoint that triggers the ladder already consumed `silencePeriod` (`loadSilenceThreshold()`) seconds of that wait, rung 1 waits the remainder: `max(MIN_RUNG1_DELAY=500ms, (initialDelay − silencePeriod) × 1000)`. Defaults → 2 s after the checkpoint = **4 s after the partner stops**.
   - **Subsequent placeholders re-fill every `subsequentDelay` seconds** (rung 1 → next filler is now one `subsequentDelay`, not 1.7 s; the old `rung2`/`rung3` collapsed into one recurring `refill()`/`scheduleRefill()`). Rung 1 is still the inline ACK token (no fetch); rung 2+ are the projection fillers from `placeholders.json`, never the same twice in a row.

2. **App's own TTS was captured by the mic as partner speech (the feedback loop).** While the filler ladder spoke, the mic stayed live (`listeningIntent` true), so SpeechRecognition transcribed the placeholder audio, appended it to `accumulatedText`, reset the silence timer, and **re-fired `handleSilencePeriod`** — which restarted the ladder at rung 1 ("begin/repeat too soon") and bumped `generationToken`, **delaying and even abandoning the already-generated responses** in favor of a regeneration from `partner statement + filler comments`. **Ken's approach (chosen over muting the mic): the app knows exactly what it spoke, so discard only that content — the mic stays on so a partner talking over the filler is still heard; only *unique* content renews the partner's turn.** Implemented as content-based echo filtering, not mic gating:
   - **`tts.js`** broadcasts speaking-state with the spoken text: `onSpeakingChange(cb)` fires `cb(true, text)` on each `speak()` start and `cb(false, null)` on end; a monotonic `speakToken` stops a superseded utterance's late `onend` from reporting a spurious end. `cancel()` also reports the end. Added `isSpeaking()`.
   - **`app.js`** wires it: `tts.onSpeakingChange((speaking, text) => speaking ? stt.noteSpokenStart(text) : stt.noteSpokenEnd())`.
   - **`stt.js`** keeps a list of `activePhrases` the app is currently speaking (each stays matchable for `ECHO_TAIL_MS = 1500 ms` past end-of-speech, to cover recognition lag/trailing audio). `onresult` runs each captured segment through `isEcho()` and **drops matches** (doesn't accumulate, and — crucially — only calls `resetSilenceTimer()` when a *non-echo* segment was heard, so pure echo never renews the turn or re-fires generation). `isEcho` matching (normalized lowercase, punctuation→spaces): **exact** (segment is the phrase), **prefix** (segment is a leading slice — the recognizer's interim builds up as a growing prefix), and **embed** (phrase appears inside the segment — recognizer padding/merging) but **embed is restricted to multi-word phrases** so a short common ACK token ("Okay.", "Right.") can't swallow real partner speech that merely contains that word (e.g. "okay so what do you think"). Empty-after-normalize segments (pure punctuation/noise) count as echo.
   - **Accepted tradeoff:** if the recognizer merges a multi-word filler verbatim with the partner's resumed words into one segment, the whole segment is dropped (the filler is far more likely to be our echo the recognizer extended than the partner reciting our 4-word filler). In practice the filler and the partner's resumed speech are separated by a pause and arrive as different segments.

CACHE_VERSION/APP_VERSION → **`0.3.4`**. **Verified in preview** (no mic/speaker — the acoustic loop itself needs the tablet): app boots clean, no console errors; `tts.onSpeakingChange` broadcasts the spoken text; the `isEcho` matching table passes for all echo cases (exact/prefix/padded filler, ACK token, noise) **and** all genuine-partner cases pass through (including "okay so what do you think" and "right after lunch we left"). *Still to confirm on the test tablet with the mic on: that real placeholder TTS is no longer transcribed back into the partner turn, and that the first placeholder now lands at the user's `initialDelay`.*

**Acknowledgment-token cleanup (Ken, June 16 2026), v0.3.5.** Ken flagged that "Okay." is a poor rung-1 token — it's a common *real* response to a greeting, so a partner hears it as the user's actual reply, not a stall (and being a short common word it also makes TTS-echo filtering fragile). Removed the response-like tokens from `ACK_TOKENS` ("Okay.", "Right.", "I see."); kept only clearly-stalling ones ("Hmm.", "Ah.", "Good question.", "Let me think.", "Oh.", "Mm.", "Well…"). **Design principle for fillers going forward: a placeholder must read as floor-holding "I'm thinking," never as a substantive answer.** The filler set is user-owned/configurable later (Configuration Model); this is just a better default.

### Conversation Engine observations Pass 1 (Ken, June 18 2026), v0.3.9

Five observations from live engine testing; four built this pass (one of the five — the multi-party note — was already future-scoped). Decisions via AskUserQuestion are recorded inline.

1. **Placeholders now cover the *choosing* window, not the AI-latency gap, and only fire for questions (obs 1 + 3, DECIDED: "only while choosing, questions only").** Ken's obs 1 ("start the placeholder timer with the arrival of suggested responses") and obs 3 ("placeholders are inappropriate for STATEMENT, and during pre-closing/closing — they fit QUESTION") are the same problem: the old flow started the filler ladder at the partner-silence checkpoint, *before* the classification came back, so it couldn't gate on action/phase and it fired during pure AI latency. **New model:** `app.js` `handleSilencePeriod` no longer calls `placeholders.start()`; the ladder is started inside `generateOptions` **after** `ingestClassification`, gated by `shouldPlayFiller(snap)` — fires only when `turn_status === COMPLETE`, not a repair-initiator, mode ≠ PRE_CLOSING_CLOSING, phase ∉ {PRE_CLOSING, CLOSING}, and `partner_action ∈ {QUESTION, INVITATION, REQUEST}` (`FILLER_WORTHY_ACTIONS`). `placeholders.js` rewritten to a **single neutral pool** (no rungs): first phrase lands `initialDelay` seconds **after the options appear** (so a quick pick cancels it via `stop()` → **no placeholder at all when not needed**), then re-fills every `subsequentDelay`. The old rung-1 ACK-token concept and the `initialDelay − silencePeriod` math are gone. **Accepted tradeoff (Ken's call):** no floor-holding during the AI-latency wait before options arrive — a brief natural pause before answering a question, deemed fine; fillers earn their keep on the long *choosing* interval.
2. **Filler phrases must be neutral/reflective, never imperative (obs 2a, + Ken's correction).** My first replacement set included "Let me think." / "Give me a second." — Ken corrected: with the flat inflection of the built-in voices, anything **imperative or directed at the partner** ("Let me think", "Give me a second", "Hold on") reads as **curt/annoyed**. New default pool (`placeholders.json`, fallback mirrored inline in `placeholders.js`): **"Good question." / "That's a good question." / "Hmm, interesting." / "That's interesting." / "Oh, interesting." / "I'm thinking about that."** — all non-imperative, multi-word/intelligible, and question-appropriate (safe since we only fire on questions). Also softened the **"Hold on" override button** text from `"Hold on, let me think."` → `"Hmm, let me think about that."`. **Principle:** fillers must read as warm engaged interest, not impatience. *(Ken, June 18 2026: these placeholder statements will eventually need to be **user-editable** — the JSON pool is the seam for that.)*
3. **Response options no longer lead with empty interjections (obs 2b, DECIDED: "strip empty interjections, keep real softeners").** The generation prompt (`llm.js`) literally told the DISPREFERRED slot to open with "Well…"/"Ah…". Changed: DISPREFERRED now opens with a **meaningful** softener that carries content ("I'd love to, but…", "I wish I could —"), not a bare interjection; added a global rule — **no move may begin with** "Ah/Oh/Um/Er/Well/So/Hmm/You know" — "get to the point." User-preferred verbosity will come from the worldview questionnaire later.
4. **TTS-echo feedback loop fixed at the trigger, not just by content match (obs 4, the Pass-6 bug).** Symptom: a placeholder ("I'll have to think about that…") showed up in the *partner* box, pushed away the displayed options, pushed a QUESTION onto the stack though nobody spoke, and options regenerated repeatedly — i.e. repeated AI round-trips. Root cause: the v0.3.4 **content** echo filter (`stt.isEcho`) drops captured segments that *match* our playback, but the recognizer mis-hears its own audio (drops/adds a word), the echo slips past, **renews the silence timer and re-fires generation** → runaway loop. Content matching can't be made robust to mis-transcription, so we added a **trigger-level guard**: `stt.js` tracks `appSpeaking` (set in `noteSpokenStart`/`noteSpokenEnd`, with the existing `ECHO_TAIL_MS` tail → `speechActive()`); while the app is speaking (or within the tail), captured audio does **not** reset the silence timer (`onresult`: `if (heardPartner && !speechActive()) resetSilenceTimer()`) and the checkpoint does **not** fire (`fireSilenceCheckpoint` re-checks every 200 ms until quiet). The mic stays on and non-echo content still accumulates (Ken's v0.3.4 "don't mute the mic" decision preserved) — our own playback just can't drive a generation checkpoint. **Residual (noted):** a mis-transcribed echo can still leave a brief cosmetic flash in the transcript and pollute the *one* next checkpoint's text, but the runaway *loop* (stack growth, option flicker, repeated round-trips) is gone. The eventual robust solution remains Phase-2 partner voice recognition.
5. **Hard-terminate control (obs 5): "End conversation" button.** New persistent override (`index.html` `#endConversationBtn`, `ui.onEndConversationClick`, `app.handleEndConversation`): stops the filler ladder, cancels TTS, stops listening (clears `manualListenArmed`), invalidates in-flight generation (`generationToken++`), drops the uncommitted partner turn, clears palette/transcript, and `engine.reset()`s to STANDBY (empty stack, floor OPEN). **No danger-confirm** (Ken's recommendation accepted) — it's the "hang up" control and committed exchanges are already logged; only the current unselected turn is discarded.

CACHE_VERSION/APP_VERSION → **`0.3.9`**. **Verified in preview** (no API key / mic — the live filler-gating, echo loop, and prompt change need Ken's key + the tablet mic): app boots clean, no console errors; version 0.3.9 in About; the End-conversation button is wired and resets status to standby with no throw; `placeholders.json` carries the neutral set with no single-syllable tokens. *Still to confirm on the test tablet:* that placeholders now fire only on questions and only while choosing, that the neutral phrasing reads better, and that real placeholder TTS no longer re-fires generation.

**Role-differentiated placeholders + per-turn cap (Ken, June 18 2026), v0.3.10 — fixes sequential-filler repetition.** Ken's follow-up on the v0.3.9 neutral set: it was semantically clustered ("Hmm, interesting." / "That's interesting." / "Oh, interesting."), so two fillers in one choosing window sound stupid even without an exact repeat — and a bigger flat pool only papers over it. **Chosen fix (my recommendation C + D, with B folded in): role-differentiated pools by position + a cap.** `placeholders.json` is now a `{ acknowledgment:[], thinking:[] }` object. The **first** filler in a window is drawn from `acknowledgment` ("Good question.", "Let me see.") — *"I heard you, I'm on it"*; **every later** filler from `thinking` ("Still thinking it through.", "Putting my thoughts together.") — *"still working on it"*. So a long window progresses naturally instead of echoing. A new Settings → Conversation control **"Maximum placeholders per turn"** (`storage` `maxPlaceholders`, default **2**, 0 = no limit) caps it: at most one acknowledgment + one thinking filler, **never two same-category fillers back to back**; past the cap the system stays quiet (silence after "Good question… still thinking it through" reads fine, and the manual "Hold on" button remains). `placeholders.js` rewritten to role pools (`pickFrom(role)` avoids the immediate repeat *within* a role; `normalizePools` tolerates a legacy flat array by using it for both roles). Pools stay **user-editable later** (the two labeled lists are the seam). Updated the Settings hint (the initial delay is now relative to *options appearing*, not the partner finishing). **Higher-quality next tier (recorded, not built): LLM-generated *contextual* second filler** — fire a tiny async call while the first static filler plays, for a topic-aware "still thinking" line — inherently non-repetitive; matches the long-recorded "contextual fillers" open item, with its token-cost caveat. CACHE_VERSION/APP_VERSION → **`0.3.10`**. **Verified in preview:** boots clean, no console errors; v0.3.10 in About; `placeholders.json` loads as `{acknowledgment:5, thinking:5}`; the "Maximum placeholders per turn" control is present, default 2. *Live sequencing (role progression + cap audible) needs the tablet mic/speaker.*

## Presentation Layer — four-region triple-coded UI (June 21 2026), v0.4.0

The real **Presentation Layer** from `UI-Design.docx` is **built and verified in preview**, replacing the throwaway degenerate diagnostic renderer (the engine itself is untouched — this is purely the renderer side of the engine→UI boundary, design §9). **Minor version bump (0.3.x → 0.4.0):** the full UI is the defining capability layer the engine was scaffolding toward (the degenerate UI was always "a separate later job"), so it crosses the minor bar like the engine did at 0.3.0.

**The four regions (`index.html` `<main>`, design §3), geometry fixed — mode changes swap card CONTENTS, never the layout:**
- **Region A — Transcript:** title + **mode chip** (`#modeChip`, the engine's inferred mode in friendly words, colored by the mode's dominant slot — informational, not a button; overriding lives in Region C). Three-state transcript **display** (§6) UNCONFIRMED amber → GENERATING blue → OPTIONS READY green, shown as a colored left accent bar + an uppercase state label — **informational only, does NOT gate generation** (generation fires on the silence period per the standing decision; the states reassure, they don't block). A **now-playing** line (`♪ <text>`) shows whatever the app is currently speaking on the user's behalf (fillers, the spoken response, prompts) so nothing is spoken invisibly (§7). Listen control.
- **Region B — Move palette:** the engine palette as **triple-coded cards** (§4) in a responsive grid (`repeat(auto-fit, minmax(15rem,1fr))` — 4 columns at Surface-landscape width = the spec's 2×2 ideal, 1 column when narrow; column count depends only on viewport, constant within a session, so geometry never reflows on mode change). Each card: **slot badge** (text) + **slot color** (hued left border, badge, latency dot) + **position** = the three redundant codes (never color alone); the **hint** is the large glanceable primary target (1.4rem ≈ 2× body, clamped to 2 lines); the **full utterance** smaller beneath; an optional **format tag**; and a **latency dot** (filled = instant, hollow = a generation round-trip on selection, e.g. repair-of-self rephrase/expand). A ≤200 ms content crossfade on each render (`palette-enter`, disabled under `prefers-reduced-motion`). A **"↻ Show me different options"** regenerate control sits in the palette footer. `SLOT_META` in `ui.js` maps every slot the engine emits (the four RESPONDING slots + REPAIR_RESPEAK/REPHRASE/EXPAND + OPENER/CLOSING) to a badge + color class.
- **Region C — Persistent controls (§3.3):** the four CA overrides **Say again / Hold on / Pardon? / Wind down** — always present, always this order, every mode, slate-colored (quiet, recognized by position). Below them a session row: **Start conversation** / **End conversation** (the latter visually distinct/red — it's the hang-up).
- **Region D — Composer access:** the "In your own words" box with **Speak / Reframe / Clear** (the free-composition invariant; Speak takes the floor, Reframe steers the AI — both unchanged from v0.3.19/0.3.20).

**Engine diagnostics** are preserved but demoted to a **collapsed `<details>`** at the bottom (`#engineDetails`) for development / bug reports — the mode chip is now the user-facing inference.

**Code:** `ui.js` — `showMoves` rewritten to render cards (was `[SLOT] text` buttons); new `setTranscriptState`, `setNowPlaying`, and a `renderModeChip` fed by the same `lastSnap`+`capturing` as the diagnostic Mode cell (so the chip flips STANDBY↔LISTENING the instant the mic toggles). `app.js` — drives the transcript states (`unconfirmed` on live partner speech, `generating` in `generateOptions`, `ready` when the palette shows, `idle` on reset/clear), and a **second `tts.onSpeakingChange` listener** feeds the now-playing line. `styles.css` — design tokens as CSS variables (`--slot-*` green/amber/blue/purple per §10), the four-region styles, card anatomy, mode chip, transcript states, reduced-motion. CACHE_VERSION/APP_VERSION → **`0.4.0`**.

**Verified in preview** (1280×800, no API key needed — static opener/closer flows + a dynamically-imported `ui.showMoves` stub exercise the renderer): app boots clean, no console errors; the INITIATING opener flow renders blue OPENER cards with filled latency dots and the chip reads "Start the conversation"; a stubbed four-slot RESPONDING palette renders the correct per-slot colors on border+badge+dot (green/amber/blue/purple), hint at 22.4px, a "counter-offer" format tag on INITIATIVE, and a **hollow** dot on the round-trip REPAIR move; chip "Choose a response" (chip-responding); transcript state green "OPTIONS READY"; now-playing shows "♪ Good question."; the grid is 4 columns at 1280px and collapses to 1 column when narrow; all four regions + composer present in the a11y tree. *Not yet exercised against the live Claude API or the tablet mic/touch — the real RESPONDING palette, the crossfade timing, touch target sizing, and scanning/eye-gaze renderers (§8, future) need Ken's key + the device.* *(Access-method adaptation §8 is future: the descriptor→renderer boundary is kept clean — `showMoves` is a pure function of the snapshot palette — so a scanning/eye-gaze renderer slots in without engine changes, but only the direct-touch renderer is built.)*

### Proportional, two-axis layout (June 21 2026), v0.4.1–v0.4.2

**Design intent (Ken): the conversation layout is sized as a predictable FRACTION of the screen's pixel capacity** — the same share on every tablet, so an element is physically larger on a 13" and smaller on a 10" but always the same proportion. Elements need NOT be the same physical size across devices, only the same share of the pixel box. **Two independent multipliers (Ken's refinement over a single `vmin` basis):** horizontal sizes scale with screen **width** (`vw`), vertical sizes with screen **height** (`vh`), each axis using its own pixel capacity so an off-3:2 window (split-screen/portrait/resized) doesn't collapse both axes to the smaller one. **Font-size is the one exception** — a glyph is a single scalar (can't scale W and H independently), so type picks one basis, `vmin` (keeps text fitting the squeezed axis = height in landscape). All wrapped in `clamp()` with bounds OUTSIDE the real fleet (~1280–1465 wide × ~853–976 tall) so within the fleet everything scales linearly. **What the platform can/can't tell us drives this:** the CSS pixel box (`innerWidth/Height`, `screen`, `devicePixelRatio`, `visualViewport`) is fully knowable and reactive; **physical size (inches) and true density (real PPI) are NOT exposed by any web API** — `devicePixelRatio` is the Windows *scaling* factor, not density. So we anchor to pixels, never millimeters; the doc's "20 mm touch target" is unenforceable and becomes the user-scalable target-size profile knob instead.
- **v0.4.1 — `app/js/viewport.js`:** logs the full platform-knowable metric set (layout viewport, full screen, available area, `devicePixelRatio`, recovered native resolution = `screen × dpr`, visual viewport) at startup and, debounced, on every resize/orientation/keyboard change — so beta testers can report their real pixel box. Physical size deliberately absent (unknowable).
- **v0.4.2 — `styles.css`:** the conversation UI sized on the two axes (vertical→vh, horizontal→vw, font→vmin); replaced the fixed `max-width:1100px` (which pinned the column to identical pixels on every device) with a width-proportional cap `min(96vw,1600px)`. Verified at 1280×853 and 1465×976: vertical metrics track height, horizontal track width, independently; no console errors.

### UI Layout Rules (Ken, June 21 2026) — living ruleset; layouts selectable in Settings

Ken is establishing the rules **every conversation-screen layout must obey, BEFORE choosing a concrete layout** (the 2×2-grid-vs-single-row palette decision is explicitly **on hold**). Expectation: **multiple selectable base layouts** — like the on-screen keyboard's 20 layouts, the user picks the conversation layout in Settings. **This list will grow as we work through the UI.**

1. **Static layouts.** The target users often have **limited motor control**; a **keyguard** (physical overlay with holes over the touch targets) makes them far more effective — and a keyguard is by nature **static** (fixed target positions/sizes). So the **base conversation UI must be static**: fixed geometry, **no reflow**. *(Compatible with the v0.4.2 proportional scaling — sizes scale with the screen, but the arrangement/positions stay fixed. "Static" = no dynamic reflow, NOT "fixed pixels"; card POSITIONS are fixed.)* The chosen layout is selectable in Settings. **Reserved real estate (Ken, June 21 2026):** because the layout is static, the card slots are shown **EMPTY — space set aside — even before the conversation starts.** Options fill the already-present slots; the grid never materializes, grows, or collapses when options arrive or clear. So the keyguard holes always line up and nothing ever moves under the user. *(Implication for the current build: the palette region must always render its fixed slot cards (empty/inactive at rest), NOT the single "Response options will appear here" placeholder that changes the layout — change this when we build the first concrete layout; the slot COUNT/arrangement waits on the held 2×2-vs-row decision.)* **Corollary:** less-frequently-used features that **can** be implemented as **modal overlays should be** — keep the static base screen lean and push infrequent functions to modals (Settings & About Me already are; candidates: openers/Start-conversation, closers/Wind-down, repair-of-self, possibly the composer).
2. **Landscape only (until the field complains).** AAC apps are almost always used in landscape; we assume landscape and do NOT engineer for portrait until a real field complaint. **Consequence: vertical space is the precious resource** — spend it deliberately; horizontal space is comparatively abundant. (Reinforces why the palette single-row vs 2×2 choice matters, and why icon-only buttons (rule 4) help.)
3. **Non-editable items are labels, not text boxes.** Anything the user can't edit — partner speech/transcript, status — is a **label**, never a text input. *(Current build already renders the transcript as a `<p>` and status as a `<div>` — good; audit the rest as layouts are built.)*
4. **Buttons are icon-only.** Anything that **can** be a button shows **just an icon, no text description** (saves space — especially vertical — and aids fast recognition). **Retain accessible names** (`aria-label`/tooltip) for screen readers and learning; only the *visible* control is the icon. **Split CONFIRMED (Ken, June 21 2026):** the move/response cards are **EXEMPT** — they exist to show the response text the user reads/speaks, so they are content, not icon-buttons. The icon-only rule targets the **control buttons**: Listen, Say again, Hold on, Pardon?, Wind down, Start/End conversation, regenerate ("Show me different options"), composer Speak/Reframe/Clear. **"Show me different options" specifically becomes an icon BUTTON** (e.g. a ↻ glyph), not the current long thin full-width box. *(Needs an icon vocabulary — a dedicated pass when we build the first concrete layout.)*
5. **Button size ∝ frequency of use.** Bigger buttons for more-frequently-used actions, smaller for rarer ones — the common case gets the easiest target (motor control + Fitts's law).
6. **A "sticky" (latched/toggled-on) action is shown as a selected button** — the engaged state uses the "pressed/selected" button styling so the user can see what is currently on. (e.g. Listen while capturing; auto-resume if surfaced as a toggle.)
7. **Related concepts sit close together.** Group controls belonging to the same idea adjacently — e.g. the partner statement and the "Pardon? / please repeat" control near each other.
8. **Modal-action assets superimpose over the elements that are inert during that action (Ken, June 21 2026).** When a modal action runs — the first being **"In my own words"** (this **confirms the composer is a modal action**, not part of the static base screen, per rule 1's "push infrequent features to modals") — its asset (the on-screen **keyboard**) is **superimposed over the base screen elements that cannot be invoked during that action**, covering the inert controls while any element still usable during the modal remains. This is how a **static physical keyguard coexists with modals** (the rule-1 tension): because the base layout is static and keyguard-backed, the modal's assets must **align to the same keyguard grid/holes**, so the one physical keyguard works in both the base and the modal state. *(Open: which base elements stay live during the compose modal — e.g. End conversation, the partner transcript label — define per modal.)*
9. **Frequently-used phrases/words panel — a BASE UI element + a new configurable set (Ken, June 21 2026; corrects the earlier draft that wrongly put it in the compose modal).** A panel of the user's frequent words/phrases that is **part of the static base layout** — NOT a modal asset, and **NOT an "In my own words" starter** (it does not feed the composer; it **speaks directly**, single/double-tap per rule 10). Its buttons **show the word/phrase as text**, **truncated with an ellipsis** when too long — so, like the response/move cards, these are **content buttons EXEMPT from the icon-only rule (4)**. Joins the family of **user-owned configurable sets** (placeholders, openers, closers, keyboard layouts → and now frequent phrases/words); gets its own editor + data file later. **Starter sets seeded in `app/js/fast-phrases.js`** (CORE/MINIMAL/SOCIAL/REGULATE) modeled on `keyboard-layouts.js` — content only; not yet wired into a layout.
   - **THE keyguard-overlay principle (Ken, June 21 2026) — this is *why* it's modeled on keyboard layouts, and it ties rules 8 + 9 together.** The phrases panel must be **grid-congruent with a keyboard layout** so a **single static keyguard overlays BOTH**: fast-phrase buttons show through the holes at rest, and the compose-modal keyboard's keys show through the **same holes** during "In my own words" (rule 8). Therefore the panel sits **where the keyboard will appear — same side (the keyboard's left/right Setting) and the same grid — NOT opposite it** (this supersedes my earlier "opposite side, two columns" guess AND the later "its side is undecided" note: it's the *keyboard's* side, sharing the grid). Phrases fill the paired keyboard layout's cells **1:1** (one phrase per key-cell, ellipsis when long), so the holes line up. The phrase *content* (fast-phrases.js) is separable from the *geometry* (which keyboard layout it pairs with); the pairing is bound in the held base-layout pass. **Likely consequence to weigh there:** this favors a **side-docked** compose keyboard for the conversation screen (a side column preserves the precious vertical space — rule 2 — and gives the phrases panel a natural home), reconsidering the older "bottom dock during conversation" default.
10. **Activation safeguard — speak on single tap vs. double tap, configurable, with a configurable double-tap interval (Ken, June 21 2026).** A spoken action (first applied to the frequent-phrases/words buttons) either **speaks immediately on a single tap** or **requires a double tap** — user-configurable — to manage **false hits** from limited motor control; the **double-tap interval** is itself a Settings value (so it can be tuned to the user). *(Likely generalizes beyond the phrases panel to other speak/commit targets — e.g. selecting a response card — confirm scope. Connects to the existing Response-Option-Display note about a double-tap confirming a selection.)*

11. **Always strive for FIXED-dimension UI elements (Ken, June 21 2026).** Prefer fixed (device-deterministic) sizes over content-sized/auto layouts — an element that's larger than its content just carries blank space, which is fine; what's NOT fine is a size that changes with content or that reaches into another element's space. **The end goal: the Settings panel (and other overlays) match the size + location of a base-UI element so that ONE physical keyguard supports EVERY screen** (conversation, Settings, About Me). First step taken (v0.5.9): the Settings panel is fixed-size, and for a bottom keyboard dock it's a fixed full-width **top-region** panel matching the conversation main area (clears the keyboard); for a side dock it's the fixed centered panel. *(This corrects a brief auto-height experiment — auto/content sizing is exactly what to avoid.)*

12. **Constant (non-customizable) buttons are icon-only with an explanatory tooltip (Ken, July 8 2026).** Restates and generalizes Rule 4 as the governing test: if a button's function is **fixed** (the user can't edit what it does/says), its face shows **only an icon** and it carries an explanatory accessible name (`aria-label` + `title` tooltip) — Listen, Start/End conversation, the repeat/hold/pardon/wind-down overrides, Settings, regenerate ("New N"), the composer Speak/Reframe/Cancel, **and "In my own words"** (the Express Panel space cell — a constant control, so it now shows a compose/pencil icon, not its text label). The contrapositive is the exemption: buttons whose face is **user content** — the response/Response-Palette cards (AI-authored text the user reads/speaks) and the Express Panel phrase/partner/feeling buttons (user-editable) — keep their **text**. **Applies across all conversation modes** (the base screen is static, so a button's face is constant across LISTENING/RESPONDING/REPAIR/INITIATING/PRE-CLOSING anyway). *Scope note:* the Settings & About Me overlays (the keyguard's "open window", supporter-assisted, infrequent) keep text buttons for readability — the rule targets the keyguard-backed conversation control surface; revisit if Ken wants those iconified too. The pre-start **Start** button stays text (onboarding affordance).

*(More rules to come. Where these conflict with the UI-Design.docx defaults — notably icon-only buttons vs. the doc's text-badge move cards, and the move-card text-hint anatomy — these field-driven rules win for the control surface; reconcile fully when we pick the first concrete layout.)*

### Conversation-screen feedback batch (Ken, July 8 2026) — committed locally, NOT version-bumped

Six items from Ken; all built and verified in preview (Python static server + a stubbed Anthropic endpoint — this dev box has no Node on PATH; TTS `onend` doesn't fire headless, so the post-speak flows were verified with a `speechSynthesis.speak` stub). Files: `app/js/{app,ui,engine,llm,icons,control-phrases}.js`.

1. **New UI Layout Rule 12** (above): constant buttons are icon-only + tooltip. Review outcome — the conversation control surface already complied except the **"In my own words"** Express Panel space cell (was text); converted it to a **compose/pencil icon** (`icons.js` `compose`, applied via `setIconButton` in `ui.renderExpressPanel`). Settings/About Me text buttons + the pre-start Start button left as text (flagged in Rule 12's scope note).
2. **User statements no longer shown as "now-playing" pre-text; appended to the transcript AFTER speaking (Ken, follow-up).** The `♪` now-playing line is now **reserved for system placeholder speech**; a `speakingUserStatement` flag (via the `speakUserStatement()` wrapper around every user-speech `tts.speak`) suppresses the now-playing line for user statements. Each user statement is committed to the transcript **AFTER** it has been successfully spoken — `handleResponseSelected` / `speakAsUserTurn` / `handleRepairOfSelf` speak first, then commit; the spoken command buttons (`handleSayAgain` / `handleHoldOn` / `handlePardon`) call `logSpokenUserTurn` after the `await`. So nothing shows during the speech (no pre-text), and the entry appears once said. *(An earlier cut of this batch committed BEFORE speaking; Ken corrected it to after.)* Placeholders still surface in now-playing (they set no flag).
3. **Reframe becomes floor-aware.** With a partner turn on the floor, Reframe reworks the **responses** around the steer (unchanged). With **no** partner turn (the user just responded / holds the floor), Reframe now generates **STATEMENTS that steer the conversation** where the user wants — new `llm.generateStatements(steer, history, context, count)` (returns `{responses:[{slot:'STATEMENT',…}]}`, one-per-cell like openers), shown via `engine.refreshPalette` + the capped `showConversationPalette`. New `SLOT.STATEMENT` meta (initiative-colored) in `ui.js`. The old "Reframe needs an active response" guard is gone.
4. **A wind-down/closing selection re-offers the closings** (incl. "Bye!") immediately — `handleResponseSelected` detects `slot === 'CLOSING'` and calls `reofferClosings()` (re-shows the closing palette and resumes listening if armed) so the user can sign off without waiting for the partner to reply to the wind-down.
5. **Conversation starters expanded to 9 (added 5)** in both `engine.DEFAULT_OPENERS` and `control-phrases.DEFAULTS.openers`; the opener/closer palettes now pack **2-per-cell when >4** (`ui.showResponses` non-responding branch) so **8-card mode fills all 8 cells** with starters (`handleInitiate`/`handleWindDown` cap the palette to `conversationPaletteCap()` = 8 or 4). *Note:* the footprint holds 8, so with 9 defaults the 9th ("Got a story for you.") doesn't show by default — it lives in the editable Controls list; existing users must **Settings → Controls → Reset to default** to pick up the new starters (file-in-folder wins over new defaults).
6. **Spoken command buttons write to the transcript** — Say again / Hold on / Ask them to repeat now push their spoken text as a user turn (`logSpokenUserTurn`), since anything spoken aloud is part of the conversation. (Placeholders are auto-fillers, not command buttons, so they stay out of the transcript — only now-playing.)

*(No `APP_VERSION`/`CACHE_VERSION` bump — committed to local `main` for Ken to test, per the standing "commit locally without bumping" rule. Not exercised: the RESPONDING-partner Reframe branch and live TTS/mic behavior — need Ken's key + the tablet.)*

### Conversation-log persistence: errors interleaved + per-conversation file fix (Ken, July 8 2026)

Ken asked whether errors land in the recorded conversation JSON, and whether turns are written as-they-occur or only at conversation end. Findings + changes (`storage.js`, `app.js`):

- **Turns are already written incrementally** — every `logPartnerSpeech`/`logUserResponse` pushes the turn and immediately `flushLog()`s (a full rewrite of `conversations/<id>.json`), so nothing is buffered to the end; a lock-up doesn't lose committed turns. Confirmed, no change needed.
- **Errors now interleave into the conversation JSON in time order.** New `storage.logErrorToConversation(entry)` (fire-and-forget from `logError`, after `appendErrorFile`) pushes an `{ timestamp: entry.ts, role:'error', context, message, version, extra? }` entry onto `currentLogData.exchanges` and flushes immediately — so the conversation file itself shows what went wrong and when, alongside the turns (not only the separate `errors.log`). Uses the error's own `ts`, so sorting exchanges by time is exact. Skipped for a private (unsaved) conversation or when no data folder is granted (errors still go to the ring buffer + `errors.log`). `app.js buildErrorReport`'s `transcriptLine` now renders `role:'error'` entries (`[error: <context>] <message>`).
- **Bug fixed — consecutive conversations no longer merge into one file.** `resetConversationId()` (called from `terminateConversation`) previously only nulled `currentConversationId`; `currentLogData`/`currentLogHandle`/`currentLogName` persisted, so the SECOND conversation of a session appended to the FIRST conversation's `<id>.json` (and its id), breaking error correlation. `resetConversationId()` now also clears those three, so each conversation writes a fresh `<id>.json`. *Known minor edge:* a deferred transcript-cleanup write for the very last exchange (commitExchange's background round-trip) that lands AFTER End/Start splits that one turn into a new file — no turn lost; a full fix would thread the id captured at commit time through the log calls.
- **Verified**: boots clean; `logError` still logs to the ring buffer with conversation id + timestamp, trips the transcript wash, and does not throw with no folder. The on-disk JSON write + the per-conversation-file fix need a granted FSA folder to confirm on the tablet (headless can't grant one) — same verification boundary as all conversation logging.

### Interrupting the partner — capture their partial speech; keep capturing after (Ken, July 8 2026)

Ken: interrupting the partner with an instant statement (e.g. an Express "Bye") was making it look like the partner never spoke — their in-progress speech wasn't recorded. Fixes + confirmations:

- **The partner's partial speech is now captured at the interruption.** `currentPartnerText` is only refreshed at a silence checkpoint, so interrupting *before* a pause left it empty and the partner's words were dropped. New `stt.getCurrentTranscript()` returns the live accumulated transcript (finalized segments + interim); app.js `heardPartnerText()` prefers it (falls back to `currentPartnerText`). `speakAsUserTurn` (Express phrase / composer Speak) and `handleResponseSelected` capture it BEFORE `stt.stopListening()`, so the partner's speech up to the interruption is committed to the transcript + JSON, interleaved before the interrupting statement.
- **No AI cleanup on the interruption fragment (Ken).** `commitExchange` gained `opts.cleanup` (default true); `speakAsUserTurn` passes `cleanup:false` — a mid-interruption fragment isn't a completed utterance, so we record the raw heard text verbatim with no cleanup round-trip. (`handleResponseSelected` keeps cleanup — it fires only after the partner paused, i.e. a complete utterance.)
- **Auto-listen keeps capturing after the interruption (confirmed, already worked).** `speakAsUserTurn` ends in `resumeOrIdle()`, which resumes listening when `manualListenArmed && loadAutoRelisten()`; `startFreshListening()` resets the accumulator so the partner's continued speech becomes a fresh partner turn. Verified: after an interrupt the mic re-starts and "…continued speech" is captured.
- **The ENTIRE session conversation is sent to the AI each turn (answers Ken's question).** `generateOptions` passes the full `conversationHistory` + the current partner text; `llm.generateResponses` maps every entry to the `messages` array (partner→user, user→assistant). Verified: `[partner partial, user "Bye", partner continued]` all reach the request. So the interruption and everything around it are in context — "not an issue," as Ken expected. (There is no history windowing yet; a rolling window is a future token-cost concern, not a correctness one.)
- **One inherent gap (noted, not a defect):** while the app is speaking the interrupting statement the mic is off (so it doesn't capture its own TTS), so partner words spoken *during* that ~1 s overlap aren't captured; capture resumes immediately after. Verified in preview with a fake recognizer (the live mic still needs the tablet).

### Word prediction — local tiers, no AI per keystroke (Ken, June 21 2026)

Resolves the prediction half of the long-recorded composition roadmap (the keyboard `pred` slots + the "AI-assisted composition" build-order item). **Predicted-word boxes on the on-screen keyboard are fully LOCAL — no API round-trip per keypress** (per-keystroke latency + token cost make AI infeasible there, and it's a solved offline problem). Three layerable, instant, offline techniques:
1. **Prefix completion** from a bundled word-frequency list (trie / sorted array, ~10–30k words ≈ 100–300 KB, precached): "th" → the / that / this / they.
2. **Next-word n-gram** (bundled bigram/trigram table, pruned to top-k per context): fills the boxes before the next word is even started.
3. **Personalized local learning**: track the user's own word/phrase frequency + recency in the data folder (the user-owned-data model), boosting their actual vocabulary over time — no network, no privacy cost.

**Two complementary tiers, kept distinct:** *local prediction* handles **every keystroke** (free, instant); the *LLM* is reserved for occasional, **explicit phrase expansion** — a few words → a full sentence in the user's voice (the existing "In My Own Words" build-order item #3, "AI-assisted composition") — **never per keypress**. So the keyboard's prediction boxes are NOT an AI feature; the AI tier is a separate, deliberate action.

**Where the boxes live (Ken, June 21 2026):** the keyboard already scaffolds them — `pred` cells in `keyboard-layouts.js` + `.kbd-pred` CSS. **Plan: convert the existing keyboard toolbar buttons (Cut / Copy / Paste / Hide) to ICON buttons (rule 4)** — they're control buttons, icon-only anyway — which **reclaims the horizontal space their text labels took, and that toolbar row hosts the word-prediction buttons.** (Prediction buttons show predicted words as text → content, exempt from rule 4, like the phrases panel and move cards.) **BUILT v0.4.4 (icons) + v0.4.5 (engine):** `icons.js` converted Cut/Copy/Paste/Hide to icons; `prediction.js` + `data/words.json` provide prefix completion + personalized local learning; the toolbar's reclaimed space hosts the prediction buttons. Personalized counts persist in `localStorage` (`aac_word_freq`) — a data-folder move + a next-word n-gram layer remain future extensions; the bundled list is a ~220-word starter to grow.

### Fast Phrases starter content (June 21 2026)

`app/js/fast-phrases.js` seeds five selectable starter sets — **CORE (24)**, MINIMAL (6), SOCIAL (16), REGULATE/pace-&-repair (16), BACKCHANNEL (12). **BUILT v0.4.3:** each phrase carries a functional **category** (`affirm`/`social`/`pace`/`repair`/`need`/`back`) keyed to `CATEGORIES` (color + tint); the panel (`ui.renderFastPhrases`, `#fastPhrases`) renders category-colored content buttons (text, ellipsis-truncated) that **speak directly** on single-tap, or on a confirming **double-tap** with a configurable interval (Rule 10) — Settings has the set selector, tap-mode radios, and interval. Placement is **provisional** (a region in `<main>`); the final home (keyboard's side, grid-congruent for keyguard overlay) is bound in the held base-layout pass. The *content* stays user-editable later (own editor + data-folder JSON). Fast phrases **commit to history and take the floor** (Ken #8, v0.5.0 — "anything spoken is part of the conversation"), via the shared `speakAsUserTurn` path. *(Open nuance: a backchannel like "Right" isn't really a full turn; per-category floor behavior is a possible later refinement.)*

### Conversation relayout — dock panel + "In my own words" modal (June 21 2026), v0.5.0

The full base-layout pass (the held decision), per Ken's spec. The conversation screen splits into a **main area** and a **dock area** that occupies the on-screen keyboard's exact real-estate (side-left / side-right / bottom, per the Settings dock choice). The dock area shows the **fast-phrase panel at rest** and the **keyboard during the "In my own words" modal** — so a single static keyguard overlays both (verified: keyboard and dock area are the same width to the pixel).
- **Fast-phrase panel** (`ui.renderFastPhrases(layoutRows, …)`, `#fpGrid`): mirrors the **selected keyboard layout cell-for-cell** (each cell's flex = the key's span). Top row `#fpControls` = the four persistent overrides (Say again / Hold on / Pardon? / Wind down), corresponding to the keyboard's toolbar row (static HTML). Phrase pool fills the non-space key cells in order; the **space cell → "In my own words"** (distinct dark bg); blank/pred and leftover cells stay blank. **ONE canonical phrase list, always shown in full — NO selectable sets (Ken, v0.5.3: "we always display all possible quick phrases").** The selectable-sets concept (CORE/MINIMAL/SOCIAL/…) is removed; `fast-phrases.js` now exports a single `PHRASES` array (~32, sized to a 5-wide layout's non-space cells) + `CATEGORIES`; the Settings set-selector and `loadFastPhraseSet`/`saveFastPhraseSet` are gone (tap-mode + double-tap interval settings remain). The list stays user-editable later (one list, no picker).
- **Response region** (`#responseArea`): a **reserved fixed footprint** — 4 cards **2×2 (side dock) / 1×4 (bottom dock)** + a full-height **"New 4"** regenerate icon button (renamed from "Show me different options" — Ken; a fresh set of four, same `handleRegenerate`). Empty card slots render at rest (Rule 1) because the input box overlays this exact space.
- **"In my own words" modal** (Rule 8): the space-cell button opens it — an input box (`#composerOverlay`: textarea + **Speak / Reframe / Cancel**) overlays the response footprint **without blurring the base UI**, and the keyboard appears over the dock area. **Any of the three buttons dismisses both.** Speak = speak + take floor + commit; Reframe = regenerate the palette from the text (one-shot) ; Cancel = discard. The always-visible composer (old Region D) is **removed** — free composition is now only via this modal.
- **Start / End conversation** moved to the transcript head (icon buttons).
- **Status bar removed from view (Ken, v0.5.2)** — `#statusBar` is now a visually-hidden `aria-live` region (announced to screen readers / available for debug, no screen space). *Caveat: error messages routed through `setStatus` are no longer visible — a transient error toast may be wanted later.*
- **Suggested-response cards doubled in height (Ken, v0.5.2)** — `min-height` clamp(8rem,18vh,14rem) (~176px on a 976-tall screen).
- **Engine diagnostics is a DEV-ONLY aid — to be REMOVED before release (Ken, June 21 2026).** The collapsed `#engineDetails` panel will not ship in the final product; kept for now for development/bug reports.
- Session-wide `body.conv-{side,side-left,side-right,bottom}` classes drive the split + the 2×2-vs-1×4 choice; `keyboard` reservation no longer pads conversation `main` (the dock area owns that space).
- **Caveat (recorded):** the keyguard **top row** can't align holes for both the 4 overrides and the keyboard toolbar's cut/copy/paste/hide+prediction (different counts) — the phrase grid below aligns perfectly; the top row is a compromise to tune when a real keyguard is cut. Also during the modal the four overrides are briefly unavailable (the row is the keyboard toolbar) until Cancel/Speak/Reframe — accepted.
- **Verified in preview** (side-right + bottom, no API/mic): panel mirrors the layout with "In my own words" at the space cell and overrides in the top row; keyboard overlaps the dock area exactly (453px = 453px); 2×2 / 1×4 response grids; 4 reserved empty cards at rest; modal overlay covers the response footprint and Cancel dismisses both; no console errors. *Live touch + the keyguard fit need the tablet.*

### Single-instance enforcement — design question (Ken, June 16 2026; my recommendation, NOT built)

Ken asked whether we can prevent multiple instances/launches of the app. **Yes, and it matters for this app specifically:** two live instances (two tabs/windows) each run their own mic + TTS, so instance A's spoken placeholder is captured by instance B's mic — **cross-instance feedback the v0.3.4 content-echo filter cannot catch** (B doesn't know what A said). Two instances also race on the FSA data-folder writes (`worldview.json` / `relationships.json` / conversation logs) and can double-speak. So single-instance is a robustness requirement, not just polish.

**Recommended approach (two layers, covers the realistic cases on a dedicated Surface in one browser):**
1. **Web Locks API (`navigator.locks.request`) for detection + a blocking "already open" screen.** On startup request an exclusive named lock (e.g. `aac-single-instance`) held for the page's lifetime via a never-resolving promise. The first instance gets it; later instances' requests stay pending → they detect another instance holds it and show a full-screen "AAC is already running in another window — switch to it" overlay instead of starting the mic/engine. Well-supported in Edge/Chrome (Chromium). Covers multiple tabs *and* windows in the same browser profile.
2. **PWA `launch_handler: { client_mode: "focus-existing" }` in the manifest** so re-*launching* the installed app focuses the running window rather than spawning a new one (the app is intended to be installed app-style on the Surface). Complements layer 1, which still backstops a manually-opened second tab.

**Boundary / limitation:** web APIs can't coordinate across *different* browser profiles or *different* browsers (Edge AND Chrome both open) — they don't share locks/localStorage/BroadcastChannel. On a dedicated device using one browser this is an acceptable boundary; if it ever matters, an FSA folder lock-file (write a PID/heartbeat into the data folder, stale-timeout to recover from crashes) is the only cross-browser option and is heavier/more fragile. *Not built — awaiting Ken's go-ahead. When built: bump version, add to the privacy/consent behavior already in flight, and note the cross-browser boundary in the docs.*

**Future: partner voice recognition as an extraneous-speech filter (Ken, June 16 2026).** When Phase-2 partner recognition lands, real-time speaker identification could filter out speech that isn't the recognized communication partner (the app's own TTS, bystanders, background voices) — a more general solution than content-matching the app's own fillers. Records alongside the v0.3.4 echo filter as the eventual upgrade path; the content filter is the no-extra-infrastructure stopgap until then. **[→ Overview To Do — Phase 2 partner recognition / privacy section]**

---

## Worldview Questionnaire (June 2026) — Build Steps 1–4 DONE; core build complete

The worldview model (Architecture Overview §6) has a concrete questionnaire design, a build-ready implementation plan, and **Build Steps 1–4 are implemented and verified** (June 14 2026). Remaining steps (5 expand content, 6 RAG-lite) are enhancements, not blockers — the core worldview feature is functional end-to-end.

**Follow-up questions — built (v0.3.13) then reverted (v0.3.14); passion-tailored depth deferred to the LLM track (Ken, June 19 2026).** v0.3.13 added inline follow-up cards: when a field with a `followups[]` array is answered, the follow-ups render as indented child cards below the parent (each with its own input/Save/Speak), and their answers inject into `buildBlock()` indented under the parent. **Ken's catch killed the design:** the only field carrying `followups` was `passions`, a **`multi`** field — so "How long have you been into it?" was asked **once for the whole list** ("music, hiking"), which is meaningless; that depth is inherently **per-item**. We then established the deeper problem: **the follow-up question itself is often a function of the answer** (Music → "play or listen? which instruments?"; Sports → "play or watch? which teams?"). A static registry can only express that via **`when:`-gated branches** authored per known value (the `when` field on each follow-up is the seam for it) — but that is an authoring treadmill that **still gives zero coverage for "in my own words" / typed passions.** **Decision: do not build static passion-specific follow-up branches.** Passion-tailored follow-ups are a **conversation feature, not a questionnaire feature** — they belong to the **LLM-driven "Conversational Worldview Collection"** already specified in Architecture Overview §6 (the AI sees the answer and asks adaptive, tailored follow-ups; answers stored as free-form per-item attrs). So: `followups` removed from `passions`; `passions` stays a clean `multi` list (already useful worldview context on its own); the entire v0.3.13 follow-up infrastructure (registry indexing of `_fu_` keys, `buildBlock` injection, `buildFollowupCard`/`wv-card-group` rendering, the `.wv-followup-card` CSS) was **reverted** rather than left as dead, half-applied code with no consumer — the correct future implementation (LLM-generated) shares none of it. *(If a single-value field ever wants a fixed generic follow-up, the pattern is re-addable; it was the `multi`+shared-answer combination that was wrong.)*

**Keyboard / field tabbing — shipped (June 14 2026).** The "About Me" questionnaire supports physical-keyboard users. **Decisions (Ken):** (1) *every* control is a Tab stop (native order — inputs and option chips alike), not a reduced inputs-only order; (2) opening a module **autofocuses its first answerable field** (accepted that on a touch device this also auto-pops the OS keyboard). Two fixes in `app/js/worldview-ui.js`: a `focusFirstField()` call at the end of `renderModule` (and on the targeted card when arriving from a "suggested next" chip), and `setBackgroundInert()` which marks the app `<header>` + `<main>` `inert` while the overlay is open so Tab stays inside the form instead of leaking out to Settings / the conversation controls behind it (cleared on close). CACHE_VERSION → `aac-v0.2.5`. Verified in preview: first field focused on module open, header+main inert while open and cleared on Done, no focusable leak outside the form (the only out-of-form stop is the pre-Start `#startBtn`, which is hidden in normal flow), no console errors. *Note — Windows touch keyboard:* when the cursor enters a worldview `<input>` the Windows on-screen keyboard (TabTip) pops; this is the **OS**, not our code (FSA-era black-box behavior already documented in "In My Own Words") — Windows auto-shows it whenever an editable element gets focus in a touch context. Levers if unwanted later: `inputmode="none"` per field, or the Chromium `navigator.virtualKeyboard` API (neither can *summon* a keyboard on demand). This is a config decision (hardware-keyboard vs. touch-only users), not a defect.

**Storage fallback (where worldview data lives without/with a stale folder) — confirmed June 14 2026.** Source of truth is `worldview.json` in the granted FSA data folder; `localStorage` key `aac_worldview` is a write-through cache (`worldview.js` `load`/`save`, `storage.js` `readFile`/`writeFile`). **No folder granted:** answers persist *only* in `localStorage` (per-browser/per-machine — not in the portable file, lost if browser data is cleared; `writeFile` is a silent no-op without a `dirHandle`). **Folder moved/renamed since last session:** `restoreDataFolder()` re-acquires the handle from IndexedDB; if the OS can't resolve it the handle is **deleted** and the app silently falls back to the `localStorage` cache, writing new answers to cache only until the user re-picks a folder.

**Cache → disk promotion (built June 14 2026).** `worldview.syncToFolder()` closes the cache-only gap: the moment a data folder becomes available it promotes cache-only answers to the portable `worldview.json`. Called at the three points a folder becomes available — `app.js` `handleStart()` (start-up restore), the Settings "Choose Folder" handler (`pickDataFolder`), and `worldview-ui.open()`. Logic: no folder → `'noop'`; folder + no on-disk file → write the cache to disk (`'wrote'`); folder + existing file → the **newer of {disk, cache} by `updated` wins** (write-through to both stores + in-memory) so neither side's recent edits are clobbered (`'wrote'`/`'adopted'`). Timestamp-wins, not field-level merge — adequate for the real scenarios (the main case is "no disk file yet"); a future field-level merge / "reconnect your folder" prompt remains a possible refinement. *Verified in preview only for the no-folder `'noop'` guard + wiring (no throw, cache write-through intact); the actual on-disk promotion needs a user-granted folder (FSA gesture) to exercise — confirm on the test tablet.*

**Cross-PC copy of `worldview.json` — bug fixed (June 15 2026), v0.2.25. The file in the folder is the source of truth — full stop.** *Symptom (Ken):* copied `worldview.json` from PC1 into PC2's data folder; on PC2 the app showed no progress. *Root cause:* this was **not** a timestamp problem, it was an "ignore the current file" problem (Ken's framing). `syncToFolder()` ran a cache-vs-disk **timestamp contest**, and `defaultProfile()` stamped `updated: now` — so on a fresh PC the empty browser cache (stamped *now*) beat the copied file (saved earlier on PC1), the cache "won," and `save()` wrote the empty profile **back over the copied file (silent data loss).** *Decision (Ken, June 15 2026): the data folder is the portable source of truth; the localStorage cache is only a same-machine mirror + a no-folder stopgap. **If a `worldview.json` is present in the connected folder, that file wins — always; the contest is gone.*** `syncToFolder` now: file on disk → **adopt it** (mirror to cache, never overwrite); no file on disk → **promote the cache** (answers entered before a folder was granted). `load()` already preferred disk; this makes the promotion path agree. Removed the timestamp comparison + the `hasContent` scaffolding; `profile.updated` is now informational only (nothing compares it). CACHE_VERSION/APP_VERSION → `0.2.25`. *Verified in preview:* app boots clean, `load()` + `syncToFolder` (noop without a folder) run with no console errors. *The on-disk adopt path needs a user-granted FSA folder — confirm the cross-PC copy now shows up on the tablet.* **Accepted trade-off:** answers entered while *no* folder was connected, then connecting a folder that *already* has a file, are dropped in favor of the file. A future "this folder and this browser differ — which do you want?" prompt is how that case becomes loss-free (the only remaining refinement here). *(This also makes [[relationship data]] portability simpler if it moves to its own file — same "file in folder wins" rule.)*

**About Me prompts for a data folder when none is assigned (June 15 2026), v0.2.26.** Without a granted folder, About Me answers persist only in the localStorage cache (machine-local, lost if browser data is cleared, not in the portable file). The About Me **home now shows a prompt banner** (`renderFolderPrompt()` in `worldview-ui.js`, `.wv-folder-prompt` styles) whenever `storage.hasDataFolder()` is false, with a **Choose data folder** button. *Why a button, not an auto-popped picker in `open()`:* `showDirectoryPicker()` requires transient user activation, and `open()` runs several `await`s (restore/loadRegistry/load/syncToFolder) before render that would consume the gesture from the click that opened About Me — so the picker would throw. The button is a fresh gesture. Clicking it → `storage.pickDataFolder()` → `wv.syncToFolder()` (file-in-folder wins per v0.2.25: adopt an existing `worldview.json`, else promote cache-only answers) → `renderHome()` (banner clears, progress reflects adopted data). Non-blocking — the user can still answer without a folder; the banner just persists until one is chosen. CACHE_VERSION/APP_VERSION → `0.2.26`. *Verified in preview:* opening About Me with no folder shows the styled banner + button, no console errors; the actual pick→adopt path needs a real FSA gesture — confirm on the tablet.

**Build Step 4 — shipped (interim Name/About You removed):** No migration was needed — there were no meaningful legacy `userName`/`userAbout` values to preserve (Ken's call). Removed the stopgap entirely: the two Settings inputs (`index.html`), their wiring (`app.js` `openSettings` + init), `storage.loadUserProfile`/`saveUserProfile`, `llm.setUserProfile` + the name/about branch of `buildProfileBlock`, and the orphaned `.setting-optional` CSS. The General tab now points users to **About Me**. The worldview profile (`buildBlock()`) is the sole personalization channel; the no-bracket safety rule remains. CACHE_VERSION → `aac-v0.2.4`. Verified in preview: Settings opens/saves with the fields gone, profile injection still carries the worldview block + no-bracket rule with no stale remnants, no console errors, no remaining references to the removed symbols.

**Build Step 3 — shipped:** the generation call now injects the worldview profile and harvests gaps.
- `app/js/llm.js`: added `setWorldviewBlock(text)`; `buildProfileBlock()` now layers worldview block → interim name/about ("Also: …") → the standing no-bracket rule. `generateResponses()` now requests a single structured JSON object `{classification:{fpp,about}, options[], missing_facts[]}` and returns `{options, classification, missingFacts}`; `parseGeneration()` parses robustly and still tolerates a legacy bare array. `max_tokens` 300 → 500. **Model unchanged (`claude-sonnet-4-6`)** — kept deliberately for the user-pays-per-token cost model; this was not a model migration.
- `app/js/app.js`: imports the worldview model; `initApp()` loads the registry + profile (cache at startup); `handleStart()` reloads the profile from the data folder once granted; `generateOptions()` calls `llm.setWorldviewBlock(worldview.buildBlock())` each turn (so edits take effect immediately), destructures `{options, missingFacts}`, and routes `missingFacts` → `worldview.recordGaps(...)`.
- `app/sw.js`: CACHE_VERSION → `aac-v0.2.3`.
- **Verified in preview** (fetch stubbed — no API key needed): structured output parses to classification + options + `missing_facts`; legacy bare-array tolerated; system prompt contains the worldview block + name + no-bracket rule; app boots with registry loaded and no console errors. *Not yet exercised against the live Claude API* — that needs Ken's key in the running app.
- **Guardrail honored:** interim Name/About You Settings fields untouched (removed in Step 4).

**"Suggested next" removed from the About Me home (June 15 2026, v0.2.31).** Ken: those questions are already asked on the first topic sub-page, so the gaps-driven "Suggested next" chips on the home screen were redundant. Removed the section from `renderHome` plus the now-dead `openModuleForField` helper and `.wv-suggested`/`-chip` CSS. Home now shows just intro → (folder prompt if needed) → **Topics** → **People & relationships** → Restart. The model's gaps subsystem stays intact (`recordGaps`/`listGaps`/`suggestedNext` still record and are available for a future surface); only the home UI section is gone. Verified in preview: home shows only Topics + People sections, no console errors.

**Build Step 2 — shipped:** `app/js/worldview-ui.js` + an "About Me" header button + a full-screen `#worldviewScreen` (index.html / styles.css), precached in sw (CACHE_VERSION → `aac-v0.2.2`). Home shows an intro, gaps-driven **Suggested next**, a module list with answered/total progress bars, and **Restart** (confirm → `resetAll`). Tapping a module opens its fields as cards (the "chunk"). Card supports every field type — choice (single-select chips + in-my-own-words), multi-with-options (toggle chips + add-your-own), free multi (comma-separated), text/number, and repeat (sub-field rows + add/remove) — plus three visible states (✓ Answered / Prefer not to say / unanswered), edit-in-place, and **Undo** for declined. Each card carries the in-flow **🔊 Speak my answer** control (Build Step 2 design intent; opt-in, speaks the saved value in the selected voice). Verified in preview: all field types persist incrementally to `worldview.json`/cache; decline/undo; progress + suggested-next update; restart clears; Done closes. *Step-3 note:* `buildBlock()` returns '' until `loadRegistry()` has run on the worldview instance — Step 3 must call `worldview.loadRegistry()` + `load()` at app init so generation can inject the profile even before the user opens "About Me".

**Build Step 1 (recap) — shipped earlier (June 14 2026), committed:**
- `app/data/worldview-questions.json` — static registry. Scope per resolved decisions: **full Tier-A** (A1 About You, A2 Where You Are, A3 People, A4 Daily Life, A5 Contact) + **Tier-C starter** (C1 Favorites, C2 Passions). Each field: `key`, `q`, `type` (text/number/choice/multi/repeat), `options`, `fills` (placeholder synonyms), `sensitive`, `defaultPrivacy`. A5 fields are `defaultPrivacy: private`.
- `app/js/worldview.js` — model layer: `loadRegistry()`, `load()`/save (data-folder `worldview.json` write-through + localStorage cache), `getField`/`setField`/`declineField`/`resetField`/`resetAll`, three states (unanswered=key absent / answered / declined=sticky), `effectivePrivacy`/`setPrivacy`, gaps `recordGaps`/`listGaps`/`clearGaps`, `getModules` (progress), `suggestedNext` (gaps-first), and `buildBlock()` (compact profile text; labels from `fills[0]`; declined + private fields surface only as a phrase-around instruction, never with their values).
- `app/sw.js` — precache list updated (worldview.js + registry), CACHE_VERSION → `aac-v0.2.1`.
- **Verified in preview:** set/decline/reset/un-decline state transitions; cache round-trip; `buildBlock()` includes shareable facts and withholds private (phone) values while listing age+phone as phrase-around; gaps add/bump/drop-answered/clear; suggested-next ordering. *Caveat:* verification exercised the in-memory + localStorage-cache path; the on-disk FSA `worldview.json` write goes through the same `storage.writeFile` that conversation logging already uses, but confirming an actual on-disk file requires a user-granted data folder (FSA needs a user gesture).
- **Not touched:** the interim Name/About You Settings fields (they stay until Build Step 4), per guardrail.

**Remaining build order:** ~~1) data model~~ ✓ → ~~2) questionnaire UI~~ ✓ → ~~3) LLM integration~~ ✓ → ~~4) remove interim Name/About You~~ ✓ (no migration needed) → **5) expand Tier-B + rest of Tier-C content (enhancement)** → **6) RAG-lite (enhancement, not needed until cross-session memory lands)**.

**Build Step 2 design intent — "Speak my answer" control (Ken, June 14 2026).** The questionnaire UI must include an on-demand **"speak my answer"** control during worldview collection, rendered **in-flow and close at hand** next to each answer — explicitly **not** in the Settings panel. Speaking an answer is opt-in/on-demand, never the automatic behavior that selecting a conversation *response* has. Rationale (scenarios that make spoken answers useful during collection): supporter-assisted entry (speech is the user's channel to the human helping); auditory proofread of free-text / "in my own words" answers; voice-match calibration (esp. B8 "Your Voice, in Your Words" — judge "does this sound like me?"); and doing the "getting to know you" session as a real conversation with another person. An **auto-speak toggle** (for the supporter-assisted and conversation-as-collection scenarios) is a reasonable later addition, but the always-available per-answer control is the requirement.

**Origin:** The interim *Your Name / About You* fields in Settings (shipped June 2026 to stop the LLM emitting `[Name]` placeholders) are a stopgap. The worldview questionnaire replaces and generalizes them. Bracketed placeholders are the signal of which personal facts conversations demand; the design turns that signal into a self-populating profile.

**Documents produced (in this folder, styled like the other design docs):**
1. `Worldview-Questionnaire-Draft1.docx` — the question bank. Three tiers: **A** Concrete Core (identity, place, people, daily life, contact), **C** Interests/Passions/Preferences (the layer the frameworks miss — Ken's explicit add), **B** Abstract Characteristics (personality via Big Five, values via Schwartz Portrait format, humor, register/style, beliefs, how-you-treat-different-people, emotional landscape, voice-in-your-words). Grounded in Big Five, Schwartz basic human values, and the AAC Communication Passport / "All About Me" tradition.
2. `Worldview-Implementation-Plan.docx` — build-ready plan. Slots into the Architecture Overview as **§17** (or expands §6). Covers data model, files, the gaps-log mechanism, conversation integration, UI flow, migration, build order, verification, and decisions-to-confirm.

**Settled design decisions:**
- **Three tiers** A (concrete, fills placeholders) / C (interests) / B (abstract, shapes voice). Abstract questions organized by *conversational lever* (what they change in output), not psychological taxonomy. Values use Schwartz **Portrait** format ("here's a person — how like you?").
- **Three field states:** Unanswered (eligible to ask/resurface) / Answered / **Declined** (sticky — never ask again, always phrase around; un-declinable). Skip ≠ Decline. Every question optional; full value at zero answers; chunked sessions.
- **Privacy model — three levels (decided Ken, June 19 2026):** A field's privacy is independent of its answer state. The three levels are:
  - **Shareable** (default): value is sent to the AI freely; the AI may use it naturally in any response.
  - **Private** (user-set flag, or `defaultPrivacy: private` in the registry): value **is** sent to the AI — the AI uses it for context — but the AI is instructed not to volunteer it spontaneously. It may appear in a response only if the user explicitly selects one that includes it. This is the correct behavior for sensitive-but-useful facts (phone number, home address, people the user wants the AI to know about without naming unprompted). *Previously, Private incorrectly withheld the value entirely — making it equivalent to Declined. That was wrong: a user who fills in their phone number intends the AI to be able to offer "My number is X" as a response option when asked; the privacy guarantee is that it isn't spoken unless the user chooses it.*
  - **Prefer not to say** (declined field): the user chose not to answer. The AI receives **no** value — only a phrase-around instruction. For information the user does not want the AI to know at all.
  - *The same three levels apply to people in the relationship graph: a non-private person is described freely; a private person's details are sent with "don't bring them up unprompted"; a person the user chose not to add is simply absent.*
- **Gaps-log mechanism:** the no-bracket / phrase-around rule stays, so gaps can't be read off the output. Instead the single combined generation call returns structured `{classification, options, missing_facts}`; `missing_facts` (normalized field keys) feed a gaps log that drives the questionnaire's "suggested next." Regex-on-output is only a fallback. This is **progressive profiling** — the questionnaire fills itself from real conversations.
- **Storage:** user-owned → `worldview.json` in the FSA data folder (localStorage cache), plus static `app/data/worldview-questions.json` registry. Contact info (A5) and beliefs (B5) are **private by default** — the AI receives the values for context but is instructed not to volunteer them; they are only spoken if the user selects a response that includes them.
- **Sequencing guardrail:** keep the Name/About You Settings fields live until the questionnaire UI ships, then migrate legacy `userName`/`userAbout` into `worldview.json` and remove them. Do NOT remove early — it would regress the live app.

**Build order (from the plan doc):** 1) data model + registry → 2) questionnaire UI → 3) LLM integration (structured `missing_facts`, profile block, gaps recording) → 4) migrate + remove Name/About You → 5) expand Tier-B/Tier-C content → 6) RAG-lite.

**Token budget / RAG timing (plan doc §6):** We are *not* close to needing a RAG database. The questionnaire profile is bounded (~1–3K tokens fully answered = 0.1–0.3% of Sonnet 4.6's 1M context; ~a penny per conversation with prompt caching). RAG is driven by *unbounded cross-session memory* (Phase 3), not the structured fields — rough trigger is an always-relevant slice exceeding ~10–20K tokens. `buildBlock()` ships as simple heuristic selection (always-include core + topical facts on-topic; many turns need none); progression is heuristic → client-side embeddings (IndexedDB) → server-side vector store, the last only if cross-session memory becomes a headline feature (awkward in a no-backend app).

**Prompt caching — never pad to reach the cache floor; capture caching by prefix structure instead (decided Ken, June 15 2026).** Ken asked whether padding a prompt to the cache minimum is worthwhile. **No.** (1) The minimum is in **tokens, not characters**, and is **model-specific**: **2048 tokens** for Sonnet 4.6 / Fable 5, **4096 tokens** for Opus 4.x, 1024 for Sonnet 4.5 and older. "2048 characters" ≈ ~500 tokens — far below the floor, so it would silently fail to cache anyway (`cache_creation_input_tokens: 0`, no error). (2) Padding with filler is a **net loss**: you pay the write premium (1.25× at the 5-min TTL) and then 0.1× per read on inert tokens that carry no value — spending tokens to "save" on tokens. Below the floor the prefix is small and already cheap to send uncached, so there's nothing meaningful to save; the floor exists precisely because the savings wouldn't matter. (3) For *this* app the natural stable prefix (the §9 instruction block + `worldview.buildBlock()` + `relationships.buildBlock()`) **likely clears 2048 tokens on its own once a real profile is filled** — and empty-profile conversations, the only ones under the floor, are exactly the cheap ones where caching wouldn't help. So there is no gap worth padding. **The real lever is structural, not padding:** caching is a strict prefix match, so to capture it we must (a) put the *stable* part first (instructions + worldview + relationships) with a `cache_control` breakpoint, and (b) move the *per-turn* engine request-context to the **end** (the `messages` array), out of the system prompt. **TODO — when caching is wired:** `llm.generateResponses` currently interpolates the engine request-context (`sequence_stack`, `phase`, `last_user_utterance` — changes every turn) **into the system prompt**, which would silently invalidate the cache on every turn. Move it out of `system` before adding `cache_control`. Pairs naturally with the multi-vendor abstraction (cache mechanics — `cache_control`, the token floors, the `usage.cache_*` fields — are provider-specific). Not built; cost-optimization task, not a Phase-1 blocker.

**Decisions resolved (Ken, June 14 2026) — these scope Build Step 1:**
1. **First-cut Tier-A set: FULL.** Author all Concrete-Core fields from the question bank now (not a smaller starter). Plus a starter of Tier-C interests (per build order).
2. **Contact info (A5): STRICT phrase-around for now.** Stored but never volunteered or shared in output. *Note / TODO:* add a per-field "assistant may share if I pick it" sharing control in a later build.
3. **Chunk size: NO FIXED CHUNK.** No enforced per-session stopping point; user answers as many or as few as they like each sitting. (The "suggested next" from the gaps log still guides ordering.)
4. **Tier-B beliefs (B5): DEFERRED** to a later build.
5. **Symbol/picture answers + supporter-assisted mode: LATER.** Build Step 1 supports typed/selected text answers only.

Also decided this session: the **type-and-speak composer** ("In My Own Words", § "Free Composition + Virtual Keyboard") is built alongside Build Step 1 as the manual worldview test pathway. See that section.

**Next-session kickoff prompt:**
> We're continuing the AI-Driven AAC worldview questionnaire work. Read the "Worldview Questionnaire (June 2026)" section of CLAUDE.md and the two design docs in this folder: `Worldview-Questionnaire-Draft1.docx` (question bank) and `Worldview-Implementation-Plan.docx` (build-ready plan). Two phases this session.
>
> **Phase 1 — Resolve the open decisions first, with me.** Plan doc §11 lists five decisions to confirm before building: (1) size of the first-cut Tier-A set (full vs. smaller starter); (2) contact info (A5) — per-field "assistant may share if I pick it" toggle vs. strict phrase-around for now; (3) target chunk size (questions per session); (4) whether Tier-B beliefs (B5) ships in this build or waits; (5) symbol/picture answers + supporter-assisted mode now or later. Use AskUserQuestion, recommend a default for each, and record my answers in this CLAUDE.md section.
>
> **Phase 2 — Build Step 1: data model + question registry** (plan §2–3). Create `app/data/worldview-questions.json` — the static registry (modules → fields with key, question text, type, answer options, placeholder synonyms `fills`, `sensitive`, `defaultPrivacy`, follow-ups), authored from the question-bank doc, scoped to whatever first-cut set we agreed in Phase 1 (Tier-A core + a starter of Tier-C interests at minimum). Create `app/js/worldview.js` — load/save `worldview.json` to the FSA data folder (follow `storage.js` patterns) with a localStorage cache; get/set/decline/reset fields; the three field states (unanswered = key absent / answered / declined = sticky); gaps add/list/clear; and `buildBlock()` returning the compact profile text for LLM injection (simple heuristic selection, no vector store — plan §6).
>
> Guardrails: do NOT touch the interim Name/About You Settings fields (they stay until Build Step 4). Honor declined/private at buildBlock time (never volunteer; phrase around). Gaps come from structured `missing_facts` in the generation response, not regex (plan §4) — that wiring is Build Step 3; for now just build the gaps add/list/clear API. Verify in the preview that worldview.js round-trips `worldview.json` through the data folder (set/decline/reset, buildBlock output, gaps add/list) before stopping. No commit or deploy unless I ask.

---

## "In My Own Words" — Free Composition + Virtual Keyboard (June 2026) — decided, not yet built

Free composition is one of the **invariants** (Configuration Model): the user must always have a path to type/compose something the AI did not generate and have the app speak it in the selected voice. The UI's **composer-access region** (UI-Design.docx four-region layout) is where this lives. "In my own words" is the user-facing name for that path.

**Why this surfaced now:** to test the worldview Tier model (and any response shaping) by hand before the AI generation path exercises it, we need a manual way to compose an utterance and speak it. So the simple composer is no longer just a far-future nicety — it's the **test harness for worldview output** and is wanted alongside Build Step 1.

### Settled decisions

- **The keyboard is the easy part; voice + return-to-UI are essentially free.** Any keyboard (OS or in-app) just deposits text into a focused field in the page. The app then speaks it via the existing `tts.speak()` path (already uses the app-selected voice), and the app UI was never actually left. No integration risk on the "speak in the app's voice" / "return to the app" requirements regardless of keyboard choice.

- **OS / browser keyboard cannot be reliably summoned, and is a black box — so it is a *fallback*, not the foundation.** Technical reality on a Windows tablet (Edge/Chrome):
  - There is **no web API to programmatically launch the Windows touch keyboard.** It auto-appears only when an editable element (`input`/`textarea`/`contenteditable`) receives focus *and* the device is in a touch context. "Invoke from a button" therefore means "focus a text field and hope the OS pops the keyboard."
  - On a Surface with the type cover attached (laptop mode) it often won't auto-pop at all. Behavior varies by tablet mode, Windows version, and browser.
  - The Chromium **VirtualKeyboard API** (`navigator.virtualKeyboard.show()`) only manages an already-available keyboard's overlay; it still requires focus on an editable element. Not a reliable on-demand "open system keyboard" switch.
  - It gives **zero control** over layout, key size, spacing, prediction, or access method — exactly the things that matter for a CP user's motor accessibility.

- **The real path is an in-app virtual keyboard.** It is the only option that:
  1. **Honors access-method independence** (engine emits a palette descriptor; renderers present it — direct select now, scanning / eye-gaze later). An OS keyboard can't participate in the future scanning renderer; an in-app keyboard *is* just another renderer.
  2. **Unlocks AI-assisted composition** — type a few words → expand into a full sentence *in the user's voice* (worldview model); personalized word/phrase prediction. The OS keyboard can touch none of this.
  3. Keeps visual + behavioral consistency (key sizing, triple-coding, dark mode, latency dots).

### Build order (decided)

1. **Type-and-speak composer — DONE (June 14 2026).** Added an "In your own words:" region to `index.html` (textarea + Speak + Clear), `ui.js` helpers (`onSpeakClick`/`onClearComposerClick`/`getComposerText`/`clearComposer`), and `handleSpeakComposed()` in `app.js` → `tts.speak()` in the selected voice. MVP behavior: speaks the composed text, guards empty/whitespace input, returns status to Listening/Ready. Works with whatever keyboard the OS offers (and a hardware keyboard in dev). This is the MVP realization of the free-composition invariant. A "use system keyboard" affordance was not added (and must never be depended on for summoning the OS keyboard). **Deferred sub-decision — RESOLVED (Ken, Pass 14, June 21 2026), v0.3.19: tapping Speak IS taking the floor, so it behaves like selecting a response.** Previously the composer only spoke the text and left the partner's turn open, recording still running. Now `handleSpeakComposed()` mirrors `handleMoveSelected`: it stops the filler ladder, invalidates in-flight generation (`generationToken++`), **stops recording** (`stt.stopListening()`), captures + clears the partner's `currentPartnerText`, speaks, **terminates the partner's statement** (`engine.selectMove({text})` pops the partner FPP → floor OPEN, records `lastUserUtterance` so Say-again works on it), clears the stale palette + composer, **commits the exchange to history** (`commitExchange(raw, text, -1)` — partner turn cleaned + logged, then the composed user turn), and **resumes listening iff auto-resume is armed** via the shared `resumeOrIdle()` (same `manualListenArmed && loadAutoRelisten()` gate as a palette pick — so it only re-listens when the user has manually started at least once this session). `commitExchange` now logs `allOptions: []` for index < 0 (free-composed / repair utterances aren't palette picks, so no stale palette is attributed to them). Verified in preview: `selectMove` pops a seeded partner INVITATION FPP (floor self→open, stack emptied); clicking Speak clears the composer + `#responseOptions`, runs the idle branch of `resumeOrIdle` (listening not armed), no console errors. *Mic-stop and the armed-resume branch need the tablet mic to confirm live.*
**"Reframe" — a second verb on the composer (proposed Ken, June 21 2026; one-shot version BUILT v0.3.20).** In addition to **Speak** (say the box text verbatim), a **Reframe** action hands the box text to the AI as *steering/context* and regenerates the suggested responses around it — "give me new options taking this into account." The box is dual-purpose: same input, two verbs.
   - **BUILT (v0.3.20):** `index.html` adds `#reframeBtn` between Speak and Clear (placeholder updated to name both verbs); `ui.onReframeClick`; `keyboard.js` `KEEP_OPEN_CONTROLS` includes `#reframeBtn` (so the on-screen keyboard doesn't hide when it's tapped — same fix as Speak/Clear). `llm.generateResponses` takes an optional `opts.steer` string → a `steerBlock` appended to the system prompt that frames the text as "additional context AND direction… being user-authored, they are TRUE and override the 'keep it general' caution" (so it is exactly the channel for supplying the specifics the v0.3.18 anti-fabrication rule forbids the model from inventing). `app.js` `handleReframe()` mirrors `handleRegenerate`: guards to an active partner turn with a palette (`if (!currentPartnerText || !lastPalette.length)`), re-runs generation with `{ steer }`, refreshes ONLY the palette via `engine.refreshPalette` (stack/mode/floor untouched — no duplicate FPP), and **clears the box on success** (kept on error for retry). Verified in preview: button renders (v0.3.20); the guard fires with no API call and keeps the box; a stubbed call confirms the steer text + guidance framing + user-authored-TRUE override reach the system prompt and the result parses. *Full success path with a real captured turn + live API needs the tablet.*
   - **Why it's strong / where it fits:** it's the channel by which the user *injects ground truth the AI lacks*, so it's the natural complement to the v0.3.18 anti-fabrication guardrail — type "I actually beat Tyler at chess last night" → Reframe → the AI can now build options around a *true* event instead of being held to the general. (A lightweight, in-the-moment version of the future episodic-events idea, thought #2a.) The text may carry content ("I'm worried about the cost") and/or direction ("keep it short, lean toward declining"); frame the box to the model as "the user's guidance for how to respond / what to convey" — no need to make the user pick a mode.
   - **Mechanism — ~80% already built (it's a *guided* regenerate).** Reuse the v0.3.16 "Show me different options" seam: re-run `llm.generateResponses` for the SAME partner turn with an optional `steer` string (alongside the existing `avoid`), then refresh ONLY the palette via `engine.refreshPalette` (stack / mode / floor untouched — no duplicate FPP). Regenerate *is* "Reframe with empty guidance," so the two can share one code path. Touches `llm.js` (a `steer` opt in the prompt), `app.js` (a `handleReframe` modeled on `handleRegenerate`), `ui.js` + `index.html` (the Reframe button).
   - **DECIDED — one-shot, not sticky (Ken, June 21 2026).** The steer applies to *this* regeneration only, then is discarded — it is NOT carried into subsequent turns. **Clear the box after Reframe** (Ken's call): both Speak and Reframe *consume* the box, so an empty box reliably means "nothing pending." Keeping the text would falsely signal that the steer is still in effect (sticky). (Unlike the earlier thought of keeping it to iterate — consistency of "empty = no pending action" wins.)
   - **The sticky/persistent version is the conversation-goal control — deferred to the Goals subsystem.** A steer that *persists* across turns is literally the **conversation goal** layer of the three-layer Goals model (see *Conversation & Relationship Goals*). That version needs a way to *see* and *clear* the standing steer and rules for layering with relationship/disposition goals — build it there, not here. **[→ folds into the Conversation & Relationship Goals build as "Reframe = the mid-conversation conversation-goal control."]**
   - **Phase:** the one-shot Reframe is **Phase 1, near-term** — no situational awareness or RAG needed, slots onto the regenerate plumbing, patch-level. **Smaller things to pin at build time:** button label ("Reframe" is Ken's word; "Steer options" an alt — must be unmistakably distinct from Speak for the target user); the no-partner-turn case (first cut can guard like regenerate's `if (!currentPartnerText || !lastPalette.length) return`, or later shape openers in INITIATING).

2. **In-app virtual keyboard (later).** A real renderer: on-screen keys sized for direct select, integrated with the app's look and the future access-method renderers.

   **Keyboard-selection design — BUILT & verified (June 14 2026); awaiting Ken's usability review on the test tablet.** Shipped: `app/js/keyboard.js` (the shared on-screen keyboard — digit row + QWERTY + shift/caps, space, backspace, Enter, and `, . ' -`; keys act on `pointerdown` + `preventDefault` so the target field keeps focus and the caret never moves; inserts at the caret and dispatches `input`; Enter saves single-line fields / inserts newline in the composer textarea). Settings → General radio "Keyboard for typing" (`index.html`), wired in `app.js` `openSettings`/`saveSettings`; `storage.loadKeyboardMode`/`saveKeyboardMode` (default `physical`); CSS for the panel + radios; `keyboard.init()` + `setMode(loadKeyboardMode())` at app init; sw precache + CACHE_VERSION → `aac-v0.2.6`. A `MutationObserver` on `#worldviewContent` sets `inputmode="none"` on dynamically-built `.wv-text` cards so the Windows keyboard is suppressed before first focus. Verified in preview (synthetic `focusin`, since the harness window isn't system-focused): onscreen mode shows the keyboard + suppresses the OS keyboard and types into both the composer and worldview fields; physical mode never shows it and leaves `inputmode` empty; Settings radio round-trips; no console errors. *Build details below remain the authoritative description.*

   **Keyboard-selection design — original spec (Ken, June 14 2026).** Motivating problem: on a Surface, Windows shows *no* on-screen keyboard when the type cover is attached in laptop position, but auto-pops its own keyboard when the cover is folded back or detached. We want the app's keyboard in that second case. **Detection finding:** the browser exposes *no* reliable signal for physical-keyboard attachment or Surface type-cover posture — `navigator.devicePosture` only reports foldable-*screen* state (`folded`/`continuous`), the Keyboard API reports layout not attachment, and there is no `isKeyboardAttached`. The only *effective-posture* inferences are reactive (watch `visualViewport`/VirtualKeyboard `geometrychange` to see whether Windows pops its keyboard on focus) or one-way (a trusted hardware `keydown` proves a keyboard exists; silence proves nothing). **Ken's decision: do NOT auto-detect or fall back. A user-set Settings parameter decides, and it drives behavior until the user explicitly changes it** ("won't be an issue in practice"). Spec:
   - **Setting:** Settings → General, two radio buttons "Keyboard for typing": ① *My physical keyboard* (default) ② *On-screen keyboard (app's own)*. Stored in `aac_settings` as `keyboardMode: 'physical' | 'onscreen'` via new `storage.js` `loadKeyboardMode()`/`saveKeyboardMode()` (default `'physical'`).
   - **Behavior:** `physical` → app renders no keyboard, inputs keep normal `inputmode` (if the OS pops its keyboard when folded, that's the OS; the user owns that choice). `onscreen` → in-scope text fields get `inputmode="none"` (the reliable Edge/Chrome switch that suppresses the Windows keyboard) and focusing one shows the app's own virtual keyboard. Read at field-focus time so a Settings change takes effect on the next field without reload.
   - **Scope:** the "In your own words" composer + the "About Me" questionnaire inputs, built as one shared keyboard component. Settings' own fields (API key) stay on OS/physical for now (Setup-tier, rare, often supporter-entered).
   - **Dependency / phasing:** the parameter is the easy ~10%; the virtual-keyboard *renderer* is the bulk and is this build-order item #2. The two MUST ship together — a setting that suppresses the OS keyboard and then shows nothing is a regression. Build = (a) `storage.js` accessors, (b) Settings radio group + wiring + CACHE_VERSION bump, (c) the shared app keyboard component (accessible key sizing for CP motor needs, inserts into focused field, Speak/Enter consistent with composer, dark mode, future scanning-renderer compatible), (d) composer + worldview focus handlers that summon/dismiss it. *Not yet started — awaiting Ken's go-ahead to build.*
3. **AI-assisted composition (later, after worldview lands).** Few-words → full-sentence expansion in the user's voice; personalized prediction.

Open sub-questions deferred to build time: where the composer sits relative to the move palette (screen allocation — already an open UI question), and whether the type-and-speak composer should also push the spoken utterance into conversation history like a selected response.

## Auto-update on launch / Start (June 14 2026), v0.2.10

**Problem (Ken):** the app didn't pick up new deployments on launch or when Start was pressed — the old "Ctrl+Shift+R dance." Two causes: (a) the network-first worker's `fetch(request)` still hit the **browser HTTP cache** (GitHub Pages serves `Cache-Control: max-age=600`), so a launch within 10 min of a deploy got stale assets; (b) the registration never rolled a redeployed worker into the running page — a new `sw.js` installed but the page kept the version it launched with.

**Fix (three parts):**
1. **`sw.js` revalidates.** Runtime fetch is `fetch(new Request(request, { cache: 'no-cache' }))` (ETag revalidation — can't serve stale within the max-age window). Precache `addAll` uses `new Request(u, { cache: 'reload' })` so the precached shell is fresh too. (`self.skipWaiting()` on install + `clients.claim()` on activate were already there.)
2. **Auto-reload on new worker (`index.html`).** A `controllerchange` listener reloads the page once when a freshly-activated worker takes control. Guarded with `hadController` (skip the first-ever install, which has no prior controller) and a `reloading` flag (no loop). `reg.update()` is called on `load` to check for a new worker every launch.
3. **Update check on Start (`app.js` `handleStart`).** `navigator.serviceWorker.getRegistration().then(r => r && r.update())` — a cheap no-op when nothing's new; if a new worker is found it activates and the `controllerchange` handler reloads. Covers the "left open, redeploy, press Start" case.

APP_VERSION / CACHE_VERSION → `0.2.10`. **Verified in preview:** worker registers + activates + controls the page, version 0.2.10 in About, no console errors, **no reload loop** on localhost (controllerchange doesn't fire after the first claim when nothing's new). *The redeploy→auto-reload path itself can't be exercised in the preview (needs a real new deployment) — confirm on the test tablet that a fresh deploy now appears on relaunch without a hard refresh.*

## "What's new" post-update notice (July 4 2026), v0.5.62

The companion to the auto-update above (Ken — modeled on the Keyguard Designer web app's system): because the app silently reloads itself into a new version, the user never sees *what* changed. After an auto-update, the freshly-loaded build now shows a short, plain-language "What's new" summary of everything that changed since the version they last saw.

**Architecture (four parts, mirroring Keyguard):**
1. **`CHANGELOG.md`** (project root) — the **user-facing** changelog, the single source of truth for the notices. Keyed by `## Version X.Y.Z` headings + a `## Unreleased (next release)` section. Entries are plain "what you can see or do differently" prose — engineering/tooling changes stay out (that's what this CLAUDE.md version table is for). **This is separate from and coarser than the version table:** not every patch needs a user-facing entry, and one entry can describe a change that shipped across several patches.
2. **`scripts/apply-release-notes.mjs`** (trigger: "apply release notes") — parses `CHANGELOG.md` → injects a `const RELEASE_NOTES = {…}` object (keyed by version string) into `app/js/whats-new.js` between `@@RELEASE_NOTES_START/END@@` markers. Reads `APP_VERSION` from `app/js/app.js`; maps `## Unreleased` → `APP_VERSION` (so a dev build can preview pending notes). **The notes are BUNDLED, not fetched** — the notice must work offline / on locked-down networks, and the freshly-loaded app already *is* the new version, so it carries its own notes. Run it after every CHANGELOG.md edit; do not hand-edit the marker block.
3. **`app/js/whats-new.js`** — `compareVersions` (semver), `collectWhatsNew(since, current)` (flat combined list, newest version first — Ken's choice), `renderWhatsNewPanel`, and `maybeShowWhatsNew(current)`. **Placement (Ken, v0.5.63 — moved out of a modal):** the announcement renders **inline into the pre-start block that overlays the transcript region** (`#whatsNewPanel`), not as a top-layer dialog — it sits in the transcript space above the centered Start button. A "Got it" button collapses the panel, leaving the Start button centered. Blue `--slot-initiative` accent card (a friendly sibling of `confirmDanger`).
4. **Storage** — `storage.loadLastSeenVersion`/`saveLastSeenVersion` in `aac_settings.lastSeenVersion`.

**Placement — Start + What's-new in the transcript region (Ken, v0.5.63).** The old full-screen `#startOverlay` (dark scrim + big centered Start) is gone. The Start button now lives in **`#startBlock`**, absolutely positioned to fill the (empty, pre-conversation) **transcript region** and flex-centered, so Start is centered in that space; the What's-new panel renders above it when an update just landed. `main.disabled` no longer dims via opacity on `main` (that would dim the Start block too, since opacity can't be un-inherited) — it blocks pointer-events and dims only the *sibling* regions (Command Bar, responses, status, dock), leaving the transcript crisp and the Start block tappable (`pointer-events: auto`). `handleStart` hides `#startBlock` (was `#startOverlay`) and the transcript becomes the live log. The Settings → About "See what's new" button was **removed** (Ken) — the notice is now purely the post-update, pre-start announcement.

**Gating (the "can't announce itself" rule):** `maybeShowWhatsNew` runs once at app load (after `initApp()`, independent of STT). If `lastSeenVersion` is **null** (brand-new install, or an app old enough to predate this feature) it just records the current version silently — the version that *introduces* the notice cannot show itself. Otherwise, if the running version is newer than `lastSeenVersion`, it collects every note in between, records the current version *before* rendering (so a reload can't re-trigger), and shows the panel.

APP_VERSION / CACHE_VERSION → **`0.5.62`** (feature) → **`0.5.63`** (inline-in-transcript placement); `whats-new.js` added to the sw precache. Also switched `.claude/launch.json` to `autoPort` (dropped the hardcoded `-l 3000`) so parallel sessions don't collide on the port. **Verified in preview (v0.5.63):** boots clean, no console errors; the Start button fills+centers in the transcript region (both axes when no panel); seeding `lastSeenVersion` older + reload renders the What's-new card inline above Start with the correct flat/newest-first 5-note list and "updated itself to version 0.5.63" intro, Start stays visible/centered-X, "Got it" collapses the card and Start recenters; the no-update path shows just the centered Start; pressing Start hides the block and enables main; the About "See what's new" button is gone. *Caveat: verifying via a real auto-update on the tablet double-loads (the SW `controllerchange` reload lands after the first render), which is correct in practice — the panel shows once the SW settles; only rapid manual `lastSeenVersion` tests need the SW already current to avoid the double-load consuming the one-shot.*

## App virtual keyboard — refinements + open design questions (June 14 2026), v0.2.11

Built (Ken's list) on top of the shared keyboard component (`app/js/keyboard.js`, `styles.css`):

1. **Keys 25% taller** — `.kbd-key min-height` 3.2rem → **4rem** (64px verified). `body.kbd-open` field clearance bumped 19rem → 23rem.
2. **Numbers + special characters on a separate page.** Two layouts: `LETTER_ROWS` (QWERTY letters + ⇧ + ⌫ + a bottom row of `123` toggle, comma, space, period, ↵) and `SYMBOL_ROWS` (digits row, two rows of specials `@#$%&*()-+` / `!?'":;/=_~`, bottom row `ABC` toggle + space + ⌫ + ↵). A `page` state + a `'page'` action key flip between them; `renderRows()` rebuilds (pointerdown handler is delegated on the root so swapping inner rows is safe). **Comma, period, space, backspace stay on the letters page per Ken;** space/backspace/enter are *also* on the symbols page because editing is impossible without them (flag if Ken wants them stripped to a strict letters-only home).
3. **Shift = one-shot, double-tap = lock.** New state machine: `'off' | 'shift' | 'lock'`. Single tap toggles off↔shift; a **double tap within 300 ms** (`SHIFT_DOUBLE_TAP_MS`) engages caps **lock**. One-shot `'shift'` auto-reverts to `'off'` after one character (`consumeShift()`); `'lock'` persists until shift is tapped again. Visuals: `.kbd-shift-on` (lighter blue, one-shot) vs `.kbd-caps` (solid dark, locked).

**NOTE — Enter key (Ken, to discuss).** `enter()` currently does newline-in-textarea / save-in-single-line. Ken flagged that **Enter may need to behave differently per context** (newline vs. save vs. speak). TODO comment left in `keyboard.js enter()`. Not yet designed. *(The "formal close/done key" half of this note is DONE — see the Hide button, v0.2.18.)*

**Hide (close) button — DONE (v0.2.18).** The toolbar above the keys now has a **Hide ⌄** button (right-aligned, dark) that dismisses the keyboard while leaving any open panel — notably **Settings** — in place (Ken: "have the keyboard close and just have the settings panel open"). `keyboard.js dismiss()`: ends preview mode, `hide()`s, and **blurs the field** so a later tap re-opens it. Works from both the typing context (API field) and the Speech-tab layout preview. Verified: Hide closes the keyboard with Settings still open and the field blurred; re-focusing the field reopens it; no console errors.

**Hide must not close About Me — DONE (v0.2.24).** Tapping the keyboard's Hide button closed the About Me screen. Cause (confirmed by hit-test): Hide runs on `pointerdown` and hides the keyboard, which frees its reserved space; the About Me header reflows to full width and the **Done** button slides into the exact spot the Hide button occupied — so the tap's trailing **click** lands on Done → `close()`. Fix: `keyboard.js dismiss()` calls `suppressNextClick()` — a one-shot capturing `click` listener that swallows the ghost click (auto-removed on first click or after 400 ms). Verified: the ghost click targeted `worldviewCloseBtn` and was `defaultPrevented`; About Me stayed open, keyboard hid; a later legitimate Done click still closes it. (General gotcha: hiding the keyboard on pointerdown reflows the page, so anything that slides under the pointer can catch the trailing click.)

**Reshow on tapping a layout setting — DONE (v0.2.19).** Tapping any keyboard-layout control (side/bottom layout selects, side-position slider) reshows the keyboard if it was hidden via Hide. The trigger is **`onpointerdown`** (not just `onfocus`), because the common case is re-tapping a select that *still has focus* after Hide — where no `focus` event fires. pointerdown fires on every tap regardless of focus state; the bottom select previews the bottom dock, the side controls the side dock.

**RESOLVED — keyboard layout (Ken, June 14 2026): Alphabetical only, QWERTY dropped.** The target users aren't touch-typists, so QWERTY had no value; **A–Z order is easiest to scan.** Shipped as the single layout (`LETTER_ROWS` = a–i / j–r / s–z+⌫ / ⇧·123·,·space·.·↵). **No layout Settings option yet** — other layouts (e.g. frequency-based) are a **later phase**, at which point layout becomes a user setting; the rows stay pure data so adding one is data, not code. (Matrix/grid layouts remain the natural future fit for scanning/eye-gaze renderers, but that's later.)

**RESOLVED — keyboard docking is CONTEXT-based, not layout- or (yet) orientation-based (Ken, June 14 2026).** Correction to the earlier note: **don't tie docking to layout.** The dock follows the *screen*, not the device orientation (for now):
- **Side dock** — **Settings & About Me** (those screens compete for *horizontal* space and want full height). Implemented: full-height right-side column, `width: min(26rem, 50vw)`; the About Me content reserves `padding-right` to clear it. (Currently the app keyboard is in scope only for About Me's `.wv-text`; Settings still uses the OS keyboard pending the open question below, but is grouped here for when/if it switches.)
- **Bottom dock** — **during conversations** (the composer; competes for *vertical* space). Implemented: full-width bottom strip, `main`/composer reserves `padding-bottom`.
- Mechanism: `dockFor(field)` in `keyboard.js` (`.wv-text` → side, else bottom) sets `kbd-dock-side`/`kbd-dock-bottom` on the root + `body`; CSS positions the panel and reserves the right axis of clearance per dock. Left-vs-right side is a future config knob.
- **Dynamic / orientation-aware docking (switch by landscape vs. portrait) is a LATER refinement**, not built. (The original "scarce axis rotates with the device" reasoning still holds for that future step.)
- **Access-method framing:** dock placement is just a **renderer** choice over the same key set — consistent with the engine-emits-descriptor / renderer-presents invariant.

**RESOLVED — Settings uses the app keyboard, side-docked (Ken, June 14 2026), v0.2.14.** The Settings API-key field is now in the keyboard's `IN_SCOPE` (`#apiKeyInput`), so onscreen mode sets `inputmode="none"` (Windows keyboard suppressed) and the app's own keyboard appears, **side-docked** (`dockFor` returns side for `#settingsDialog` fields). **Top-layer fix:** the Settings dialog is a `showModal()` dialog (top layer, makes normal-flow content inert), so a body-appended keyboard would render behind it and be dead. `show()` therefore **reparents the keyboard into the open dialog** (`field.closest('dialog[open]')`) so it shares the dialog's top layer and stays interactive; it reparents back to `<body>` for the composer/worldview. The keyboard is `position:fixed` and the dialog sets no containing block (drag uses `left/top`, not `transform`) and `overflow:hidden` doesn't clip fixed descendants, so it still docks to the screen edge. *Trade-off accepted:* the app keyboard loses OS copy/paste/autofill for the API key — Ken chose the app keyboard anyway (a paste affordance could be added later). *Overlap:* a centered dialog and the side keyboard overlap (the field's far side sits under the keys); the **draggable panel** clears it (the reason it was made draggable). Auto-repositioning the dialog clear of the keyboard is a possible later polish; not done (a `transform` shift would break the fixed keyboard's edge docking, so any auto-shift must use `left`/`margin`).

This also **moots the old "panel + Windows keyboard can't both fit vertically" issue** — there's no Windows keyboard in Settings anymore, and the side dock leaves the panel its full height. (The constant panel size from v0.2.11, `#settingsDialog { height: min(30rem, calc(100vh - 3rem)) }`, stays.)

**Auto-nudge the panel clear of the keyboard — now CSS-driven (v0.2.15, reworked v0.2.20).** When a **side** keyboard is showing, the centred Settings panel re-centres into the free region beside it. **v0.2.20** moved this from JS (`updateSettingsNudge`, removed) to **pure CSS keyed off the keyboard's `body` classes** — `body.kbd-open.kbd-dock-side.kbd-side-right #settingsDialog { right: min(26rem,46vw); max-width: … }` and the `.kbd-side-left` mirror. Why: the JS version keyed off the *slider position* and only ran on open/mode/slider change, so after **previewing a bottom keyboard then switching to side** the nudge went stale (Ken's repro). CSS keyed off the actual keyboard state is automatically correct — nudges only when a side keyboard is really shown, on the right side, and **not** for a bottom keyboard or a hidden one. The **`max-width: min(560px, calc(100vw - min(26rem,46vw) - 2rem))`** cap fixes the keyboard-on-left case that pushed the panel's right edge **off-screen** (it only bit when `viewport − keyboard < panel width`, i.e. windowed/narrow — hence intermittent). A **dragged** panel keeps its inline `left/top` (drag wins). Verified on the narrow (812px) preview: bottom-preview → panel centred (no nudge); side-left → panel right & on-screen; side-right → panel left & on-screen.

**Settings panel + keyboard — three fixes (v0.2.21, from Ken's 7-step repro).**
1. **No keyboard race on tab switch.** A modal `<dialog>` autofocuses its first focusable control — the API-key field — which popped the keyboard in *real-typing* mode on open; tapping the Speech & Input tab then raced a blur/hide against the preview-show, so the keyboard could vanish. Fix: `#settingsHeader` is `tabindex="-1"` and `openSettings()` focuses it after `showModal()`, so the dialog opens with **no keyboard**; it appears only when a field or a layout setting is tapped. (The harness couldn't reproduce the race — its window isn't OS-focused so the autofocus never popped the keyboard there — but removing the autofocus removes the root cause.)
2. **Bottom keyboard pushes the panel up.** A bottom keyboard covers the lower screen, so `body.kbd-open.kbd-dock-bottom #settingsDialog { top: 1rem; bottom: auto; max-height: calc(52vh - 1.5rem) }` pins the panel to the top region (was left centred, overlapping the keys).
3. **Dragged panel re-snaps on keyboard change.** Dragging pins the panel with inline `left/top` (overriding the CSS nudge), so a later side switch overlapped. `keyboard.js setDock()` now dispatches a `kbd-dock-change` event when the effective dock/side actually changes; `app.js` `resetSettingsPosition()` clears the inline drag (`settingsDragged` flag) on that event, on Close, and on cancel — so the panel re-snaps to the auto-position that clears the keyboard. Drag still works *within* a stable keyboard state.

Verified in preview: open → header focused, no keyboard; tap API field → keyboard shows + typing works; bottom layout → panel pinned top, clears the keys; drag then switch to a side layout → inline position cleared, panel re-snaps clear of the side keyboard; no console errors.

**About Me content width (v0.2.20 → reverted v0.2.22).** History: cards were full-viewport-width; v0.2.20 capped to a 40rem centred column to make them "thinner." **v0.2.22:** Ken wanted the questions to **fill the available width** (a 40rem column left big empty margins on a wide window, and didn't fill the space beside the keyboard). Removed the `max-width`/`margin:auto` cap — `#worldviewContent` now fills its container minus a reasonable side margin (`padding: 1.25rem 2rem 3rem`). Because the side-keyboard reservation lives on **`#worldviewScreen` padding** (= `min(26rem,46vw)`, the keyboard width), the content box is already "window − keyboard", so the questions fill **all** the non-keyboard space automatically (and the full window when no keyboard is shown) — no JS measurement needed. Verified at 1600px: no keyboard → content 0–1600 (cards ~1521); keyboard-left → content 416–1600; keyboard-right → content 0–1184.

**Scrollbar — gap + width (v0.2.23, Ken).** The scroll container's right-edge scrollbar hugged a **right-side** keyboard (its right margin is *padding*, inside the scrollbar), so reaching for it risked hitting a key. Fix: the keyboard-on-right `#worldviewScreen` reservation gets an extra **`+ 1.5rem`** so the scroll container (and its scrollbar) sits a gap away from the keys (keyboard-on-left is unaffected — scrollbar stays far right). Verified gap = 24px between scrollbar and keyboard. **Wide scrollbars:** `#worldviewContent`/`#settingsContent` `::-webkit-scrollbar { width: 22px }` with a high-contrast thumb — Chromium-only (the Surface browsers), far easier to grab for limited-motor-control users. Tunable.

**Layout live-preview on the Speech & Input tab (v0.2.17).** Problem (Ken): the side keyboard vanished when switching to the Speech & Input tab — exactly when you want to watch it as you change layouts — because the keyboard is tied to a focused text field and that tab has none (only selects + the slider). Fix: a **preview mode** in `keyboard.js` (`previewing` flag + `previewShow(dock)`/`previewHide()`): shows the keyboard with no `activeField` (keys are inert), hosted in the open Settings dialog (top layer). The `focusout` handler returns early while `previewing` so blurring the API field to click a tab doesn't hide it. `app.js`: activating the Speech & Input tab (in onscreen mode) calls `previewShow('side')`; focusing/changing a layout select previews that dock (`previewShow('bottom'|'side')`) and re-renders; leaving the tab or `setMode('physical')` calls `previewHide()`. **Cleanup gotcha:** the dialog `close` event proved unreliable here (fired by neither programmatic `close()` nor observed), so dismissal cleanup (clear nudge + `previewHide`) is done **explicitly in the Close handler** and via a **`cancel`** listener for Escape — not the `close` event. Verified in preview: keyboard stays on the Speech tab and updates on layout/side change; hides on tab-away, Close, and cancel/Escape; real API-field typing still works.

**Cut / Copy / Paste toolbar (v0.2.15).** A persistent `.kbd-toolbar` (Cut, Copy, Paste) sits above the keys, **layout-independent** (built once in `keyboard.js` `build()`; `renderRows()` clears only `.kbd-row` so the toolbar survives layout/page switches). Buttons act on `pointerdown`+`preventDefault` (field keeps focus/selection) → `handleTool()` uses the async Clipboard API: copy/cut `writeText(selection)` (cut also deletes the selection); paste `readText()` → `insert()` at the caret. This restores the clipboard affordances the OS keyboard would have given — **paste in particular for the long `sk-ant-…` API key**. *Clipboard needs a focused document + user gesture (both true in real use); it's blocked in the unfocused preview harness, so cut/copy/paste are verified for wiring (Cut removes the selection) but the actual clipboard round-trip must be confirmed on the tablet.*

APP_VERSION / CACHE_VERSION → `0.2.11`. **Verified in preview** (on-screen mode, composer focused): keys 64px tall; letters page has no digit row + a `123` toggle; symbols page shows digits + specials + `ABC`/space/⌫/↵; one-shot shift capitalizes one letter then reverts (`Ab`); double-tap engages caps lock (`CD` stays upper); single tap (after >300 ms) unlocks; page toggles both ways; no console errors.

### Selectable layouts + window-aware docking (v0.2.13)

The twenty layouts from `Keyboard-Layout-Options.docx` are now **implemented as data and user-selectable**.
- **`app/js/keyboard-layouts.js`** — all 20 layouts (S1–S10 side, B1–B10 bottom) encoded as rows of typed cells (`char`/`space`/`action`/`blank`/`pred`), each cell carrying a `span` (= flex weight, so any row width fills the keyboard and wide keys keep their proportions). Plus a per-dock `SYMBOLS` page (side 5-wide / bottom 10-wide) for the 123 key, and ordered `SIDE_LAYOUTS`/`BOTTOM_LAYOUTS` lists for the menus. **Layout is data, not code** — adding one (e.g. a future frequency layout) is a new entry here.
- **`keyboard.js`** — renders the selected layout for the current dock; `renderRows()` is span-aware and renders `blank`/`pred` cells as inert fillers. New API: `setSideLayout`/`setBottomLayout`/`setSideDockPosition`. The side dock now positions left **or** right (`kbd-side-left`/`kbd-side-right`).
- **Three Settings (Speech & Input tab):** "Bottom-docked keyboard layout" (select, default B1), "Side-docked keyboard layout" (select, default S1), and a binary **Left/Right slider** for the side dock (default Right). Stored in `aac_settings` (`bottomLayout`/`sideLayout`/`sideDockPosition`); `storage.js` accessors; live-applied on change and persisted on Save; set on init.
- **Docking is still context-fixed for now:** conversation → bottom, About Me → side (Ken: revisit conditions after more conversation-UI design).

**Layout-independent fixes (the v0.2.12 side-dock problems Ken flagged):**
- **Side dock fills its column** — rows `flex:1` (removed the `justify-content:center` that left empty bands above/below). Keys size from row height. Fixes wasted vertical space + tiny keys.
- **About Me uses all remaining horizontal space** — removed `#worldviewContent`'s `max-width:800px`/centering; it now fills the width, and only the keyboard's docked side is reserved (`padding-left`/`-right`).
- **Conversation field stays visible** — bottom dock caps at `48vh` and the content reserves `padding-bottom:50vh`, so the focused composer scrolls clear above the keys (verified: composer bottom above keyboard top).
- **Both docks are window-size aware** (full-screen or windowed): key heights `clamp(2.2rem,7.5vh,4rem)`, side width `min(26rem,46vw)`, bottom cap `48vh`, reservations in `vh`/`vw`.

APP_VERSION / CACHE_VERSION → `0.2.13`. **Verified in preview:** Settings shows the 3 controls (10+10 options, slider); selecting B3 renders the 3-row 13-wide bottom keyboard; About Me side dock fills the column height and renders S1, content reserves the keyboard side and otherwise fills width; switching to S6 + Left re-renders the 11-row layout docked left with the reservation flipped; composer stays above the bottom keyboard; no console errors. *Touch + real-window behavior to confirm on the tablet.*

## Settings Panel — usability pass (June 14 2026), v0.2.8

Six Settings-panel improvements shipped (Ken's list), modeled on the Keyguard Designer web app's settings panel (`keyguard-designer-web/app.html`, the second working directory). *(Superseded in v0.2.16: the **Save button was removed** — every control now applies AND persists immediately; the "Save persists / Close keeps for the session" wording in items 1 and 3 below is historical.)*

1. **"Cancel" → "Close".** Footer button relabeled (`index.html`). Behavior unchanged — it dismisses without persisting; **Save** persists.
2. **Draggable modal.** Added a dark title-bar `#settingsHeader` ("Settings" + faint "drag to move" hint) as the drag handle. `initSettingsDrag()` in `app.js` (adapted from Keyguard's `mousedown` drag, but using **pointer events** + `setPointerCapture`): on first drag it converts the native `<dialog>`'s UA centering to pixel `left/top` with `margin:0`, then tracks the pointer clamped to the viewport. Stays modal — the backdrop still blocks the app behind it — but can be moved aside. Position persists for the session.
3. **Live-apply of UI/behavior settings while the modal is open.** Settings that change app behavior now take effect on `change`, not only on Save, so (with the panel dragged aside) the effect is visible/audible immediately — the pattern Keyguard uses. Wired in `openSettings()` via `.onchange`: voice (`tts.setVoice`), silence threshold (`stt.setSilenceThreshold`), keyboard mode (`keyboard.setMode`). Save persists to storage; Close keeps the live changes for the session. This is the **foundation for future UI-customization settings** Ken flagged as coming.
4. **Point-release version in About.** `APP_VERSION` constant in `app.js` (currently `0.2.8`) injected into `#aboutVersion`; About now reads "Version 0.2.8 — Phase 1 Proof of Concept". **Bump `APP_VERSION` alongside `sw.js` CACHE_VERSION every release** — crucial for beta-tester bug reports.
5. **Scrolling panel.** Dialog is a flex **column** (header + footer pinned via `flex-shrink:0`, `#settingsContent` scrolling via `flex:1; min-height:0` on `#settingsLayout`, `max-height:calc(100vh - 3rem)`). **Two gotchas (the v0.2.8→0.2.9 fix below):** (a) `display:flex` on a `<dialog>` **drops the UA's auto-centering** (computed margin → 0, pins top-left) — restored with `inset:0; margin:auto` on the base `dialog`. (b) Putting `display:flex` on the *base* `dialog` selector also **overrode the UA's `dialog:not([open]) { display:none }`** (author origin beats UA regardless of specificity), so the panel was CSS-visible at all times — it showed at startup *and* "Close" appeared to do nothing (the button handlers are wired in `openSettings()`, which never ran, and `.close()` is a no-op on a not-truly-open dialog). **Fix:** scope the flex layout to **`dialog[open]`** so a closed dialog stays `display:none`.
6. **General tab shortened (Ken's call: "add Speech + Conversation tabs").** Tabs are now **General** (API Key, About You pointer, Data Folder) / **Speech & Input** (Voice, Keyboard for typing) / **Conversation** (silence period, auto-resume, + the former Placeholders delays folded in) / **About**. The standalone Placeholders tab is gone. Tab-switching JS is generic (`data-tab`), so no handler changes were needed.

**Touch drag:** `#settingsHeader` has `touch-action: none` so the browser doesn't claim the touch gesture for scroll/pan (without it a touch drag moved the panel only a few mm).

CACHE_VERSION → `aac-v0.2.9`; APP_VERSION → `0.2.9`. **Verified in preview:** panel hidden at startup (`display:none`, not in DOM box); opens to `display:flex` via the button; **Close actually closes** (`open:false`, `display:none`); tabs distribute correctly; version 0.2.9 in About; drag moves precisely and clamps to viewport edges; centered on default open; long (Conversation) tab scrolls on a short viewport with footer pinned; no console errors. *Note:* drag + `touch-action` tested via synthetic PointerEvents (harness window isn't OS-focused and synthetic events bypass `touch-action`) — **confirm real touch drag on the test tablet.**

## Versioning

Format: **major.minor.patch** (e.g., `0.3.8`). All pre-release versions use major `0`.

**When to bump which digit (agreed Ken, June 18 2026):**
- **Patch (third digit)** — the default for almost everything: bug fixes, refinements to existing behavior, and small self-contained features that *extend* the existing feature set. Mental test: "Is this changing or fixing how an existing capability works?" → patch.
- **Minor (second digit)** — reserved for a substantial **new architectural layer or subsystem** that later work builds on (e.g. `0.2` continuous partner capture; `0.3.0` the Conversation Engine). The bar is deliberately high: "new layer," not just "new feature" — the worldview questionnaire and relationship graph shipped as `0.2.x` patches because they slotted into the existing app rather than restructuring it. Roughly aligns with crossing a Phase boundary or adding a Phase's defining capability. Mental test: "Did this add a genuinely new subsystem the app didn't have before?" → minor.
- **Major (`1.0` and beyond)** — **Ken's call alone** (Ken, June 18 2026), based on maturity and readiness. Claude Code does not make the 0.x→1.0 transition without Ken explicitly directing it.

**Release process — the PC is dev, GitHub is release (Ken, July 4 2026; formalized in [RELEASING.md](RELEASING.md)).** This **supersedes** the earlier "commit and push every completed step" rule — testers are coming online, so we are now conservative about what reaches GitHub. The full ritual lives in `RELEASING.md`; the essentials:
- **Commit to local `main` = save work; push `main` = release.** A GitHub Actions workflow deploys `app/` to Pages on **every** push to `main`, so **any** push (even docs-only) reaches users. Between releases Claude **commits each change locally but does NOT push** — local commits sync/back up via OneDrive. There is one branch (`main`); no `dev` branch.
- **Changelog-as-you-go.** Every likely-**user-visible** change gets a plain-English bullet under `## Unreleased (next release)` in `CHANGELOG.md`, **in the same commit as the code**, written the way a *target user* reads it (not engineering language). `CHANGELOG.md` is the single source of truth for the in-app "What's new" notes (via `scripts/apply-release-notes.mjs`).
- **Pre-bump.** At the end of each release the dev copy is pre-bumped to the **next patch** (`APP_VERSION` + `CACHE_VERSION`), so the dev About screen always reads higher than the last public release and you know you're on a dev build. It's provisional (local, unpushed); the **final digit is decided at release**.
- **Trigger phrase "bump Conversant"** (Ken only, after he's verified `CHANGELOG.md`) runs the whole ritual **through the push** with no second confirmation: finalize the digit → rename `## Unreleased` to `## Version <final>` → regenerate the bundled notes → add the version-table row below → commit → push (deploys) → pre-bump the next cycle. The CLAUDE.md version table is **released-versions-only** (rows added at bump, not during dev).

Phase-to-version mapping (update as releases are tagged):

| Version | Phase | Notes |
|---------|-------|-------|
| 0.1     | 1     | Core conversation loop proof of concept |
| 0.2     | 1     | Continuous partner capture; persistent "Please repeat" control; auto-resume-listening setting |
| 0.2.8   | 1     | Settings panel usability pass: Close label, draggable modal, live-apply settings, point-release version in About, scrolling panel, General tab split into Speech & Input + Conversation tabs |
| 0.2.9   | 1     | Fix v0.2.8 regression: dialog flex layout scoped to [open] so the panel no longer shows at startup and Close works; touch-action:none on the drag handle for full-distance touch drags |
| 0.2.10  | 1     | Auto-update on launch/Start: SW revalidates (no-cache) to beat the GitHub Pages HTTP cache; controllerchange auto-reload rolls in a redeployed worker; update check on Start |
| 0.2.11  | 1     | App keyboard: 25%-taller keys, numbers/specials moved to a separate page, one-shot shift with double-tap caps lock; Settings panel fixed to a constant size per tab |
| 0.2.12  | 1     | App keyboard: dropped QWERTY for an Alphabetical layout (only layout for now); context-based docking — side dock for About Me/Settings, bottom dock for conversation |
| 0.2.13  | 1     | App keyboard: 20 selectable layouts (data-driven) + Settings (side/bottom layout selects, left/right slider); side dock fills its column, About Me fills width, conversation field stays above the keyboard, both docks window-aware |
| 0.2.14  | 1     | Settings panel uses the app keyboard (side-docked) instead of the Windows keyboard; keyboard reparents into the modal dialog's top layer so it stays interactive over the modal |
| 0.2.15  | 1     | Auto-nudge the Settings panel clear of the side keyboard (opposite side); layout-independent Cut/Copy/Paste toolbar above the keyboard |
| 0.2.16  | 1     | Removed the Settings Save button — every control applies AND persists immediately (live test bench, esp. for side-dock keyboard layouts); Close just dismisses |
| 0.2.17  | 1     | Settings keyboard-layout live preview: the keyboard stays shown on the Speech & Input tab (no focused field) and re-renders as you change layout/side, so layouts can be tried without it vanishing |
| 0.2.18  | 1     | App keyboard Hide button (toolbar) — dismisses the keyboard while leaving the Settings panel open; blurs the field so a later tap reopens it |
| 0.2.19  | 1     | Tapping a keyboard-layout setting reshows the keyboard if hidden — pointerdown trigger covers re-tapping an already-focused select (no focus event) |
| 0.2.20  | 1     | Fix Settings nudge with keyboard on left (CSS-driven off body classes; width-capped so it can't go off-screen; no stale nudge after a bottom-keyboard preview); About Me content is a readable centred column that clears the keyboard on either side |
| 0.2.21  | 1     | Settings+keyboard fixes: no API-field autofocus (no keyboard race on tab switch); bottom keyboard pushes the panel up; a dragged panel re-snaps clear when the keyboard dock/side changes |
| 0.2.22  | 1     | About Me questions fill the available width (removed the 40rem cap) — full window with no keyboard, and all the non-keyboard space beside a side keyboard |
| 0.2.23  | 1     | Gap between the About Me scrollbar and a right-side keyboard (scrollbar no longer hugs the keys); wide (22px) high-contrast scrollbars for limited-motor-control users |
| 0.2.24  | 1     | Fix: tapping keyboard Hide no longer closes About Me — swallow the ghost click that landed on the Done button after the keyboard's space was freed and Done slid under the pointer |
| 0.2.25  | 1     | Fix: a `worldview.json` copied into the data folder from another PC is now respected — the file in the connected folder is the source of truth (no cache-vs-disk timestamp contest); the cache is promoted only when no file exists on disk yet |
| 0.2.26  | 1     | About Me prompts for a data folder when none is assigned — a banner with a "Choose data folder" button (fresh user gesture for the FSA picker) so answers persist to the portable file instead of browser-only cache |
| 0.2.27  | 1     | Relationship graph: people/relationships moved to their own `relationships.json` model (nodes + edges) with a People editor in About Me, LLM injection (private people withheld), and a one-time migration of the former worldview "People" module (A3) into the graph |
| 0.2.28  | 1     | Removed the in-app A3→graph migration (single user, little data — re-enter via the People editor); old A3 answers stay orphaned/unused in worldview.json |
| 0.2.29  | 1     | People editor relationship field is now a standard grouped list + "Other…" (free text) instead of free text; single relationship per person for now |
| 0.2.30  | 1     | Destructive actions (Restart, Remove person) now use a distinct red "danger" confirmation modal (`confirmDanger`) instead of native `confirm()`, so they don't look like routine permission popups |
| 0.2.31  | 1     | Removed the "Suggested next" section from the About Me home (those questions are already on the first topic sub-page); gaps subsystem retained in the model |
| 0.2.32  | 1     | Start Listening gating: never auto-listens at startup; auto-resume fires only after the user manually starts listening at least once this session (`manualListenArmed`), and a manual Stop re-arms the requirement |
| 0.3.0   | 1     | Conversation Engine (Phase-1 full mode set: sequence stack, five modes, four-slot move palette, REPAIR-OF-SELF, persistent overrides, §6 filler ladder, §9 combined classify+generate contract) behind a degenerate diagnostic UI; no transcript-confirmation gate (generation fires on the silence period) |
| 0.3.4   | 1     | Test-suite Pass-1 fixes: filler ladder honors the user's Settings (first placeholder lands `initialDelay`s after the partner stops; re-fills every `subsequentDelay`s — was hardcoded 0.8s/1.7s); STT discards its own TTS echo by content match (mic stays on) so placeholder speech no longer renews the partner's turn, delays, or abandons the generated responses |
| 0.3.5   | 1     | Removed response-like acknowledgment tokens ("Okay.", "Right.", "I see.") from the rung-1 filler set — they read as the user's actual reply (e.g. to a greeting), not a stall, and are fragile for echo filtering |
| 0.5.36  | 1     | **On-screen keyboard now serves the Express Panel editor too (Ken, June 28 2026).** Same one-line scope fix as v0.5.34, applied to the other Settings editor: added `#expressEditor input` to `IN_SCOPE` (`keyboard.js`) so tapping a phrase/feeling text box on the Express Panel tab summons the app keyboard when "Keyboard for typing" = On-screen. The partner "Choose a person" control is a `<select>`, not an `<input>`, so it's correctly excluded. Verified in preview: a Express-editor input matches scope, opens the keyboard (hosted in the dialog), and gets `inputmode=none`; no console errors. |
| 0.5.37  | 1     | **Keyboard control row + header buttons sized to the Express Panel controls — Spatial Stability (Ken, June 28 2026).** The keyboard's top control strip (`.kbd-toolbar`) now lays out as FOUR equal ¼-width cells — **Cut / Copy / Paste / Hide** — each matching a `.persist-btn` override (Say again / Hold on / Pardon? / Wind down) hole-for-hole, so one keyguard overlays both. Was: Cut/Copy/Paste at flex-1 sharing the row with a flex-3 prediction reservation + a right-pushed auto-width Hide, so the three tools were ~⅙-width and unaligned. Changes: `.kbd-tool-hide` dropped `margin-left:auto`/`flex:0 0 auto`/extra padding → equal 4th cell; during the "In my own words" modal it's `visibility:hidden` (was `display:none`) so its cell stays RESERVED and Cut/Copy/Paste keep ¼ width + alignment. **Word prediction → interim overlay** (Ken explicitly deferred a dedicated prediction layout — "think further"): `.kbd-preds` is now `position:absolute; inset:0` over the control row, shown only while there's a prefix (`updatePredictions` toggles `.kbd-preds-active`) and `display:none` otherwise so taps fall through to the tools — it neither steals the tool cells' width nor adds a row that would offset the keys from the `#epGrid`. **Header buttons** `#aboutMeBtn`/`#settingsBtn` given `min-height:var(--btn-min-dim)` + `min-width:6rem` + persist-btn padding/font → same size as an Express Panel button. Verified in preview both docks: toolbar ↔ `#epControls` pixel-identical (bottom 1265×40, cells 312px at x7/325/643/961; side 364×40, cells ~87px) hole-for-hole; predictions overlay the same real-estate and clear back to the tools; header buttons 88×40 = an express button; no console errors. **Open (Ken):** whether Hide is still needed (each panel has its own close path) — if it goes, the 4th cell needs a new occupant to keep the 4-hole alignment; and the permanent word-prediction layout. *(Superseded by 0.5.38 — Hide removed, 3 buttons.)* |
| 0.5.38  | 1     | **Keyboard control row → THREE buttons in the left-three Express Panel cells; Hide removed (Ken, June 28 2026).** Ken: we don't need a 4th button as long as the three present (Cut / Copy / Paste) occupy the same real-estate as the **left-three** override buttons. `.kbd-toolbar` is now a `grid-template-columns: repeat(4, 1fr)` (still matching the four `#epControls` ¼-cells); the three tools auto-fill the **left three cells** (= Say again / Hold on / Pardon? holes) and the **4th cell stays empty**. The **Hide button is dropped** (every context the keyboard serves has its own dismissal — the "In my own words" modal: Speak/Reframe/Cancel; Settings: Close; About Me: Done), resolving the v0.5.36/v0.5.37 "is Hide still needed?" question; the `.kbd-tool-hide` / `kbd-no-hide` CSS was removed (the `dismiss()`/`suppressNextClick()` helpers + `handleTool('hide')` branch are left intact and unreferenced, so Hide is trivially re-addable). Word prediction stays the interim overlay (still TBD). Verified in preview both docks: 3 tools occupy the left-three cells hole-for-hole (bottom x7/325/643 ×312px, 4th cell at x961 empty; side x909/1001/1094 ≈87px) with `repeat(4,1fr)`; bottom-dock screenshot shows 3 icon buttons + empty 4th cell + no Hide; no console errors. |
| 0.5.39  | 1     | **Word-prediction DISPLAY dropped for now; infrastructure kept (Ken, June 28 2026).** Ken: drop the on-keyboard prediction buttons for now — several button-size questions need to be worked out first that will influence how word prediction should be brought into the UI. CSS-only change: `.kbd-preds` is now permanently `display:none` (removed the `.kbd-preds-active`/`:not(...)` show/hide toggle), so the prediction overlay never appears even while typing. **Everything else is untouched** — `prediction.js`, `updatePredictions` (still computes predictions + toggles `.kbd-preds-active`), `applyPrediction`, and word-learning on space/enter all keep running, so personalized frequency data still accrues and re-enabling is a small CSS restore (documented inline in both the CSS rule and the `keyboard.js` build comment). Verified in preview: typing "Th" still computes 3 predictions and sets `kbd-preds-active`, but `getComputedStyle(.kbd-preds).display === 'none'` and the toolbar shows only Cut/Copy/Paste (4th cell empty); no console errors. |
| 0.5.53  | 1     | **On-screen keyboard stays up the whole time About Me is open (Ken, June 30 2026).** Opening the About Me tab in Settings now displays the keyboard immediately and keeps it shown until Done — previously it only appeared on field focus, and `renderHome()` deliberately hid it on the topic list (which has no field). New `showDockKeyboard()` helper in `worldview-ui.js` (`keyboard.previewShow(loadKeyboardDock())`, no-op in physical mode) is called from `renderHome()` in place of the old `keyboard.hideKeyboard()`, so the keyboard persists across home↔module navigation; focusing a card field upgrades the preview to a typing keyboard via the existing `focusin` path; `close()` still takes it down and reopens Settings. The dock is reserved on `#worldviewScreen` at all times (v0.5.25), so the keyboard fills the blank dock band rather than covering the topic list. Also: `keyboard.previewShow()` now clears any leftover inline-prediction ghost when entering preview (so a ghost from a card you were typing in doesn't linger over the dock). Verified in preview (on-screen / bottom dock): About Me opens with the keyboard shown, it stays up drilling into a topic (field focused, typing keyboard) and back to "All topics", and Done hides it + reopens Settings; no console errors. |
| 0.5.77  | 1     | **Interrupting the partner captures their partial speech (Ken, July 8 2026).** Cutting in with an instant statement (Express phrase / composer Speak) no longer drops the partner's in-progress speech: new `stt.getCurrentTranscript()` exposes the live accumulated transcript, `app.heardPartnerText()` captures it before stopping the mic, and it's committed (transcript + JSON) interleaved before the interrupting statement. `commitExchange` gained `opts.cleanup`; the interruption path passes `cleanup:false` so the fragment is recorded verbatim (no AI cleanup). Confirmed (no change needed): auto-listen resumes capture after the interruption, and the entire conversation is sent to the AI each turn. Verified in preview with a fake recognizer; live mic needs the tablet. |
| 0.5.76  | 1     | **Conversation-screen feedback batch + log persistence (Ken, July 8 2026).** (1) User statements now append to the transcript AFTER being spoken; the `♪` now-playing line is reserved for system placeholders (`speakUserStatement` flag suppresses it for user speech). (2) **Reframe is floor-aware**: with a partner turn it reworks the responses around the typed steer; with the user holding the floor it generates steering **STATEMENTS** (`llm.generateStatements`, new `SLOT.STATEMENT`, one-per-cell). (3) Selecting a wind-down/closing **re-offers the closings** (incl. "Bye!") so the user can sign off without waiting. (4) **5 new conversation starters** (9 total in `engine.DEFAULT_OPENERS` / `control-phrases.DEFAULTS.openers`); opener/closer palettes pack **2-per-cell when >4** so 8-card mode fills all 8 (`showConversationPalette` caps to `conversationPaletteCap()`). (5) Say again / Hold on / Ask them to repeat **write to the transcript** (`logSpokenUserTurn`). (6) **"In my own words" → compose icon + tooltip** (new UI Layout **Rule 12**: constant buttons are icon-only). (7) **Errors interleave into the conversation JSON** in time order (`storage.logErrorToConversation`, flushed immediately). (8) Bug fix — `resetConversationId()` now clears the log target, so consecutive conversations no longer merge into the first conversation's `<id>.json`. Verified in preview (Python static server + stubbed API/TTS); on-disk JSON writes need a granted FSA folder to confirm on the tablet. |
| 0.5.75  | 1     | **Transcript error indicator — faint-red wash on any logged error (Ken, July 8 2026).** A non-verbal heads-up that the app hit an error mid-conversation (a hiccup the user/partner should expect and pass to support) — no worded message on screen; details still go to the error log. Wired at the single choke point `storage.logError`, so it trips on **thrown** API/parse failures AND the **silent** no-usable-responses paths (empty/malformed `responses[]`, INCOMPLETE classification, superseded generation token) that never throw. `logError` dispatches an `aac-error-logged` event (keeps storage decoupled from the UI); `app.js` listens → `ui.setTranscriptError(true)`; `ui.setTranscriptError` toggles `has-error` on `#transcript`; `showResponses` clears it (sticky-until-next-success), as does conversation reset (Start/End). `.transcript-log.has-error` = faint red bg `rgba(220,53,69,0.08)` + red border, 0.25s fade. The old worded "amber box" (`showResponseError`) left in place for now; CLAUDE.md TODO tracks deciding its disposition before public release. Verified in preview: boots clean; the event trips `has-error` on `#transcript` and the rule computes to the faint-red bg + red border; removing the class restores transparent/grey. |
| 0.5.73  | 1     | **Error log grouped by conversation + Copy bundles the transcript (Ken, July 7 2026).** Follow-up to v0.5.72. `renderErrorLog()` now groups entries by `conversation` (newest conversation first, `(none)` last) instead of a flat chronological list, via `groupErrorsByConversation()`. The Copy button no longer copies the raw textarea — `buildErrorReport()` assembles a full report: for each conversation with errors, its transcript followed by that conversation's errors. The transcript comes from new `storage.readConversationLog(id)` (reads `conversations/<id>.json` back), or, when the id is the still-open conversation (`storage.getConversationId()`), from the live in-memory `conversationHistory` (covers an error that fired before the turn was committed to disk); otherwise "[not available — no data folder / not saved]". So a pasted bug report carries the conversation, not just the error line. Verified in preview: grouping (2 conversations newest-first), the report structure (header + per-conversation transcript/errors), Copy → "Copied ✓"; on-disk transcript needs a granted FSA folder to exercise. |
| 0.5.72  | 1     | **Persistent error log correlated to the conversation (Ken, July 7 2026).** Errors (API 429/529 failures, JSON parse failures, transcript cleanup, repair/reframe/regenerate) previously went only to the now-hidden status bar + console, so an intermittent live-demo failure — most importantly "no response options" — left no trace. New `storage.logError(context, message, extra)` writes each error to a capped (200) localStorage ring buffer AND appends it to `errors.log` in the data folder (FSA `createWritable({keepExistingData:true})` + seek-to-end append), stamped with the app version and a per-conversation id. That id comes from `ensureConversationId()` and is SHARED with the conversation log filename (`<id>.json`) — reset in `terminateConversation` via `storage.resetConversationId()` — so an error matches its conversation (both derive from the same id; second-resolution, same as the existing log filename). Viewer in Settings → About (`#errorLogView` textarea + count + Copy + Clear-with-`confirmDanger`); `renderErrorLog()` populated on Settings open. Catch sites wired: `generateOptions`/`regenerate`/`reframe`/`repairSelf`/`cleanupTranscript`. Generation failures also now surface VISIBLY in the response region via new `ui.showResponseError(msg, onRetry)` (amber dashed box + "Try again"), replacing the blank look. Verified in preview: viewer renders seeded entries with conversation id; logError stamps version + shared/stable-within-conversation id + resets; showResponseError renders + retry fires; Clear opens the danger dialog; no console errors (the data-folder append needs a granted FSA folder to exercise, same path as conversation logging). **Root-cause still open:** the log is the instrument to confirm whether the demo "no options" is API 429/529 (no retry yet) — automatic retry-with-backoff is the likely follow-up fix. |
| 0.5.71  | 1     | **Word-prediction acceptance: tap the field, not a separator (Ken, July 7 2026).** The inline ghost completion is no longer accepted by a word-separator — space / comma / period / Enter now insert literally and dismiss the ghost (`handleKey` in keyboard.js: `learnCurrentWord()` + `clearGhost()` instead of `acceptGhost()`). This fixes the silent, invisible swap where "Yes" + space became "Yesterday" (the user may not even be looking at the box). Acceptance is now an explicit **tap anywhere in the active field** — a new document `pointerdown` handler: when `ghostWord` is showing and the tap's `closest(IN_SCOPE)` is the `activeField`, it `preventDefault()`s (keeps focus, caret stays at the end) and `acceptGhost()`s. The whole field is one big FIXED acceptance target, so there's no moving inline tap target to chase (keyguard-safe) and no silent separator acceptance; the `#predGhost` overlay is `pointer-events:none` so the tap lands on the field. Applies to every predicted field (composer + About Me), so prediction stays acceptable everywhere now that separators don't accept; the only trade-off (Ken's call — "this region won't be used as a typical composition space") is that a tap while a ghost is showing no longer positions the caret. Verified in preview via dispatched `pointerdown` (accepts, `defaultPrevented`) + all separator cases (space/comma/period/Enter insert literally); real trusted-tap needs the device (the preview click tool doesn't drive pointer events through the overlay). No console errors. |
| 0.5.70  | 1     | **Composer keyboard keep-open fix + single-row composer buttons + "Reload the app" button (Ken, July 7 2026).** (1) The real device fix for "keyboard doesn't display for In my own words": `servingPanelOpen()` (keyboard.js) now also treats an open `#composerOverlay` as a keyboard-serving surface, so a stray `focusout` while the composer is open (a reflow, or the auto-resumed mic re-rendering just after the field gains focus) no longer hides the keyboard. This never reproduced in the headless preview because it has no window focus, so no focus events fire — the earlier v0.5.69 `showFor()` summon was necessary but insufficient without this. (2) `#composerResponses` is now a single-row 2-column grid in BOTH docks (dropped the side-dock stacking), so Speak/Reframe/Cancel sit in one horizontal row in side mode like bottom; still mirrors `#responseArea`'s flex split so Cancel = "New 4" width. (3) New Settings → About **"Reload the app"** button (`#reloadAppBtn`) — a keyboard-free Ctrl+Shift+R for a tablet: `reg.update()` + clears all `caches`, then `location.reload()` (network-first SW re-fetches fresh code). Non-destructive (committed exchanges already logged). Verified in preview: single-row both docks, keyboard stays up on stray focusout while composer open (and Cancel still dismisses both), reload button present/wired and clears caches; no console errors. *(Note: the original report that prompted the keyboard digging turned out to be operator error — "Keyboard for typing" was set to Physical — but the keep-open fix is a real robustness improvement kept regardless.)* |
| 0.5.69  | 1     | **Self-started-conversation logging + composer keyboard fixes; composer-button alignment (Ken, July 7 2026).** Three items. (1) `storage.logUserResponse` now lazily calls `startConversationLog()` when no log exists yet (mirroring `logPartnerSpeech`) — a conversation opened by the user (an opener or an Express Panel phrase) had its first turn dropped, and a conversation that never reached a committed partner turn was never written at all; both are now recorded from the first user turn. (2) New `keyboard.showFor(field)` summons the on-screen keyboard deterministically; `openComposer` calls it instead of relying on the `#composerInput` `focusin` side effect, which can be swallowed (field already focused → `.focus()` is a no-op; focus race after an Express phrase auto-resumes listening) — so "In my own words" no longer opens with no keyboard. (3) Composer buttons mirror the response footprint for Spatial Stability in BOTH docks: Speak + Reframe are wrapped in `#composerResponses` (flex:9, reusing `.palette-grid`'s grid + gaps) so they tile the card region (side 2×2 → stacked full-rows; bottom 1×4 → two side-by-side pairs), and Cancel mirrors `#regenerateBtn` exactly (flex:1 1 0, `--btn-min-dim` floor, no padding) = the "New 4" width at every viewport; the overlay's horizontal padding was dropped and its border redrawn as an inset box-shadow so buttons reach the footprint edges. Verified pixel-exact in preview (side + bottom): Speak/Reframe = card region, Cancel = New 4; no console errors. |
| 0.5.68  | 1     | **"What's new" notice layout — fill the transcript, tighter header (Ken, July 4 2026).** The card now fills the ENTIRE transcript control (`#whatsNewPanel` = `flex:1 1 auto; align-self:stretch`; `#startBlock` padding 0). Dropped the "The app updated itself to version X…" intro line and the ✨ graphic. Moved the dismiss button into the header row on the right, relabeled **"Close"**, and exempted it from the global `--btn-min-dim` floor (`#whatsNewPanel .whatsnew-ok { min-height:0 }`, id-qualified to beat the 0,4,1 global rule) so it's short. Net: the scrolling notes list went ~45%→~72% of the card height. Verified in preview: card fills the section (0px delta both axes), header one row (title left / Close right), Close 32px, list 72%, Close dismisses + records seen, no console errors. |
| 0.5.67  | 1     | **"What's new" notice rendered IN the transcript area, not a centered modal (Ken, July 4 2026) — keyguard/Spatial Stability.** The centered top-layer modal could put parts of the dialog (incl. "Got it") under keyguard bars. Now the notice is an inline card in `#whatsNewPanel` inside the pre-start block, which overlays the **Transcript control's footprint** (a keyguard opening), so the whole card + "Got it" stay reachable. Flow: app re-renders post-update (knows the transcript's location) → user presses Start → `whatsNew.pending()` returns notes → `renderPanel()` shows the card + hides `#startBtn`, main stays dimmed → "Got it" → `markSeen` + `finishStart()` (hide start block, enter conversation). No-notice path calls `finishStart()` directly. Replaced the `.whatsnew-dialog` modal with `#whatsNewPanel` card CSS. Verified in preview: panel + "Got it" measured within the transcript bounds, seen recorded only on "Got it", no re-show, no console errors. |
| 0.5.66  | 1     | **"What's new" notice moved to AFTER Start, as a persist-until-"Got it" modal (Ken, July 4 2026).** Was an inline panel in the pre-start block (where the SW auto-update reload could make it flash); now `maybeShowWhatsNew` is called from `handleStart` (after Start, when the page is stable) and renders a top-layer `<dialog>` (`showWhatsNewModal`) that only closes on "Got it" — Escape (`cancel` preventDefault) and backdrop clicks are blocked; `lastSeenVersion` is recorded only on that acknowledgment. Removed the unused `#whatsNewPanel` element + its inline-card CSS (back to `.whatsnew-dialog`). Verified in preview: no modal pre-Start, modal appears on Start (`:modal`), Escape blocked, "Got it" closes + records seen, no re-show after. |
| 0.5.65  | 1     | **Rollback leg of the dry run + "What's new" persists until dismissed (Ken, July 4 2026).** Removed the 0.5.64 test caption (the rollback release). Fixed the announcement disappearing too fast to read: it was marked seen at *render* time, so the service-worker `controllerchange` auto-update reload wiped it (a flash). Now `lastSeenVersion` is recorded only on acknowledgment — "Got it" (`markSeen` in the button handler) or pressing Start (`whatsNew.markSeen(APP_VERSION)` in `handleStart`) — so the panel re-renders across the update reload and stays until dismissed. Verified in preview: panel survives a reload, clears only on Got it/Start, doesn't reappear after. |
| 0.5.64  | 1     | **Release-process dry run + a welcome line under Start (Ken, July 4 2026).** Added a "Ready when you are." caption beneath the Start button on the opening screen — a trivial user-visible change used to exercise the newly-formalized dev→release→"What's new"→rollback loop end to end before testers come online. First release cut via the "bump Conversant" ritual (finalize digit → rename `## Unreleased` → regenerate bundled notes → table row → commit → push). |
| 0.5.63  | 1     | **What's-new notice moved into the transcript space; About "See what's new" button removed (Ken, July 4 2026).** The post-update announcement no longer pops as a full-screen modal — it renders **inline in the pre-start block that overlays the (empty) transcript region**, above the **Start button, which is now centered in that space** (replacing the old full-screen `#startOverlay` scrim). `main.disabled` dims only the sibling regions now (not `main` itself, so the Start block stays crisp/tappable). "Got it" collapses the panel, leaving Start centered. Removed the Settings → About "See what's new in this version" button (and `showWhatsNewForVersion`). Verified in preview: Start centers in the transcript both axes (no update) / stays centered-X with the card above (update), Got-it recenters, Start hides the block + enables main, no console errors. |
| 0.5.62  | 1     | **"What's new" post-update notice (Ken, July 4 2026) — modeled on the Keyguard Designer web app.** After the app auto-updates itself, the freshly-loaded build shows a short, plain-language summary of what changed since the version the user last saw. Four parts: a user-facing `CHANGELOG.md` (single source of truth), a generator `scripts/apply-release-notes.mjs` that bundles it into `app/js/whats-new.js` (BUNDLED not fetched — works offline), the modal + semver logic in `whats-new.js` (flat combined list, newest first), and `storage.lastSeenVersion` gating. Shows once per update; the version that introduces it can't announce itself (null→silent baseline). Also on-demand at Settings → About → "See what's new in this version". Blue-accent top-layer `<dialog>` sibling of `confirmDanger`. Switched `.claude/launch.json` to autoPort. Verified in preview: baseline-silent first load, modal on simulated update with correct flat/newest-first notes, show-once, on-demand button, no console errors. |
| 0.5.60  | 1     | **User-Manual review batch 2 — About Me fixes, UI/manual terminology, in-document comments (Ken, July 3 2026).** Four field requests + the eight Word comments in the User Manual. **Code:** (1) **Decline is now reversible — "Prefer not to say" no longer destroys a saved answer.** `worldview.declineField` stashes the prior value in `prevValue` (never sent to the AI — `getField`/`buildBlock` still see null); new `undeclineField`/`hasStashedAnswer`; the declined card's Undo restores the answer ("Undo — bring my answer back") or re-opens the question if there was none. Fixes the data loss Ken hit (decline → Done-without-Save → answer gone). (2) **Two "Done" buttons disambiguated.** The in-module / People bottom button "Done" → **"‹ Back to topics"** (it returns to the topic list, same as the top "‹ All topics"); the header **"Done"** (closes About Me → Settings) is now the only "Done". (3) **UI↔manual terminology aligned** (request 3 + comment 23): "Minimum gap" → **"Minimum spacing"**, "Optional Responses silence period" → **"Silence period"**, "Initial/Subsequent Placeholder Statement Delay" → **"Initial/Subsequent placeholder delay"**. **Manual (`Conversant-AAC-User-Manual.docx`, OneDrive not git):** comment 25 — `filler`→`placeholder` scrubbed from the **tables** the v0.5.59 pass missed; comment 31 — removed "backchannel" from §7.1 and corrected the phrase-color list to the real defaults (affirm teal / social indigo / pace orange / repair purple / needs pink / continuer gray); comment 20 — §6 intro now enumerates all nine tabs, added a **6.5 Keyguard Design Tab** subsection (About → 6.6), and fixed the §6.3 Speech table (dropped the stale "Text size" row, added "Keyboard separation"); comment 36 — rebuilt the §8 Persistent Buttons table with an **Icon column + all nine Command Bar buttons** (icons rendered from `icons.js` to 88px PNGs via a browser→disk bridge and embedded); comment 38 — filled the empty "Tip" with the family/friends opener-closer tip; "Eight buttons" → "Nine buttons". Comment 26 (placeholder timers reset on partner resume) was already shipped in v0.5.59. **Verified in preview:** boots clean at 0.5.60, no console errors; the four renamed labels present; decline→undecline round-trips (value withheld while declined, restored on undo); About Me shows one "Done" (header) + "‹ Back to topics" (bottom). Manual re-verified: no "filler" anywhere, nine-tab intro, six §6 subsections, §8 table = 3 cols × 9 rows with 9 embedded icons. |
| 0.5.59  | 1     | **User-Manual review batch — code changes (Ken, July 3 2026).** Six program changes from a mixed manual-review list (the remaining items are recorded as To Dos below). (1) **Per-conversation privacy — "Don't save this conversation."** New Command Bar toggle `#privacyBtn` (shield-with-slash `noSave` icon; amber `.private-on` selected state, Rule 6) + a Settings → Conversation default checkbox `#noSaveDefaultInput`. When active the conversation is NOT written to the data folder: `storage.setConversationSaving(false)` makes `startConversationLog`/`logPartnerSpeech`/`logUserResponse` no-op entirely (no file, no turns). Runtime flag `conversationPrivate` seeds from the Settings default (`loadNoSaveDefault`) and re-seeds at every Start/End conversation (`terminateConversation`) so a per-conversation choice never carries over; the button toggles it live. (2) **About Me tab moved below General** in the Settings nav (order: General · About Me · Text Size · Speech & Input · …). (3) **Response-card text size** setting (`responseFontSelect` → `--response-font-scale`, applied to `.response-text`), joining the existing Transcript/Composer/Express scales. (4) **New "Text Size" tab** — all four size selects (Response cards, Transcript, "In my own words", Express Panel) moved off the over-long Speech & Input tab onto their own short tab (Ken chose this over reorganizing in place). (5) **Placeholder timers reset when the partner resumes** after a pause — `stt.init` gained an `onPartnerSpeech` callback fired alongside `resetSilenceTimer` (genuine, non-echo partner content only); `app.handlePartnerResumed` calls `placeholders.stop()` so a scheduled/playing placeholder can't speak over the partner. (6) **"Reset buttons and gaps to default" moved above "Keyboard separation"** on the Speech tab (it doesn't reset that value). Also: **`filler`/`fillers` → `placeholder`/`placeholders`** throughout the app code (comments + the two identifiers `FILLER_WORTHY_ACTIONS`→`PLACEHOLDER_WORTHY_ACTIONS`, `shouldPlayFiller`→`shouldPlayPlaceholder`) and the User Manual — grid-spacer "filler" (keyboard/CSS) is a different sense and kept; and the Express Panel **"Backchannel" category → "Continuer"** (`express-items.js` key `back`→`cont`, label; manual bullet). **Verified in preview** (1280×800): boots clean, no console errors; tab order + Text Size tab + response-font select + privacy button/icon + selected-state color (#b9770e) + Speech-tab reset-above-separation all confirmed; Express Panel renders with the Continuer tint intact. *Live mic behavior (placeholder-reset on resume) and the on-disk no-save gate need the tablet.* |
| 0.5.52  | 1     | **"Keyboard separation" setting (Ken, June 30 2026).** New Settings → Speech & Input slider (`dockSepSlider`, storage `dockSepPos`, default 0) controls the gap BETWEEN the dock (keyboard / Express Panel) and the rest of the UI, independent of the inter-button `--grid-gap`. New `--dock-sep` CSS var (set in `applyButtonSizing()`, 0–4rem); it pushes only the MAIN content away from the dock — the dock keeps its edge footprint, so the keyguard holes don't move. Applied on every surface that reserves the dock: conversation main (`#conversationMain` padding side / `#dockArea` margin-top bottom), `#settingsDialog`, and `#worldviewScreen`, both side and bottom docks. **Deliberately excluded from the "Reset buttons and gaps to default" button** (Ken — it's an independent layout preference, not button sizing). Verified in preview: gap applies on bottom (dock margin-top) + side (content padding) + Settings + About Me; persists + restores on init; reset leaves it untouched; no console errors. |
| 0.5.51  | 1     | **Bug-list batch (Ken, June 29 2026).** Eleven fixes/features from a field-report list. (1) Backspace no longer leaves an inline word-prediction ghost that reads as a "selected" suffix — backspace now `clearGhost()`s and doesn't re-predict until typing forward (`keyboard.js`). (2) Composer **Speak / Reframe** now fold a showing prediction ghost into the text first (`keyboard.acceptPendingGhost()` called in `handleSpeakComposed`/`handleReframe`) — "What is your n" + ghost "name" → speaks "…name". (3) **Settings gear** redrawn as a crisp filled Material cog (the stroked Feather gear read as a blob). (4) **"Reset buttons and gaps to default"** button (Settings → Speech & Input) → `storage.resetButtonSizing()`. (5) **− / + stepper buttons** flank the three size sliders for precise control (the slider "grew uncontrollably" when dragged right); slider remains for coarse moves. (6) Removed the prediction strip from bottom layout **B10** (display dropped in v0.5.39 anyway). (7) **Keyboard layouts renamed** to "Side Keyboard 1…10" / "Bottom Keyboard 1…11" (the old descriptive names no longer matched the grids). (8) "New 4" regenerate **tooltip → "New 8"** when Suggestions-per-category = 2 (`ui.setRegenerateLabel`). (9) **Done from About Me returns to the Settings panel** (`worldview-ui` `onClose` hook → `openSettings`). (10) **Settings tab padding** restored (`.tab-panel.active { padding: max(--grid-gap, 0.6rem) }`; Express + About excluded) — it was 0 at the default gap. (11) **4 default openers** to match the 4 closings + 4 reserved Response-Palette cells (editor already redefines all). (12) **Selected response now appears in the transcript immediately** — `commitExchange` renders the user turn at once and cleans the partner transcript in the background (was awaiting the cleanup round-trip first, lagging the user's words several seconds). (13) **Per-surface text-size settings** for the Transcript, the "In my own words" composer, and the Express Panel buttons (CSS `*-font-scale` vars; Settings selects). *Not done (answered, not built):* generating 32 responses to cache the unshown ones — LLM output is serial, so 32 ≈ 8× the response-generation latency, which would delay the first 4; keep generating 4/8 and regenerate on demand. Verified in preview: boots clean, no console errors; steppers/reset/font-scales/regenerate-label/layout-names/B10/gear all confirmed. *Live on-screen-keyboard ghost behavior needs the tablet (focus events don't fire in the headless preview).* |
| 0.5.50  | 1     | **Conversation layout solver — bottom dock (Ken, June 30 2026).** Adds the bottom-dock branch to the v0.5.49 solver (same model, axes swapped): the dock's expandable axis is VERTICAL, so **button-size grow makes the dock taller** (`--conv-dock-h`), the **transcript yields vertically** to its 3rem floor, and **command (10vh) + response (30vh) stay fixed** (they have no free direction to grow). Bottom CSS: transcript `flex:1` with a min-height floor; command/response fixed `flex:0 0 10vh/30vh`. Shrink (button-size left) grows the gap, same as side. Verified at 1280×800 bottom: default 30/10/30/30 unchanged; button-size max → dock 30%→55% (436px), transcript →44px (3rem floor), command/response unchanged; keyboard congruent (reads the same `--dock-h`); no console errors. Same first-cut approximations as side (calibration, gap-grows-the-dock, aspect-ratio, min-gap dock inset) remain to tune. |
| 0.5.49  | 1     | **Conversation layout solver — side dock, FIRST CUT (Ken, June 30 2026).** Wires the three sliders into the side-dock layout per Ken's change-rules (priority min-gap › button-size › gap-size). New **"Minimum gap" slider** (Settings → Speech & Input; storage `minGapPos`, default 0). `applyButtonSizing()` is now a JS solver (runs on init / settings change / window resize) that computes region sizes + the effective gap into CSS vars (`--conv-dock-w`, `--conv-transcript-v`, `--grid-gap`, `--gap-min`); the side-dock CSS consumes them (dock width = `--conv-dock-w`; transcript height = `--conv-transcript-v`; command:response keep 1:6 of the rest). **Button-size slider default = MIDDLE** (= the % budget); **right grows** (dock widens → main narrows; transcript shrinks to a 3rem floor → command/response grow vertically), **left shrinks** (buttons fill less, gap grows). **Gap** floored at min-gap; **min-gap** raises the gap and the gap slider follows (one-way/sticky — lowering min-gap leaves the gap). Verified at 1280×800 side: default 30/30/10/60 unchanged; button-size max → dock 30%→82%W, transcript →44px (3rem floor), command/response grow V; min-gap 40 → gap+slider follow to 8px, then lower min-gap → gap stays (sticky); no console errors. **FIRST CUT / pending (Ken to react + next steps):** the BOTTOM-dock solver (still fixed % default; button-size inert there); calibrating the slider's right end to the exact last-yielder-at-floor point; the precise shrink curve, freed-space split (currently command:response 1:6), per-button aspect-ratio restoration, and insetting the fixed dock by min-gap — all reasonable approximations now, to tune against what Ken sees. |
| 0.5.48  | 1     | **Bottom-dock default percentage layout + outer-padding spec (Ken, June 30 2026).** Mirrors v0.5.46 for the bottom dock per Ken's spec: dock = **30% HEIGHT, full width** at the bottom (`body.conv-bottom { --dock-h: 30vh }`, replacing the button-size-driven height); the remaining 70% height splits **transcript 30 / command bar 10 / response palette 30** of the viewport (3:1:3). Command bar 8 buttons ⅛ width; response palette **1×4** cards (each 90%/4 width, full height) + "New 4" 10% (9:1). The side block was generalized to cover both docks (shared per-component fill rules; dock-specific budget + dock size). Also wired the **outer padding around the cluster to `--gap-min`** (the main-area side; insetting the fixed dock still deferred with the min-gap slider). gap/min-gap still default 0 (flush); button-size/gap CHANGE behavior still deferred (sliders inert for the conversation layout). Verified at 1280×800 bottom: transcript/command/response/dock = 30/10/30/30 (y 0/240/320/560, full width), 8 command buttons ⅛ width, response 1×4 (cards 90% + New-4 10%), keyboard congruent with the dock (1280×240 @ y560), no console errors. |
| 0.5.47  | 1     | **Dock surround padding → ½·gap (Ken, June 29 2026).** The dock's inner panel (`#expressPanel` + the keyboard `#appKeyboard`) had a hardcoded `padding: 0.5rem` around the button grid (leftover from before the gap model); per spec the dock's internal surround is **½·gap**, so at the gap=0 default the buttons sit flush to the dock edges. Keyboard kept congruent (bottom keeps its safe-area inset). |
| 0.5.46  | 1     | **Side-dock default percentage layout (Ken, June 30 2026) — first step of the layout-budget approach.** Implements Ken's explicit side-dock spec as the static default (at gap = 0, min-gap = 0; the button-size/gap/min-gap CHANGE rules are still deferred, so those sliders don't drive the side dock yet). **Regions:** Dock = **30% width, full height** (`body.conv-side { --dock-w: 30vw }`, replacing the v0.5.43 button-size-driven width); the remaining 70% width splits vertically **transcript 30% / command bar 10% / response palette 60%** via `flex: 3/1/6` (auto-absorbs gaps). **Command bar** is now its OWN region (moved out of `#transcriptSection` in `index.html` so it has an independent vertical budget) — 8 buttons each ⅛ width × full region height. **Response palette:** cards take 90% width (the 2×2 grid, each card 50% height) + the "New 4" button 10% width × full height (`flex: 9/1`). Components surrounded by ½·gap, separated by 1·gap. **Default gap → 0** (flush; `GAP_MIN_REM` 0, `DEFAULT_BTN_GAP_POS` 0) — borders still separate buttons. **Bottom dock untouched** (the command-bar HTML move is mode-agnostic; verified bottom still renders: full-width command bar region + bottom dock strip). Verified at 1280×800 side-right: dockArea 384×800 (30%), transcript 896×240, command 896×80, response 896×480 (30/10/60), 8 command buttons ⅛ width, palette 89%/New-4 11%, keyboard congruent at 30% width, no console errors. *Known/expected: the button-size & gap sliders are inert for the side dock until the change-rules land; at gap = 0 the Settings/About-Me surface paddings (which share `--grid-gap`) are also 0.* |
| 0.5.45  | 1     | **Fix: Express Panel buttons overflowed the dock and fell off (Ken, June 29 2026).** Regression from v0.5.44: the global `button { min-height: var(--btn-min-dim) }` floor excluded the keyboard keys (`.kbd-key`) but NOT the Express Panel buttons (`.ep-btn`). So Express buttons were pinned to the button-size minimum while the keys shrank to fit — and when `rows × button-size` exceeded the constrained dock extent (vertical for a side dock; worst at large button sizes), the Express grid overflowed and the last row ("In my own words") fell off the bottom, offsetting every button above it. The keyboard fit because its keys shrink. **Fix:** treat `.ep-btn` exactly like `.kbd-key` — excluded from the global floor (`:not(.ep-btn)`) and given `min-height: 0` so the row stretch drives the height and the grid always fits the dock (text clips via the existing `overflow:hidden`); removed the stale `body.conv-bottom .ep-btn` clamp. Principle (Ken): side docks are vertically constrained / bottom docks horizontally constrained; the Express Panel maps 1:1 onto the keyboard with no location properties of its own, and the keyboard's keys can never fall off — so the Express buttons can't either; button size reaches the dock grid through the **dock size** (which grows with `--btn-min-dim`), never a per-button min. Verified in preview at MAX button size: side dock 470×850 — Express ↔ keyboard pixel-congruent (9 rows), "In my own words" fully visible (bottom 843 ≤ 850 vh, was overflowing); bottom dock congruent (4 rows); no console errors. |
| 0.5.44  | 1     | **Button size + gap apply to EVERY surface (Ken, June 29 2026).** Ken's definition: a "button" is anything tappable with an action (so the response cards, the "New 4" button, composer buttons, and the Settings / About Me controls are all buttons), and the gap is also the *padding* around non-button elements (a keyguard rail surrounds the transcript too), so **gap applies to any UI element**. Implemented: (1) a global rule — `button:not(.kbd-key):not(.kbd-tool):not(.kbd-pred-btn) { min-height: var(--btn-min-dim) }` plus `.icon-btn { min-width: var(--btn-min-dim) }` — so every button on every surface is at least the button size (icon buttons square-ish); the **"New 4"** regenerate button width → `var(--btn-min-dim)`. (2) `--grid-gap` extended beyond the dock to the conversation main area (region gaps + outer padding), the response area / palette / stacked-card gaps, the Command Bar, and the overlay **surface paddings** (`#settingsContent`, `.dialog-actions`, `#worldviewContent`). **One technical exception (Ken acknowledged the risk):** the on-screen-keyboard **keys** are sized BY the dock band (itself derived from `--btn-min-dim`), so a hard min there would overflow the fixed dock — they scale through the dock, not a min. **Boundary I drew (flag to Ken):** fine typographic spacing inside Settings/About Me (e.g. `.setting-group` margins, chip-row gaps) was left as-is — squeezing those to the gap value hurts readability with no keyguard benefit (those overlays are the keyguard's "open window", not under it). At the default gap (0.4rem) the overlay paddings read tight; the gap slider tunes it. Verified in preview: command bar / Settings Close / Paste / regenerate all = button size (58px at ~4rem); surface paddings = the gap; boots clean, no console errors. |
| 0.5.43  | 1     | **Button sizing reworked — button size is authoritative, gap expands the dock (Ken, June 29 2026).** Fixes how the v0.5.41 sliders behaved. The dock was `max(floor, count×size+gaps)`, and the `min(26rem,46vw)` floor masked the setting on side layouts (few columns → never exceeded the floor), so raising button size did nothing visible and raising the gap stole width from the buttons. **Revised model (Ken's):** the dock's scarce axis is now sized DIRECTLY as `count × --btn-min-dim + gaps + pad` (floor removed, still capped at 70vw/72vh). So **button size is authoritative** (sets the actual key width in a side dock / key height in a bottom dock) and raising it grows the dock along its expandable axis — side dock **widens** (Settings/main area shrinks + reflows), bottom dock grows **taller** (transcript shrinks). Raising the **gap** now also grows the dock the same direction and **never shrinks the buttons** (verified: side cell width 95px→95px as gap went max). Setting **renamed "Minimum button size" → "Button size"**; default recalibrated (range 2.75–6.5rem, default ≈4rem so a 5-wide side dock ≈ the old width). **Command Bar gap** now tracks the spacing setting too (`#listenControls` → `var(--grid-gap)`; verified 5.8px→20.4px). *Known caveat: a side dock is full-height (Spatial Stability), so its WIDTH responds fully but vertical gap still comes out of key height (no room to grow vertically); the dominant axis per dock behaves correctly.* Verified in preview: side dock 299→512px (button size max), →571px + Settings 768→709px (gap max, button width preserved); bottom dock 241→412px (button size); no console errors. |
| 0.5.42  | 1     | **Inline word prediction — ghost completion, accept on any separator (Ken, June 29 2026).** Word prediction returns to the UI as an **inline ghost** instead of a button row (which had no keyguard-safe home): as the user types on the on-screen keyboard, the single best completion appears **inline, right after their text**, drawn **bold** in a tunable color (`--pred-ghost-color`, default a high-contrast blue) on a subtle tint (`--pred-ghost-bg`) — bold + tint mark it provisional WITHOUT relying on low contrast (legible for low vision; bold also relaxes WCAG to 3:1). **Accept by typing any word-separator** (space / comma / period / `? ! ; :` / Enter / etc. — anything outside the predictor's `[A-Za-z']` word set): the separator completes the ghost word, then is itself inserted (space→space, comma→`,`), so there's **no moving tap target** (keyguard-safe, no contenteditable). Implemented in `keyboard.js` as a **non-interactive mirror overlay** (`#predGhost`): it copies the field's font/padding/border and renders the typed text transparent (reserving exact width) + the styled suffix, positioned over the field so the real text shows through as one line; shown only when the **caret is at the end** of the value (true appending) so nothing follows the ghost. Scope: all on-screen-keyboard fields except the API key; capitalization matches the typed prefix; the old hidden prediction-button infrastructure (`prediction.js`, learning on space/enter) is reused. Verified in preview: "Th"→bold "e" (case-matched "The"); space accepts → "The "; "ca"+comma → "The can,"; ghost box aligns to the field exactly; mirrored text transparent; clears on blur/field-end; no console errors. *Single-line inputs that overflow horizontally can clip the ghost past the right edge (accepted limitation); the composer textarea wraps so it's fine. Physical-keyboard typists don't get the ghost (the on-screen keyboard drives accept-on-separator) — a possible later extension.* |
| 0.5.41  | 1     | **"Minimum button size" + "Button spacing" settings (Ken, June 29 2026).** Two **unitless sliders** (Settings → Speech & Input) let the user scale touch targets for motor control / finger size and set the inter-button gap (= the bar width on a 3D-printed keyguard). Both drive the existing `--btn-min-dim` token + a new `--grid-gap` token; slider positions (0–100) map to rem in `app.js applyButtonSizing()` so the user picks by visual "feel" (the web can't read physical screen size, so abstract is the honest unit). Default positions reproduce the historical 2.75rem / 0.4rem exactly. **Dock grows from the grid (pure CSS):** `--dock-w`/`--dock-h` are now `min(cap, max(floor, calc(rows/cols × --btn-min-dim + gaps + pad)))` — bottom dock grows **taller**, side dock grows **wider**, exactly enough that every key / Express cell clears the minimum; JS sets `--kbd-rows`/`--kbd-cols` for the active layout. Since every consumer (keyboard, Express Panel, Settings & About Me dock reservations) reads those tokens, all stay aligned under one keyguard, and the Express Panel (shared grid) is covered by the same calc. **The transcript yields** the squeeze (a 3rem floor; shrinks in height for a bottom dock, width for a side dock) so buttons win; caps (72vh / 70vw) keep the main area usable at extreme sizes. The rendered dock gaps (`#epGrid`/`.ep-row`/`#appKeyboard`/`.kbd-row`) now reference `var(--grid-gap)` so the gap is real, not just in the formula. Setup-tier (changing either moves the keyguard holes). Verified in preview (1280×800, rem=14.6px): defaults unchanged; bottom dock floor 384→438 (mid)→576 (72vh cap); side dock floor→653 (large)→capped; command buttons grow to 4.5rem and the transcript shrinks to absorb it; gap token reaches rendered cells (1.4rem→20.4px); both sliders populate from storage, apply live, and persist; no console errors. |
| 0.5.40  | 1     | **Title bar removed; Command Bar consolidates the control surface (Ken, June 29 2026).** Pre-work for the button-size / word-prediction questions. (1) **Title bar deleted** — the `<header>` (app title + About Me + Settings buttons) is gone; the conversation main area fills from the top. (2) **About Me** moved off the title bar into the **Settings panel** as its own tab (first tab); clicking it closes Settings and launches the existing full-screen questionnaire overlay (no panel of its own — special-cased in `initSettingsTabs`; `ui.onAboutMeClick`/`aboutMeBtn` removed). (3) **Command Bar** = the row where Start Listening sat (`#listenControls`). It now holds, as equal-width **icon-only** buttons (Rule 4): Start Listening, Start conversation, End conversation (red), the two repeat controls, Hold on, Wind down, and **Settings** (new gear icon). (4) The two repeat controls were **renamed speaker-specifically** (Ken — disliked "Pardon", "Say again" is speaker-non-specific): self-repeat → **"Repeat what I said"** (replay icon, re-speaks the user's last words), partner-repeat → **"Ask them to repeat"** (the old Pardon?). (5) The four override buttons were **removed from the Express Panel** (`#epControls` gone — the panel is now just `#epGrid`). (6) **Cut/Copy/Paste toolbar removed from the keyboard** — its key rows now start at the dock top, matching the Express Panel which also lost its top row, so one keyguard still overlays both; word-prediction infrastructure (`predWrap`, hidden) is preserved. (7) A **Paste button** was added beside the **API Key** field (relabeled from "Claude API Key") in Settings to replace the keyboard's removed clipboard paste for the long `sk-ant-…` key. Verified in preview (1280×800): boots clean, no console errors; 8 icon buttons in the Command Bar with the correct labels; About Me tab launches the overlay and closes Settings; Express Panel has no override row; keyboard has no tools; v0.5.40. |
| 0.5.35  | 1     | **Dock position/layout settings reframed around the Express Panel (Ken, June 28 2026).** The "on-screen keyboard position" and "keyboard layout" settings also drive the always-visible Express Panel's location and grid (one keyguard overlays both per Spatial Stability), so physical-keyboard users still need them — but the keyboard-centric labels read as a non-sequitur (Ken). Chosen fix (AskUserQuestion): **relabel + rehint in place**, not decouple (decoupling would break the one-keyguard principle). `index.html` Speech & Input tab: "On-screen keyboard position" → **"Express Panel & keyboard position"**, both layout selects → **"Express Panel & keyboard layout"**, hints rewritten to say the grid is shared so one keyguard fits both and the setting applies **even if you type on a physical keyboard**; the which-side hint names the Express Panel too. "Keyboard for typing" (physical vs on-screen) left as the only genuinely keyboard-only control. No behavior change (`expressLayoutRows()` already reads `loadKeyboardDock()`/`loadSideLayout()`/`loadBottomLayout()`). User Manual §6.2 entries + version updated to match. |
| 0.5.34  | 1     | **On-screen keyboard now serves the Settings → Controls editor (Ken, June 28 2026).** Tapping a text box on the Controls tab (the Hold on / Pardon? phrases and the opener/closer cards) had no effect because the editor's inputs weren't in the keyboard's `IN_SCOPE` — so with "Keyboard for typing" = On-screen keyboard, nothing appeared. Added `#controlEditor input` to `IN_SCOPE` (`keyboard.js`); the existing `focusin` path then suppresses the OS keyboard (`inputmode="none"`, applied just-in-time) and shows the app keyboard, reparented into the Settings dialog's top layer like the API-key field. Behavior keys off the user's keyboard-mode setting only — physical-keyboard attachment is never detected, so it works regardless of whether one is attached. Verified in preview: a real `focusin` on a Controls input opens the keyboard (docked, hosted in the dialog) and sets `inputmode=none`; no console errors. |
| 0.5.33  | 1     | **Editable control phrases — User-Manual review item #3 (Ken, June 28 2026).** The persistent override controls' spoken text and the opener/closer cards are now user-editable (Settings → **Controls** tab), so each can be reworded to sound like the user. New portable model `control-phrases.js` (`control-phrases.json` in the data folder + `aac_control_phrases` cache, the v0.2.25 "file in folder wins" reconciliation) holds `holdOn` / `pardon` (single phrases) and `openers` / `closers` (ordered card lists). New `control-phrases-editor.js` editor (reuses the Express-editor `.ee-*` styles): single-field rows for Hold on / Pardon?, add/reorder/delete lists for Openers and Closings, a read-only note that **Say again** has no editable phrase (it re-speaks the user's own last words), and a danger-confirmed Reset. **Openers are templates with a `{name}` token** — `engine.applyName()` substitutes the active Partner's name and, when no Partner is active, drops the token *and* an adjacent comma with punctuation repair so it still reads cleanly ("Hi {name}, got a minute?" → "Hi, got a minute?"). `engine.js` OPENERS/CLOSERS became injectable via `setConversationPhrases()` (inline defaults mirror the model so the engine still works standalone); `handleHoldOn`/`handlePardon` read their text from the model at tap time; app loads/syncs the model at init, in `handleStart`, and on Choose-Folder, and re-injects the engine on every edit via the editor's `onChange`. Blank rows are kept in storage (so the editor can hold a transient empty row to type into) but filtered out before reaching the palette. Verified in preview: boots clean; Controls tab renders all four sections + note + Reset; editing Hold on persists; editing an opener flows through to the engine palette; Add grows the list, focuses the new row, and the typed card reaches the engine; Reset restores defaults; named vs. no-name opener forms both read cleanly. *Live TTS of an edited phrase + the on-disk `control-phrases.json` write need the tablet (FSA + speaker).* |
| 0.5.32  | 1     | **User-Manual review fixes, four of five (Ken, June 28 2026).** (1) **"Maximum placeholders per turn" = 0 now means NONE** for users who find fillers artificial/robotic — `0` was taken by "No limit"; that option moved to value `-1`, a literal `0 (none)` was added, and `placeholders.start()` schedules nothing when the setting is 0. (2) **Initial-placeholder-delay clock starts at the partner's PAUSE, not at options-arrival** (reverses the v0.3.9 basis — a slow AI round-trip was leaving dead air; Ken: "responses can be very long in coming"). New `placeholders.arm()` is called at the silence checkpoint (`app.js handleSilencePeriod`) to start the initial-delay clock; `start()` (still gated on the classification — questions only) then waits only the *remainder* of `initialDelay`, firing immediately if generation already consumed it. Holding the utterance until classification keeps fillers off statements/closings. (3) **Pardon? now discards only the partner's LAST statement**, not the whole accumulated turn — `stt.js` tracks finalized segments and `dropLastStatement()` pops just the last one (keeping earlier good sentences for the re-speak to append to); `handlePardon` sets `currentPartnerText` to the remaining text. (4) **Restored a UI surface for the gaps log** (removed in v0.2.31, so progressive-profiling gaps were being *recorded* but never shown — answering Ken's "I haven't seen new questions appear"). About Me home now shows a **"Questions worth answering"** section listing only fields the AI actually needed in real conversations (`worldview.listGaps()`, most-asked first), each deep-linking to its field card (`renderModule(moduleId, focusKey)` scrolls+focuses it); renders nothing when there are no open gaps, so it never duplicates the topic pages. *Note: gaps only accrue from live Claude-API turns that return `missing_facts`, so testing #4 needs Ken's key on the tablet.* Item #3 from the review (user-editable spoken text for the persistent controls + Wind-down/opener/closer cards) is deferred to its own editor pass. Verified in preview: boots clean; dropdown shows `0 (none)`/`No limit`; a seeded gap renders the section and deep-links to the Passions card (focused); `dropLastStatement`/`arm`/`start` load and run without error. |
| 0.3.6   | 1     | Three keyboard/Settings bug fixes (Ken): (1) API key now persists on Settings Close/Escape even if a paste didn't fire an `input` event (belt-and-suspenders save from the field value); (2)+(3) the on-screen keyboard no longer hides when focus moves to an in-panel button (Save in About Me, controls in Settings) — fixing both "keyboard disappears on Save" and the "must press Save twice" bug (hiding-on-focusout reflowed the layout and stole the first click). Panels' own close paths (`worldview close()`, Settings Close/Escape, `renderHome`) dismiss the keyboard explicitly via new `keyboard.hideKeyboard()`. |
| 0.3.7   | 1     | Fix v0.3.6 keyboard-retention regression (Ken: Save still hid the keyboard). The v0.3.6 focusout check kept the keyboard only when `e.relatedTarget` was a button inside the panel — but tapping a `<button>`, especially via touch, often leaves `relatedTarget` null, so the check missed and the keyboard hid. Now `focusout` keeps the keyboard whenever a panel it serves is still open (`servingPanelOpen()` — `#settingsDialog[open]` or visible `#worldviewScreen`), independent of `relatedTarget`. Composer (no panel open) still hides on blur as before; explicit close paths + Hide button still dismiss it. |
| 0.3.8   | 1     | On-screen keyboard starts each field in one-shot Shift (Ken) so the first letter is capitalized — proper nouns in About Me (Carl, Chicago, Mom) and sentence starts in the composer; reverts to lowercase after that one character. Set in `keyboard.js show()`. Exception: the Settings API-key field (`#apiKeyInput`) starts Shift-off — its value is case-sensitive and lowercase (`sk-ant-…`). |
| 0.3.9   | 1     | Conversation Engine observations Pass 1 (Ken): placeholders fire on response-arrival and only for questions (not statements/closings), covering the *choosing* window not the AI-latency gap (a quick pick plays none); neutral non-imperative filler set ("Good question." / "Hmm, interesting." …) — imperatives read as curt through the flat voices; response options no longer lead with empty interjections ("Ah/Well/So"); TTS-echo loop fixed at the trigger (no generation checkpoint fires while the app is speaking + echo tail), killing the runaway stack-growth/option-flicker; new "End conversation" hard-terminate control → STANDBY |
| 0.3.10  | 1     | Placeholders are role-differentiated by position to stop sequential fillers sounding repetitive: first filler acknowledges ("Good question."), later ones say you're still thinking ("Still thinking it through."); `placeholders.json` is now `{acknowledgment, thinking}`; new Settings "Maximum placeholders per turn" (default 2) caps how many play so you never hear two same-category fillers back to back |
| 0.3.11  | 1     | About Me improvements: address_terms removed from About You (per-person nicknames replace it); People editor adds nickname + Lives with me fields; Private badge made visually distinct from Prefer not to say; Contact & Logistics shows 🔒 in Topics list and per-field privacy notes; Close friend added to relationship dropdown; keyboard input scrolls to cursor when text fills the narrowed field |
| 0.3.12  | 1     | Fix privacy model: Private now sends the value to the AI with a "don't volunteer" instruction rather than withholding it entirely (which made Private equivalent to Prefer not to say and useless). Prefer not to say remains the control for information the user does not want the AI to know at all. UI labels and CLAUDE.md updated to reflect the correct three-level model. |
| 0.3.13  | 1     | Inline follow-up questions: a field with `followups[]` shows its follow-ups as indented child cards below the parent once answered, each with its own input/Save/Speak; answers inject into the LLM profile block under the parent. (Superseded by 0.3.14.) |
| 0.3.14  | 1     | Reverted 0.3.13: the only `followups` host was `passions` (a `multi` field), so one shared follow-up answer for "music, hiking" was meaningless — depth is per-item, and the follow-up question is often a function of the answer (Music→instruments, Sports→teams), which a static registry can't scale to (no coverage for typed passions). Passion-tailored follow-ups deferred to the LLM-driven Conversational Worldview Collection (Architecture Overview §6); `passions` stays a clean list and the follow-up infrastructure was removed rather than left as dead code. |
| 0.3.15  | 1     | UI + CE fixes (Ken, June 19 2026): removed the redundant "About You" hint block from Settings → General; "In your own words" Speak no longer needs two presses (keyboard kept up when a composer control is tapped, so the reflow doesn't steal the first click); People editor "Edit" no longer jumps the page to the top when editing someone below the fold (scrolls their form into view instead); "Hold on" speaks "Let me think about that." (dropped the unintelligible leading "Hmm,"). CE Pass 6: repeated Pardon? no longer stacks multiple `REPAIR*` (dedup — one open repair at a time), the partner's re-speak pops it AND clarifies the existing question in place rather than pushing a duplicate, so the stack again holds a single question. |
| 0.5.12  | 1     | Move cards: **dropped the hint**, the response text is now the large, easy-to-read primary element. **Suggestions per category setting (1 or 2, max 2; Ken)** — choosing 2 yields **8 cards total** (2 stacked in each of the four category cells, same footprint as 4). Renderer groups the palette into 4 fixed category cells (each 1–2 stacked options); `llm.generateResponses` `perCategory` opt requests 2 distinct options/slot when set. |
| 0.5.26  | 1     | Express Panel editor: Partner entry uses a "Choose a person" dropdown (shows name + nickname in parentheses) replacing the separate name and nickname text inputs; panel button label unchanged (nickname if set, name if not). |
| 0.5.27  | 1     | **Keyboard toolbar ↔ Express Panel control-row alignment** (Ken). The keyboard's Cut/Copy/Paste/Hide toolbar (`.kbd-toolbar`, was `min-height: 2.4rem`) was shorter than the Express Panel's four override buttons (`#epControls`/`.persist-btn`, ~44px from `0.7rem` vertical padding), so the keyboard's keys sat higher than the phrase grid and the two dock layouts didn't overlay hole-for-hole. Both control rows are now pinned to ONE shared height and the grids beneath start at the same y in **both** docks (verified bottom: rows at y=420; side: y=51). Introduces the **`--btn-min-dim`** token (root, default 2.75rem) = "minimum button dimension," the single seam a future Settings control will raise to enlarge targets fleet-wide; here its first job is alignment. Both `#epControls` and `.kbd-toolbar` use `flex: 0 0 var(--btn-min-dim)`; `.kbd-tool`/`.kbd-pred-btn`/`.persist-btn` floor their `min-height` to it; `.persist-btn` vertical padding removed (height now comes from row stretch, not padding, so it can't grow past the keyboard's toolbar). Content buttons (keys/phrase cards) deliberately NOT floored to the token yet — the bottom dock is a fixed `48vh` band of ~12 stretched rows, so a hard floor there would overflow; growing content buttons is the future Settings job that must reconcile with the fixed dock. |
| 0.5.28  | 1     | **Symbols page now matches the active letters layout's geometry** (Ken — Spatial Stability). The 123 (symbols/numbers) page was ONE fixed grid per dock (5-wide side, 10-wide bottom), so when the selected letters layout was a different shape (e.g. the 3-wide `S6`), tapping 123 changed the whole grid and a single static keyguard's holes no longer lined up. Replaced the static `SYMBOLS` object with `buildSymbolsPage(letterRows)` in `keyboard-layouts.js`: it GENERATES the symbols page from whichever letters layout is active — identical rows, identical spans, action/space/blank/pred cells left exactly in place, only each LETTER cell swapped for a symbol (from an ordered `SYMBOL_POOL`: digits → common specials → rarer, surplus letter cells go blank if the pool runs out) and the 123 key relabeled to ABC. `keyboard.js` `currentRows()` calls it for the active layout. Geometry is therefore guaranteed congruent between the two pages for every layout — verified all 21 (S1–S10, B1–B11) congruent letters↔symbols. |
| 0.5.29  | 1     | Keyboard-layout Settings menus drop the internal `S1`/`B1` ids from the option labels (they meant nothing to the user) — show only the human-readable name; the id stays as the `<option>` value for storage (`fillLayoutSelect` in app.js). |
| 0.5.31  | 1     | **End conversation clears the active Partner & Feeling toggles** (Ken) — the next person/mood shouldn't inherit this conversation's selections. New `clearInfluencers()` (nulls both + re-renders the panel so the selected rings drop) called from `handleEndConversation`. Deliberately NOT in the shared `terminateConversation()` — Start conversation reuses that and still needs the active Partner to personalize its openers ("Hi Tim, …"). Verified: a feeling ring active → End conversation → 0 rings. |
| 0.5.30  | 1     | **Stamp every turn with the active Partner / Feeling in the conversation log** (Ken). The Partner and Feeling toggles (v0.5.14) were session-runtime only — they shaped generation (`buildSituationBlock`) but were never persisted. Now each logged turn carries a **situation stamp** for Phase-3 review: `partner` (`{ id: personId\|null, label }` — keeps the stable relationship-graph `personId` when the Partner is a known person, so the log can join back to the person, plus the display label) and, on the user turn, `feeling` (`{ id, text }`); each is `null` when its toggle is off. Stamp-every-turn (not a change-event) so the state at any turn is self-contained and survives the toggles changing mid-conversation. New `partnerStamp()`/`feelingStamp()` helpers in app.js feed `storage.logUserResponse` (partner + feeling) and `storage.logPartnerSpeech` (partner = who spoke; the seam for future multi-party). NOT pushed into the in-memory `conversationHistory` that feeds the LLM (the situation block already injects it each turn — would be redundant). Boots clean; the on-disk write needs a granted FSA folder + live conversation to exercise (tablet). |
| 0.5.25  | 1     | **Spatial Stability across all UIs — two fundamental layouts, one keyguard** (Ken). Every screen now adheres to the base conversation layout = **MAIN AREA + DOCK AREA**, with the **dock region constant across Conversation, About Me, and Settings** so a single physical keyguard overlays them all. Two layouts only: **SIDE** (keyboard/Express-Panel a fixed-width full-height column on the chosen edge) and **BOTTOM** (a fixed-height full-width strip). Decisions (Ken): the keyguard need only hold the **dock** constant (the main area is its open window); the dock is **empty/reserved** on About Me & Settings; **Settings becomes a fixed main-area panel** (drag removed). Changes: new `--dock-w`/`--dock-h` tokens (single source of truth); **bottom dock is a fixed `--dock-h` (48vh)** that the Express Panel AND the keyboard both fill (rows stretch) so they occupy the identical band; **side dock is a full-height fixed column** (so the app header no longer pushes the Express Panel below the keyboard — they now share y0→bottom; main area + header confined beside it); **About Me reserves the dock at all times** (content in the main-area rect, dock blank, keyboard fills it on focus); **Settings** fills the main-area rect (`inset:0` minus the dock), dock reserved, drag/nudge code deleted. Verified both docks: keyboard ↔ Express Panel ↔ Settings/About Me dock regions are pixel-identical (bottom: 0,416 1280×384; side: full-height `--dock-w` column); danger-confirm dialog still centers. Realizes UI Layout **Rule 11**'s goal. |
| 0.5.24  | 1     | **Express Panel editor: "Choose a name" dropdown, wider; editor uses full width** (Ken). The partner-row people dropdown's placeholder is now **"— Choose a name —"** (was "— Type a name —") and is **~2× wider** (`.ee-name-select` flex `2 1 16rem`). To give it room, the **Express Panel settings tab is exempted from the Settings two/three-column layout** (`column-count: auto`) — the editor is a vertical list of rows that needs full width, so rows no longer wrap (select 395px ≈ 2× the 198px Name box at Surface width). |
| 0.5.23  | 1     | **Express Panel editor: insert in place** (Ken — the v0.5.22 select-then-scroll-to-toolbar flow was convoluted). Each row now has a **＋** button that opens an inline **Phrase / Partner / Feeling** picker right above that row; choosing a type inserts the new item there and **focuses its input** (and scrolls it into view) so the user can type immediately — no scrolling up to the toolbar and back down. The toolbar's add buttons now simply append to the end (and seed an empty list), also focusing the new row. Replaces the v0.5.22 ↧ marker + `insertBeforeId` + status line with `pickerForId`/`pendingFocusId` and `buildInsertBar()`. |
| 0.5.22  | 1     | *(superseded by 0.5.23)* **Express Panel editor: insert a new item before a selected one** (Ken — replaces the v0.5.21 auto-household feature, now **reverted**). Each editor row has a **↧ marker**: tap it to choose a target row, then **+ Phrase / + Partner / + Feeling** inserts the new item *above* that row (tap again or "Add at the end instead" to clear → append, the default). A status line shows where the next item lands; the target row is highlighted; consecutive adds stack above the same target (tracked by id, so it survives the re-render). Solves "new items land at the bottom and moving them up is many taps" — and, per Ken, household members stay as real editable items (so you can remove people you live with but rarely talk to, and add people you don't live with but talk to often), so the automatic household injection isn't needed. Reverted from 0.5.21: the `expressAutoHousehold` setting + toggle, `expressItemsForPanel()`, and the `worldview-ui` onClose hook. |
| 0.5.21  | 1     | *(reverted in 0.5.22)* **Auto household Partner buttons in the Express Panel** (Ken). With a new Settings toggle (Express Panel tab, **on by default**) the panel auto-adds a Partner button for each person marked **"lives with me"** in About Me → People, injected right **after the feelings** so they form a row or two just below them. Generated from the relationship graph at render time (`expressItemsForPanel()` in app.js — id `live:<personId>`), so they track adds/edits/removes and aren't stored in the editable list (and aren't shown in the editor; manage via About Me). Anyone already added manually as a Partner (same personId) isn't duplicated. The panel refreshes when About Me closes (new `worldview-ui.init({onClose})` hook). The option exists because the panel's default layout predates any people; it no-ops until household people are added. `storage.loadExpressAutoHousehold` (default true). |
| 0.5.20  | 1     | **Express Panel editor: pick button COLOR, not category** (Ken). A phrase item's `cat` only sets its button color, and the category names ("Affirm / deny"…) mean nothing to the user — so the editor's category `<select>` is replaced by a row of **color swatches** (the colors available for that type); clicking one sets `cat` and live-recolors the panel button. Partner/Feeling have one fixed color per type, shown as a single non-editable swatch. `cat` is still the stored value (rendering unchanged); only the editor UX changed. New `colorControl()` in express-editor.js + `.ee-swatch*` styles. |
| 0.5.19  | 1     | **Rename "move" → "response" in code too** (Ken — reverses the earlier "keep `move` as the internal CA term" decision). Identifiers `buildMoveCard`→`buildResponseCard`, `showMoves`→`showResponses`, `handleMoveSelected`→`handleResponseSelected`, `selectMove`→`selectResponse`, `paletteFromMoves`→`paletteFromResponses`; the LLM JSON contract key `moves`→`responses` (prompt + `parseGeneration` + `engine.ingestClassification`/`refreshPalette` + app); CSS `.move-card`→`.response-card`, `.move-cell`→`.response-cell`, `.move-response`→`.response-text`, `.move-badge`/`.move-latency`→`.response-*`; and all "move"/"Move" comments + prompt prose → "response". **Deliberately preserved** (these are genuine *motion*, not the response concept): `cursor: move`, "drag to move", "the caret never moves", "geometry never moves", "move the dock area", "move between devices". The **design docs were also scrubbed** `move`→`response` (Configuration-Model, Conversation-Engine-Design/Overview/Test-Script, UI-Design, Product & Architecture Overviews) — same motion-preserving rules ("never move", "cursor moves through", "move it off", etc. kept). Net: "move" no longer denotes the response concept anywhere in code, product UI, or our docs; it remains only in the external CA literature. Verified end-to-end in preview (llm→engine→ui data path + live opener flow) and by re-scanning every doc (only motion uses remain). |
| 0.5.18  | 1     | **Complete the Express Panel rename** (Ken: "there are no existing user settings — I am the only user — so rename all references"). Renamed everything the v0.5.17 pass had kept for back-compat/churn-avoidance: persisted Settings key `fastPhraseTapMode`→`expressTapMode`; DOM ids `#fastPhrases`→`#expressPanel`, `#fpGrid`→`#epGrid`, `#fpControls`→`#epControls`, `#phraseEditor`→`#expressEditor`; CSS classes `.fast-phrases-panel`→`.express-panel`, `.fp-*`→`.ep-*`, `--fp-*`→`--ep-*`, editor `.pe-*`→`.ee-*`; Settings tab id `phrases`→`express`; and all "fast/quick phrase" comment + user-visible strings ("Speaking an Express Panel phrase", "Reset the Express Panel?"). Removed the now-pointless localStorage migration shim in express-panel.js. Only the CA term `move` is intentionally kept. Verified: boots clean, panel + editor + toggles work under the new selectors. |
| 0.5.17  | 1     | **Express Panel goes portable + module rename** (Ken, June 26 2026 — the two "yes to both" follow-ups). (1) **Data-folder portability:** new `express-panel.js` model persists the item list to `<data folder>/express-panel.json` (source of truth) + localStorage cache `aac_express_items`, with the v0.2.25 "file in folder wins" reconciliation (`load`/`getItems`/`setItems`/`resetItems`/`syncToFolder`); loaded at init + adopted/promoted in `handleStart` and the Choose-Folder handler, like worldview/relationships. One-time migration reads the old `aac_settings.fastItems` location so existing edits carry over. (2) **Rename for consistency:** `fast-phrases.js`→`express-items.js`, `fastphrase-editor.js`→`express-editor.js`; JS identifiers/comments `fast*`→`express*` (`renderExpressPanel`, `handleSpeakExpressItem`, `expressLayoutRows`, `loadExpressTapMode`, etc.). **Kept on purpose:** the CA term `move` (move card/`showMoves`/`SLOT`), and the internal DOM ids / CSS selectors (`#fastPhrases`, `#fpGrid`, `.fp-*`) — renaming those is churn/risk with no user benefit; the persisted Settings keys (`fastPhraseTapMode`, radio `name`) also kept so user settings survive. |
| 0.5.16  | 1     | **Naming decisions + Product Overview review pass** (Ken, June 26 2026). Settled product-facing names: the fast/quick-phrase panel → **Express Panel**; the four AI suggestion cards / their area → **Response Palette** / **response cards** (replacing "Move Palette"/"move"; "move" stays only as the internal CA term); the fixed-layout principle (nothing on screen moves → motor planning + one keyguard) → **Spatial Stability**. App: Settings tab + panel aria-label renamed to "Express Panel". Silence trigger range changed to **0.5–3.0s in 0.5s steps** (default 2.0) in v0.5.15. The `Conversant AAC Product Overview.docx` was revised per Ken's review — corrections: removed the never-built "low-confidence words underlined" claim and the "three clearly marked transcript states" / "confirm transcript" step (generation fires on the pause; the require-confirmation-first gate is *planned, not in Settings*); cards are color-coded by category, no badge; "four or eight cards" (1 or 2 per category); relative card positions (upper-left etc.); nicknames always used for people; "Private" means the SAME for facts and people (known to AI, never volunteered) vs "prefer not to say" (withheld); device can be Windows tablet / Chromebook / Mac; on-screen keyboard also serves fold-back touchscreen laptops; local models are NOT a Phase-2 target; added a Spatial Stability section, an Express Panel Phase-1 item, and a "Further Out — Future Directions" roadmap (speaker-aware capture, multi-party, conversation review, social-skills practice, neural/ElevenLabs voices, streaming generation, messaging/social connectivity, symbol support). Docs sync via OneDrive (not git). |
| 0.5.14  | 1     | **Editable fast phrases + Partner & Feeling influencer toggles** (Ken, June 26 2026). The fast-phrase panel is now a user-editable, ORDERED, TYPED item list (Settings → **Phrases** tab). Three item types: **phrase** (speaks on tap, category-colored), **partner** (a TOGGLE — who you're talking with; brown), **feeling** (a TOGGLE — current mood; cyan). Item order = panel position (editor ↑↓ re-maps; extra items beyond the layout's cells overflow/hide). Editor: add/edit/reorder/delete + "Reset to default" (danger-confirm); a partner can be **picked from People I Know** (carries personId + nickname) or typed free-form. One active partner + one active feeling at a time (tap again = off, tap another = switch; shown as a selected ring + ✓, Rule 6). **Effects:** active Partner personalizes the openers ("Hi Tim, got a minute?", via `engine.initiate({partnerName})`) and injects "you're talking with X, address them as X" into generation; active Feeling injects a mood-steer line. Both via the new `llm.setSituationBlock()` (appended in `buildProfileBlock`), set each turn alongside worldview/relationships. Data: `storage.loadFastItems/saveFastItems` (localStorage; stable ids on defaults so toggle-state tracks by id; future: move to the data folder for portability). New module `fastphrase-editor.js`. The old single hardcoded `PHRASES` list is replaced by `DEFAULT_ITEMS`. |
| 0.5.13  | 1     | **Start-conversation bug fix + screen-real-estate cleanup + nicknames + QWERTY** (Ken, June 26 2026). (1) **Bug:** opening with a pre-question ("Can I ask you something?") then the partner's "sure" produced options as if the *partner* had asked the opener. Fix in `engine.js`: selecting an OPENER now pushes a **USER-opened** sequence (floor → partner); when the partner's COMPLETE reply lands and the innermost open sequence is USER-opened (non-repair), it's an **SPP from the partner** — pop it, push no partner FPP, floor → user to LEAD. `buildRequestContext` exposes `user_holds_floor_to_lead`; `llm.js` prompt tells the model to generate *lead/continue* moves (treat "sure"/"go ahead" as a go-ahead, not a question). (2) **UI cleanup:** removed the "Conversation" label, the engine **mode chip**, and the **Engine diagnostics** `<details>` (kept the guarded JS — a new launch path TBD if needed later); **Start/End conversation** widened to flex-fill their row like the Say-again/Hold-on persist controls (room for more buttons later); move cards **dropped the category badge + latency dot** (color + position carry category; all cards spoken instantly). (3) **Nicknames:** `relationships.buildBlock()` now instructs the model to address people by their preferred term ("mom", not "Mary") when a nickname is set. (4) **QWERTY:** new bottom layout **B11** "QWERTY (touch-typist, 3-row)" — letters in QWERTY order, grid-aligned (q/a/z share column 1), space on the bottom row, specials on the shared symbols page; selectable in Settings. Field-tabbing (a listed item) was already shipped. |
| 0.5.7–0.5.11 | 1 | Conversation-screen polish: border on the conversation box (0.5.7); Settings panel bigger + two-column no-scroll + fixed broken tab nesting (0.5.8); Settings fixed-dimension per dock, never overlaps the keyboard (0.5.9, UI Rule 11); End/Start conversation clear the window + cards, empty move cards show slot colors at rest (0.5.10); auto-start recording after the opening statement (0.5.11). |
| 0.5.6   | 1     | Transcript box → **scrolling conversation log at FIXED height** (Ken): it scrolls internally and the layout never grows (static keyguard) — bound the app to the viewport (`body height:100vh; overflow:hidden`) with a min-height:0 flex chain. Both parties shown with **different fonts** (partner serif/left, user sans-bold/right); renamed to "Conversation"; **removed the light/input-like background** (not an input). Fast-phrase buttons **word-wrap** (3-line clamp) instead of ellipsis. Word-prediction dictionary expanded ~220→~560 words so 4–7 char prefixes keep matching. |
| 0.5.0   | 1     | **Conversation relayout — fast-phrase panel on the keyboard's real-estate + "In my own words" modal** (Ken, June 21 2026). Screen splits into a main area + a dock area (the keyboard's exact footprint, side or bottom per the dock choice). Dock shows the fast-phrase panel at rest, the keyboard during the modal → one static keyguard overlays both. Fast-phrase grid mirrors the selected keyboard layout cell-for-cell; space cell → "In my own words" (distinct dark); top row → the four persistent overrides; phrases fill remaining cells from the set's pool (leftovers blank). Response region = reserved fixed footprint: 4 cards 2×2 (side) / 1×4 (bottom) + a full-height "New 4" regenerate icon. "In my own words" opens a modal (input box Speak/Reframe/Cancel overlaying the response footprint, base UI not blurred; keyboard over the dock); any of the 3 dismisses both. Composer Region D removed. Fast phrases + composer Speak commit to history and take the floor (shared `speakAsUserTurn`). |
| 0.4.6   | 1     | Keyboard dock is now ONE user choice (Ken, June 21 2026), replacing context-based docking: Settings → on-screen keyboard position = Bottom or Side (then Left/Right); `dockFor()` returns that choice for every typing context (conversation composer, About Me, Settings), so a single keyboard shows wherever typing is appropriate. Settings reveals only the relevant controls per choice; preview follows the chosen dock. Default bottom. (Conversation+side spacing deferred to the panel-location step.) |
| 0.4.5   | 1     | Local word-prediction engine (`prediction.js` + `data/words.json`): prefix completion over a bundled frequency list + personalized local learning (the user's own word counts boosted ahead of the dictionary), fully offline, no AI per keystroke. The keyboard toolbar hosts the prediction buttons in the space reclaimed by the icon conversion; tapping one inserts it (matching the typed prefix's capitalization) + a space and learns it; word boundaries also learn. LLM stays for explicit phrase expansion only. |
| 0.4.4   | 1     | Icon pass (Rule 4): new `icons.js` inline-SVG vocabulary (currentColor, em-sized); conversation control buttons (Listen, Say again, Hold on, Pardon?, Wind down, Start/End, regenerate, Speak, Reframe, Clear) + keyboard toolbar (Cut/Copy/Paste/Hide) are now icon-only, each keeping its accessible name; "Show me different options" is a compact icon button. Content buttons (move cards, fast phrases) keep text. |
| 0.4.3   | 1     | Fast-phrases panel (Rule 9): category-colored base-UI quick-speak grid (`fast-phrases.js` CORE/MINIMAL/SOCIAL/REGULATE/BACKCHANNEL starter sets), content buttons (text, ellipsis-truncated) that speak directly; six functional categories each a distinct color (secondary cue). Single-tap or confirming double-tap with a configurable interval (Rule 10). Settings: set selector, tap-mode, interval. Placement provisional (final home = keyboard's side, keyguard-overlay). |
| 0.4.2   | 1     | Two-axis proportional layout (Ken, June 21 2026): the conversation UI is sized as a predictable fraction of the screen, with horizontal dimensions scaling off width (`vw`) and vertical off height (`vh`) independently, and font-size off `vmin`; all `clamp()`-bounded outside the device fleet so it scales linearly within it. Replaced the fixed `max-width:1100px` content cap (identical pixels on every device) with a width-proportional `min(96vw,1600px)`. |
| 0.4.1   | 1     | Viewport diagnostics (`viewport.js`): logs the full platform-knowable display metric set — layout viewport, full screen, available area, devicePixelRatio (the Windows scaling factor), recovered native resolution, visual viewport (reflects the OS keyboard overlay) — at startup and on every resize/orientation/keyboard change, so beta testers can report their real pixel box. Physical size/true PPI deliberately absent — no web API exposes them. |
| 0.4.0   | 1     | **Presentation Layer** — the full four-region triple-coded UI from `UI-Design.docx`, replacing the degenerate diagnostic renderer (Ken, June 21 2026). Region A transcript + mode chip + three-state display (informational, no gate) + now-playing line; Region B move palette as triple-coded cards (slot badge + color + position; glanceable hint; full text; format tag; filled/hollow latency dot; ≤200ms crossfade); Region C four persistent CA overrides + session Start/End; Region D composer (Speak/Reframe/Clear). Engine diagnostics demoted to a collapsed `<details>`. Renderer-only — engine untouched; descriptor→renderer boundary kept clean for future scanning/eye-gaze renderers. Minor bump: the defining UI layer the engine scaffolded toward. |
| 0.3.20  | 1     | "Reframe" — a second verb on the "In your own words" composer (Ken, June 21 2026): instead of speaking the box text, hand it to the AI as steering/context and regenerate the suggested responses around it (a guided regenerate reusing the v0.3.16 `refreshPalette` seam + a new `steer` opt in `llm.generateResponses`). One-shot — the box is cleared on success so it never reads as a persistent steer; the sticky/conversation-goal version is deferred to the Goals subsystem. The steer is framed as user-authored ground truth, so it's the channel for supplying the specifics the v0.3.18 anti-fabrication rule won't let the AI invent. |
| 0.3.19  | 1     | "In your own words" composer Speak now takes the floor like a selected response (Ken, Pass 14): it terminates the partner's open turn (`engine.selectMove` pops the partner FPP), stops recording, commits the exchange to history, and resumes listening iff auto-resume is armed (shared `resumeOrIdle` gate). Resolves the long-deferred "should composed utterances push to history?" question — yes. |
| 0.3.18  | 1     | Anti-fabrication guardrail in the generation prompt (Ken, June 20 2026): the AI was inventing specific autobiographical events — e.g. "I played a game with Tyler last night and beat him!" — because the prompt said "speak as the user / the most likely thing THIS user would say" with no constraint against making up episodes. Added a top-priority "speak only to what is real" rule: never invent specific events/outcomes/scores/dates/places/names not given; MAY use standing profile facts (habitual activities, interests, people) and general/non-committal replies; keep answers GENERAL when a specific detail is unknown ("Been playing online games with friends lately") rather than fabricating particulars. Also constrained the DISPREFERRED slot's "account/reason" so it can't invent a concrete excuse (no fake "dentist appointment at 3"). First concrete implementation of the conversational-honesty principle (Further Design Thoughts #2). |
| 0.3.17  | 1     | People editor scroll: editing a person below the fold no longer moves the page on Save/Cancel either — the list scroll position is captured when Edit is pressed and restored when the edit form closes, so the page returns exactly where it was (heights match: the form becomes a card again). Fresh entry from the home screen still lands at the top. |
| 0.3.16  | 1     | "Show me different options" regenerate control (Ken, June 19 2026): when the offered palette isn't quite right, re-runs generation for the SAME partner turn with the rejected options passed as `avoid` (llm.js), refreshing ONLY the palette via `engine.refreshPalette` (stack/mode/floor untouched — no duplicate FPP). Whole-palette regenerate; per-move regenerate + multiple-options-per-slot recorded as a to-do. Also merged the two misheard-partner controls into one: the old "Pardon?" (managed the stack but kept the garbled capture) and "Please repeat what you said." (discarded the capture but ignored the stack) each did half the job; the single **Pardon?** now does both — speaks "Sorry, I didn't catch that. Could you say it again?", discards the captured transcript, AND pushes the (deduped) repair sequence. The standalone "Please repeat" persistent control is gone; its slot now holds the regenerate control. |

---

## Relationship Data — its own graph model, separate from the questionnaire (decided + BUILT June 15 2026, v0.2.27)

**BUILT (June 15 2026), v0.2.27 — the relationship graph is live end-to-end.** Files/wiring:
- **`app/js/relationships.js`** — the model layer. Graph shape `{ version, updated, migratedFromWorldview, people:[{id, name, private, attrs:{about}}], edges:[{from, to, type, attrs}] }`; the user is the implicit node `"me"` and a person's relationship is the `type` of the `me→person` edge (person↔person edges are supported by the schema, not yet by the UI). API: `load`/`save`/`syncToFolder` (**same "file in the folder wins" rule as worldview v0.2.25** — its own cache key `aac_relationships`), `listPeople` (denormalized name+relationship+about+private), `addPerson`/`updatePerson`/`removePerson`, `resetAll`, `count`, `migrateFromWorldview(wv)`, and `buildBlock()`.
- **`buildBlock()`** contributes a "People in my life:" block to the LLM profile. **Private people are never named or described** — surfaced only as a phrase-around instruction by their relationship (mirrors the worldview privacy rule).
- **No migration (Ken, June 15 2026).** A first cut wired an idempotent `migrateFromWorldview()` into app boot to import the old module A3 answers; **removed** — Ken is the only user and had entered little relationship data, so he re-enters people via the People editor rather than carry migration code (and no standalone script either). The old A3 answers remain in `worldview.json` but are **orphaned** (no longer in the registry, so `worldview.buildBlock()` ignores them) — harmless dead data.
- **Registry change** (`app/data/worldview-questions.json`): **module A3 deleted**; its one genuinely-flat field `address_terms` ("what do you call people") moved into **A1 About You**. People/pets are now the graph, not questionnaire fields.
- **UI** (`worldview-ui.js`): About Me home shows a **"People & relationships"** section with a "People in Your Life" row (count) → a dedicated **People editor** (`renderPeople`): per-person cards (name (relationship) — about, Private badge, Edit/Remove) and an add/edit form (name, relationship, about, "Private — never name this person" checkbox). Reuses `.wv-text` so the on-screen keyboard's existing scope + side-dock + `inputmode="none"` handling applies automatically. **Restart** now clears the graph too.
- **Relationship picker (v0.2.29):** the relationship field is a **standard grouped `<select>`** (`REL_GROUPS`: Family / Partner / Friends & social / Care & support / Pet — 30 options) **+ "Other…"** which reveals a free-text field. Case-insensitive match on edit re-selects a standard option or falls back to Other (prefilled). **One relationship per person for now** (single `me→person` edge type); multiple relationships with one person ("cousin" + "wife") is a deliberate later refinement (Ken, June 15 2026).
- **LLM wiring**: `llm.setRelationshipsBlock()` + inclusion in `buildProfileBlock()`; `app.js` `generateOptions()` calls it each turn alongside `setWorldviewBlock`. `app.js` init + `handleStart` load/sync the graph.
- CACHE_VERSION/APP_VERSION → `0.2.27` (build); → `0.2.28` (migration removed). `relationships.js` added to the sw precache. **Verified in preview:** model CRUD + private-withholding `buildBlock`; the People editor add→display→mark-private→badge flow; registry shows no A3 and `address_terms` under A1; after migration removal `migrateFromWorldview` is gone with CRUD/editor still working; no console errors. *On-disk persistence to `relationships.json` needs a granted FSA folder — confirm on the tablet.* **Scope held:** small typed people+edges with a free `attrs` bag — no graph DB, no third-party-relations editor yet.

**Decision (Ken, June 15 2026): relationship data is a separate concern in its own file — `relationships.json` — not flat question/answer fields.** Ken's insight: relationship data has a fundamentally different structure from the worldview questionnaire. It is a **graph** — *nodes* (people) and *edges* (relationships), **both of which carry their own data** — whereas the questionnaire is flat key→answer.

**Why the current model is wrong for this.** Today module **A3 "People in Your Life"** stores people as flat `repeat` lists (`household`, `family_key`, `pets` = arrays of `{name, relationship}` tuples; `friends_key` = a name list). That cannot represent: per-person attributes (how you address them, where they live, what you share, topics to avoid, how often you see them); the *same person* appearing in multiple lists with one identity; **edge** attributes (closeness, current state — "we're not talking right now" — shared history); or relationships *between third parties* ("my sister is married to my brother-in-law"). This matters specifically because the architecture's **relationship modeling** + Phase 2 **partner recognition** ask "the partner in front of me is *X* — who are they to me, what's our history?" — a node+edge lookup, not a questionnaire answer.

**Agreed shape (build-ready spec; coexists with the flat questionnaire):**
- New file in the FSA data folder: `relationships.json`, shaped `{ version, updated, people:[{id, name, attrs:{address_term, lives, likes, avoid, …}}], edges:[{from, to, type, attrs:{closeness, seen, notes, …}}] }`. `from:"me"` for the user↔person edges; person↔person edges allowed. `attrs` is a free-form bag per node and per edge (typed later as patterns emerge).
- New model layer `relationships.js` mirroring `worldview.js` conventions: data-folder write-through + localStorage cache, **and the same "file in the folder wins" reconciliation just settled in v0.2.25** (see the cross-PC fix above) — its own `syncToFolder`. Node/edge CRUD; privacy per node (contact-info-style strict phrase-around carries over); a `buildBlock()` contribution to the LLM profile (and, with partner recognition, a *focused* lookup for the current partner).
- The flat questionnaire **stays** for flat facts (name, place, interests, personality/values). The **A3 "People" module becomes the capture UI** that writes nodes/edges into the graph instead of `repeat` tuples; a migration reads existing A3 `repeat` answers into `people`/`edges`.

**Scope guardrail (Ken's "small, not a graph DB"):** build a small typed people+edges structure with a free `attrs` bag — *not* a full graph database or a third-party-relations editor — enough now, RAG-friendly later. Build is a dedicated session (model → migration → A3 UI rework → LLM wiring), authorized separately. Not started.

---

## Conversation & Relationship Goals — three-layer model (design recorded June 15 2026; NOT built)

**Ken's insight (June 15 2026):** everyone has goals for every conversation — consciously or not — from "maintain the connection with this person" to "steer the conversation toward X." Goals should be a first-class input to generation. The key framing: **"relationship goals" is really *three distinct things on three time horizons*,** and they map to three different stores — lumping them together is what makes it hard.

**The three layers:**
1. **Disposition** — how you generally are with people (durable, trait-level): "I keep things light," "I want to be seen as capable." Not tied to one person or conversation; the *prior/default stance*.
2. **Standing relationship goal** — what you want from a *specific relationship* (durable, person-level): "stay close to my sister," "I want my boss to see me as competent." Stable across many conversations with that person.
3. **Conversation goal** — what you want from *this exchange right now* (ephemeral, session-level): "today I need to ask Dad for help," "steer toward planning the trip." Set at/just-before conversation start, injected into that conversation's prompt, then discarded.

**Mapping to existing stores (two of three already have a home):**
| Layer | Store | Notes |
|---|---|---|
| Disposition | `worldview.json` **Tier-B** (how you treat different people, register/values) | Exactly what Tier-B is for; designed, not yet built. |
| Standing relationship goal | `relationships.json` **edge `attrs`** (e.g. `attrs.goals`) | **Clean fit today, no schema change** — a goal is an attribute of the `me→person` edge. |
| Conversation goal | **Session runtime** (Conversation Engine state), optionally appended to the `conversations/*.json` log for Phase-3 review | Genuinely new, but it's runtime state, **not a new persistent store**. |

**Recording mechanism (per layer):**
- Standing relationship goal → a **"what I want from this relationship"** control in the **People editor** (the one built in v0.2.27), writing to the edge `attrs`.
- Conversation goal → set at **conversation initiation** (pairs with Phase-1 canned starters and Phase-2 partner recognition — "you're talking to Dad; what do you want from this?"), with a quick mid-conversation control (fits the persistent-controls region); stored in session memory.
- Disposition → Tier-B questionnaire when built.
- **Expression format — DECIDED (Ken, June 15 2026): curated menu + free text.** A short list of common goals (stay close / be understood / be seen as capable / keep it light / get support / repair things) **plus** an "in my own words" option — fast to pick (matters for the CP target user) yet still personal. (Not menu-only, not free-text-only.)

**Why it matters / how it's used:** goals shape and **rank the four-slot move palette** (PREFERRED / DISPREFERRED / INITIATIVE / REPAIR): "maintain connection" → favor affiliative PREFERRED; "steer toward X" → surface a high-ranked INITIATIVE toward X; "wind down" → favor PRE-CLOSING. They become explicit generation inputs, **layered most-specific-wins** exactly like the prompt blocks already are: disposition (worldview) → standing relationship goal (graph) → conversation goal (session).

**Nice property:** goals are **never spoken** — they only steer generation — so they sidestep the privacy machinery entirely (unlike facts, a goal can't be accidentally said aloud).

**Open sub-questions (not yet decided — pin before building, so they aren't silently assumed):**
1. **Single vs. multiple goals per layer.** The mapping sketches `attrs.goals` (plural), but we only decided single-*relationship* (the relationship picker). Whether a relationship or a conversation can hold more than one goal at once is undecided. (Parallels the single-relationship-per-person call.)
2. **User-set vs. system-suggested goals.** Everything here assumes the user sets the goal explicitly. Whether the system may ever *infer/suggest* a goal (from partner recognition, history, etc.) is open — current default is explicit/user-set only.
3. **Per-person vs. relationship-type goals.** "With authority figures I want to seem competent" is a goal on a relationship *type*, not one person. The graph stores `type` on the edge, not as its own node, so type-level goals would need a convention (or a small types table). For now goals would attach per-person only.

**Status:** design recorded; expression format decided (curated menu + free text); three sub-questions above left open. **Build deferred** (Ken: "just record the design") — revisit when the conversation-engine work matures. Standing relationship goals are the smallest first build when we resume (they slot straight onto the existing graph edge + People editor).

---

## Further Design Thoughts (June 15 2026) — recorded, not built

A batch of Ken's thoughts captured this session. **Status: recorded only — none are implemented.** Items tagged **[→ Overview To Do]** also need to land in the Architecture and/or Product Overview docs; those edits are deferred and listed in the **Overview-Document To Do List** section below (Ken: don't spend editing time on the Overview docs for a couple of thoughts — let them accumulate and batch).

1. **Multi-party conversations — future feature.** Today the model assumes one communication partner. Supporting more than one partner in a conversation (knowing who said what, who a response is directed to, turn-taking among several people) is a future capability, not Phase 1/2. Touches STT (speaker diarization), the transcript UI, partner recognition (Phase 2), and the sequence-stack engine. **[→ Overview To Do]**

2. **Conversational honesty — the user is a person, not an encyclopedia (design principle).** The system must make it natural for the user to say **"I don't know,"** **"I think X but I could be wrong,"** or to state a sincere belief that happens to be wrong. It must **not** turn the user into a know-it-all / information oracle / smart speaker that others can interrogate or exploit. Rationale: the system speaks *as* the user, not *for* them — it mirrors the user's actual knowledge state and fallibility, it does not optimize for being maximally helpful/correct like an assistant. Concrete implications: the move palette should be able to surface "I don't know" and hedged/uncertain framings (good fit with the existing **DISPREFERRED = hedge + account** slot); the generation system prompt must steer toward authentic, fallible self-expression and away from assistant-style omniscience. This is a guardrail against the AAC user being treated like Google. **[→ Overview To Do]**

   **2a. No fabricated autobiography — the fabrication corollary of honesty (Ken, June 20 2026; partial fix shipped v0.3.18).** Distinct, equally critical failure Ken caught live: with Tyler in the relationship graph and "I play online games with Tyler and friends most evenings" in the profile, the partner asks "what have you been up to?" and the AI invents a **specific episode** — "I played a game with Tyler last night and beat him!" The model treated the user as a fictional character and fantasized events in his life. Fix shipped in v0.3.18 (prompt rule in `llm.js`: standing/habitual profile facts are fair game, *specific episodes/outcomes/dates/scores not given* are forbidden; keep answers general — "been gaming with friends lately" — when a specific is unknown; DISPREFERRED account can't invent a concrete excuse either). **Decisions (Ken, June 20 2026):**
   - **Prompt-only, no verification pass.** Ken's reasoning (correct, and worth keeping): a second-pass verifier has **no more ground truth than the generator** — both know only the standing profile, neither knows whether the user actually beat Tyler last night. Verification could at best catch episodes that *contradict* known facts; it can never *confirm* an unstated episode is true. So autobiography is unverifiable without the autobiography, and the prompt rule is the right primary (and only) defense for now. Probabilistic, accepted.
   - **The real test comes with episodic-event support.** "Keep it general" is correct *only because the system currently has no record of actual events.* When we add a way for the user to **record real events in their life** (an episodic/journal store — the natural complement to the standing worldview profile, and a relative of the Phase-3 user-authored "my views" RAG documents in thought #7), the AI will have true episodes to draw on, and the honest test becomes: does it use the *recorded* events correctly rather than inventing ones? Revisit the "general only" stance then. **[→ Overview To Do — pairs with the conversational-honesty entry]**

3. **"What this system is NOT" — add an explicit section (content below).** The system is **not** a personal assistant and has no intention of becoming one. It does not: review or triage your email, make appointments or manage your calendar, take notes in class, prepare study materials / sample tests, do research tasks, or otherwise act as a productivity agent. It is a **conversation** tool — it helps a non-speaking person hold real-time, interactional conversation *as themselves*. Scoping this explicitly protects the product's focus and sets partner/supporter expectations (it is not a smart speaker, not Siri/Alexa, not an LLM chatbot bolted to a tablet). **[→ Overview To Do — this is primarily a Product Overview positioning section]**

4. **"Start Listening" must be a deliberate, per-session user action — never automatic at startup. BUILT (June 15 2026), v0.2.32.** *Implementation:* `app.js` `manualListenArmed` session flag — set `true` by a manual start in `toggleListening`, set `false` by a manual stop; the automatic stop when a response is selected does **not** clear it. `handleResponseSelected` auto-resumes only when `manualListenArmed && storage.loadAutoRelisten()`. Startup was already clean (`handleStart()` never auto-listens). APP_VERSION/CACHE_VERSION → `0.2.32`. Rules satisfied:
   - Start Listening **never** takes effect simply because the app launched/started. Microphone capture begins only when the user explicitly invokes Start Listening.
   - The user must invoke Start Listening **at least once during a session** before any automatic listening can occur.
   - **Auto-resume** (the existing `auto-resume-listening` setting) is gated behind that first manual invocation: it only resumes listening after a spoken response *if* the user has manually started listening at least once this session.
   - If the user **turns listening off** during a session, the gate resets — auto-resume will not fire again until the user manually invokes Start Listening once more.
   **[→ Overview To Do — privacy/consent behavior belongs in both overviews]**

5. **Recording indicator for the communication partner (privacy — open question, my recommendation: yes).** Question Ken raised: do we need a visible UI/hardware indication that recording is taking place, primarily for the partner's benefit? **My take: yes, recommended and arguably ethically/legally prudent.** Points: (a) the partner is "unaware of system internals" by design, but a mic-active indicator reveals *no* internals — it only signals capture, which is a different thing; (b) audio-capture consent varies by jurisdiction (one-party vs. two-party/all-party consent recording laws), so a clear "listening" indicator is good practice; (c) **we don't store partner audio** — STT transcribes and the audio is discarded — but the *transcript* is stored, so "listening" is the honest signal; (d) on a Surface tablet we control only on-screen UI, so the near-term answer is a **prominent on-screen recording indicator**; a hardware capture LED (like a webcam light) is the gold standard but only relevant to a future dedicated-hardware / AR-glasses form factor. Pairs with item 4 (deliberate Start Listening) — together they make capture visible and intentional. **[→ Overview To Do]**

6. **Age 16+ target — rationale + an appropriateness filter (open question, my recommendation below).** Ken isn't certain why he set 16+; candidate reasons: (a) scarcity of free youth/preteen TTS voices; (b) concern the AI might suggest responses inappropriate for under-16s. On the filter: **yes, an appropriateness filter is achievable.** Layered options, cheapest first: (1) a **system-prompt register instruction** in `buildProfileBlock()` (set the appropriateness band); (2) an explicit content guard on generated options; (3) provider-side safety settings. **Where to set it so it can't be trivially flipped:** Ken's instinct (a password/PIN-protected Settings area) is right — it fits the existing **Setup-tier, supporter-assisted** settings concept. **Important framing:** the system speaks *as the user*; an adult user gets to choose their own register (even a coarse one) — it's their voice. So the appropriateness filter is really for **minors and supporter-managed accounts**, not a global nanny on every user. Design it as an **optional, supporter-locked profile setting** (default off for adult self-managed use; on/locked for minor accounts), not an always-on filter. **[→ Overview To Do]**

7. **Around RAG (Phase 3), accept user-authored documents representing the user's views.** When RAG support lands, let the user feed in documents (essays, prior writing, position statements, faith/values statements, etc.) that capture their views on various topics. These become retrievable worldview context — a richer, user-authored complement to the structured questionnaire. Fits the worldview/RAG progression already recorded (heuristic → client-side embeddings → vector store) and Phase 3's worldview shaping. **[→ Overview To Do — worldview/RAG section]**

8. **System requirements — be able to recommend CPU/disk specs; understand what onboard AI adds (my analysis).** Two distinct regimes:
   - **Current cloud architecture (Claude/other API):** requirements are **modest** — any modern Windows tablet running current Edge/Chrome, a few hundred MB for the cached app + local data folder, and a network connection. We should publish a simple recommended-spec sheet for this now (CPU/RAM are not the bottleneck; network reliability + a decent mic are).
   - **Onboard / local AI (running the LLM on-device):** this is a **major architectural fork**, not a setting. It introduces: a capable **GPU or NPU** with substantial **VRAM** (~8–24 GB depending on model/quantization), **16–32 GB+ system RAM**, **multi-GB model files on disk** (~4–40 GB each), plus **thermal/battery** load on a tablet — and, at tablet-feasible model sizes, **noticeably weaker quality** than frontier cloud models. It also strains the current "static web app, no backend, runs in a browser" design: in-browser local inference means **WebGPU (WebLLM/MLC)**, which is heavy and limited. Recommendation: treat onboard AI as a **separate future track with its own (much higher) spec sheet**; keep cloud as the baseline. **[→ Overview To Do — system-requirements section; onboard AI as a future track]**

9. **Future: support near-real-time and batched (asynchronous) communication too.** Beyond live face-to-face conversation, the system should eventually support **near-real-time and batched** channels — email, messaging, and stored text/speech (compose now, send/play later). This broadens the product from live conversation to the user's fuller communication life and connects to the Product Vision's framing (today's AAC users are *locked into* near-real-time/batch; we'd be supporting those modes *well*, by choice, rather than as a limitation). Future phase. **[→ Overview To Do]**

---

## Overview-Document To Do List (batch when enough accumulate)

Pending edits to **`AI-Driven AAC Architecture Overview.docx`** and **`AI-Driven AAC Product Overview.docx`**. Don't edit those docs for one or two items — let these accumulate and do a batched editing pass. (Ken, June 15 2026.) Each item notes which doc and the source thought.

- **Product Overview — add a "What This System Is Not" section.** (Thought #3 above.) Not a personal assistant / productivity agent / smart speaker; a conversation tool that speaks *as* the user.
- **Product Overview (+ Architecture, generation principle) — conversational honesty.** (Thought #2.) The user can be uncertain, wrong, or say "I don't know"; the system must not make them an encyclopedia/oracle to be exploited.
- **Both overviews — multi-party conversations as a future capability.** (Thought #1.)
- **Both overviews — deliberate per-session Start Listening + no auto-listen at startup (privacy/consent behavior).** (Thought #4.)
- **Both overviews — recording indicator for the communication partner (privacy/consent).** (Thought #5.) On-screen now; hardware LED a future-hardware concern.
- **Product Overview — target-age (16+) rationale; Architecture — supporter-locked appropriateness filter** (Setup-tier, for minors/supporter-managed accounts; not a nanny on adult self-managed use). (Thought #6.)
- **Architecture (worldview/RAG, Phase 3) + Product Overview — accept user-authored "my views" documents into RAG.** (Thought #7.)
- **Architecture — system-requirements section: recommended specs for the cloud build now; onboard/local AI as a separate future track with much higher requirements.** (Thought #8.)
- **Both overviews — future near-real-time and batched communication (email, messaging, stored text/speech).** (Thought #9.)

---

## Development phases (Ken, July 8 2026) — to-do organizing convention

Ken introduced a three-stage rollout vocabulary for organizing the to-do list by *when* work must be done:
- **alpha** — **where we are now.** Users = Ken + a few user representatives (trusted, hand-held). Data and conversations are Ken's own or demo material.
- **beta** — a few **real users** (real non-speaking individuals, real communication partners, real private conversations, real API keys, on their own devices).
- **public release** — general availability.

**The gating principle:** a risk's phase is set by when it can actually bite. Anything that captures/stores a *real* third party's speech, risks *real* user data, or exposes a *real* API key becomes load-bearing at **beta** (not alpha, where the data is Ken's own and the devices are supervised). Purely architectural hardening that needs an external decision (DNS, encryption design) tends to land at **public release**.

*Scope note:* this convention is applied below to the **Security Remediation Backlog** only. The pre-existing items under **Open Questions (remaining)** and the **Overview-Document To Do List** are **not yet** re-sorted by phase — Ken floated doing that project-wide ("we might need to start organizing the to do list by development phase"). Retrofitting the whole list is a separate, deliberate pass (many judgment calls per item); do it when Ken confirms, don't silently restructure the existing lists.

---

## Security Remediation Backlog (from the July 8 2026 security review)

Source: `security issues.docx` in the project root (10 findings, SEC-1…SEC-10, severity-ranked). Organized here by the development phase above. **Phase-1 quick fixes are DONE**; the rest are staged by when they gate the rollout.

### Alpha (done / in progress)

- **DONE — Phase-1 quick fixes (committed to local `main`, July 8 2026, no version bump).**
  - **SEC-2 (High) — "Don't save this conversation" no longer leaks partner speech.** `storage.logError` strips `extra.partner` when the conversation is private (backstop at the storage choke point, so future call sites are covered); `app.js buildErrorReport` withholds the live transcript of a private conversation from the copied bug report. *(User-facing CHANGELOG note added.)*
  - **SEC-4 (Medium) — stale `-Helix2` app copy removed from the deploy root.** Moved all 16 `-Helix2` files `app/` → `experiments/` (never deployed); `.gitignore` hard-guards `app/**/*-Helix2*`. That copy had an unescaped `innerHTML` sink predating the current UI's escaping.
  - **SEC-8 (Low) — removed the dead raw-`innerHTML` branch** from the worldview `el()` helper so it can't become an XSS sink via future misuse.
  - **SEC-10 (Low) — repo hygiene:** `.gitignore` now covers `experiments/`, `__pycache__/`, `unpacked_*/`, and the one-off `*.py` doc scripts.
  - **SEC-1 masking sub-point — DROPPED (not a defect).** The API-key field is already masked (`#apiKeyInput { -webkit-text-security: disc }`), which works on the Edge/Chrome targets. Only the *origin* half of SEC-1 remains (below). **Doc TODO:** correct `security issues.docx` — retract the "shown in plain text" claim in SEC-1 and re-scope it to the origin issue only (re-rate to Medium on its own).

### Before beta (real users, real third-party speech, real data/keys)

- **Settings persistence to the data folder + per-device profiles (Ken, July 8 2026) — PRE-BETA, see the dedicated "Settings persistence" design section below.** Today all Settings live only in origin-scoped, machine-local `localStorage`, so the SEC-1 origin move would reset every tester's settings and settings don't travel across a tester's devices. Move them into the data folder, split portable vs. device-specific, key the device-specific set by an app-minted id with a user-named "which device is this?" picker. **Ken (July 8 2026): this must go in prior to beta.**
- **SEC-7 (Medium) — prominent on-screen recording indicator + a "what the mic does with audio" note in the user manual.** As soon as beta captures a *real, unaware* communication partner (and bystanders), audio-consent law (one-party vs. all-party) is live, and Web Speech recognition ships that audio to Google/Microsoft. Make the "listening" state conspicuous for the partner's benefit; document that recognition is cloud-based in the supported browsers. *(Already recorded as a design item in Further Design Thoughts #5 and the Overview To Do — this pins it to the beta gate.)*
- **SEC-9 (Low, but a data-integrity issue) — build the already-designed Web Locks single-instance guard.** Two open instances race on the whole-file data-folder writes (worldview/relationships/logs) and cross-feed mics. Real users make silent data loss real. *(Full approach already specified under the v0.3.4 "Single-instance enforcement" subsection — this is the build.)*
- **SEC-6 (Medium), near-term portion — data-folder privacy notice at folder-selection time.** Warn that a OneDrive/cloud-synced folder replicates conversations to the cloud, and recommend a local folder for sensitive use; document BitLocker/account-password as the present-day baseline in the manual. Real users' transcripts record what caregivers/family/medical providers said in private.
- **SEC-3 (Medium) — add a Content Security Policy** (`<meta http-equiv>`): `default-src 'self'; script-src 'self'; connect-src 'self' https://api.anthropic.com; …`. Requires first moving the inline SW-registration script (`index.html:439`) to a file (or hashing it). Backstop for the escaping discipline as the editor/panel surface grows; test hard in preview (CSP breaks silently).
- **SEC-5 (Medium), documentation portion — write down the prompt-injection boundary** in the overviews: private facts are protected by *instruction, not isolation*; "Prefer not to say" is the only level that withholds a fact from the model; the manual-selection gate (no option is ever auto-spoken) is a permanent invariant.

### Before public release (architecture / needs a Ken decision)

- **SEC-1 (Medium) — give the app its own origin.** Serving from `volksswitch.github.io` shares localStorage (API key + cached personal data) with every other Volksswitch Pages project, so an XSS/compromise in *any* of them exposes this app's key. Fix = a custom subdomain (e.g. `app.volksswitch.org`): a one-line `CNAME` in `app/` **plus DNS + GitHub Pages custom-domain config — Ken's infra action.** *(Contingent risk — needs a compromise elsewhere on the account — hence Medium and public-release-gated, but pull earlier if a second Pages project starts loading third-party scripts.)*
- **SEC-1 (Medium) — supporter-locked (PIN) Setup tier for the key field**, so casual device access can't read/change the key. Fits the already-planned Setup-tier / supporter-assisted settings concept.
- **SEC-6 (Medium) — optional passphrase-based data-folder encryption** (WebCrypto AES-GCM over the JSON files). Feature-sized; trades away the "just copy the file between PCs" simplicity. Warranted before any clinic/multi-user deployment.
- **SEC-5 (Medium) — flag response cards that contain a Private-marked value.** The app knows the private values it injected, so it can badge any generated option that includes one — making a prompt-injection-induced disclosure conspicuous at selection time.

---

## Settings persistence — data folder + per-device profiles (Ken, July 8 2026) — PRE-BETA, not yet built

**Problem.** Every Setting currently lives only in `localStorage` (`aac_settings`), which is **origin-scoped and machine-local**. Two consequences: (a) the SEC-1 dedicated-origin move would reset every tester's settings to defaults (the new origin gets an empty `localStorage`), and (b) settings never travel with the data folder across a user's machines. The fix is to persist settings to the **data folder** (the origin-independent source of truth). **Ken (July 8 2026): this must ship before beta.**

**Why not one settings file, and why not key by hardware.** The data folder is shared across machines (OneDrive), so a single `settings.json` would make two devices fight over screen-dependent values. Settings split into two kinds (below). And we **cannot key by the real machine** — the browser exposes *no* hardware/system identity (hostname, serial, MAC, `COMPUTERNAME`) by design (anti-fingerprinting). `navigator.userAgent` gives OS+browser only, identical across two of the same tablet. So there is no automatic "system name as a subfolder."

**Design decisions (Ken, July 8 2026):**
- **App-minted device id + user-friendly name (not hardware-derived).** On first launch on a machine, mint a random UUID, store it in that machine's `localStorage` (per-machine, does not sync — Chrome/Edge profile sync does not sync site `localStorage`), and ask the user for a label ("Ken's Surface"). Device-specific settings go to `settings/<uuid>.json` in the data folder, with the friendly name stored inside the file.
- **Two files — portable vs. device-specific:**
  - **Device-specific** (`settings/<uuid>.json`): keyboard dock/layout/side (`keyboardDock`, `sideLayout`, `bottomLayout`, `sideDockPosition`), button sizing (`buttonSizePos`, `buttonGapPos`, `minGapPos`, `dockSepPos`), the font scales, `voiceURI`, and `keyboardMode` (physical/onscreen). These depend on the physical screen, the voices installed on that machine, and whether that device has a physical keyboard.
  - **Portable** (a shared `settings.json`): `silenceThreshold`, `initialDelay`/`subsequentDelay`, `maxPlaceholders`, `expressTapMode`, `doubleTapMs`, `noSaveDefault`, `responsesPerCategory`. These are the *person's* preferences and should be identical on every device.
  - *(Ambiguous, treated device-specific for now: font scales and `keyboardMode` — rendered size and keyboard availability track the device; revisit if a user wants them to travel.)*
- **Load on launch, write on change.** After the folder connects, read the local uuid → load `settings/<uuid>.json` + the shared `settings.json`; the folder is source of truth (same **"file in folder wins"** rule as worldview v0.2.25). Any settings change writes back to the folder.
- **"Which device is this?" picker — the graceful path for the origin move, a new/replaced device, or cleared browser data.** When no local uuid matches a file in `settings/`, present the friendly names already there and let the user **adopt** one (re-bind this machine) or **create** a new named profile. This is what makes the SEC-1 migration painless: re-pick folder → pick device → layout/voice/timings return. (The local uuid is itself lost on the origin move, so the picker, not the uuid, is what recovers the profile.)
- **API key stays OUT of the folder — re-entered per machine.** Putting the plaintext `sk-ant-…` key in a possibly-cloud-synced folder collides with **SEC-6** (unencrypted-at-rest). `lastSeenVersion` and the usage counters also stay in `localStorage` (per-install / cosmetic).

**Ties to:** **SEC-1** (the dedicated-origin migration that motivated this) and the long-planned **Cross-device transfer** feature — this *is* the mechanism that makes moving between tablets work, not merely surviving the origin change.

**Build (when authorized):** storage schema (the two files + a `settings/` subfolder + the device-id accessor), load/save wiring in `storage.js` and `app.js` (`applyButtonSizing`, `openSettings`/`saveSettings`, init), the device-profile picker UI, and a one-time migration reading the current `aac_settings` into the split files. **Status: PRE-BETA, not yet built.**

---

## Open Questions (remaining)

> **SESSION HANDOFF — where we left off (June 30 2026).** A fresh session should start here. Shipped: v0.5.40 (title bar removed → Command Bar; About Me into Settings) through v0.5.46 (side-dock default percentage layout). **Layout-budget approach now underway (Ken's plan):** (1) define a default layout as %-of-screen per region for each dock mode → (2) subdivide each region → (3) THEN define how button-size / gap / minimum-gap changes perturb it. **v0.5.46 did step 1+2 for the SIDE dock** (dock 30%W; transcript 30 / command 10 / response 60 V; command-bar 8×⅛; palette 90% + New-4 10%; gap default 0). Live threads:
> 1. **Both dock default % layouts DONE** (side v0.5.46, bottom v0.5.48). **Change-rules solver DONE for BOTH docks as a FIRST CUT** (side v0.5.49, bottom v0.5.50) — the JS solver in `applyButtonSizing()` wires button-size (grow/shrink), gap, and the minimum-gap slider into both layouts (priority min-gap › button › gap; side grows the dock width / command+response vertical, bottom grows the dock height / transcript yields). **Next: Ken reacts on the tablet and tunes**, then the refinements: calibrate the slider's right end to the true last-yielder-at-floor; gap-size should grow the expandable dock dimension (currently it only sets the rendered gap, so a big gap shrinks dock buttons in the constrained axis); the freed-space split (side command:response 1:6); per-button aspect-ratio restoration; and insetting the fixed dock by min-gap (the outer-padding deferral).
>    - **Spacing spec (Ken, June 30 2026):** OUTER padding around the whole top-level-component collection (screen-edge → cluster) = **minimum-gap** (NOT ½·gap or 1·gap); inter-component separation = **1·gap**; the dock's internal surround (dock edge → its buttons) = **½·gap** (shipped v0.5.47). `--gap-min` token added (root, default 0) as the seam; the outer-padding wiring — including **insetting the fixed-position dock** by min-gap — lands with the min-gap slider in the change-rules (at 0 it's flush/invisible, so deferred to be done testably).
> 2. **Command Bar override/interrupt matrix** — undesigned: what each Command Bar button tears down when pressed mid-modal (e.g. "Wind down" while "In my own words" is open). See that TO DO entry below.
> 3. **Defaults to eyeball on the tablet** — at the default gap (0.4rem) the Settings/About Me surface paddings read tight (gap now doubles as panel padding); the **inline word-prediction ghost color** (`--pred-ghost-color`) needs a contrast check on-device. Both are one-line tweaks once seen.
> 4. **Stale handoff doc** — `Changes-2026-06-29-for-User-Doc-Update.docx` (made for Sonnet to update the user-facing docs) predates v0.5.43/0.5.44, so it still says "Minimum button size" and the old sizing behavior. Regenerate it before handing to Sonnet (generator: scratchpad `gen-changes-doc.js`).

- **TO DO — decide the disposition of the "amber box" (`showResponseError`) before public release (Ken, July 8 2026; not built).** On a *thrown* generation error (`generateOptions`/`regenerate`/`reframe` — API/parse failure), the Response Palette is currently replaced by a worded box — the literal error string (e.g. "Couldn't get responses: 429 Too Many Requests") + a manual **"Try again"** button (`ui.showResponseError`, [ui.js](app/js/ui.js), rendered into `#responseOptions`; styles `.response-error*`). This conflicts with Ken's "no error messages in the UI — they mean nothing to the user and just confuse" stance. It was **left in place for now** (v0.5.74's faint-red transcript wash is the new non-verbal heads-up for *all* logged errors, thrown and silent). **Decide before public release:** most likely **remove `showResponseError` entirely** and rely on the transcript wash + **silent automatic retry-with-backoff** for the dominant transient API 429/529 case (recorded as the likely real fix for the demo "no options" failure), leaving the response cards simply empty until a retry succeeds. Note: the transcript wash already covers the *silent* no-usable-responses paths (empty/malformed `responses[]`, INCOMPLETE classification, superseded generation token) that never throw and never showed the box anyway — so removing the box loses no coverage. Ken has **never actually seen the box** because generation hasn't thrown in his testing. Touches `ui.js` (drop `showResponseError` + `.response-error*` CSS), `app.js` (the four `showResponseError` call sites → wash-only + optional retry).
- **TO DO — dedicated issue-reporting web page, optionally with transcript upload (Ken, July 3 2026, User-Manual comment 28; not built — outside the app).** Ken wants a web page specifically for reporting issues (today the manual §12 just links to the generic `volksswitch.org/index.php/contact-us/`). Open question he raised: should a beta tester be able to **upload the specific conversation transcript** where the problem occurred? *My take (recorded for when this is built):* yes, and it's low-effort to support because conversation logs are already JSON files in the user's data folder (`conversations/<timestamp>.json`) — the report form just needs a file-picker that accepts one of those. Caveats to design in: (a) transcripts can contain private content, so uploading must be **explicit + previewable** (show what's being sent) and honor the new per-conversation "Don't save" choice (a not-saved conversation has no file to attach); (b) strip or flag the partner's raw audio-derived text vs. cleaned; (c) include the app version + the viewport-metrics log (`viewport.js`) automatically for context. This is Volksswitch web-infrastructure work, not app code — the app side is at most an "Export this conversation for a bug report" affordance. Pairs with the existing "include the version number when reporting" guidance.
- **TERMINOLOGY (Ken, July 3 2026): the floor-holding statements are "placeholders", never "fillers."** The v0.5.59 rename scrubbed `filler`/`fillers` → `placeholder`/`placeholders` from app code (comments + identifiers) and the User Manual. Use "placeholder" going forward in code, UI, and docs. Exception kept: the grid-spacer "filler" in `keyboard.js`/`keyboard-layouts.js`/`styles.css` is a different sense (an inert layout spacer) and stays. CLAUDE.md's historical log entries keep the old word where they describe past work; that's history, not current terminology.
- **TO DO — "My Places" in the Worldview model, WITH per-place facts + Express Panel places (Ken, July 3 2026; not built).** When a places layer is added to the worldview: (a) each place is a record with **arbitrary named facts**, e.g. `Place: Starbucks` → `Location: 123 Main Street` (only when specific to one location), `favorite drink: mocha latte`, etc. — a free `attrs` bag like the relationship-graph nodes, not fixed fields. (b) **Express Panel gains a "place" item type** mirroring the existing **Partner** support (v0.5.14): a place is a toggle/influencer that tells the AI where the user is, feeding `buildSituationBlock`. Pairs naturally with Phase-2 situational awareness (GPS) as the automatic place source, and with the context-sensitive page-sets idea. Model it on `relationships.js` (nodes + `attrs`) + the Partner influencer plumbing.
- **TO DO — user-editable placeholder phrases (Ken, July 3 2026; not built).** Add an editor to add/edit/reorder/remove the placeholder statements. `placeholders.json` is already the seam (`{ acknowledgment:[], thinking:[] }`, both pools user-editable "later"); build it like the Express Panel / control-phrases editors — a portable data-folder file (`placeholders.json` moves into the data folder with the "file in folder wins" reconciliation) + its own Settings editor. Keep the two role pools (first filler acknowledges, later ones say "still thinking"). Joins placeholders / Express items / openers-closers as the configurable, data-folder-portable sets.
- **TO DO — save/restore Settings as named bulk configurations (Ken, July 3 2026; not built).** A way to snapshot the full Settings state, name it, and switch between multiple named configurations (like the on-screen-keyboard layout picker, but for the whole Settings bundle). Would let a user (or supporter) keep, e.g., a "quiet room" vs. "noisy café" profile, or an experimental profile alongside a known-good one. Relates to the Configuration Model's **named profiles + one-tap revert** concept (already specified there as the experimentation loop) — this is the concrete "export/name/restore the whole `aac_settings` bundle" build. Design qs: which stores are included (just `aac_settings`, or also the data-folder JSONs — worldview/relationships/express/control-phrases?); storage location (data folder for portability); and the switch UX. Ken framed it as a "maybe" — record + revisit.
- **TO DO — opener and closer statements dimensioned by partner (Ken, July 3 2026; not built).** Today the openers/closers (Controls editor, `control-phrases.js`) are ONE global list with a `{name}` token substituted from the active Partner. Ken wants them **per-partner**: a given person can have their own opener/closer set, so "Start conversation" with Mom offers Mom-specific openings. This is the same "dimension a configurable set by partner" pattern as the context-sensitive page-sets idea and the Express Panel Partner influencer. Likely a per-person `attrs.openers`/`attrs.closers` on the relationship graph (falling back to the global list when a person has none), surfaced in the People editor. Pairs with the Conversation & Relationship Goals model (also per-relationship). Decide: fully replace vs. augment the global list per partner; where edited (People editor vs. Controls tab).
- **PINNED — directional gaps + button-size handling in the CONSTRAINED direction (Ken, June 29 2026; design unfinished, NOT built).** Follow-on to the v0.5.41–v0.5.44 button-size/gap work. Ken's refinements to capture before resuming: **(1) Gaps need NOT be equal H vs V** in the keyboard / Express Panel. The "Button spacing" slider should grow the gap only in the direction the dock can **expand** (horizontal for a side dock — it widens into the panel; vertical for a bottom dock — it grows into the transcript). **(2)** In the **constrained** direction (vertical for a side dock = full-height column; horizontal for a bottom dock = full-width strip) the gap can't grow without shrinking the buttons. **(3) Precedence: minimum gap › button size › spacing.** Add a **"Minimum gap" setting** (new slider) that is a hard floor on the gap in every direction and **outranks button size** — i.e. button size can't increase (in a given direction) if doing so would push that direction's gap below the minimum; button size can always expand in an unrestricted direction. So at max button size the constrained-direction gap sits at the minimum and the buttons stop growing rather than shrink; **reducing button size frees room for the constrained-direction gap to grow**. **(4) Command Bar:** increasing the gap should also open space **above and below** the Command Bar buttons (taken from the transcript), not just left/right. **OPEN (Ken is to define): the simple rule for how gap + button size resolve in the constrained direction** — specifically what fills the leftover once buttons are at their set size (gap absorbs it / gap caps at the spacing value with empty space / buttons fill). Ken: "I need to come up with a simple rule." A clean CSS path exists (clamp()/calc() keyed off `--kbd-rows`/`--kbd-cols` + `100vh`/`100vw` for the constrained extent), and a `--gap-min` token + a Minimum-gap slider, but **do not build until Ken supplies the rule.** *(An inert token scaffold `--gap-cross`/`--gap-h`/`--gap-v` was prototyped then reverted to keep the tree clean.)*
- **TO DO — define what each Command Bar button can OVERRIDE / interrupt (Ken, June 29 2026; not built):** with the controls consolidated into the Command Bar (v0.5.40), we need to specify, per button, what state it interrupts when pressed mid-action — especially when a **modal action is open**. Ken's example: if the user has opened **"In my own words"** (the composer modal overlaying the response footprint, keyboard over the dock) and then presses **"Wind down"**, the composer should be **dismissed and the standard base UI restored** (the response palette / Response Palette reservation reappears, keyboard hidden) before the Wind-down (pre-closing) flow runs. So a Command Bar press is not just "do my action" — it may first need to **tear down whatever modal/transient state is active** and return to the base screen. This generalizes: which Command Bar buttons cancel the composer modal? which cancel an in-flight generation? which are no-ops vs. which seize the floor? Tie into UI Layout **Rule 8** (modal-action assets superimpose over inert base elements) — the inverse question is which base controls stay *live* during a modal and what they do to it. Produce a small matrix (button × current-state → effect) and wire the teardown consistently. Pairs with the existing engine override handlers (`handleWindDown`/`handleInitiate`/etc.) which currently assume the base screen is showing.
- **DONE (v0.5.41) — user-facing button-size + spacing settings (Ken, June 29 2026).** Built as **two unitless sliders** (Settings → Speech & Input): **"Minimum button size"** drives `--btn-min-dim`; **"Button spacing"** drives a new `--grid-gap` (= keyguard bar width). Resolved the open questions: **(1) one global scalar** (Ken: "affects all buttons"); per-region sizing per Rule 5 (bigger for more-frequent actions) is a deferred refinement a single scalar can't express. **(2) Content buttons reconcile by GROWING THE DOCK, not flooring the keys:** `--dock-w`/`--dock-h` = `min(cap, max(floor, rows/cols × --btn-min-dim + gaps + pad))`, so the bottom dock grows taller / side dock grows wider just enough that keys clear the minimum (the v0.5.27 overflow worry is gone — the container yields, not the key). **(3) Unitless slider** (not presets/mm — mm is unenforceable without real DPI). **(4) Setup-tier** (moves keyguard holes). The transcript is the shrink victim (3rem floor); caps (72vh/70vw) keep the main area usable. **This UNBLOCKS the word-prediction placement** (next item) — the button-size questions Ken wanted settled first are now settled.
- **TO DO — explore a "phrase expansion" (abbreviation) feature (Ken, June 29 2026; not built):** let the user type a short abbreviation that auto-expands to a longer phrase on a word boundary — e.g. **"btw" → "by the way"**, "omw" → "on my way", "ty" → "thank you". This is a **fully local, deterministic, user-configurable** map (no AI, no network — like the rest of the local prediction tier) — distinct from both (a) the inline word *prediction*/completion (v0.5.42, which completes a prefix to a single real word) and (b) the LLM-driven "AI-assisted composition" (few words → a full sentence in the user's voice). **Two behaviors per entry, because some abbreviations expand and others should just be *spoken* differently (Ken, June 29 2026):** (i) **text expansion** — replace the typed abbreviation in the box with the full phrase ("btw" → "by the way"), which changes both what's displayed and what's spoken; (ii) **spoken-form override** — the displayed text stays the abbreviation but TTS says it differently, e.g. **"lol" should be spoken as the initialism "L-O-L"** (spelled out), not read as the word "lol" or expanded to "laughing out loud". Behavior (ii) is exactly the **[[display-text-vs-spoken-text]] / pronunciation-lexicon** TODO (display form ≠ spoken form, applied as a TTS-time substitution) — so the abbreviation feature and the pronunciation lexicon likely share one user-owned map/editor: each entry has a display form + an optional expansion (rewrites the box) and/or an optional spoken form (overrides TTS), with "spell it out letter-by-letter" as a built-in spoken mode for initialisms. **Natural seam:** the same word-boundary handling in `keyboard.js` where a separator is typed — when the just-typed "word" exactly matches an abbreviation in the map, replace it with the expansion before inserting the separator (mirrors how `acceptGhost()` already rewrites the word at the boundary). **Design questions to settle:** (1) it should be a **user-editable set** (joins placeholders / Express items / openers-closers as a configurable, data-folder-portable list with its own editor); (2) case handling (match case-insensitively? preserve/capitalize the expansion to match?); (3) whether an expansion can itself be a multi-word phrase with punctuation; (4) interaction with the inline ghost (an abbreviation like "btw" shouldn't also show a misleading word-completion ghost — probably suppress the ghost when the prefix matches a known abbreviation, or show the expansion AS the ghost); (5) ship a small starter set of common abbreviations as the default. Likely a small, high-value patch once the editor pattern is reused. **[relates to [[inline word prediction]] and the AI-assisted-composition build-order item]**
- **DONE (v0.5.42) — word prediction returns as an INLINE GHOST, not buttons (Ken, June 29 2026).** Resolved the "where do prediction buttons live" problem by NOT using buttons: the completion shows **inline in the text box** (bold, tinted, tunable color) and is accepted by typing **any word-separator** (space/comma/period/…), so it costs zero grid space, needs no keyguard alignment, and has no moving tap target. Built in `keyboard.js` (non-interactive mirror overlay `#predGhost`) — see the v0.5.42 row. The old `.kbd-preds` button overlay stays hidden/unused (infra preserved). *Remaining/optional:* prediction for physical-keyboard typists (the ghost is currently on-screen-keyboard only); showing >1 candidate (inline is single-suggestion by nature).
- **FUTURE — non-English language support (Ken, June 27 2026; not built).** Conversant AAC will support languages other than English in a future version. No architecture audit is needed before then — the modular design means adding language support is mostly content/configuration work, not structural change. Known touchpoints to revisit when this feature is scoped: (1) **STT** — `SpeechRecognition.lang` needs to become a user-settable language preference (`stt.js`; currently defaults to the browser's language); (2) **TTS** — voice selection already exposes language-tagged voices via `speechSynthesis.getVoices()`; the picker just needs language grouping; (3) **LLM prompt** — `llm.js` `buildProfileBlock()` needs a language instruction ("respond in Spanish") when a non-English language is configured — a prompt addition, not a structural change; (4) **Hardcoded content** — `placeholders.json`, Express Panel defaults (`express-items.js`), openers/closers (`engine.js`), and worldview question text (`worldview-questions.json`) would each need per-language versions; the files/seams exist; (5) **Word prediction** — `data/words.json` is English-only; non-English languages need their own frequency lists (`prediction.js` is otherwise language-agnostic); (6) **Keyboard layouts** — the layout data structure already supports arbitrary key arrangements; non-Latin scripts need new layout data, not code changes; (7) **UI chrome** — all button labels, settings names, and hint text are hardcoded English strings; full UI i18n would require extracting these to a localization file (a UI pass, not an architecture change). *No audit required now; revisit this list when the feature is formally scoped.*
- **FUTURE — context-sensitive page sets for fast phrases / partners / feelings (Ken, June 26 2026; not built):** swap the fast-phrase panel's content based on *context* — where I am, who I'm talking with, or what I'm doing — so the phrases (and the partner/feeling influencers) on offer fit the situation. A natural extension of the user-editable fast-phrase sets + the Partner/Feeling toggles (v0.5.14). Connects to Phase-2 situational awareness (GPS / partner recognition / calendar) as the context source, and to the editor (a "page set" becomes a named, context-bound layout the user defines). Record now; build after the editor + situational awareness land.
- **TO DO — multiple options per move slot (Ken, June 19 2026; not built):** let the AI optionally provide more than one PREFERRED / DISPREFERRED / INITIATIVE option. This changes the **four-slot, positionally-stable palette** (preferred always first, repair always last) that is fixed for motor automaticity, so it needs a deliberate design choice between two shapes: **(a) a configurable count per slot** (e.g. 2 PREFERRED, 1 DISPREFERRED) rendered as a grouped/expandable palette — more powerful but erodes the stable-geometry invariant; or **(b) a per-slot "more like this" expander** that keeps the four-slot geometry and reveals extra options on demand. Recommendation leans (b) to preserve the invariant. Pairs with the **whole-palette "Show me different options" regenerate** shipped in v0.3.16 (regenerate replaces the set; this adds *more within* a slot). Decide the shape before building; touches `llm.js` (moves schema / count), `engine.js` (`paletteFromMoves` priority/ordering), and `ui.js` (rendering).
- **TO DO — pronunciation: display vs. spoken text for proper nouns + static statements (Ken, June 16 2026; not built):** browser voices (Google + Microsoft) mispronounce proper nouns and ignore prosody punctuation, so the only usable text-level fix is **phonetic respelling** — which means the displayed string and the spoken string must diverge. Build: (1) a **pronunciation lexicon** (display-form → spoken respelling) applied as a **TTS-time substitution on every utterance**, so names are corrected everywhere they're spoken — including inside LLM-generated responses the user never typed; (2) **`{ display, speak }` capture UI** for proper-noun answers in About Me + the People editor (testable in-flow via "Speak my answer"); (3) the same `{ display, speak }` pair for static statements (app-provided: placeholders, "Please repeat what you said.", openers/closers, ACK tokens, `placeholders.json`; and user-embedded custom phrases), `speak` falling back to `display`. Open question to settle first: per-field pairs feeding a global map vs. a standalone user-managed pronunciation dictionary. Reuses the **Response Option Display** compressed-display/expanded-speech model. Full spec in the **Display text vs. spoken text** bullet under the TTS-inflection item below. Smallest first build: the TTS-time substitution step + a simple lexicon (pays off immediately for both questionnaire read-back and generated responses).
- **TO DO — single-instance enforcement (approach decided, not built; Ken, June 16 2026):** prevent multiple live instances/launches of the app. Approach: **Web Locks API** (`navigator.locks.request` for an exclusive lifetime-held lock → later tabs/windows show a blocking "already running" screen instead of starting mic/engine) + **PWA manifest `launch_handler: { client_mode: "focus-existing" }`** (re-launching the installed app focuses the running window). Why it matters: two instances feed each other's mics with their own TTS (cross-instance feedback the v0.3.4 content-echo filter can't catch) and race on the FSA data-folder writes. Boundary: web APIs can't coordinate across different browsers/profiles (one-browser dedicated device assumed; an FSA folder lock-file is the only cross-browser option, heavier). Full spec in the **Single-instance enforcement** subsection under the v0.3.4 Test-suite Pass 1 section.
- **Relationship data restructure — BUILT (v0.2.27):** `relationships.json` graph model, People editor in About Me, LLM injection. See the **Relationship Data** section above. Remaining refinements: per-person richer attrs, person↔person edge editing in the UI, and confirming on-disk persistence on the tablet.
- **Conversation & relationship goals — design recorded, not built:** three-layer model (disposition → standing relationship goal → conversation goal) mapping to worldview Tier-B / `relationships.json` edges / session runtime; expression = curated menu + free text. See the **Conversation & Relationship Goals** section above. Standing relationship goals are the smallest first build (slot onto the existing graph edge + People editor).
- Response option UI layout: `UI-Design.docx` now specifies the four-region layout and move palette; remaining question is screen allocation between traditional AAC vocabulary and the AI-facilitated regions (composer-access region is the integration point; ultimately user-configurable per the Configuration Model)
- Worldview questionnaire: design complete and the five open decisions resolved (June 14 2026); **Build Steps 1–4 implemented and verified — core worldview feature complete** (data model + registry, questionnaire UI, LLM integration, interim Name/About You removed). See the **Worldview Questionnaire (June 2026)** section. Remaining steps (5 expand Tier-B/C content, 6 RAG-lite) are enhancements.
- Placeholder utterances: currently drawn randomly from a static JSON file (app/data/placeholders.json). Predictable fillers will become a joke to communication partners over time. LLM-generated contextual fillers would sound more natural and could acknowledge the topic, but must be evaluated against token cost impact. The architecture already supports user-funded API keys, so any added cost is borne by the user. *Design now specified:* the filler ladder in `Conversation-Engine-Design.docx` §6 (rung 1 static token ≤1 s, rung 2 static-or-contextual, rung 3 re-fill); contextual fillers are the rung-2 LLM option in the Configuration Model. Remaining work is implementation and the token-cost evaluation.
- TTS inflection: browser TTS has limited control (pitch, rate, volume on SpeechSynthesisUtterance). Natural-sounding placeholder delivery matters — monotone fillers will sound robotic. Voice banking / cloned voices (future) would give much better inflection control. Evaluate whether pitch/rate tweaks on fillers can improve naturalness in the near term.
  - **SSML does NOT work through the browser Web Speech API (finding, Ken's question June 16 2026).** The Windows built-in (SAPI/OneCore) and Google Chrome voices we use are driven via `SpeechSynthesisUtterance`, which has **no SSML support** — passing `<speak>`/`<prosody>`/`<break>` markup as the utterance text is read literally or stripped, not parsed (Chrome has never supported SSML here; don't depend on it). So inserting SSML "modifier characters" into a phrase to control inflection/timing is not an option with the current stack. **What actually works in-browser, three levers:** (1) **per-utterance** `pitch` (0–2), `rate` (0.1–10), `volume` (0–1) — set on the whole utterance, not mid-phrase; (2) **split a phrase into multiple chained utterances** each with different pitch/rate to fake an inflection contour (crude but the only way to vary prosody within a sentence); (3) **punctuation** — commas / periods / ellipses / em-dashes genuinely affect pause length and some intonation in most engines (this is the real "insert characters" answer), and `?`/`!` nudge intonation on some voices (engine-dependent, subtle). **Full SSML** (`<prosody rate/pitch>`, `<break time>`, `<emphasis>`, `<say-as>`) requires a **cloud TTS engine** — Google Cloud Text-to-Speech, Azure Speech, or Amazon Polly — which is a different architecture (network + per-character cost, user-funded like the LLM) and would plug into the same swappable TTS abstraction as voice banking. Near-term naturalness work is therefore limited to levers 1–3; richer control is the cloud-TTS / voice-banking future track.
  - **Reference doc:** `Shaping-Speech-Through-Text.docx` (generator `generate-tts-pronunciation-doc.js`) — practical per-character guide (punctuation for pacing/intonation, characters that change pronunciation, the per-voice caveat, a testing protocol via the "Speak my answer" button, and placeholder before/after candidates). Regenerable on request.
  - **EMPIRICAL FINDING (Ken tested, June 16 2026): punctuation/special characters for *prosody* are IGNORED by both the Google and Microsoft browser voices.** Commas/periods/ellipses/em-dashes did not produce usable pause or intonation changes on the voices we ship with — so **lever 3 (punctuation for pacing/intonation) is effectively a dead end** with the current stack. The doc's Section 3 is therefore over-optimistic for these engines and should be de-emphasized when next regenerated. **What remains usable at the text level is pronunciation, not prosody: phonetic respelling (and syllable hyphenation) of words the voice says wrong** — this is the lever Ken is investigating for proper nouns. Prosody control (pauses, pitch contour) is left to the code levers (per-utterance pitch/rate/volume, phrase-splitting) and, for real control, the cloud-TTS future track.
- **Display text vs. spoken text — needs TWO properties, and it extends to proper nouns in user data (Ken, June 16 2026; NOT built).** A consequence of the pronunciation work above: when a word is shown one way but must be *said* another way (phonetic respelling so the voice pronounces it correctly), the displayed string and the spoken string diverge. This applies in two places:
  - **Static UI statements** (app-provided AND user-embedded): placeholders, "Please repeat what you said.", "Sorry, what was that?", "Hold on, let me think.", the openers/closers, the rung-1 ACK tokens, `app/data/placeholders.json`, and any user-authored static text (custom starters/fillers, canned phrases) → each becomes a **`{ display, speak }` pair** (clean text shown; respelled text spoken), `speak` falling back to `display` when absent.
  - **Proper nouns in About Me / relationship data (Ken's June 16 2026 extension):** names of people, places, pets, organizations entered in the questionnaire and the People editor are exactly the words browser voices mispronounce. Each proper-noun answer should carry a spoken form alongside its displayed form (testable right there via the in-flow "Speak my answer" button).
  - **This is the same pattern already specified for response options** (compressed *display* hint vs. expanded *spoken* text — see **Response Option Display**); generalize that one model, don't invent a second.
  - **Design note / my recommendation (raise before building):** proper nouns differ from static strings in one way that matters — they also appear inside **LLM-generated responses the user never typed** (a generated reply that names a friend). A per-field `{display, speak}` fixes the *displayed answer* but NOT the generated occurrence. The robust mechanism is a **pronunciation lexicon** (display-form → spoken respelling) applied as a **TTS preprocessing substitution on every utterance** — so one entry corrects the name everywhere it is spoken (questionnaire read-back, generated responses, starters). Per-field pairs are the *capture UI* for that lexicon. Open question: keep both (per-field pair feeds a global map) vs. a standalone user-managed pronunciation dictionary. Touches placeholders module + JSON, engine OPENER/CLOSER palettes, hard-coded prompt strings in `app.js`/`ui.js`, the worldview + relationships data models, and a TTS-time substitution step. Not built — record now so it's designed in when those strings/fields are next touched.
