# Document sync tracker

Tracks when each **product document** was last reviewed for content currency
against the code + design record, so "update documents X, Y, Z" has a precise,
unambiguous starting point.

## What's tracked

Every document in **`Documents/`** whose name begins with **"Conversant AAC"** (Ken,
July 8 2026; the documents moved out of the project root on August 11 2026) —
excluding `.bak` backups. These are the living product
documents; the `Other/` folder (research, article drafts, archived versions) is not
tracked.

**Two audiences, two vocabularies (Ken, August 1 2026).** The engineering documents
(Architecture Overview, Platform Measurements, and CLAUDE.md alongside them) name the two configurations by
browser engine — **Chromium** and **WebKit** — because that is the actual causal
variable and it makes "install Chrome on the iPad" impossible to write. The
reader-facing documents (Product Overview, both User Manuals) must **never** use
those words; they say **"Windows, Chromebook and Mac"** and **"iPad"**, and describe
the iPad's two modes as "in the Safari browser" and "installed on your Home Screen".
Longer but recognizable beats concise but unexplained.

## How this works (why not modification date)

A document's file modification date is **not** a reliable "is it current" signal — a
format-only edit bumps the mtime without changing content. Instead, each document
records the **git commit it was last reviewed against** (`At commit`), plus a human
date. The change history to review against lives in git: the code, `CLAUDE.md` (the
design decisions), and `CHANGELOG.md` are all version-controlled; the `.docx` files
themselves are git-ignored (OneDrive), so their timestamps were never the right
anchor.

## Spoken Settings help is guarded by a TEST, not by this process (Ken, August 2 2026)

Ken asked that `settings-help.json` — the words spoken when you arm the **?** in the
Settings title bar and tap a control — *"be included in the Doc-sync process so that
it is updated when a new control is added to Settings."*

**It is enforced one better: `tests/settings-help.test.mjs` fails the build** if a
Settings control, radio group, tab or `data-help` section has no entry, if an entry
points at something that no longer exists, if a phrase runs past 35 words, or if the
bundled copy has drifted from the root file. So a new setting *cannot* ship without
its spoken help, and no one has to remember a checklist item.

**What still needs a human**, and why it is listed here rather than left to the test:

- The test proves an entry **exists**, never that it is **true**. Change what a
  control does and the test stays green while the app says something wrong.
- **Re-run `node scripts/apply-settings-help.mjs` after editing the JSON.** The test
  catches a missed run, but only once someone runs the tests.
- The register is the point: **15–25 words, written to be heard.** Do not paste from
  the manuals — that prose was written to be read, and several entries there run past
  eighty words. The 35-word cap is a backstop, not the target.

## Generators cannot be run casually (August 17 2026)

Most of these documents are produced by a script in `scripts/doc-generators/`, and a sync
pass edits the `.docx` directly. Those two facts together mean **running a generator can
silently revert a document** — a sweep on August 17 2026 found six that would have.

- **Every document is now hash-guarded.** A generator refuses to overwrite unless the
  document is byte-identical to what it last produced (`doc-manifest.json`). Fold the
  document's edits into the generator, then run it. Details and the override are in the
  header of `doc-paths.js`.
- **Check without touching**: `OUTPATH=<scratch>.docx` redirects the write so you can diff
  first. This is the first step of any fold, and the guard stands aside for it.
- **After editing a `.docx` in a sync pass, fold it back in the same pass**, or the next
  person meets a refusal and has to reconstruct what you did.
- **Currently stale, guarded, and awaiting a dedicated pass:** Architecture Overview,
  Conversation Engine Design, Conversation Engine Overview. Their documents are correct;
  their generators are behind.

## Release first, then sync (Ken, August 1 2026)

**Push the app before syncing the reader-facing documents, not after.** The August 1
sync documented three things that were still sitting in local commits — the Settings
**Speech** tab, the **Buttons & Keyboard** rename, and the **X** that replaced the
Close button — so for as long as they stay unpushed, both User Manuals and the
Product Overview describe a screen no tester has. That is the one failure a manual
must not have.

- **Reader-facing** (Product Overview, both User Manuals): sync **after** the push, so
  "the documents describe what is live" is invariantly true and they can be handed
  out without checking first. If they are synced early, **do not distribute them
  until the release lands.**
- **Engineering** (Architecture Overview, Platform Measurements, CLAUDE.md): may run
  ahead of deployment. They are design records, and the design is settled before the
  code ships.
- **`At commit` must name a commit that exists on `origin/main`.** A stamp pointing at
  a local-only commit asserts currency against a history nobody else has. Where a sync
  had to run early, stamp it at the release commit once the push lands.

## A document under review is NOT a candidate for syncing (Ken, September 8 2026)

**Tracked changes or comments mean STOP** — do not edit that document at all until they
are accepted and cleared, which is Ken's call and never an automated one. Check with
`python scripts/doc-tests/check-tracking-artifacts.py`; `check docs` reports it too.
Full reasoning, and the corruption it caused on September 8, is in CLAUDE.md.

## Nothing was asking whether a sync was due (September 8 2026)

`python scripts/doc-tests/check-doc-currency.py` reads the table below and reports any
**reader-facing** document not reviewed since a release shipped, naming the versions it
has missed. It runs as a step of the release ritual, and it is the answer to a failure
this file could describe but not detect.

**Three releases went out with no document touched and nothing said so.** 0.10.6, 0.10.7
and 0.10.9 all shipped undocumented; the whole of the 0.10.7 work was missing from all
three User Manuals. **The changes were recorded correctly** - in `CHANGELOG.md` and in
`CLAUDE.md` - so this is not the "miss a recording" frailty the purification pass is
aimed at. It is simpler and worse: **the sync simply never ran, and no part of the
process asked.**

**Scoped to the four reader-facing documents on purpose.** Two dozen design records are
permanently behind some release, and a check that is always red is one people learn to
scroll past. A design record being older than the last release is usually correct; a
manual being older than the last release means a tester is holding a description of a
screen that no longer exists.

**Trigger phrase (Ken):** **"sync docs"**, optionally naming documents — e.g. "sync
docs", "sync docs Product Overview", "sync docs the two overviews and the user manual".

**To bring a document current** ("sync docs: Product Overview"):
0. **Back up first (MANDATORY):** copy the `.docx` to `Documents/Doc Backups/<name> <YYYY-MM-DD_HHMMSS>.docx`
   before touching it. Never edit a product document without a fresh backup in the
   same pass.
1. `git log <that doc's At-commit>..HEAD` — the exact commits since its last review.
2. Read the `CLAUDE.md` / `CHANGELOG.md` entries added in that window.
3. Filter to what's relevant to *that* document's scope, verify any UI detail against
   source, and apply the edits.
   **Update the title page’s "Last updated" line to today’s date.** Mandatory on every sync; add the line
   beneath the byline if it is not there yet. The byline’s date is the CREATION date and
   never moves, so on its own it cannot tell a reader whether what they are holding is
   current.
4. Update the doc's row here: `Status` = `✓ current`, `Last reviewed` = today,
   `At commit` = current `HEAD`, and a note of what the pass covered + any residual.

The tracker bounds the window; judging relevance within it is done at review time.

## Baseline (Ken chose option (a), July 8 2026)

Each document gets a real catch-up review *before* its baseline is stamped to a
commit; a completed row shows `✓ current` with `At commit` = the `HEAD` at review
time, and a `⚠ catch-up pending` row's `At commit` is not yet a trustworthy baseline.

**Done July 8 2026: the three beta-facing docs — Product Overview, Architecture
Overview, User Manual** — caught up and then re-synced for the **0.5.78** release
(stamped at commit `5f672f5`). The remaining design docs are still pending (next
catch-up pass). The Architecture Overview has an image residual noted in its row
(Figures 6 & 7).

## The title page carries the date the document was last brought current (Ken, August 21 2026)

Ken: put the modification date **next to or in place of the creation date**, not on a
line of its own. So it sits on the byline itself — *Kenneth R. Hackbarth | Volksswitch.org
| June 2026 | Last updated August 20, 2026* - and where the byline's own date is already
the same day, "Last updated" simply replaces it rather than printing the date twice.

The rule had existed for a while, but a document only picks the line up when it is next
synced, so only two had one. All 23 prose documents now carry it.

**Five documents had no byline at all** - Continuous Partner Capture, Conversation Engine
Overview, Keyboard Layout Options, Keeping Costs Down and Practice Mode. Rather than give
those a date floating on its own, Ken had them given a proper byline (August 21 2026), so
every document's title page now names an author and a date in the same place. **The
creation month came from each document's own page footer**, which has always carried it -
so the byline states what the document already said about itself, rather than a date
guessed from a file timestamp. Keeping Costs Down is bylined *Conversant AAC* rather than
Ken, matching Backup Compatibility: both are written for the person using the app, not
about the project.

**The date on each is that document's `Last reviewed` date from the table below, not the
date of this sweep.** Stamping today on all of them would have claimed every document was
brought current today, which is exactly the false signal this tracker exists to avoid.
Nothing was reviewed for content in this pass, so no row's `Last reviewed` or `At commit`
moved.

**The Partner Card is deliberately excluded.** It is a print-and-cut sheet of eight
identical cards handed to a communication partner - the date would print on the card
itself, where it has no reader.

## Page headers are uniform: "Conversant AAC - <Document Name>" (Ken, August 21 2026)

Swept alongside the byline work, because a title page that names the product and a page
header that names something else is the same defect twice. Every document now carries the
same right-aligned 9pt grey italic line on every page.

Three were **wrong**, not merely missing: Continuous Partner Capture and Keyboard Layout
Options still said **"AI-Driven AAC"**, the working name dropped when the product was named
in June 2026; and iPad Platform Measurements was headed **"iPad Architecture"** - the name
of the document it was split out of and which is retired, so every page pointed at
something that no longer exists. Seven more had **no header at all** (Configuration Model,
Practice Mode, Security Issues, UI-Design, Worldview Implementation Plan, and both User
Manuals, whose two sections each needed one).

**Two residuals, not fixed here because they are footers, not headers:** Configuration
Model and UI-Design have **no footer at all**, so they carry neither the
"Volksswitch.org | Month | For internal use" line nor the "Page m of n" numbering the
June 15 2026 convention requires. They are the only two documents missing it.

## Documents

| Document | Status | Last reviewed | At commit | Outstanding / notes |
|---|---|---|---|---|
| Conversant AAC Product Overview.docx | ✓ current | 2026-09-09 | `5ee4199` | **Reviewed Sep 9 2026 for 0.10.14 - NO EDIT NEEDED, restamped.** 0.10.14 is import mechanics: a progress card, a restart prompt and a guard against a second press. This document says what the product IS, and nothing in it was made wrong by any of that - its export passages were already corrected for 0.10.13 earlier today. *Prior:* **Synced Sep 9 2026 for 0.10.13.** One sentence, and it was made wrong by this release rather than merely dated: the iPad mode-switch passage said to "export the data first and import it afterwards", which since 0.10.13 carries the data and NOT the settings. It now names both files. The rest of the document is unaffected - it describes what the product IS, and the backup split is mechanism. *Prior:* **Reviewed Sep 8 2026 for 0.10.12 - NO EDIT NEEDED, restamped.** 0.10.12 is Settings appearance, a practice-partner refinement and button labels; none of it reaches a document that describes what the product IS rather than how its panel looks. |
| Conversant AAC Architecture Overview.docx | ✓ current | 2026-09-08 | `90312e3` | **Synced Sep 8 2026 for 0.10.10.** Five paid transcription backends and five paid voices, not two; stt-rest.js and tts-rest.js added to the module list, with the note that the three newest are catalog data rather than modules; the TTS-interface entry records the 0.10.10 adapters. Also corrected a claim that was wrong before this release: only Deepgram streams words as they are spoken - Azure batches too, and the document said otherwise by implication. |
| Conversant AAC User Manual (Windows Chromebook Mac).docx | ✓ current | 2026-09-09 | `5ee4199` | **Synced Sep 9 2026 for 0.10.14.** The import walkthrough gained what Ken needed and did not have: a large restore takes a while, the app holds the screen and counts its way through, and the reader should WAIT rather than tap anything else - which is the exact mistake that cost him his import on an Android tablet - and it asks for a restart when it finishes. *Prior:* **Synced Sep 9 2026 for 0.10.13.** The section they described was renamed and split: "Backup & transfer" is now "Data and settings backup", holding "Your data" and "Your settings" separately. Rewritten: a data export no longer carries settings, settings have their own export and import, the settings file takes every saved profile and which one is in use, exporting offers to fold unsaved changes into the current profile first, and a profile whose name you already use arrives as "Home (2)" rather than replacing yours. Both files are written into the data folder where there is one.  **One passage was made WRONG by the release rather than merely dated** and is the reason this could not wait: the folder-switch passage sent the reader to "Export my data" to carry settings profiles across, which that file no longer holds. **The Placeholders reorder arrows were removed in this release and needed no edit** - no manual ever documented them. *Prior:* **Synced Sep 8 2026 for 0.10.12.** Two rows: the practice partner voice now always comes from the same service you speak with and resets to "Auto" when that changes; and testing a key also fetches your account's voices and says how many it found, or says it could not. The Settings appearance work in this release is not described here - the manuals say what a control DOES, not what it looks like. |
| Conversant AAC User Manual (iPad).docx | ✓ current | 2026-09-09 | `5ee4199` | **Synced Sep 9 2026 for 0.10.14.** The import walkthrough gained what Ken needed and did not have: a large restore takes a while, the app holds the screen and counts its way through, and the reader should WAIT rather than tap anything else - which is the exact mistake that cost him his import on an Android tablet - and it asks for a restart when it finishes. *Prior:* **Synced Sep 9 2026 for 0.10.13.** The section they described was renamed and split: "Backup & transfer" is now "Data and settings backup", holding "Your data" and "Your settings" separately. Rewritten: a data export no longer carries settings, settings have their own export and import, the settings file takes every saved profile and which one is in use, exporting offers to fold unsaved changes into the current profile first, and a profile whose name you already use arrives as "Home (2)" rather than replacing yours. Everything still leaves by the share sheet, there being no folder to write into.  **One passage was made WRONG by the release rather than merely dated** and is the reason this could not wait: the mode-switch and second-iPad passages, and the section 5.2 walkthrough, all said to export the data - which since this release leaves the settings and profiles behind on the old device. **The Placeholders reorder arrows were removed in this release and needed no edit** - no manual ever documented them. *Prior:* **Synced Sep 8 2026 for 0.10.12.** Same two rows as the Windows manual. |
| Conversant AAC User Manual (Android).docx | ✓ current | 2026-09-09 | `5ee4199` | **Synced Sep 9 2026 for 0.10.14.** The import walkthrough gained what Ken needed and did not have: a large restore takes a while, the app holds the screen and counts its way through, and the reader should WAIT rather than tap anything else - which is the exact mistake that cost him his import on an Android tablet - and it asks for a restart when it finishes. *Prior:* **Synced Sep 9 2026 for 0.10.13.** The section they described was renamed and split: "Backup & transfer" is now "Data and settings backup", holding "Your data" and "Your settings" separately. Rewritten: a data export no longer carries settings, settings have their own export and import, the settings file takes every saved profile and which one is in use, exporting offers to fold unsaved changes into the current profile first, and a profile whose name you already use arrives as "Home (2)" rather than replacing yours. Both files are written into the data folder where there is one.  **One passage was made WRONG by the release rather than merely dated** and is the reason this could not wait: the folder-switch passage sent the reader to "Export my data" to carry settings profiles across, which that file no longer holds. **The Placeholders reorder arrows were removed in this release and needed no edit** - no manual ever documented them. *Prior:* **Synced Sep 8 2026 for 0.10.12.** Same two rows as the Windows manual. |
| ~~Conversant AAC User Manual.docx~~ | **retired 2026-08-01** | — | — | **Split into the two self-contained manuals above** (Ken, Aug 1 2026: *"humans MUCH prefer a single document rather than trying to manage two so self-contained is the way to go"*). Moved to `Other/Previous Doc Version/Conversant AAC User Manual (pre-split 2026-08-01).docx`. **Shared work applied to both halves before splitting:** the Settings restructure that shipped today — the new **Speech** tab (one table covering the Deepgram key, hearing, speaking voice, and the practice partner voice), "Speech & Input" renamed **Buttons & Keyboard** with the two voice rows moved out of its table, §§6.4–6.8 renumbered to 6.5–6.9 with every cross-reference chased, the tab count corrected to eleven, and the Settings **Close button replaced by the X in the title bar** in five passages. **STANDING RULE from here on: a change to shared behavior must be applied to BOTH manuals in the same pass — a sync that touches one and not the other is incomplete.** *Prior pass (for history):* | Caught up through v0.5.98 (from the stale `3c407de` baseline, ~20 releases). Fixed: turn-completion no longer gated (v0.5.82 reversal), About Me is now a Settings tab not a full-screen overlay with "Done" (v0.5.83), wind-down/closing described as two distinct steps (v0.5.93), layout names "Side/Bottom Keyboard N" → "Side/Bottom Layout N" (v0.5.85), stale "Section 6.6" self-reference fixed to 6.8. Added: Settings profiles row (General), Practice partner voice row + closed-set choice-chip new §4.6 (Speech/Conversation tabs), new §6.5 Practice Tab (v0.5.97/0.5.98), Keyguard Design tab content for Keyboard/Transcript separation (v0.5.84), API-key Test button + no-key pre-start prompt + AI-optional Note (v0.5.79/v0.5.94), listening chime + pulsing Listen button (v0.5.96/v0.5.98 once-per-conversation nuance), "Actually, before you go —" decline-closing note, Troubleshooting row for blue-italic uncleaned transcript, four new Glossary terms (Closing, Wind-down statement, Choice card, Practice Mode). All UI wording verified against index.html/app.js/engine.js/keyboard-layouts.js source. Word's TOC auto-field refreshes on next open in Word. |
| Conversant AAC Backup Compatibility.docx | ✓ current | 2026-09-08 | `90312e3` | **Synced Sep 8 2026 for 0.10.10.** Every mention of "your Deepgram or Azure key" now reads as the speech-service key, and points at the one Keys section. NOT flagged by check-doc-currency.py - it is not classified reader-facing, so it was found only by sweeping every document for the stale claim. Worth reclassifying: this one is written for the person using the app. |
| Conversant AAC Conversation Engine Design.docx | ✓ current | 2026-08-24 | `6d5249a` | **Synced Aug 24 2026 for 0.8.0 (the Express Panel bands).** The Express Panel choice-chip paragraph corrected to the far end of the Context band, with a standing reservation of four. *Prior:* Caught up to v0.5.93–v0.5.98. Added §4.1 "Closed-Set Turns" (offered_options, CHOICE/CHOICE_OTHER/CHOICE_ASK/CHOICE_REPAIR, Express Panel choice chips, steering persistence, renderer pure-family note) with a Figure 3a worked example; added §5.3 "Two-Step Closing, Pagination, and Declining a Closing" (WIND_DOWN vs CLOSING, no-LLM paging, CLOSING_DECLINE/reopenFromClosing); updated the mode table's PRE-CLOSING/CLOSING row; added offered_options to the §9.2 schema; added two §9.3 obligations bullets (offer-vs-mention, CLOSING definition); updated the Phasing and Traceability tables. **RESIDUAL:** no new figure for the CHOICE-card flow (no image toolchain on this box) — Figures 1–6 untouched. |
| Conversant AAC Conversation Engine Overview.docx | ✓ current | 2026-09-08 | `0072923` | **Synced Sep 8 2026 for 0.10.7.** The slot table gained a Promise row: an option that asks for a moment rather than answering, offered only where the user certainly has the answer, marked so the sequence stays open, and never placed first - a constraint enforced in the engine because prompt wording could not hold it. |
| Conversant AAC Express Panel Design.docx | ✓ current | 2026-09-05 | `24cabda` | **Amended Sep 5 2026 for 0.10.3.** It asserted the Express-cell rule as current fact - that every button in the app took its minimum size from an Express Panel button - and that rule was removed in 0.10.3 along with the "Button size" slider. The paragraph keeps its point (button spacing is not an Express Panel setting, because that gap is the width of a keyguard bar) and now says the stronger version of the argument is history. *Prior:* **Synced Sep 2 2026 for 0.9.3.** Two open questions closed. The starting set is DONE — the therapists' twenty-six phrases replaced the temporary set, wholesale rather than merged, delivered by a one-time seed revision that touches the Always band alone so band sizes, situational lists and any Context button the user had touched survive. And the band-shape default is CLOSED: whole rows, one Context row, no Flex. The mark question stays open, correctly — a shape ships as the default and whether it is the right one is still for the prototype. |
| Conversant AAC Configuration Model.docx | ✓ current | 2026-07-27 | `09a51ae` | **Silence period explained properly (Ken, Aug 9 2026): *“Ensure that the User Manuals (and maybe other documents) fully/properly explain what the Silence period parameter controls/represents.”*** Three defects, not one. The stated **default was 2 seconds** (it has been **0.5** since Aug 7 2026) and the **range 0.5–3.0** (a **0** option was added Aug 9), so both figures were wrong everywhere they appeared. And nothing said what the setting *represents*: the pause is a **checkpoint, not a stop** — recording continues, the partner’s turn ends when the **user answers**, and nothing is ever spoken unprompted, which is exactly what makes a short setting safe. That omission mattered because the natural reading of the old wording (“sends the partner’s speech to the AI”) is that a short setting risks cutting someone off, when the real cost is **discarded AI requests, i.e. money**. Settings-registry row rewritten: name **TRP silence threshold → Silence period**, default **1.5 s → 0.5 s**, range **0.5–3.0 → 0–3.0**, and the “why experiment” column re-pointed at the real trade-off, since its false-TRP framing predates the July 10 2026 reversal. Content is current; **At commit restamps at the next release push.** *Prior pass:* Synced for v0.5.93–v0.5.98: Build Status tab list corrected to ten tabs (added Practice, fixed ordering to match the shipped app); content-sets bullet notes the wind-down/closings split (v0.5.93); added two concrete registry-table rows — "Listening chime" (§4.1) and "Choice buttons in Express Panel" (§4.2) — each dated and explained as post-this-document additions, consistent with the doc's existing "Build Status" framing. Named settings profiles (v0.5.83) already covered from the prior sync. |
| Conversant AAC UI-Design.docx | ✓ current | 2026-09-09 | `c125c14` | **Restamped Sep 9 2026.** The stamp said `c93b349`, which is EARLIER than the commit that added the work the note describes - the row was edited inside `ff390a3` itself, so it could not name its own commit. Verified in the document rather than assumed: section 13 "Control Standards" is present, and so is the color rule ("in the panels, color means danger or state, never emphasis"). Advanced to the 0.10.12 release commit, which is where both landed. **Color rule added Sep 8 2026 (Ken).** In the panels, color means DANGER or STATE and never emphasis - the five button colors that had grown across Settings and About Me read as inconsistency rather than hierarchy, and nothing in a settings panel is more primary than the button beside it. The conversation surface keeps its accent fill, where a primary action genuinely exists. **Section 13 completed and ENFORCED Sep 8 2026.** Two rows were missing the standards they were written for - a Settings button now records its one appearance (a soft-gray fill, real border, small radius, taken by any button that declares nothing) and a text field records that it takes the same minimum height as a button. Section 13 now ends by naming tests/ui-standards.test.mjs, which measures the running app against it: writing the standards down was never the answer, since every one of them was already decided when the panel drifted. **Amended again Sep 8 2026:** every "Test this voice" / "Test partner voice" button is now just "Test" (Ken), so section 13 says so - and says the one thing that makes a shared label safe, that these two are told apart by WHERE THEY SIT rather than by their words. **Added Section 13 "Control Standards" Sep 8 2026 (Ken).** A catalogue of the sixteen control types and, for each, its face, its size, its behavior and what it must never do - plus the two questions that decide any control: is its face a FIXED function or CONTENT, and is it on the conversation surface or a supporter-assisted overlay. Nothing here is new policy; every entry was already settled, one at a time and months apart, and the section exists because a standard that cannot be looked up drifts - which it had. The old Section 13 is renumbered 14. **Amended Sep 5 2026 for 0.10.3.** The 20 mm touch-target passage said the physical-size goal became a user-scalable "Button size" setting. That slider is gone; the passage now says the borders between the screen areas are dragged directly, which grows the targets in the region enlarged and takes the room from the transcript. *Prior:* Synced for v0.5.93–v0.5.98: added a Build Status bullet + in-place §4/§12/§13 coverage of the new CHOICE-card family and Express Panel choice chips (framed as a deliberate, geometry-only exception to Rule 1 — cell count/position/size stay fixed, only cell content changes); added a "Static palettes" note (§13) covering openers/wind-downs/closings pagination via "New N" / re-pressing Wind down and the pinned "Actually, before you go —" decline-closing card; added the listening chime + pulsing Listen button (§3.3/§13/Rule 6), including the v0.5.98 once-per-conversation-with-auto-resume refinement. Not touched: pre-start screen sequencing and the API-key prompt (out of this doc's scope) and Practice Mode's Settings tab (Settings navigation isn't described here). Figures untouched (no image toolchain). |
| Conversant AAC Continuous Partner Capture.docx | ✓ current | 2026-09-08 | `0072923` | **Synced Sep 8 2026 for 0.10.9, and one older error corrected.** The placeholder entry still said a placeholder waits until the turn is classified complete; the ladder has been driven by partner silence alone since Aug 7 2026. Rewritten, and the easing-off behavior added. |
| Conversant AAC Keyboard Layout Options.docx | ✓ current | 2026-07-27 | `09a51ae` | Reviewed for v0.5.93–v0.5.98 — no relevant changes. Confirmed via `git log 9380c34..HEAD -- app/js/keyboard.js app/js/keyboard-layouts.js` (empty). Neither file changed in this window; the only nearby touch was a new Express Panel choice-chip color constant (express-items.js), unrelated to keyboard geometry/layouts. |
| Conversant AAC Worldview Implementation Plan.docx | ✓ current | 2026-09-08 | `0072923` | **Synced Sep 8 2026 for 0.10.7.** A second "added after the fact" scope note: the private Health & Safety module, and questions written by conversations rather than by the registry - which closes the unbounded-tail gap this plan left open, fed by the same missing-facts channel now given the registry's real field names. |
| Conversant AAC Sounds Like Me.docx | ✓ current | 2026-09-02 | `1fa27f6` | **Synced Sep 2 2026 for 0.9.3.** §4.3's provenance argument survives intact and its facts changed: the shipped phrases are the therapists', not ours, so "every one written by us" became "neither is the user's" — which is the claim the section actually rests on. §5.5 was **"The Express Panel ships half-populated"** and is now **"Provenance marking: whose words are in a cell"**, per Ken's decision of September 2 2026 to drop the half-populated directive outright — bands eliminated the need for it, so the section records the drop in one paragraph and gets on with provenance, which is the half that was always doing the work. The endowed-progress citation went with it. |
| Conversant AAC Security Review.docx | ✓ current | 2026-08-01 | `251cb03` | **SEC-1’s shared-origin half recorded as FIXED (Aug 1 2026).** Status-update heading extended to "July 16 & 27 and August 1, 2026" with a new entry: the app moved to its own origin `conversant.volksswitch.org`, closing the contingent cross-project risk; the finding’s own heading is flagged `[origin FIXED…]`. Notes that the *other* half of SEC-1 — plaintext key readable by anyone holding the device, and the proposed supporter-locked PIN Setup tier — is unchanged and still open, and points at Architecture Overview §18 for the operational dependencies the move introduced. *Prior pass:* Added a July 27 addendum to the Status Update section (heading now "July 16 & 27, 2026"): SEC-7's agreed near-term build scope shipped — start-of-listening chime (v0.5.96, once-per-conversation when auto-resume is on since v0.5.98) + a pulsing red Listen button — verified against app/js/chime.js and app/css/styles.css. Added a matching "Status: Partially built" note under the SEC-7 finding itself. User-manual passage, printed device card, and the deferred rear-camera-LED idea remain outstanding/not built. |
| Conversant AAC Strategic Assessment.docx | ✓ current (point-in-time) | 2026-07-18 | `409ef3c` | Claude's independent strategic review answering Ken's four questions (problem importance, approach effectiveness, gaps/priorities, alpha-beta plan). **Point-in-time snapshot — does NOT participate in "sync docs"**; supersede with a new assessment rather than editing this one. Generator: `generate-strategic-assessment-doc.js` (project root, docx-js). Its two flagged Architecture Overview errors (free-tier claim, hardware discrepancy) were fixed in the 2026-07-27 sync. |
| Known Issues web page (`volksswitch.org/index.php/conversant-aac-known-issues/`) | live on WordPress | 2026-08-09 | n/a | **THE PAGE IS THE ONLY COPY. Nothing in this repo mirrors it, and nothing should** — a second copy is exactly what got the .docx retired. The app links to it from Settings -> Troubleshooting (`KNOWN_ISSUES_URL` in `app.js`); if the address ever changes, that constant and Beta Test Plan section 9 both change. A starter version of the content was handed to Ken on Aug 9 2026 and is disposable. **WHAT BELONGS ON IT (Ken, Aug 9 2026):** *"I don't think it should include plans for future releases even if those plans would mitigate or nullify the issue. I think the best source of known issues will be those that come from the beta testers and are waiting to be fixed or tested internally."* **No roadmap** — the retired document proved why: its issues aged fine and its "Status: ... is planned" lines all went wrong. **The one status that is NOT a plan and is kept:** each item says **Being worked on** or **Not changing for now**, because "wait" and "work around it" are different reactions and the reader cannot choose without it. No dates, no version numbers, no feature names. **Two parts, because tester reports alone leave the page empty on day one** and the standing limitations are precisely what five testers each report in week one: *what the app can't do well yet* (slow-moving) and *reported by testers, being dealt with* (turns over). A third short part, *things that look like faults but aren't*, carries the red tint, the refusal to answer general-knowledge questions, the empty Express Panel cells and the reload action. **An item comes off when its fix has been released** — that discipline is what keeps the page honest. **It is public**, so plain wording, no tester names, no internal ticket language. **Live as of Aug 9 2026** (page id `23303`). **TO DO: give Claude a way to edit it directly** so a session's findings reach it in the same pass — the mechanism, the credential handling and the trap in the API's base URL are recorded in CLAUDE.md under Open Questions. |
| ~~Conversant AAC Known Issues.docx~~ | ⛔ RETIRED 2026-08-09 | 2026-08-09 | `6b5b241` | **Retired by Ken (Aug 9 2026); replaced by the web page `https://volksswitch.org/index.php/conversant-aac-known-issues/`, which the app links to from Settings → Troubleshooting.** Moved to `Other/Previous Doc Version/`. **Does NOT participate in "sync docs" — do not edit it, do not regenerate it, and do not bring it back.** **Why a page and not a document, stated once so it is not re-argued:** a list bundled with a release is out of date the day the next release ships, and this one proved it — by the time it was retired, item 7 (pronunciation) had shipped in 0.7.0, item 9 (settings not travelling) had been superseded by the named-profile workflow, and item 10 said the app runs on a Windows tablet only, which stopped being true at 0.6.0. **What rotted was not the issues but the "Status:" lines, which were roadmap promises** — that is the origin of Ken's rule that the page carries no plans for future releases. *Prior state:* Checked all 11 items against commits since 96b5ba4 (Practice Mode, closed-set/choice-chip turns, API-key Test button, listening chime, conversation-pane layout fixes). #1 (placeholder/echo collision), #4 (multi-party), #8 (single-instance guard) confirmed still accurate/open — no edit. Added a manual-workaround sentence to #9 ("Settings don't travel between devices yet") pointing to the Settings → General "Settings profiles" save/load feature as today's cross-device copy option. No new items added. |
| Conversant AAC iPad Platform Measurements.docx | ✓ current | 2026-08-01 | `e905113` | **New Aug 1 2026**, split out of the retired iPad Architecture document: the probe evidence record, kept separate from the architecture because measurements age differently from design — when a later iPadOS lands, this is the document that says what to re-run and what to distrust. Carries the marking scheme, the measured six-run matrix (Table A), the three follow-on findings (bookmarking does not rescue persistence; Home Screen and Safari tab are separate storage silos; all Home Screen runs report an identical Safari user-agent), and the tested-device record. **Added in this pass:** the July 31 voice measurement — 68 voices with no Ava at any tier after installing Enhanced/Premium, the roster's composition, why it is a fixed curated list rather than an enumeration of what is installed, that every voice reports itself as the default (so that flag is useless on iPadOS), and the iPadOS 26 rename of Spoken Content to Accessibility → Read & Speak. Built by copying the iPad document and cutting, so the house style is inherited exactly. **Residual:** no figures; one internal reference to the retired document's sources list. |
| ~~Conversant AAC iPad Architecture.docx~~ | **retired 2026-08-01** | — | — | **Merged into the Architecture Overview §5 and §17, and its evidence into Platform Measurements.** Moved to `Other/Previous Doc Version/Conversant AAC iPad Architecture (merged into Architecture Overview 2026-08-01).docx`. Generator `generate-ipad-architecture-doc.js` is now stale and should not be re-run — it would recreate the document this merge deliberately removed. *Prior pass, for history:* | **Rewritten July 30 2026 against measured probe results** (iPad 10th gen, iPadOS 26, Safari 26.6; six runs + two follow-ups). Claims now carry `[MEASURED]` (30), `[SOURCE]` (13), `[RESEARCH]` (18, only where untested), `[PROBE]` (2, still open). Three original `[RESEARCH]` claims were **overturned by measurement** and corrected in place: continuous recognition works, `getVoices()` returns 68 voices, and the `100vh` trap did not reproduce in a tab. New §7 "Two Modes" carries Ken's decision to ship both a Safari-tab mode and a Home Screen mode; new front-matter results matrix (Table A) and §8.0 device record (Table 6). **Residual:** no figures (no image toolchain); cloud-transcription pricing still deliberately unquoted; §11 lists 6 open questions, notably keyboard suppression (the probe's detection method was unsound). *Superseded description below:* **Originally created July 29 2026** — build-ready specification for running Conversant on iPadOS (Ken's request). Generator: `generate-ipad-architecture-doc.js` (project root, docx-js). Claims are marked `[SOURCE]` (read from the codebase at v0.5.99), `[RESEARCH]` (public documentation, cited in §12), or `[PROBE]` (unresolved — answered by `ipad-probe.html` on real hardware). **Not yet actionable:** the probe has not been run, so §7.1 (tab vs Home Screen) and the §4 speech tier are open. **Companion instrument:** `ipad-probe.html` at the project root. Was hosted at a separate throwaway Pages repo July 29–30 2026, **run on an iPad 10th generation, and the repo deleted by Ken on July 30 once results were in** (verified gone). The probe source remains in this repo and can be re-hosted if a later iPadOS release needs re-testing. The six raw reports are the primary evidence record and live on Ken's Desktop under `iPadOS probe/`. **Residual:** no figures (no image toolchain on this box); cloud-transcription pricing deliberately not quoted, to be confirmed at build time. |
| Conversant AAC Practice Mode.docx | ✓ current | 2026-07-27 | `09a51ae` | **Newly added to tracking.** Generated ~v0.5.97; corrected the launch/exit description for the v0.5.98 move of Practice Mode from the pre-start screen into a Settings → Practice tab: step 1 now reads "Open Settings and select the Practice tab" (was "tap Practice a conversation" on the opening screen); the ending step now says End conversation / the Practice tab's "End practice" return to the normal conversation screen, not an opening screen — verified against app/js/app.js (startPractice/endPractice/handleEndConversation) and the Settings tab list in app/index.html. **⚠ Process note:** this edit was made without the mandatory pre-edit backup (an agent process slip) — recovery, if ever needed, relies on OneDrive version history rather than a local `Doc Backups/` snapshot. |
| Conversant AAC Beta Test Plan.docx | ✓ current | 2026-09-06 | `8c74e76` | **Synced Sep 6 2026 for 0.10.4 (Azure).** Deepgram remains what the plan assumes; Azure is named as the way out of a sign-up that will not complete, which is a problem Ken actually hit with the first tester, and the setup step says what to do differently with an Azure key. |
| Conversant AAC Practice Scenarios.docx | ✓ current (new) | 2026-08-20 | `ef43975` | **Describes a feature that is NOT BUILT — written as finished documentation at Ken's request (Aug 20 2026) so that, if the feature goes ahead, this is the template its documentation follows rather than a proposal needing a rewrite.** A title-page status panel says so; **remove it only when the feature ships.** *What is true of the shipped app:* a scenario is a CHARACTER BRIEF, not a script (nothing pre-written, no branching); the six bundled scenarios; the mic being off throughout; End conversation ending a practice in one tap; and the honesty rule that the app will not invent facts about the user's life. *What is described but does not exist:* the “Details for this practice” box, the “how they are behaving” setting, the per-scenario voice, and all of sections 6 (authoring) and 7 (worked examples). **Ken's decision that shapes it (Aug 20 2026): the full range of partner behavior is available and WHICH one is encountered is the user's choice, held on the scenario.** Section 5.3 states the one line that holds at every setting — the partner is difficult about the SITUATION, never about the user's disability or how they communicate — and that is deliberately not adjustable. **⚠ ONE ASSUMPTION PENDING KEN:** §5.2 says the behavior MOVES during a conversation in response to what the user says. He has not ruled on it; that section is what changes if he wants it fixed. *Also unconfirmed:* the disposition list itself (§5.1) is a proposal, and §3's claim that a scenario voice does not affect the spoken-help voice is a design intent, not measured behavior. Regenerate: `node scripts/doc-generators/generate-practice-scenarios-doc.js`. |
| Conversant AAC Partner Card.docx | ✓ current (new) | 2026-08-15 | `c36a49e` | **The printed partner-disclosure card (SEC-7), written Aug 15 2026 — the physical half of partner awareness, and the last thing the Beta Test Plan promised that did not exist.** The screen faces the USER, so an on-screen indicator reaches the partner only on a glance; the signals that actually reach them are audible (the chime, the amber Notice button) and PRINTED. **Ken chose the shape:** 3.5 x 2.5in, eight to a Letter sheet, disclosure plus `conversant.volksswitch.org` so a partner who is uneasy has somewhere to go. **The headline is word-for-word the Express Panel Notice button**, so the spoken and printed disclosures say the same thing. **⚠ EVERY CLAIM WAS CHECKED AGAINST SHIPPED BEHAVIOR and none may drift:** the words DO go to an online service (there is no on-device recognizer on any configuration), the TEXT is kept on the device, the AUDIO is not, and nothing is spoken unless the user picks it. **It makes no legal claim** — a card discloses, it cannot obtain consent, and recording law is for counsel. **⚠ NOT YET SEEN ON PAPER:** there is no renderer on this machine, so the geometry was verified by measuring the file (8.5 x 11in Letter, 8.5in wide used of 8.5, 10.8in tall of 11, eight cards, rows EXACT at 2.5in) rather than by looking at it. **Row heights are EXACT, which CLIPS silently** — the text measures about 1.9in in a 2.5in card, so it is safe today, but anyone lengthening it must open the file. Regenerate: `node scripts/doc-generators/generate-partner-card-doc.js`. |
| Conversant AAC Partner Privacy.docx | ✓ current | 2026-09-06 | `8c74e76` | **Reviewed Sep 6 2026 for 0.10.4 (Azure) — no change needed.** The capture passage says "the paid service" rather than naming one, so it stayed true when a second arrived. |
| Conversant AAC Keeping Costs Down.docx | ✓ current | 2026-09-06 | `8c74e76` | **Synced Sep 6 2026 for 0.10.4 (Azure).** Both settings rows carry the third choice and the actual rates: Deepgram cheaper to hear (46 cents/hour against $1.00), Azure cheaper to speak (1.6 cents against 3 per 1,000 characters) with a monthly allowance that renews. |
| Conversant AAC Measurement Plan.docx | ✓ current (new) | 2026-08-16 | `03a3bf2` | **New, August 16 2026.** Ken, prompted by the Strategic Assessment's three measurement gaps: *"generate a document describing the kinds of user and partner metrics that we are capable of generating with the current product and which metrics might be important and which we don't currently have associated measurements/reporting."* The capability had never been written down in one place — it was scattered across the decision record, the Beta Test Plan appendix, and the code. **Written against the SHIPPED code (`usage-summary.js`, `weekly-send.js`, `diagnostics.js`, `storage.js`), not the design notes, so where the two disagree the code wins and the disagreement is named.** Four layers: what is reported today; what is already written to every saved conversation and read by nothing (eight measures, no new capture — the largest cheap win); what needs the unbuilt event capture and is therefore permanently lost if it is not built before the first tester's first week; and the partner and reading-load gaps. **Ten findings from checking the code against the plans, three of which change what an existing number means:** the weekly report is CUMULATIVE, so the week-over-week retention curve — the agreed headline number — cannot be seen even though every timestamp is present; a "don't save" conversation contributes NOTHING, not even counts, contrary to the recorded decision; and the system-information block is sent nearly every week rather than only on change, because the spending counters live inside the settings, which makes the tester-facing sentence "sent only when something changes" inaccurate. Also names the one-timestamp fix (record when the cards appear) that converts the existing wait figure into the reading-load measure the Strategic Assessment says has never been taken, and the partner-return proxy already sitting in the partner stamps. Generator `generate-measurement-doc.js`, regenerable. **REVISED AND REGENERATED the same day, after Ken directed that the first five items of §10 be built.** The revision marks §4, §5 and §7 as built, records Ken's correction that reading load is read **plus select** (so the clock stops at the user's first action of any kind, which is what catches the turns where the reading was so heavy they gave up and typed instead), marks findings 1 and 3 as fixed, adds the defect found while verifying (Start conversation recorded an ended conversation with no turns), and rewrites §10 as what was built versus what is left — the partner questionnaire now being the largest remaining gap, and not code. Restamped at the 0.7.6 release push. |
| Conversant AAC Your Security and Privacy.docx | ✓ current (new) | 2026-09-06 | `8c74e76` | **Reviewed Sep 6 2026 for 0.10.4 (Azure) — no change needed.** It speaks of "the voice or transcription service" throughout and names no company, so nothing went stale. |
| Conversant AAC Conversation Flow.docx | ✓ current | 2026-09-09 | `c125c14` | **Unblocked and reviewed Sep 9 2026 - NO EDIT NEEDED, restamped.** It had been marked blocked because it carried tracked changes and comments; Ken has since cleared them, and the artifact check now reports it clean. Its content had already been brought up to 0.10.10 in that earlier pass and could not be stamped at the time - re-verified against the shipped code rather than taken on trust: the four batching services are named alongside the two that stream (only the built-in recognizer and Deepgram stream; Azure, OpenAI, Google Cloud and ElevenLabs each hand over one phrase at a time, per `stt-rest.js`), the placeholder ease-off matches `placeholders.js` (2 seconds a step, stopping after four exchanges), the promise card matches the `defers` option in `llm.js`, and transcript cleanup is genuinely gone with `heard_uncertain` in its place. **0.10.11 and 0.10.12 do not reach this document** - both are Settings appearance, key boxes, Test buttons and the practice-partner voice, none of which is part of a conversation. Passes "check docs" with zero errors and Word opens it. **Fourth edition, August 30 2026 — a new section 12 and figures 6–9: the whole conversation as a classic engineering flow chart, four pages, boxes and diamonds and numbered off-page connectors.** Ken supplied a hand-drawn chart of his own and asked for the flow "in a format and a level of detail similar to" it, adding *"Don't assume that I've properly captured all the flows possible. Refer to the code to ensure correctness."* **Added rather than substituted:** figure 1 is a three-lane summary answering *who does what*, where these four pages answer *what happens next, and on what condition*. Both are wanted, and figure 1's own section now points forward to section 12. **⚠ SIX THINGS HIS DRAFT HAD THAT THE CODE DOES NOT, all read off the shipped source and all now encoded in the figures — they are the corrections most worth keeping, because every one of them is a natural assumption to make.** (1) **It gated the app's silence timer behind the recognizer's own end-of-speech verdict. There is no such gate.** `resetSilenceTimer` is called from `afterIngest`, i.e. from the ARRIVAL OF A CHUNK, so the clock is restarted by every chunk — interim results included — and the recognizer's verdict is ignored outright (`stt.js` binds no `onspeechend`; `stt-deepgram.js` reports `speech_final` as ordinary final text, with the reason stated in a comment: the Silence Period is a user setting and must mean the same on both backends). Two consequences the figure draws: the clock can never run out mid-sentence, and it starts LATER than the moment the person actually stopped. (2) **The silence timer and the placeholder timer are SEQUENTIAL, not parallel** — `placeholders.arm()` is called from `handleSilencePeriod`, i.e. after the Silence Period has already elapsed. What *is* parallel is the placeholder ladder and the AI request, which is the whole of figure 7. (3) **The partner resuming does not stop the silence timer, it RESTARTS it**; what stops is the placeholder ladder (`handlePartnerResumed` → `placeholders.stop()`). (4) **The ladder does not end at the second phrase** — it loops on the subsequent delay until `maxPlaceholders`, and 0 means none are ever scheduled (`arm()` returns early). (5) **"4 or 8 options" is one of four outcomes**, alongside a closed-set palette, the goodbyes on a `CLOSING` classification, the three repair-of-self cards on `is_repair_initiator`, and the superseded case — which is discarded on arrival and never shown at all. (6) **Auto-resume is gated on `manualListenArmed` as well as the setting**, and an opener opens the microphone unconditionally (`handleResponseSelected`, `wasOpener` branch). **Section 12.5 states what the four pages deliberately do not draw** (repeating yourself, Practice Mode, Settings, the keyboard), so a reader is not left inferring that a missing path does not exist. **⚠ REGENERATING COST THE FORMATTING PASSES AND THEY HAD TO BE RE-APPLIED BY HAND — worth knowing before the next fold:** the document's only drift from its generator was `keep-with-next` on the headings and the imported "Conversant" table style, both applied by post-passes that the generator knows nothing about, so a `FORCE_DOC_WRITE=1` run silently produced 18 checker errors on a document that had been clean. `fix-headings-and-gaps.py` plus `apply-table-style.py --from` the Windows manual restored both. Passes "check docs" with zero errors, and Word opens it. **Restamped: the commit named above is on `origin/main`** (stamped at the last commit on `origin/main`; the review was against the current working tree). *Prior:* **Third edition, August 27 2026.** Ken on the second edition's pause timeline: *"It needs to show the user, STT module, app separately. The app should be broken into the placeholder statement logic and the AI prompt logic. It should make clear that the STT is sending chunks of words and how they are bundled, when the STT module determines that the user has stopped talking and when (if) that determination is sent to the app, the different timers that start at that point. A second picture should show how that timeline changes if the partner resumes talking."* And, separately: *"the original document documented the figures at the same level of detail (not much). Ensure that you are now providing descriptive documentation that matches the detail of the figures."* **Both the coarse four-track figure and the five-band pause timeline were replaced by an EIGHT-LANE PAIR** — the other person / speech recognition / its own verdict / the words the app has / the Silence Period clock / holding phrases / asking the AI / you — drawn over the same eighteen seconds so Figure 4 (they resume) can be read directly against Figure 3 (they do not). Sections 5 and 6 were rewritten to walk every lane rather than caption the picture; the document went from about 2,900 words to about 4,600, and section 10 was expanded to match Figure 5. **⚠ THE ANSWER TO KEN'S "WHEN (IF) IS THAT DETERMINATION SENT TO THE APP" IS THE MOST IMPORTANT THING THE FIGURES SAY, and it is counter-intuitive: BOTH recognizers make their own end-of-utterance decision, BOTH send it, and the app acts on NEITHER.** Read off the code, not the design record — `stt-deepgram.js` reports `speech_final` as ordinary final text with the rule stated in a comment (*the app's own silence period decides when a turn is over … neither is allowed to drive turn-taking*), `stt.js` never binds `onspeechend` (grep count: zero), and `resetSilenceTimer` is called from the ARRIVAL OF A CHUNK — which is why the Silence Period can never run out mid-sentence and why it starts later than the moment the person actually stopped. **Also settled from the code: a request superseded by a later pause is DISCARDED on arrival, not shown** (`generateOptions` takes `++generationToken` and returns early when it has moved on), which is what Figure 4 draws; the note beside it says the other case — answer back before the next pause, so it is shown and then replaced — is equally normal. The house test caught "cancelled" twice on the first pass. Passes "check docs" with zero errors, and Word opens it. |
| Conversant AAC Weekly Report Handling.docx | ✓ current | 2026-08-31 | `6b4328f` | **Synced Aug 29 2026 for 0.8.4.** Ken: *“add the problem report section to the report handling document”,* after being told that the receiving end of a PROBLEM report — the one a tester writes and sends deliberately — was the half nothing described. **New section 6, “When a Tester Writes In About a Problem”,** placed after the alerts section because it arrives by mail too: how one reaches you by each of three routes and what is in it; that it is **answered the same day, before anything is known**, because a tester who writes in and hears nothing stops writing in and that closes the only channel carrying words; the six-step order to read one in, opening with *take their note at face value* since it is the only sentence in the system written by the person using the app; **what to do when the report cannot answer it**, which will be often and is a property of the design — the event record carries counts and no words, so asking for one saved conversation file is the move, and it is asked for, never treated as owed; how to close one out in four shapes; and two things not to do with it (it is a person, not a data point; never quote a transcript outside the project, because the other person in it consented to nothing). Sections 6–9 became 7–10. The problems tab was added to the section-2 table, and section 2’s “no transcripts, by construction” claim now says WEEKLY reports, since the problems tab is the deliberate exception. **⚠ FOLDING THE DRIFT BACK WAS THE REAL WORK, and it is the standing hazard: the Aug 27 sync edited this .docx directly, so the generator had gone stale and running it would have silently discarded a table row (“Words doubted”) and an entry (the seventh misread).** Both are in the generator now, the document is regenerated, restyled with the four formatting passes, and back in `doc-manifest.json` so the overwrite guard is armed again. “Check docs”: zero errors, Word opens it. *Prior:* **Synced Aug 27 2026 for 0.8.3.** The tidy-up pass that rewrote what the recognizer heard was removed this release and replaced by a MEASURE, so the document gained the two things a reader of a report now needs. **(1) A row in the setup-and-health table for "Words doubted"** - how often the AI suspected a word was misheard, broken down by person, place and recognizer - with the instruction to read it as a COMPARISON and never as a rate: a figure much higher with one person or in one room is the finding, and is often fixable (a quieter corner, a closer microphone, the paid recognizer). **(2) A seventh entry in "Reading These Numbers Honestly" - the figure is a FLOOR, not an error rate.** The AI can only doubt a word when something looks wrong, so a mishearing that leaves an ordinary sentence ("can" for "can't", "Tuesday" for "Thursday") is invisible to it, and those are the errors that matter most. The true rate is always higher by an unknown amount. It earns its place because a quiet room and a noisy cafe are judged by the same blind judge, so the difference between them means something; the entry says in terms never to quote it as "the recognizer is 94% accurate". The intro count went six -> seven. **Nothing had to be REMOVED:** the cleanup measurement was added and taken out inside the same dev cycle, so no released report ever carried it and the document never described it. **Passes "check docs" with zero errors.** The four tables were the S7 finding - no table style at all - which turned out to be fixable rather than a standing gap: the "Conversant" style exists only in the Windows manual, where Ken built it in Word, so `apply-table-style.py` gained a `--from` option that copies the definition out of the manual into any other document. That is the commonest complaint the checker makes across the whole set (29 findings), and it is now one command per document. `fix-headings-and-gaps.py` set keep-with-next on 15 headings; `apply-doc-style.py` and `fix-docx-lists.py` found nothing to change. *Prior:* **New, August 16 2026.** Ken, having had the five measurement items built: *"put together a plan for processing and reporting on the weekly reports."* The receiving end — everything after a report leaves the device, which nothing had described. **Written for a fifteen-minute weekly routine over a cohort of about five, and that size governs the whole document**: with five testers none of this is statistics, so the stated output of the routine is *one question per tester for the next check-in*, never a chart. Covers where a report lands (the Sheet's `weeks` tab, `reports` tab, and the raw payload column); the six-step Monday read, ordered from what matters most; how to read each number and what to do when it moves, in three tables (headline / early-warning / setup-and-health); the three things that should reach Ken **without** anyone looking, with **"a tester has gone quiet" named as the highest-value alert in the system because it is the only one where acting early still changes the outcome**; what to report back (a per-tester page before each check-in, a cohort view monthly, and explicitly **no averages, no significance tests, and no ranking of testers** — the one with the fewest conversations is the most informative person in the study); six ways the data will otherwise be misread; the setup checklist; and the three-part cadence. **The honest limitations are stated as properties, not defects:** a private conversation contributes nothing at all, early weeks will be practice-heavy, the two speech configurations are different products, iPad history can be erased by the device, and deliberation time overstates reading. Generator `generate-report-handling-doc.js`, regenerable. Restamped at the 0.7.6 release push. **Sync Aug 31 2026:** two claims overtaken by Ken’s decision that weekly-report errors do NOT mail while problem reports always do. Section 5 said the error alert was “built into the receiver; it needs an address putting in”, and section 9 still listed putting the address in as a to-do — the address has been set since Aug 21, and the error alert is off deliberately, not pending. Both now say so, with the reason. |
| Conversant AAC Settings Defaults Review.docx | ✓ current | 2026-09-08 | `90312e3` | **Synced Sep 8 2026 for 0.10.10.** The two speech rows list five paid options rather than two, and the open question about starting a user on a paid voice now notes that Google Cloud has a renewing free allowance as well as Azure. Also not flagged by the currency check - same gap as Backup Compatibility. |

*(`Conversant AAC Conversation Engine Design.bak.docx` and `Conversant AAC UI-Design-Helix2.docx` are backups/stray copies and are intentionally not tracked.)*
| Conversant AAC Color Schemes.docx | ✓ current (new) | 2026-09-05 | `24cabda` | **Amended Sep 5 2026 for 0.10.3.** Two sentences said "button size is its own setting" when arguing that a color scheme is the wrong tool for a non-color need. The argument holds and the setting does not; both now say how large the buttons are follows from how the screen is divided and how far apart they sit. *Prior:* **New, September 3 2026.** Ken was asked whether the app supports changing colors for people with visual impairments; the seven schemes were built that day and this document is the case for them. It has to stand up to a therapist or an eye clinician asking “why those, and why is that enough”. **⚠ THE COMPLETENESS ARGUMENT IS THE POINT, and it is a claim about a taxonomy of NEEDS rather than of tastes:** two axes that can be dialed independently — how bright the background is, and how hard the contrast is pushed — plus one orthogonal axis for color discrimination. Stated that way it can be checked, and **its one deliberate gap can be named** (there is no bold-dark scheme, because the dark scheme is already drawn with strong boundaries). Stated as “seven nice options” neither would be possible. Section 5 is that grid. **Seven figures, captured from the real app** by `scripts/capture-color-schemes.mjs` — same conversation in every one, so the scheme is the only difference. **⚠ EVERY REFERENCE WAS VERIFIED AGAINST A SOURCE, not written from memory** (the standing rule after the July 2026 probe found half the remembered platform facts wrong): WCAG 1.4.3 and 1.4.11 for the thresholds; Legge/Rubin/Luebker and Rubin/Legge for why low vision needs more contrast; Piepenbrock and colleagues for the finding that dark-on-light is better for people WITHOUT visual disorders, which is why Default stays the default and is cited **against** over-claiming dark mode; Viénot/Brettel/Mollon for the simulation method; the AAO clinical statement and EyeWiki for cerebral visual impairment in roughly half to two thirds of children with cerebral palsy, with photophobia among its features; and Wilkinson/Jagaroo plus Thistle/Wilkinson for the AAC-specific caution that color coding has produced **mixed** results, which is why no scheme relies on color alone. **⚠ THE MOST USEFUL FACT FOUND WHILE WRITING IT: cataract impairs color vision along the blue-yellow axis specifically**, so acquired tritan-type loss is far commoner than the under-0.01% congenital figure suggests — which is a second, independent reason the color-blind scheme codes the category by the bar's SHAPE as well as its color. **⚠ SECTION 8 IS LOAD-BEARING AND MUST NOT BE SOFTENED: nobody with any of these conditions has tried any of it.** It has been measured and rendered, which is not the same as validated; a later edit quietly upgrading “measured” to “validated” would make the document dishonest. **⚠ THE FIGURES IN SECTION 6 ARE MEASURED AND WILL GO STALE IF A PALETTE MOVES** — they already did once during writing, when darkening the dispreferred amber for badge legibility shifted the Default protanopia number from 17.5 to 17.0. Recompute from the stylesheet rather than trusting the prose. Generator `scripts/doc-generators/generate-color-schemes-doc.js`. Passes “check docs” with zero errors and Word opens it. **Restamped: the commit named above is on `origin/main`.** |
| Conversant AAC Speech Provider Guide.docx | ✓ current | 2026-09-08 | `90312e3` | **Synced Sep 8 2026 for 0.10.10 (rev 8).** Five of the six services are now in the app, so every "In the app today" cell changed and the sections arguing for a second choice were rewritten as a record of what shipped. Two errors that predate this release are also corrected: the speaking table still said Azure was not in the app (it shipped in 0.10.4), and the hearing section said ElevenLabs does not transcribe - it does, through Scribe, and the app can use it. Its rate is deliberately left out of the tables rather than guessed. |
| Conversant AAC Pragmatics.docx | ✓ current (new) | 2026-09-06 | `24cabda` | **New, September 6 2026.** A speech and language therapist asked Ken how well Conversant supports pragmatics. The answer existed but was scattered across the two Conversation Engine documents, the closed-set decision, the closing-sequence work, the per-partner profile and the honesty rule, and none of it was addressed to a clinician. **⚠ THE AUDIENCE SPLIT IS THE WHOLE FORMAT DECISION: a clinician's vocabulary is fair game and ours is not.** Adjacency pair, repair, register, preference organization and second-pair-part are used without apology, because that is the reader's own language and hedging it would read as evasion; no file name, function name or engineering term appears anywhere. **⚠ EVERY BEHAVIORAL CLAIM WAS CHECKED AGAINST SHIPPED SOURCE, not written from the design record**, because a clinician may recommend the app on it: the four slots and the CHOICE family, the three repair-of-self operations, WIND_DOWN / CLOSING / CLOSING_DECLINE, the command faces ("Wrap up", "Ask them to repeat", "Repeat what I said", "New 4"), the shipped timing defaults (silence 0.5s, first holding phrase at 2s, then every 10s, at most 2 a turn), and the six practice categories. **⚠ SECTION 5 AND SECTION 6 ARE LOAD-BEARING AND MUST NOT BE SOFTENED.** Section 5 states the four gaps a trained ear notices inside one session — no continuers, one partner only, no control of tone of voice, and interruption that works but is not fast — plus meetings; a later edit that trims them would make the document a sales sheet. Section 6 says that **no clinical evaluation has been carried out**, that the beta measures are descriptive rather than efficacy, and that nothing here is a claim about recording law. **The one uncomfortable section is 3.10, and it is deliberate:** the risk that a user defers to the machine because they believe it communicates better than they do, stated with the three design constraints that work against it and an admission that it is not solved. **Section 7 cites the CA sources by what they explain** rather than as a reading list, so a therapist can check the design against a literature they already know. Generator `scripts/doc-generators/generate-pragmatics-doc.js`. **⚠ IT SHIPS A PDF** (Ken emails it to clinicians), so a sync that changes it must re-export — `scripts/doc-generators/export-pdf.ps1`, after any contents refresh — or the copy the reader opens is the stale one. Passes "check docs" with zero errors and Word opens it. |
