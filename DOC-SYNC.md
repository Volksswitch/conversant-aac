# Document sync tracker

Tracks when each **product document** was last reviewed for content currency
against the code + design record, so "update documents X, Y, Z" has a precise,
unambiguous starting point.

## What's tracked

Every document in **`Documents/`** whose name begins with **"Conversant AAC"** (Ken,
July 8 2026; the documents moved out of the project root on August 11 2026) —
excluding `.bak` backups. These are the living product
documents; the `Other/` folder (research, article drafts, archived versions) is not
tracked.

**Two audiences, two vocabularies (Ken, August 1 2026).** The engineering documents
(Architecture Overview, Platform Measurements, and CLAUDE.md alongside them) name the two configurations by
browser engine — **Chromium** and **WebKit** — because that is the actual causal
variable and it makes "install Chrome on the iPad" impossible to write. The
reader-facing documents (Product Overview, both User Manuals) must **never** use
those words; they say **"Windows, Chromebook and Mac"** and **"iPad"**, and describe
the iPad's two modes as "in the Safari browser" and "installed on your Home Screen".
Longer but recognizable beats concise but unexplained.

## How this works (why not modification date)

A document's file modification date is **not** a reliable "is it current" signal — a
format-only edit bumps the mtime without changing content. Instead, each document
records the **git commit it was last reviewed against** (`At commit`), plus a human
date. The change history to review against lives in git: the code, `CLAUDE.md` (the
design decisions), and `CHANGELOG.md` are all version-controlled; the `.docx` files
themselves are git-ignored (OneDrive), so their timestamps were never the right
anchor.

## Spoken Settings help is guarded by a TEST, not by this process (Ken, August 2 2026)

Ken asked that `settings-help.json` — the words spoken when you arm the **?** in the
Settings title bar and tap a control — *"be included in the Doc-sync process so that
it is updated when a new control is added to Settings."*

**It is enforced one better: `tests/settings-help.test.mjs` fails the build** if a
Settings control, radio group, tab or `data-help` section has no entry, if an entry
points at something that no longer exists, if a phrase runs past 35 words, or if the
bundled copy has drifted from the root file. So a new setting *cannot* ship without
its spoken help, and no one has to remember a checklist item.

**What still needs a human**, and why it is listed here rather than left to the test:

- The test proves an entry **exists**, never that it is **true**. Change what a
  control does and the test stays green while the app says something wrong.
- **Re-run `node scripts/apply-settings-help.mjs` after editing the JSON.** The test
  catches a missed run, but only once someone runs the tests.
- The register is the point: **15–25 words, written to be heard.** Do not paste from
  the manuals — that prose was written to be read, and several entries there run past
  eighty words. The 35-word cap is a backstop, not the target.

## Generators cannot be run casually (August 17 2026)

Most of these documents are produced by a script in `scripts/doc-generators/`, and a sync
pass edits the `.docx` directly. Those two facts together mean **running a generator can
silently revert a document** — a sweep on August 17 2026 found six that would have.

- **Every document is now hash-guarded.** A generator refuses to overwrite unless the
  document is byte-identical to what it last produced (`doc-manifest.json`). Fold the
  document's edits into the generator, then run it. Details and the override are in the
  header of `doc-paths.js`.
- **Check without touching**: `OUTPATH=<scratch>.docx` redirects the write so you can diff
  first. This is the first step of any fold, and the guard stands aside for it.
- **After editing a `.docx` in a sync pass, fold it back in the same pass**, or the next
  person meets a refusal and has to reconstruct what you did.
- **Currently stale, guarded, and awaiting a dedicated pass:** Architecture Overview,
  Conversation Engine Design, Conversation Engine Overview. Their documents are correct;
  their generators are behind.

## Release first, then sync (Ken, August 1 2026)

**Push the app before syncing the reader-facing documents, not after.** The August 1
sync documented three things that were still sitting in local commits — the Settings
**Speech** tab, the **Buttons & Keyboard** rename, and the **X** that replaced the
Close button — so for as long as they stay unpushed, both User Manuals and the
Product Overview describe a screen no tester has. That is the one failure a manual
must not have.

- **Reader-facing** (Product Overview, both User Manuals): sync **after** the push, so
  "the documents describe what is live" is invariantly true and they can be handed
  out without checking first. If they are synced early, **do not distribute them
  until the release lands.**
- **Engineering** (Architecture Overview, Platform Measurements, CLAUDE.md): may run
  ahead of deployment. They are design records, and the design is settled before the
  code ships.
- **`At commit` must name a commit that exists on `origin/main`.** A stamp pointing at
  a local-only commit asserts currency against a history nobody else has. Where a sync
  had to run early, stamp it at the release commit once the push lands.

**Trigger phrase (Ken):** **"sync docs"**, optionally naming documents — e.g. "sync
docs", "sync docs Product Overview", "sync docs the two overviews and the user manual".

**To bring a document current** ("sync docs: Product Overview"):
0. **Back up first (MANDATORY):** copy the `.docx` to `Documents/Doc Backups/<name> <YYYY-MM-DD_HHMMSS>.docx`
   before touching it. Never edit a product document without a fresh backup in the
   same pass.
1. `git log <that doc's At-commit>..HEAD` — the exact commits since its last review.
2. Read the `CLAUDE.md` / `CHANGELOG.md` entries added in that window.
3. Filter to what's relevant to *that* document's scope, verify any UI detail against
   source, and apply the edits.
4. Update the doc's row here: `Status` = `✓ current`, `Last reviewed` = today,
   `At commit` = current `HEAD`, and a note of what the pass covered + any residual.

The tracker bounds the window; judging relevance within it is done at review time.

## Baseline (Ken chose option (a), July 8 2026)

Each document gets a real catch-up review *before* its baseline is stamped to a
commit; a completed row shows `✓ current` with `At commit` = the `HEAD` at review
time, and a `⚠ catch-up pending` row's `At commit` is not yet a trustworthy baseline.

**Done July 8 2026: the three beta-facing docs — Product Overview, Architecture
Overview, User Manual** — caught up and then re-synced for the **0.5.78** release
(stamped at commit `5f672f5`). The remaining design docs are still pending (next
catch-up pass). The Architecture Overview has an image residual noted in its row
(Figures 6 & 7).

## Documents

| Document | Status | Last reviewed | At commit | Outstanding / notes |
|---|---|---|---|---|
| Conversant AAC Product Overview.docx | ✓ current | 2026-08-20 | `6495435` | **Ken reviewed the document and left 20 comments plus 8 tracked edits; worked through them Aug 20 2026.** *Three factual corrections, each verified against the code rather than the record:* **(1) the timing paragraph confused the partner pausing with the speech recognition reporting a pause**, and gave the silence period as “0.5 to 3 seconds, two seconds by default” when it is **0 to 3, default 0.5**. Worse, it implied the options *arrive* at that point — they are only *requested* there; what actually covers the discomfort threshold is the **placeholder**, spoken 2 s after the pause. Both the body and the Glossary now say so. **(2) “Good question.” was still cited as the placeholder** in two places, six weeks after the pool was made deliberately neutral (Aug 7 2026) precisely because the placeholder is spoken *before* the app knows what kind of turn the partner took. The text now states that reasoning, so the constraint cannot be casually undone by a later edit. Also corrected: a placeholder follows **any** completed partner turn, with the single exception of a repair initiator — not only questions/invitations/requests. **(3) the document contradicted itself on the double-tap safeguard.** Ken caught it; the code settles it — `tapMode` reaches only `renderExpressPanel`, and a response card is a plain single click. Both wrong passages fixed; the Access Methods paragraph was already right. *Also corrected:* **five scenarios → six** (the controls tour shipped and nothing said so). *Claims Ken challenged as unbuilt:* the Conversation Goals section promised a control “planned for an upcoming release” without saying that **the middle layer already ships** (a standing per-person goal in the people editor) — it now names what is built and what is not, and drops “how they are ranked,” which contradicted the settled position-carries-category rule; **“expand a few words into a full sentence” never existed** and is replaced by what Reframe actually does; **“social-skills practice” as a roadmap item was largely already built**, so it is narrowed to the part that is not (coaching on the choices the user made). **One challenge that did NOT hold: the blue-italic “couldn't be tidied up” transcript styling is real and shipped** — left as written. *Audience:* Ken asked for a document aimed at the general public, so the beta-reporting paragraph was rewritten from tester-facing to product-facing, and now also records the **Aug 9 2026 decision that reporting is opt-in at public release** — the privacy claim keeps its exception without the document reading like tester onboarding. “neither API key” referred to a paid service not yet introduced at that point, now “none of the keys”. The “right way to try the app” endorsement is gone from both the body and the table, and a caveat added that built-in voice quality varies a great deal by device — on an iPad the choice is narrow and mechanical, so nobody should judge how the app sounds without hearing the paid voice. *Formatting:* **headings now carry keep-with-next at the STYLE level** (none of the three heading styles ever had it), which fixes every orphaned heading at once rather than one at a time. **⚠ AND THE LESSON WORTH KEEPING: 18 body paragraphs had lost their paragraph spacing and 10 bullets had lost their bullet numbering**, all of them in sections added by hand in past sync passes (Not a Smart Speaker, Practice Mode, The AI Is a Layer, Where It Runs, the whole Further Out list). **The Aug 17 fold-back did not catch this because it compared paragraph TEXT and not formatting** — the generator's own `bullet()` calls were correct all along, so the drift existed only in the document. A text-only comparison will keep missing this class; compare paragraph properties too. *Both loose ends closed the same day, and one of them corrected me.* The terminal period he typed on one roadmap bullet was removed: he confirmed he had been noticing that the section had no periods, which led him to the real question — whether it was meant to be a bullet list at all. It was, and no bullet list in this document takes terminal periods. **And the legal term does belong here, not in the Beta Test Plan — Ken: *’Since the document addresses the "initial target user" it has to go here. It applies to the beta testers but it also applies to the target users when the app initially goes live.’* I had filed it as a beta-eligibility question because that is the context he raised it in; it is a standing property of who the app is for.** The eligibility bullet now reads *Age 18 or older, and holding legal capacity*, with the reasoning stated (the app speaks aloud in the user’s own name to real people, which is a decision they should be free to make for themselves), and a **Glossary entry** defines the term including that it normally arrives with the age of majority and can be limited by a guardianship or conservatorship. *Comment 38 is not a document change at all* — it is an app feature request (show that a fresh round trip is in progress, and let the user cut it short) and is tracked separately. **Folded back into `generate-product-overview-doc.js` in the same pass, per that file's own rule, including Ken's 8 tracked edits as accepted; verified by generating to a scratch file and diffing — 230 paragraphs, zero differences.** `doc-manifest.json` was deliberately NOT restamped: the live document now carries comments and tracked changes that a regeneration would destroy, so the overwrite guard should keep refusing until someone means it. **Restamped at the 0.7.8 release commit (`6495435`), which is what put it on origin/main.** *Prior pass:* **New “Access Methods” subsection under *Who It’s For Right Now* (Aug 17 2026), prompted by an SLP asking Ken whether someone with ALS could use the app.** The document had been saying the opposite of the truth: it listed **eye gaze among the FUTURE expansions**, when a gaze or head-tracking device running in computer-control mode already works today — verified against the code, which never asks what kind of pointer it is dealing with, and needs no dragging, right click, hover or pinch anywhere. That future-expansion sentence now lists switch scanning, literacy and age only, and the access-method bullet names the trackers explicitly. **The four things the new subsection has to keep saying**, because each is a question an SLP will ask: the device must be in the mode that moves the pointer and dwells (usually NOT the mode the equipment starts in, which is why “it didn’t work” reports are likely); button size and spacing are the tuning surface, while dwell time is set on the tracker; a response card takes ONE selection (the confirming second tap is Express-Panel-only and off by default); and **switch scanning does not exist**, which is the real limit for a progressive condition and is stated plainly rather than softened. ALS is named as an example, and one caveat is raised that nobody asked about — driving the screen by gaze is attention not spent on the partner’s face. Glossary *Direct Select* extended to match. **The generator was FOLDED BACK the same day and is authoritative again** — its body was rebuilt from this document and verified to reproduce it exactly (223 paragraphs and table rows, zero differences), so `generate-product-overview-doc.js` no longer reverts four months of hand edits. **The rule it now carries in its own header: prefer editing the generator; if a sync edits the .docx directly, fold it back in the same pass; and never run a generator straight over its own document to “check” it — generate to a scratch file and diff first.** The same hazard exists for any other generator whose document has been hand-edited; this is just the one that was caught. *Prior pass:* **Beta exception added to "Private by Design" (Aug 8 2026), because the section's flat claim stopped being true the moment the weekly report was armed.** One paragraph at the end of that section: during the beta, test devices send a weekly report of counts, timings and errors; **it carries the tester's assigned name, so it is a record with their name on it, not an anonymous one**; it never carries anything that was said, and a transcript goes only when a tester deliberately attaches one having read it first; testers can read the contents, review what has been sent, and switch it off in the app; and it ends with the beta. **Placed as a stated exception rather than by softening the opening claim** — "nothing is sent to any external server except the AI API" is still what the shipped product does, and weakening it permanently to cover a temporary arrangement would misdescribe the product to every future reader. **Restamp at the next release push.** *Prior pass:* **"Not a Smart Speaker" added (Aug 8 2026, Ken: *"This decision about not allowing the user to be treated as a smart speaker should be called out as a conscious decision that was made and documented in the product overview."*).** New Heading 2 subsection closing out *Speaking As the User, Not For the User* — the app will not answer general-knowledge questions on the user's behalf; his reasoning that a partner asking is itself evidence the fact is not shared ground; Reframe as the route for a fact the user does have; and **the system takes the user at their word** about what they know, with the "About Me" expertise question named and the quantum-mechanics example he gave. One sentence added to the *"About Me" Questionnaire* description pointing at that question. **Deliberately NOT documented: that a wrong expertise claim will be spoken confidently** — it follows from taking them at their word and belongs in the design record, not a positioning document. **Restamp at the next release push** (the code is committed locally and unpushed). *Prior pass:* **Practice Mode expanded (Aug 7 2026, Ken: *"the description of Practice mode is very limited in all the user facing documents. We need to talk more about its function and value"*).** It had ONE bullet in the Phase-1 feature list for a capability the Strategic Assessment calls the highest-value item at the alpha-to-beta stage. Added a **"You Can Practice Before It Counts"** subsection under *What Makes This Different* (placed after "In Your Own Words") arguing the three reasons it matters more than a rehearsal feature usually would — the first real conversation disproportionately decides whether someone keeps the device; the never-moves layout only pays off with repetition, and practice is where repetition is free; and using no microphone makes it a clean diagnostic separating app problems from room problems — plus the same-controls point (Start Listening cues the partner rather than opening the mic, so what is learned transfers). Added a matching **Glossary** entry, alphabetical order verified. **Restamp at the next release push.** *Prior pass:* **Synced for 0.6.3 (Aug 3 2026).** New **My Places** subsection under "The System Learns Who You Are" (after People and Relationships) and a matching Glossary entry, both stating the no-fixed-fields design rather than implying a schema. A Phase-1 feature bullet. **Phase 2 reworded** — it opened by promising situational awareness as wholly future, which is no longer true: it now says Phase 1 already delivers the user-declared half (tap a place, tap a partner) and Phase 2 is the system inferring the situation for itself. **Also corrected here, matching the app and both manuals:** two instances of the circular privacy phrasing ("named only if the user selects a response that includes them"), which describes the speaking gate as though it were a rule the AI follows while writing options. *Prior pass:* **Unified across both platforms (Ken, Aug 1 2026).** New "Where It Runs" section (both device families, Safari-only on iPad, the two ways to run it there) with a browser-tab vs Home-Screen comparison table carrying implications, not just facts — the document's first table, so borders are set directly (the template has no bordered table style). "Private by Design" now states where data lives per platform (pickable folder vs. app-private storage + Export/Import); "Who It's For" adds iPad with the Safari-only caveat; a new paragraph under "No Subscription" introduces the two optional paid services **as a later step, not the default** (Ken: users trial before taking on a second account). Roadmap + TTS glossary corrected — paid neural voices have shipped; voice banking is what remains ahead. **Cleared two residuals:** the July 8 filler→Placeholder rename finally landed here (7 passages) *and* the glossary's stale "tiered filler ladder" mechanism was rewritten to the shipped behavior; glossary restored to alphabetical order (also fixing a pre-existing Response Palette misplacement). **Residual:** the age-16 criterion still cites "current availability of suitable text-to-speech voices" as part of its rationale — weaker now that paid neural voices ship and younger voices are reachable; left alone deliberately, since changing an eligibility rationale is Ken's call. Generator `generate-product-overview-doc.js` is stale — the .docx has been the source of truth since the July syncs. *Prior pass:* | Synced for the v0.5.79–v0.5.98 window. Resolved the AI-optional pending item (new "The AI Is a Layer, Not a Lifeline" section). Added: listening chime note, closed-set/choice-card behavior, Practice Mode feature bullet, wind-down→closing flow note, in-app API-key guidance clause. **Residual:** Glossary still says "Filler / Filler Phrase" — the July 8 Filler→Placeholder rename previously claimed for this doc does not appear to have actually landed here; not investigated further (predates this window). |
| Conversant AAC Architecture Overview.docx | ✓ current | 2026-08-20 | `6495435` | **Synced across the 0.6.4–0.7.8 window (149 commits, Aug 9–Aug 20 2026).** *Corrections — things the document asserted that were no longer true:* **§9 Application Structure still listed the original SEVEN modules** when the app has ~46, and described `stt.js` and `tts.js` as browser-only when **both have had a shipped Deepgram backend since 0.5.99** — rewritten, plus a new paragraph grouping the significant additions by job rather than listing forty-six files. **§10 Open Interfaces listed Deepgram under *Future* for both STT and TTS**; it is the shipped second adapter for each, and the entry now records *why* it was chosen over cheaper rivals (browser-direct auth, so the no-server principle survives). **`placeholders.js` quoted phrases that no longer exist** (“Just a second…”) and described the ladder as firing at the silence checkpoint; **§11 Timing was worse — it still described the question-flavored pool (“Good question.”) that was REMOVED in August 2026** precisely because choosing between pools required the classification and so chained the ladder to the AI round-trip. Both now state the partner-statement-independent rule and the repair-initiator exception. **§9 Settings said ten tabs; there are twelve** — Speech & Input split into two, and Troubleshooting was added (the error log moved there from About). **§8 said five bundled scenarios; there are six** — the controls tour, which is a different KIND of scenario (scripted, no API key) and is noted as such. **§10 Data Stores claimed “API keys (encrypted)”, which is false** — they are plain text in origin-scoped localStorage, excluded from profiles/exports/backups, with encryption still an open security item; and its opening line assumed the File System Access API everywhere, which is wrong on WebKit (OPFS). **⚠ AND §9 STILL CARRIED THE CIRCULAR PRIVACY CLAUSE** — *“spoken only if the user selects a response that includes it”* — the exact defect corrected in the app and in §6 on August 3 2026. §6 was fixed then and this summary paragraph was missed, which is the lesson: **a rule stated in two places gets corrected in one.** It now names the two prompts the model can actually act on while authoring (the partner asks; the user steers with Reframe) and says why the selection-gate phrasing names no mechanism. *Additions — two subsystems that had no home here:* a **§6 “Voice Layer”** subsection (forced choice as the instrument, and the four decisions that carry it: ask what they WOULD SAY rather than what sounds like them; catchphrases stay on Express Panel buttons and are never generated; per-person register lives on the graph edge, not the questionnaire; behavior outranks self-report — plus the Cyrano risk named rather than solved), and a **§9 “Instrumentation and Error Reporting”** subsection (the counts-not-content rule, the Troubleshooting tab, the weekly beta report, transcripts only on deliberate attachment, and the opt-in reversal at public release). **Method note:** patched as strings INSIDE runs, never rebuilding a paragraph, because this document carries 9 drawings, 10 tables and bold lead-in runs that a rebuild would flatten — verified after: root element byte-identical to the backup, all 35 namespace declarations intact, 10 tables and 9 drawings still present, every inserted bold lead-in bold. **Its generator (`generate-doc.js`) remains deliberately STALE and is GUARDED** — confirmed it refuses to overwrite — so it cannot revert this; folding ~70 pre-existing differences plus these back is its own pass, not a drive-by. **Residuals, unchanged:** Figures 6 & 7 still depict the old transcript-gate/ladder design and §12 still describes the vision-level UI rather than the shipped app — both need an image toolchain this box does not have, and §12 is a rewrite rather than a patch. **Also not covered in this pass** (flagged rather than silently skipped): prompt caching (§5/§11), the pronunciation lexicon beyond its one-line mention, and the 0.7.8 palette-busy cue in §12. *Prior pass:* **Silence period explained properly (Ken, Aug 9 2026): *“Ensure that the User Manuals (and maybe other documents) fully/properly explain what the Silence period parameter controls/represents.”*** Three defects, not one. The stated **default was 2 seconds** (it has been **0.5** since Aug 7 2026) and the **range 0.5–3.0** (a **0** option was added Aug 9), so both figures were wrong everywhere they appeared. And nothing said what the setting *represents*: the pause is a **checkpoint, not a stop** — recording continues, the partner’s turn ends when the **user answers**, and nothing is ever spoken unprompted, which is exactly what makes a short setting safe. That omission mattered because the natural reading of the old wording (“sends the partner’s speech to the AI”) is that a short setting risks cutting someone off, when the real cost is **discarded AI requests, i.e. money**. Here the figures were stale **and the stated rationale contradicted a settled decision**: the settings list still read *“TRP silence threshold (default 1.5 s; range 0.5–3.0 s) — shorter risks responding before the partner finishes”*, but since **July 10 2026** a checkpoint only **prepares** options silently and the turn ends when the user answers, so there is no false-TRP action left to risk. Rewritten to name the real trade (superseded, discarded generations — a token cost) and the 0 s behavior. Two stale **“Optional Responses silence period”** labels corrected to the shipped **“Silence period”**. Content is current; **At commit restamps at the next release push.** *Prior pass:* **Synced for 0.6.3 (Aug 3 2026).** New **§6 "Places Model"** subsection: own model and file, name-plus-arbitrary-named-facts with the reasoning for having no schema, facts as an ordered list rather than a map, and the two derived blocks split by what changes when (standing list vs. the situational "here now" block). Placed *before* the privacy subsection so the three privacy levels can cover fields, people and places together — and that subsection now says so. **§14 Situational Awareness gained a leading note** recording that the user-declared half shipped in Phase 1, so the section is not read as wholly future; the sensor-driven items below it are unchanged. Local-storage inventory now lists places. **Also corrected here:** the Privacy Model's "the AI may only include it if the user explicitly selects one that contains it", replaced with the two real prompts plus an explicit note on *why* naming them is load-bearing — this is the architecture document, so the reasoning belongs here rather than just the rule. *Prior pass:* **Later the same day:** new **§18 Hosting and Operational Dependencies** — what serves what (and that web hosting and DNS are separate services, so the volksswitch.org *website* going down does not affect the app), a dependency ledger table covering every way the system can be taken off the air with its blast radius, and §18.3 on recovering from a DNS outage (including why the revert lever is wrong for a short one). §4 Sustainability Model gained a cross-reference: "no infrastructure to defund" does not mean "no dependencies", and the domain registration is now an availability control for the app. §17.1 rewritten when the second iPad deployment was removed. | **The iPad Architecture document is merged in and retired (Ken, Aug 1 2026).** One codebase was a deliberate decision, so there is one architecture document; two would have drifted into describing two architectures. **§5 Technical Decisions** rewritten at the seams rather than annotated: Target Hardware now states the two configurations on the **engine** axis and retires the Windows-only claim (its FSA-picker leg is gone, replaced by OPFS on WebKit); Local Data Storage carries the OPFS branch and *why* Export/Import stops being a convenience there; TTS records the shipped paid cloud voice and the measured iPad voice roster; Cross-Device Transfer records Export/Import as built; and a **new Speech Recognition decision** was added — it had never been stated as a decision at all, despite now having two selectable backends. **New §17 Platform Configurations**: 17.1 the two configurations + the naming rule + the `platform.js` invisibility mechanism (rewritten later the same day when the second iPad deployment and its namespace module were removed — one codebase, now one deployment); 17.2 the two WebKit modes with a measured comparison table and the confirmed pincer; 17.3 a complete seam-by-seam divergence table, plus what the iPad gains and what is out of reach; 17.4 evidence, carrying the **50%-research-error finding** as a standing instruction on how far to trust unmeasured platform claims, and the four open questions. The stranded closing paragraph was moved to the end and rewritten. **Residual:** Figures 6 & 7 still depict the old transcript-gate/ladder design, and §12 still describes the vision-level UI rather than the shipped app — both carried forward from the July 27 pass, both needing an image toolchain this box does not have. | Synced for the v0.5.79–v0.5.98 window. Resolved the AI-optional pending item (new §9 subsection). Rewrote §11's end-of-utterance section for the July 10 turn-completion-gate reversal (with CA-faithfulness rationale) + JSON contract/Table 2 updates; added a new §11 "Closed-Set (Alternative-Question) Turns" subsection; rewrote §8 Practice Communication Partner from future-tense to built/shipped; fixed the Strategic Assessment's two flagged errors (false Claude "free tier" claim in §4/§16; Windows-only vs. Chromebook/Mac hardware discrepancy, now aligned with the Product Overview); updated the stale "six tabs" Settings list to ten; corrected three stale "Ask them to repeat discards the capture" descriptions (reversed v0.5.87). **RESIDUAL (carried forward):** Figures 6 & 7 still depict the old transcript-gate/ladder design and need regenerating — no image toolchain on this box. **New residual:** §12 "User Interface Design" still describes the original vision-level UI (mode chip, triple coding, transcript-confirmation-gate framing) rather than the shipped app, unlike the sibling UI-Design.docx which got a full reconciliation July 16 — out of this window's scope, flagged for the next pass. |
| Conversant AAC User Manual (Windows, Chromebook, Mac).docx | ✓ current | 2026-08-17 | `03a3bf2` | **Synced Aug 17 2026 for the Settings → Troubleshooting tab, which neither manual had ever documented.** The tab shipped with the August instrumentation work and both manuals stopped at About, so three things were wrong at once: the **error log was documented on the About tab**, where it no longer lives; **“report a problem” was described as a message the user composes by hand**, quoting the version number themselves, when the app now attaches all of that; and the **known-issues page, the usage summary and the weekly report were entirely absent.** New §6.9 covers the six groups on the tab and About becomes §6.10. **The 0.7.6 additions are folded in** — the usage summary is now week-by-week as well as cumulative, and a problem report carries what the app was doing in the minutes beforehand (counts and timings, no words). **Also fixed while there: the About tab never documented the running cost estimate**, which is odd in a product whose funding model is that the user pays per conversation. **⚠ DELIBERATE OMISSION — the *Tester name* box on that tab is NOT described**, per the Beta Test Plan §5.1, which calls it beta-only and says it is not in the manual. It is the one control on the tab a reader will see and not find here; **document it if the box outlives the beta**. Note also that **automatic weekly sending is default-on for the beta only** and reverts to opt-in at public release — the §6.9 wording describes the switch rather than the default, so it survives that change unedited. Contents is a Word field and re-numbers itself on open. *Prior pass:* **Synced for 0.7.0 + 0.7.2 (Aug 11 2026), both manuals in one pass — this cleared the whole outstanding list.** (1) **§6 Settings** gained two paragraphs on how a tab now behaves: one column, every heading opens and closes, **all closed on arrival**, opening one leaves the others alone, and what you opened is remembered until the app is closed. A second paragraph covers **nested sections** and states the idiom explicitly — *a control indented alongside several settings rather than having a heading of its own applies to all of them* — naming **Button sizes and gaps** and **Screen openings**. (2) **§7.3 Editing the Express Panel was REWRITTEN, not amended**: it described a list editor that no longer exists. Seven steps for the one-button-at-a-time editor plus five notes — the panel is live beside the tab so you edit the real thing in place; the tapped button stays highlighted; add / move / delete / Done; the conversation gate on defining a cell; the full-panel warning that **names the button that will be deleted**; Delete unavailable on an empty button and Move down as the way to be rid of one; the leftover-buttons offer. The stale Tip (“items that do not fit are simply not shown”) was replaced — the app now refuses to create an unreachable button. **⚠ The Partner-linking note was overwritten and restored in the same pass**, extended to cover Place. (3) **0.7.0 items, all written:** “How to say it” with its speaker button for a phrase, a person’s name AND nickname, and a place name — each stating that only the voice sees the respelling; **Sound Check** in §5.3; the paid voice no longer dropping out for a single sentence (Speech table). (4) **Listen mid-turn is a PAUSE** — the Start Listening row now says what ends a partner’s turn (you answer, ask them to repeat, or end the conversation — never the button), plus a Troubleshooting row for anyone who learned the old behavior. **Residual:** none. *TOC is an auto-field; it refreshes on next open in Word.* (5) **Windows only — the browser’s full-screen message**, cleared from the pending list: the “To exit full screen, press [Esc]” notice comes from the browser, not the app, appears at every start while the setting is on, and draws [Esc] as something tappable that is not; the way out is the checkbox. Added to the **Use the whole screen** row. *Prior passes:* **⚠ ADDED BY 0.7.2 (Aug 11 2026), not yet written — the Settings panel was reorganized, so every Settings instruction needs re-reading.** Tabs are **one column** and each heading now **opens and closes**, all closed when a tab is opened, so any wording that implies everything is visible at once is wrong; sections can hold sections (Button size / spacing / minimum spacing now sit inside **Button sizes and gaps** with the reset button, and Title Bar Height sits inside **Screen openings**), which changes where a reader is told to look. The **Express Panel tab** no longer lists every button: it says "tap a button in the Express Panel to edit or move it" and then shows that one button with **add before / add after / move up / move down / delete / done** — so the current step-by-step for editing the panel describes a screen that no longer exists. Also: adding a button to a full panel warns and deletes the last one; **Delete** is unavailable on an empty button (move it to the end instead); and the **Controls** tab is sectioned too. Shared behavior, so it lands in BOTH manuals in the same pass. **Silence period explained properly (Ken, Aug 9 2026): *“Ensure that the User Manuals (and maybe other documents) fully/properly explain what the Silence period parameter controls/represents.”*** Three defects, not one. The stated **default was 2 seconds** (it has been **0.5** since Aug 7 2026) and the **range 0.5–3.0** (a **0** option was added Aug 9), so both figures were wrong everywhere they appeared. And nothing said what the setting *represents*: the pause is a **checkpoint, not a stop** — recording continues, the partner’s turn ends when the **user answers**, and nothing is ever spoken unprompted, which is exactly what makes a short setting safe. That omission mattered because the natural reading of the old wording (“sends the partner’s speech to the AI”) is that a short setting risks cutting someone off, when the real cost is **discarded AI requests, i.e. money**. Section 9.2 rebuilt: three plain-language lead paragraphs on what it is, what the pause does and does not mean, and what a short setting actually costs — then the tuning bullets, including that **0 seconds is not “no wait”** but “go when the speech recognition itself reports they have stopped”, whose timing varies because the recognizer is judging. Walkthrough sentence, Settings-reference row and glossary row all corrected. **Also fixed in passing:** both manuals quoted **“Good question.”** as the example placeholder in two tables; that phrase was removed from the app on Aug 7 2026 (it presumes a question was asked, and placeholders now play after any turn), so it now reads “I’m thinking about that.” Both manuals done in one pass per the self-contained rule. **Restamp at the next release push.** *Prior pass:* **Practice Mode expanded (Aug 7 2026, Ken: *"the description of Practice mode is very limited in all the user facing documents. We need to talk more about its function and value"*).** Section 6.6 was purely mechanical — how to open the tab, five steps, no statement of why anyone would use it. Added a lead-in plus six bulleted reasons (before a first real conversation; to build the speed the never-moves layout is designed to pay off; to rehearse a specific upcoming situation; to tell an app problem from a room/microphone problem, since practice uses no mic; to test a settings change safely; to show family or a therapist how it works). **Also added the limitation Ken asked for:** in a real conversation, starting one turns listening on straight away, but in Practice Mode nothing happens until Start Listening is tapped unless auto-resume is on — so a practice conversation that seems to stop dead after the user's turn is behaving correctly. Both manuals updated in the same pass (self-contained rule); content is platform-neutral. **Restamp at the next release push.** *Prior pass:* **⚠ PENDING (Ken, Aug 5 2026) — what the Listen button does mid-turn.** Shared behavior, so it lands in BOTH manuals in the same pass. Stopping and restarting Listen while the other person is still mid-turn no longer discards the first half of what they said — it is a **pause**: their words are kept and the rest is added on. Say plainly what ends their turn: replying, asking them to say it again, or ending the conversation — **not** tapping the button. Worth a Troubleshooting row too, since the old behavior is what a returning user will have learned to expect. Also note that **"Ask them to say that again" is now the only way to scrap a garbled capture** — the button no longer does it by accident. **⚠ PENDING (Ken, Aug 3 2026) — the browser's own full-screen notification.** Entering full screen raises a message from the **browser, not the app**: *"<address> — To exit full screen, press [Esc]"*. A web page cannot style, move, or suppress it, and it fades on its own after a few seconds. Two reasons it needs a sentence in the manual, both Ken's: it appears **on Start, not only when the setting is toggled**, so anyone who leaves "Use the whole screen" on meets it at the beginning of every session; and it **draws the Esc key as what looks like a tappable button, which it is not** — it is browser chrome, so a tap does nothing, and a touch-only user with no physical keyboard cannot act on the only instruction on screen. Put it beside the "Use the whole screen" row in the §6 General table, worded so it does not read as the app misbehaving, and say plainly that the way back out is the same checkbox. **UNVERIFIED on iPad** — Safari's equivalent message, and whether it appears at all in a Home Screen app, have not been measured; check on the device before writing the iPad half rather than assuming it matches (the 50%-research-error finding). **Amended Aug 3 2026 (post-0.6.3):** “Use the whole screen” now defaults **OFF** — the General-tab row in both manuals rewritten to match, and the Windows manual’s keyguard section gained the **taskbar auto-hide** step from Ken’s actual workflow, without which a full-screen capture is taller than the size the openings file quotes and looks wrong when it is not. **Synced for 0.6.2 + 0.6.3 (Aug 3 2026), both manuals in one pass.** Added **§5.5 Adding the Places You Go** (steps, the “what / is” fact shape, the no-set-list reasoning, Private). Express Panel section: item types three → four, a new **Place toggle** row noting it is the one influencer that SURVIVES End conversation, and the End-conversation note corrected to match. **Filled a `<add a description>` placeholder** that had been sitting in the Express Panel table — the choice buttons now have a real description, including that the tapped one stays lit. General tab gained **Use the whole screen** (worded per platform: taskbar and browser bars on a computer, Safari toolbars on iPad with the note that a Home Screen app has little left to hide). Keyguard Design: two sliders → three, with **Screen edge margin** and the warning that unlike the other two it does move every hole. Settings intro now covers the **? spoken help**. Button size row notes that Settings controls take their size from it. **Removed the stale “Voices offered to the app” passage** — the control is gone from the app. *TOC is an auto-field; it refreshes on next open in Word.* **Residual:** none outstanding. *Superseded pending items follow:* **PLUS 0.6.3 (released Aug 3 2026 as `b2602a0`), not yet documented:** the new **"Use the whole screen"** checkbox on Settings → General — on by default, and it re-cuts a keyguard, so it belongs beside the other hole-moving controls in the keyguard section as well as in the §6 General table. And **My Places:** a new About Me section for the places you go and the facts you record about each one, and a new **place** button type in the Express Panel that tells the assistant where you are — the same shape as the partner button, so §7 (Express Panel) and the About Me section both need it, including that a place, unlike partner and feeling, **stays selected when a conversation ends**. Shared behavior, so it lands in BOTH manuals in the same pass. **⚠ ONE ITEM STALE (Aug 2 2026): the About tab's "Voices offered to the app" readout was REMOVED from the app** (Ken) — the passage added in this very sync now describes a control that is not there, including the close-and-reopen note attached to it. Delete it on the next sync; nothing else in the voice material is affected (the "Show this device's joke voices" checkbox stays). **PLUS 0.6.2 (released Aug 2 2026), not yet documented:** the Settings **?** button and spoken help (tap it, then tap a control to hear what it does); the new **Screen edge margin** slider on the Keyguard Design tab, which moves the whole app in from the screen edges *including the dock*, so it re-cuts a keyguard; and Settings buttons/dropdowns now sized off the Express Panel button, which makes the title bar and every control noticeably larger. The §6 Settings tables and the keyguard section are what need revisiting. **Synced Aug 2 2026 (release 0.6.1 window).** The staged Settings help text was folded in and `SETTINGS-HELP-TEXT.md` deleted. Most of it was **already present** in the manual’s Settings tables — the keyguard re-cut warnings, silence period and placeholder timing were all there — so this was a merge, not an insertion. The genuine gaps: **Backup & transfer**, which was absent from the entire manual despite being Tier 1 (what a backup holds, that API keys are never in one); the **export save-prompt note**, clearing the CLAUDE.md TO DO — the browser names the site in its save dialog, which reads as though data is leaving, and it is not; the Controls tab’s **`{name}` token** and **why "Say again" is absent from it**, both undiscoverable from the screen; and the About tab’s **voice diagnostic**, including that some systems only reveal a new voice after a full close-and-reopen. Tier-3 items (the four Text Size entries, "Which side", "Reset to default") were deliberately not carried — each label already says it, per the staging file’s own triage. | **Address updated (Aug 1 2026):** the app now serves from `https://conversant.volksswitch.org/` (SEC-1, its own origin) — §2.2/§3.1 and the quick-reference row updated; the old address 301-redirects but bookmarks should be changed. | **Troubleshooting row added (Ken, Aug 1 2026):** "The app will not load at all" — nothing of yours is lost, points at the data folder on the computer. Deliberately reassurance rather than an explanation: the DNS/hosting detail behind it lives in Architecture Overview §18, which this reader does not need. | **Half of the Aug 1 2026 split** (see the row below for the shared work). Platform-specific: cover subtitle names the platform; the introduction states which manual this is and sends iPad readers to the other one; the hardware table's "Not supported: … iPad" row became "Covered separately" (iPad is a supported platform now, just documented elsewhere); the software table notes Safari is not supported *on a computer*; a new passage after the API-key note introduces the optional paid voice as a later step; the install tip is rewritten around the screen-space benefit, which costs nothing here.  **⚠ ADDED BY 0.7.0 (Aug 8 2026), not yet written:** the **"how to say it"** field (Express Panel phrases, a person's name AND what you call them, and places) with its 🔊 — spelling a word the way it sounds is what works, and only the voice sees it, so the button, the person, the place and the transcript keep the real spelling; **empty Express Panel cells** now show as outlined buttons and tapping one sets up a button in that exact spot (not during a conversation); the **Express Panel is live while its Settings tab is open**; the **🔊 on each phrase** in that tab; and the **Sound Check** module in About Me. Also worth a line: the paid voice no longer drops to the device voice for a single sentence after a quiet stretch.  **Synced for 0.7.3 (Aug 15 2026) — four edits, identical in both manuals, verified by a full paragraph-and-table diff against the pre-edit backups (8 changed lines, nothing else moved).** (1) §7.3's note said a button *"cannot be added or changed while a conversation is under way"* — the August 7 rule, superseded: editing is now **Settings-only**, so tapping an empty cell on the conversation screen does nothing at any time. (2) The "add a button" step now says the empty cell is **in the panel beside the tab**, since that is the only place it works. (3) The "tap any button" step says **one tap is enough in Settings even if you use double tap** — the exact thing that stopped an SLP dead on 0.7.2, because only phrase buttons honor that setting and the feeling buttons beside them worked. (4) The **Activation: single tap or double tap** settings row says it applies while you are talking, not in Settings. **No change needed for the API-key jump fix** — neither manual documents the pre-start "Add an API key" button, so it restored intended behavior nothing described. **Synced again for 0.7.4 (Aug 15 2026), two more edits in §7.3, again identical in both and again verified by a full paragraph-and-table diff (4 changed lines, nothing else moved).** (1) The typing step now says the **cursor does not start in the text box** and that **Enter puts the keyboard away** — with the reason, since it is what makes the advice worth taking: the keyboard covers the Express Panel while it is up, and the panel is what Add before / Add after / Move are judged against. *(The Done step already said it dismisses the keyboard; Enter is the quicker route, not a replacement.)* (2) The Partner/Place note now adds that **Partner, Feeling and Place buttons each have one fixed color, shown as a small sample rather than a set of choices** — the thing an SLP pressed expecting a palette.  **⚠ PENDING FROM 0.7.5 (Aug 15 2026): the CONTROLS TOUR is not mentioned.** Settings → Practice now offers **"A tour of the buttons"**, which walks the Command Bar one control at a time, describes the right button when the wrong one is pressed, and **needs no API key**. The Practice section of each manual currently implies the whole tab requires a key — that gate is now per-scenario, so that is the part which is actually WRONG rather than merely missing. The tour is also the natural first thing to send a new user to, so §3 Getting Started is worth a pointer.  **Synced for 0.7.5 (Aug 15 2026) — six edits, identical in both, verified by a full paragraph-and-table diff (13 changed lines each, one paragraph added, nothing else moved).** §6.6 said Practice *“needs your API key”*, which the per-scenario gate made **wrong rather than merely incomplete** — it would have told a tester the one thing they can do on day one was unavailable. It now scopes the key to the five conversation scenarios, carries a paragraph on **A tour of the buttons**, points the "before your first real conversation" bullet at it, and §4 sends a new user to it first. **Also fixed, pre-existing and unrelated to the tour:** step 3 said to tap the X to leave Settings, but `startPractice` has been closing the dialog itself — measured in the running app on a conversation scenario, not only on the tour. **⚠ THE NEAR-MISS WORTH KEEPING: a `startswith` matcher hit TWO paragraphs.** “Tap the X at the right of the Settings title bar” opens both the Practice step and an **About Me** instruction, so the About Me paragraph was silently overwritten with Practice text. **The full diff is the only reason it was caught** — it was restored from the backup and the diff re-run. Exactly the failure this file already records from the Beta Test Plan pass, arriving through a different door: not "did I make my edits" but "did I make ONLY my edits". |
| Conversant AAC User Manual (iPad).docx | ✓ current | 2026-08-17 | `03a3bf2` | **Synced Aug 17 2026 for the Settings → Troubleshooting tab, which neither manual had ever documented.** The tab shipped with the August instrumentation work and both manuals stopped at About, so three things were wrong at once: the **error log was documented on the About tab**, where it no longer lives; **“report a problem” was described as a message the user composes by hand**, quoting the version number themselves, when the app now attaches all of that; and the **known-issues page, the usage summary and the weekly report were entirely absent.** New §6.9 covers the six groups on the tab and About becomes §6.10. **The 0.7.6 additions are folded in** — the usage summary is now week-by-week as well as cumulative, and a problem report carries what the app was doing in the minutes beforehand (counts and timings, no words). **Also fixed while there: the About tab never documented the running cost estimate**, which is odd in a product whose funding model is that the user pays per conversation. **⚠ DELIBERATE OMISSION — the *Tester name* box on that tab is NOT described**, per the Beta Test Plan §5.1, which calls it beta-only and says it is not in the manual. It is the one control on the tab a reader will see and not find here; **document it if the box outlives the beta**. Note also that **automatic weekly sending is default-on for the beta only** and reverts to opt-in at public release — the §6.9 wording describes the switch rather than the default, so it survives that change unedited. Contents is a Word field and re-numbers itself on open. *Prior pass:* **iPad-only fix: “Reload the app” told the reader it saved them pressing Ctrl+Shift+R, a shortcut an iPad has no keyboard for.** **Synced for 0.7.0 + 0.7.2 (Aug 11 2026), both manuals in one pass — this cleared the whole outstanding list.** (1) **§6 Settings** gained two paragraphs on how a tab now behaves: one column, every heading opens and closes, **all closed on arrival**, opening one leaves the others alone, and what you opened is remembered until the app is closed. A second paragraph covers **nested sections** and states the idiom explicitly — *a control indented alongside several settings rather than having a heading of its own applies to all of them* — naming **Button sizes and gaps** and **Screen openings**. (2) **§7.3 Editing the Express Panel was REWRITTEN, not amended**: it described a list editor that no longer exists. Seven steps for the one-button-at-a-time editor plus five notes — the panel is live beside the tab so you edit the real thing in place; the tapped button stays highlighted; add / move / delete / Done; the conversation gate on defining a cell; the full-panel warning that **names the button that will be deleted**; Delete unavailable on an empty button and Move down as the way to be rid of one; the leftover-buttons offer. The stale Tip (“items that do not fit are simply not shown”) was replaced — the app now refuses to create an unreachable button. **⚠ The Partner-linking note was overwritten and restored in the same pass**, extended to cover Place. (3) **0.7.0 items, all written:** “How to say it” with its speaker button for a phrase, a person’s name AND nickname, and a place name — each stating that only the voice sees the respelling; **Sound Check** in §5.3; the paid voice no longer dropping out for a single sentence (Speech table). (4) **Listen mid-turn is a PAUSE** — the Start Listening row now says what ends a partner’s turn (you answer, ask them to repeat, or end the conversation — never the button), plus a Troubleshooting row for anyone who learned the old behavior. **Residual:** none. *TOC is an auto-field; it refreshes on next open in Word.* (5) The full-screen pending item **does not apply here** and was dropped rather than written: the control is not offered on iPad at all, and the §6 General table correctly has no such row. *Prior passes:* **⚠ ADDED BY 0.7.2 (Aug 11 2026), not yet written — the Settings panel was reorganized, so every Settings instruction needs re-reading.** Tabs are **one column** and each heading now **opens and closes**, all closed when a tab is opened, so any wording that implies everything is visible at once is wrong; sections can hold sections (Button size / spacing / minimum spacing now sit inside **Button sizes and gaps** with the reset button, and Title Bar Height sits inside **Screen openings**), which changes where a reader is told to look. The **Express Panel tab** no longer lists every button: it says "tap a button in the Express Panel to edit or move it" and then shows that one button with **add before / add after / move up / move down / delete / done** — so the current step-by-step for editing the panel describes a screen that no longer exists. Also: adding a button to a full panel warns and deletes the last one; **Delete** is unavailable on an empty button (move it to the end instead); and the **Controls** tab is sectioned too. Shared behavior, so it lands in BOTH manuals in the same pass. **Silence period explained properly (Ken, Aug 9 2026): *“Ensure that the User Manuals (and maybe other documents) fully/properly explain what the Silence period parameter controls/represents.”*** Three defects, not one. The stated **default was 2 seconds** (it has been **0.5** since Aug 7 2026) and the **range 0.5–3.0** (a **0** option was added Aug 9), so both figures were wrong everywhere they appeared. And nothing said what the setting *represents*: the pause is a **checkpoint, not a stop** — recording continues, the partner’s turn ends when the **user answers**, and nothing is ever spoken unprompted, which is exactly what makes a short setting safe. That omission mattered because the natural reading of the old wording (“sends the partner’s speech to the AI”) is that a short setting risks cutting someone off, when the real cost is **discarded AI requests, i.e. money**. Section 9.2 rebuilt: three plain-language lead paragraphs on what it is, what the pause does and does not mean, and what a short setting actually costs — then the tuning bullets, including that **0 seconds is not “no wait”** but “go when the speech recognition itself reports they have stopped”, whose timing varies because the recognizer is judging. Walkthrough sentence, Settings-reference row and glossary row all corrected. **Also fixed in passing:** both manuals quoted **“Good question.”** as the example placeholder in two tables; that phrase was removed from the app on Aug 7 2026 (it presumes a question was asked, and placeholders now play after any turn), so it now reads “I’m thinking about that.” Both manuals done in one pass per the self-contained rule. **Restamp at the next release push.** *Prior pass:* **Practice Mode expanded (Aug 7 2026, Ken: *"the description of Practice mode is very limited in all the user facing documents. We need to talk more about its function and value"*).** Section 6.6 was purely mechanical — how to open the tab, five steps, no statement of why anyone would use it. Added a lead-in plus six bulleted reasons (before a first real conversation; to build the speed the never-moves layout is designed to pay off; to rehearse a specific upcoming situation; to tell an app problem from a room/microphone problem, since practice uses no mic; to test a settings change safely; to show family or a therapist how it works). **Also added the limitation Ken asked for:** in a real conversation, starting one turns listening on straight away, but in Practice Mode nothing happens until Start Listening is tapped unless auto-resume is on — so a practice conversation that seems to stop dead after the user's turn is behaving correctly. Both manuals updated in the same pass (self-contained rule); content is platform-neutral. **Restamp at the next release push.** *Prior pass:* **⚠ PENDING (Ken, Aug 5 2026) — what the Listen button does mid-turn.** The other half of the shared item on the row above — same wording, same pass; see it for the full description. Worth remembering that this is the platform where the truncated-counting symptom was first met, so the Troubleshooting row matters more here. **⚠ PENDING (Ken, Aug 3 2026) — the browser's own full-screen notification.** Entering full screen raises a message from the **browser, not the app**: *"<address> — To exit full screen, press [Esc]"* on Chromium. A web page cannot style, move, or suppress it. Two reasons it needs a sentence in the manual, both Ken's: it appears **on Start, not only when the setting is toggled**, so anyone who leaves "Use the whole screen" on meets it at the beginning of every session; and it **draws the Esc key as what looks like a tappable button, which it is not** — it is browser chrome, so a tap does nothing, and a touch-only user with no physical keyboard cannot act on the only instruction on screen, which bites hardest here (an iPad has no Esc key at all unless a keyboard is attached). **SUPERSEDED SAME DAY — none of the above applies to this manual any more.** Measured on Ken's iPad (iPadOS 26.6): "Use the whole screen" does nothing in a Home Screen app (fullscreen does not displace the iOS status bar) and breaks Settings and the Listen button in a Safari tab, so **the control is no longer offered on iPad at all**. What this manual needs is therefore the opposite of a description: **remove the "Use the whole screen" row from the §6 General table**, since a reader will not find that checkbox on their device. Note also that Safari's fullscreen exit control is a persistent tappable **X**, not the Esc bubble described above — but with the feature gone there is no longer anywhere in this manual that needs to mention it. Belongs beside the "Use the whole screen" row in the §6 General table, worded so it does not read as the app misbehaving, and saying plainly that the way back out is the same checkbox. **Amended Aug 3 2026 (post-0.6.3):** “Use the whole screen” now defaults **OFF** — the General-tab row in both manuals rewritten to match, and the Windows manual’s keyguard section gained the **taskbar auto-hide** step from Ken’s actual workflow, without which a full-screen capture is taller than the size the openings file quotes and looks wrong when it is not. **Synced for 0.6.2 + 0.6.3 (Aug 3 2026), both manuals in one pass.** Added **§5.5 Adding the Places You Go** (steps, the “what / is” fact shape, the no-set-list reasoning, Private). Express Panel section: item types three → four, a new **Place toggle** row noting it is the one influencer that SURVIVES End conversation, and the End-conversation note corrected to match. **Filled a `<add a description>` placeholder** that had been sitting in the Express Panel table — the choice buttons now have a real description, including that the tapped one stays lit. General tab gained **Use the whole screen** (worded per platform: taskbar and browser bars on a computer, Safari toolbars on iPad with the note that a Home Screen app has little left to hide). Keyguard Design: two sliders → three, with **Screen edge margin** and the warning that unlike the other two it does move every hole. Settings intro now covers the **? spoken help**. Button size row notes that Settings controls take their size from it. **Removed the stale “Voices offered to the app” passage** — the control is gone from the app. *TOC is an auto-field; it refreshes on next open in Word.* **Residual:** none outstanding. *Superseded pending items follow:* **PLUS 0.6.3 (released Aug 3 2026 as `b2602a0`), not yet documented:** the new **"Use the whole screen"** checkbox on Settings → General — on by default, and it re-cuts a keyguard, so it belongs beside the other hole-moving controls in the keyguard section as well as in the §6 General table. And **My Places:** a new About Me section for the places you go and the facts you record about each one, and a new **place** button type in the Express Panel that tells the assistant where you are — the same shape as the partner button, so §7 (Express Panel) and the About Me section both need it, including that a place, unlike partner and feeling, **stays selected when a conversation ends**. Shared behavior, so it lands in BOTH manuals in the same pass. **⚠ ONE ITEM STALE (Aug 2 2026): the About tab's "Voices offered to the app" readout was REMOVED from the app** (Ken) — the passage added in this very sync now describes a control that is not there, including the close-and-reopen note attached to it. Delete it on the next sync; nothing else in the voice material is affected (the "Show this device's joke voices" checkbox stays). **PLUS 0.6.2 (released Aug 2 2026), not yet documented:** the Settings **?** button and spoken help (tap it, then tap a control to hear what it does); the new **Screen edge margin** slider on the Keyguard Design tab, which moves the whole app in from the screen edges *including the dock*, so it re-cuts a keyguard; and Settings buttons/dropdowns now sized off the Express Panel button, which makes the title bar and every control noticeably larger. The §6 Settings tables and the keyguard section are what need revisiting. **Synced Aug 2 2026 (release 0.6.1 window).** The staged Settings help text was folded in and `SETTINGS-HELP-TEXT.md` deleted. Most of it was **already present** in the manual’s Settings tables — the keyguard re-cut warnings, silence period and placeholder timing were all there — so this was a merge, not an insertion. The genuine gaps: **Backup & transfer**, which was absent from the entire manual despite being Tier 1 (what a backup holds, that API keys are never in one); the **export save-prompt note**, clearing the CLAUDE.md TO DO — the browser names the site in its save dialog, which reads as though data is leaving, and it is not; the Controls tab’s **`{name}` token** and **why "Say again" is absent from it**, both undiscoverable from the screen; and the About tab’s **voice diagnostic**, including that some systems only reveal a new voice after a full close-and-reopen. The iPad half of every platform-split entry was used: the Backup row states that Export is the *only* way to get data off the device, and the export note covers the Files round trip and why neither direction can be automated. | **Address updated (Aug 1 2026):** the app now serves from `https://conversant.volksswitch.org/` (SEC-1, its own origin) — §2.2/§3.1 and the quick-reference row updated; the old address 301-redirects but bookmarks should be changed. | **Troubleshooting row added (Ken, Aug 1 2026):** "The app will not load at all" — nothing of yours is lost, points at the app’s own storage and ties the reassurance to why exporting still matters. Deliberately reassurance rather than an explanation: the DNS/hosting detail behind it lives in Architecture Overview §18, which this reader does not need. | **The other half of the split.** Rewritten wherever an instruction assumed Chrome/Edge or a pickable folder — verified by scanning for every such term afterwards, with no leftovers. §2.1/2.2 hardware and software tables rebuilt for iPad/Safari/iPadOS 17+ (with *why* it must be Safari); a new Deepgram section explains what the key buys and exactly when it is required; §3.1 replaced with the **two ways to run it** (Safari page vs. Home Screen app), a differences-and-implications table, how to Add to Home Screen, and the warning that the two are separate storage and need an Export/Import to move between; §5.2 "Setting Up a Data Folder" became "Keeping Your Data Safe" built on Export/Import; §6.1's folder row replaced by the storage-durability control the app actually shows there (verified against `app.js` `deviceMode`); the keyguard title-bar-height instruction now covers both ways of running it. **Flagged, not changed (a code issue, not a doc one):** the Settings → Conversation checkbox is literally labeled "Do not save conversations to **my data folder**", which is wrong on an iPad. The manual quotes it verbatim so the reader can find it and then explains the mismatch; the real fix is a platform-neutral label in `index.html`.  **⚠ ADDED BY 0.7.0 (Aug 8 2026), not yet written:** the **"how to say it"** field (Express Panel phrases, a person's name AND what you call them, and places) with its 🔊 — spelling a word the way it sounds is what works, and only the voice sees it, so the button, the person, the place and the transcript keep the real spelling; **empty Express Panel cells** now show as outlined buttons and tapping one sets up a button in that exact spot (not during a conversation); the **Express Panel is live while its Settings tab is open**; the **🔊 on each phrase** in that tab; and the **Sound Check** module in About Me. Also worth a line: the paid voice no longer drops to the device voice for a single sentence after a quiet stretch.  **Synced for 0.7.3 (Aug 15 2026) — four edits, identical in both manuals, verified by a full paragraph-and-table diff against the pre-edit backups (8 changed lines, nothing else moved).** (1) §7.3's note said a button *"cannot be added or changed while a conversation is under way"* — the August 7 rule, superseded: editing is now **Settings-only**, so tapping an empty cell on the conversation screen does nothing at any time. (2) The "add a button" step now says the empty cell is **in the panel beside the tab**, since that is the only place it works. (3) The "tap any button" step says **one tap is enough in Settings even if you use double tap** — the exact thing that stopped an SLP dead on 0.7.2, because only phrase buttons honor that setting and the feeling buttons beside them worked. (4) The **Activation: single tap or double tap** settings row says it applies while you are talking, not in Settings. **No change needed for the API-key jump fix** — neither manual documents the pre-start "Add an API key" button, so it restored intended behavior nothing described. **Synced again for 0.7.4 (Aug 15 2026), two more edits in §7.3, again identical in both and again verified by a full paragraph-and-table diff (4 changed lines, nothing else moved).** (1) The typing step now says the **cursor does not start in the text box** and that **Enter puts the keyboard away** — with the reason, since it is what makes the advice worth taking: the keyboard covers the Express Panel while it is up, and the panel is what Add before / Add after / Move are judged against. *(The Done step already said it dismisses the keyboard; Enter is the quicker route, not a replacement.)* (2) The Partner/Place note now adds that **Partner, Feeling and Place buttons each have one fixed color, shown as a small sample rather than a set of choices** — the thing an SLP pressed expecting a palette.  **⚠ PENDING FROM 0.7.5 (Aug 15 2026): the CONTROLS TOUR is not mentioned.** Settings → Practice now offers **"A tour of the buttons"**, which walks the Command Bar one control at a time, describes the right button when the wrong one is pressed, and **needs no API key**. The Practice section of each manual currently implies the whole tab requires a key — that gate is now per-scenario, so that is the part which is actually WRONG rather than merely missing. The tour is also the natural first thing to send a new user to, so §3 Getting Started is worth a pointer.  **Synced for 0.7.5 (Aug 15 2026) — six edits, identical in both, verified by a full paragraph-and-table diff (13 changed lines each, one paragraph added, nothing else moved).** §6.6 said Practice *“needs your API key”*, which the per-scenario gate made **wrong rather than merely incomplete** — it would have told a tester the one thing they can do on day one was unavailable. It now scopes the key to the five conversation scenarios, carries a paragraph on **A tour of the buttons**, points the "before your first real conversation" bullet at it, and §4 sends a new user to it first. **Also fixed, pre-existing and unrelated to the tour:** step 3 said to tap the X to leave Settings, but `startPractice` has been closing the dialog itself — measured in the running app on a conversation scenario, not only on the tour. **⚠ THE NEAR-MISS WORTH KEEPING: a `startswith` matcher hit TWO paragraphs.** “Tap the X at the right of the Settings title bar” opens both the Practice step and an **About Me** instruction, so the About Me paragraph was silently overwritten with Practice text. **The full diff is the only reason it was caught** — it was restored from the backup and the diff re-run. Exactly the failure this file already records from the Beta Test Plan pass, arriving through a different door: not "did I make my edits" but "did I make ONLY my edits". |
| ~~Conversant AAC User Manual.docx~~ | **retired 2026-08-01** | — | — | **Split into the two self-contained manuals above** (Ken, Aug 1 2026: *"humans MUCH prefer a single document rather than trying to manage two so self-contained is the way to go"*). Moved to `Other/Previous Doc Version/Conversant AAC User Manual (pre-split 2026-08-01).docx`. **Shared work applied to both halves before splitting:** the Settings restructure that shipped today — the new **Speech** tab (one table covering the Deepgram key, hearing, speaking voice, and the practice partner voice), "Speech & Input" renamed **Buttons & Keyboard** with the two voice rows moved out of its table, §§6.4–6.8 renumbered to 6.5–6.9 with every cross-reference chased, the tab count corrected to eleven, and the Settings **Close button replaced by the X in the title bar** in five passages. **STANDING RULE from here on: a change to shared behavior must be applied to BOTH manuals in the same pass — a sync that touches one and not the other is incomplete.** *Prior pass (for history):* | Caught up through v0.5.98 (from the stale `3c407de` baseline, ~20 releases). Fixed: turn-completion no longer gated (v0.5.82 reversal), About Me is now a Settings tab not a full-screen overlay with "Done" (v0.5.83), wind-down/closing described as two distinct steps (v0.5.93), layout names "Side/Bottom Keyboard N" → "Side/Bottom Layout N" (v0.5.85), stale "Section 6.6" self-reference fixed to 6.8. Added: Settings profiles row (General), Practice partner voice row + closed-set choice-chip new §4.6 (Speech/Conversation tabs), new §6.5 Practice Tab (v0.5.97/0.5.98), Keyguard Design tab content for Keyboard/Transcript separation (v0.5.84), API-key Test button + no-key pre-start prompt + AI-optional Note (v0.5.79/v0.5.94), listening chime + pulsing Listen button (v0.5.96/v0.5.98 once-per-conversation nuance), "Actually, before you go —" decline-closing note, Troubleshooting row for blue-italic uncleaned transcript, four new Glossary terms (Closing, Wind-down statement, Choice card, Practice Mode). All UI wording verified against index.html/app.js/engine.js/keyboard-layouts.js source. Word's TOC auto-field refreshes on next open in Word. |
| Conversant AAC Backup Compatibility.docx | ✓ current (new) | 2026-08-05 | `0ad748e` | **New Aug 5 2026**, from Ken's question “how compatible are the backup files written on an iPad compared to those written on a PC?”. Short reader-facing answer (~2 pages): the package format is identical in both directions, so what needs saying is which settings describe the *device* rather than the person. Covers what transfers, the four things to set up again after a cross-device import (both keys, voice, keyboard mode, transcription choice), why the layout looks close but not identical and why a keyguard never transfers, the Export/Import flow on each platform, and two gotchas — restore into an app at least as new, and choose a data folder first or saved conversations have nowhere to go. Generator `generate-backup-compatibility-doc.js`, regenerable. UI labels verified against `app/index.html`; behavior against `data-transfer.js` + `storage.js`. **Reader-facing, so no engine names** per the two-audiences rule. **Residual:** an earlier draft claimed the app reports what it restored — it does not (the summary is shown *before* importing, then the app reloads); corrected before generating, but if a post-import report is ever added, this document's closing paragraph should say so. |
| Conversant AAC Conversation Engine Design.docx | ✓ current | 2026-07-27 | `09a51ae` | Caught up to v0.5.93–v0.5.98. Added §4.1 "Closed-Set Turns" (offered_options, CHOICE/CHOICE_OTHER/CHOICE_ASK/CHOICE_REPAIR, Express Panel choice chips, steering persistence, renderer pure-family note) with a Figure 3a worked example; added §5.3 "Two-Step Closing, Pagination, and Declining a Closing" (WIND_DOWN vs CLOSING, no-LLM paging, CLOSING_DECLINE/reopenFromClosing); updated the mode table's PRE-CLOSING/CLOSING row; added offered_options to the §9.2 schema; added two §9.3 obligations bullets (offer-vs-mention, CLOSING definition); updated the Phasing and Traceability tables. **RESIDUAL:** no new figure for the CHOICE-card flow (no image toolchain on this box) — Figures 1–6 untouched. |
| Conversant AAC Conversation Engine Overview.docx | ✓ current | 2026-07-27 | `09a51ae` | Caught up to v0.5.93–v0.5.98. Added "When the Partner Offers the Options" subsection (CA framing: closed-set turns narrow conditional relevance to a named set) after Preference Organization; added a paragraph to Openings and Closings on the two-step wind-down/closing split and declining a partner-initiated closing; added a sixth illustrative exchange ("Answering from a menu the partner offered") matching the existing five, updated "Five short exchanges" → "Six"; added/updated rows in the CA-concept, response-type, and mode tables. **RESIDUAL:** no new figure (no image toolchain on this box). |
| Conversant AAC Configuration Model.docx | ✓ current | 2026-07-27 | `09a51ae` | **Silence period explained properly (Ken, Aug 9 2026): *“Ensure that the User Manuals (and maybe other documents) fully/properly explain what the Silence period parameter controls/represents.”*** Three defects, not one. The stated **default was 2 seconds** (it has been **0.5** since Aug 7 2026) and the **range 0.5–3.0** (a **0** option was added Aug 9), so both figures were wrong everywhere they appeared. And nothing said what the setting *represents*: the pause is a **checkpoint, not a stop** — recording continues, the partner’s turn ends when the **user answers**, and nothing is ever spoken unprompted, which is exactly what makes a short setting safe. That omission mattered because the natural reading of the old wording (“sends the partner’s speech to the AI”) is that a short setting risks cutting someone off, when the real cost is **discarded AI requests, i.e. money**. Settings-registry row rewritten: name **TRP silence threshold → Silence period**, default **1.5 s → 0.5 s**, range **0.5–3.0 → 0–3.0**, and the “why experiment” column re-pointed at the real trade-off, since its false-TRP framing predates the July 10 2026 reversal. Content is current; **At commit restamps at the next release push.** *Prior pass:* Synced for v0.5.93–v0.5.98: Build Status tab list corrected to ten tabs (added Practice, fixed ordering to match the shipped app); content-sets bullet notes the wind-down/closings split (v0.5.93); added two concrete registry-table rows — "Listening chime" (§4.1) and "Choice buttons in Express Panel" (§4.2) — each dated and explained as post-this-document additions, consistent with the doc's existing "Build Status" framing. Named settings profiles (v0.5.83) already covered from the prior sync. |
| Conversant AAC UI-Design.docx | ✓ current | 2026-07-27 | `09a51ae` | Synced for v0.5.93–v0.5.98: added a Build Status bullet + in-place §4/§12/§13 coverage of the new CHOICE-card family and Express Panel choice chips (framed as a deliberate, geometry-only exception to Rule 1 — cell count/position/size stay fixed, only cell content changes); added a "Static palettes" note (§13) covering openers/wind-downs/closings pagination via "New N" / re-pressing Wind down and the pinned "Actually, before you go —" decline-closing card; added the listening chime + pulsing Listen button (§3.3/§13/Rule 6), including the v0.5.98 once-per-conversation-with-auto-resume refinement. Not touched: pre-start screen sequencing and the API-key prompt (out of this doc's scope) and Practice Mode's Settings tab (Settings navigation isn't described here). Figures untouched (no image toolchain). |
| Conversant AAC Continuous Partner Capture.docx | ✓ current | 2026-08-11 | `69fead4` | **Stop/start mid-turn is a PAUSE (Aug 11 2026)** — the last pending item on this row, now written as a fifth bullet under *Robustness Additions Since This Design*, which is where a capture-state transition belongs. States the invariant (a partner turn ends at a **floor change**, never at a mic toggle) and what the old behavior actually lost — the buffer *and* the already-written transcript line, silently. **Residual:** none. *Prior passes:* **Silence period figures added (Ken, Aug 9 2026).** This is the document that defines the checkpoint model, and it described the behavior correctly but never stated the numbers. The settings row now carries the **0.5 s default** and the **0–3.0 s range**, with 0 firing on the recognizer’s own end-of-speech signal rather than a timer. The rename history sentence is left intact deliberately — it records the old labels rather than using them. *Prior pass:* **⚠ PENDING (Ken, Aug 5 2026) — stop/start mid-turn is a PAUSE, not a turn boundary.** Squarely this document's territory: it is a capture-state transition. Stopping and restarting Listen while the partner still holds the floor now KEEPS what they have said and appends the rest; the turn ends at a **floor change** (user picks a response, asks for a pardon, ends the conversation) and never at a mic toggle. Previously the restart cleared the buffer *and* overwrote the already-written transcript line with the shorter text — reproduced as LISTEN-TEST Test 3 on Aug 5 2026. The reasoning, the invariant and the `resumeListening()` / `resumePartnerCapture()` split are in CLAUDE.md under "Listen stop/start is a PAUSE". *Prior pass:* Synced for v0.5.93–v0.5.98: added a "Robustness Additions" bullet and a Settings Summary row for the start-of-listening chime (v0.5.96) — a capture-state transition, squarely this doc's territory — including the v0.5.98 refinement to once-per-conversation when auto-resume is on vs. every manual Start when it's off. The v0.5.93 wind-down/closing split is response-generation behavior, not capture, and was left out as out of scope. |
| Conversant AAC Keyboard Layout Options.docx | ✓ current | 2026-07-27 | `09a51ae` | Reviewed for v0.5.93–v0.5.98 — no relevant changes. Confirmed via `git log 9380c34..HEAD -- app/js/keyboard.js app/js/keyboard-layouts.js` (empty). Neither file changed in this window; the only nearby touch was a new Express Panel choice-chip color constant (express-items.js), unrelated to keyboard geometry/layouts. |
| Conversant AAC Worldview Implementation Plan.docx | ✓ current | 2026-07-27 | `09a51ae` | Re-verified July 27 2026 — no worldview/relationship-graph changes landed since the last review (checked git log for app/js/worldview.js, app/js/relationships.js, app/data/worldview-questions.json: no hits in this window). No edit needed. |
| Conversant AAC Sounds Like Me.docx | ✓ current (new) | 2026-08-05 | `0ad748e` | **New Aug 5 2026.** Build plan for the voice layer — how suggested responses come to sound like a specific person. Generated by `generate-sounds-like-me-doc.js` (regenerable). Restamped at the 0.6.7 release commit, which carries the place/partner prompt work it describes. **Audit content that will go stale as it is executed:** §3.2 asserts the registry holds only A1/A2/A4/A5/C1/C2 with no Tier B — Phase 1 changes that, and Table 1 must be re-derived from `app/data/worldview-questions.json` rather than trusted. §3.1 quotes the generation prompt and the four profile blocks; Phase 0 adds a fifth. **Retire or rewrite this document once Phases 0–2 land** — it is a plan, not a description of the product. |
| Conversant AAC Security Issues.docx | ✓ current | 2026-08-01 | `251cb03` | **SEC-1’s shared-origin half recorded as FIXED (Aug 1 2026).** Status-update heading extended to "July 16 & 27 and August 1, 2026" with a new entry: the app moved to its own origin `conversant.volksswitch.org`, closing the contingent cross-project risk; the finding’s own heading is flagged `[origin FIXED…]`. Notes that the *other* half of SEC-1 — plaintext key readable by anyone holding the device, and the proposed supporter-locked PIN Setup tier — is unchanged and still open, and points at Architecture Overview §18 for the operational dependencies the move introduced. *Prior pass:* Added a July 27 addendum to the Status Update section (heading now "July 16 & 27, 2026"): SEC-7's agreed near-term build scope shipped — start-of-listening chime (v0.5.96, once-per-conversation when auto-resume is on since v0.5.98) + a pulsing red Listen button — verified against app/js/chime.js and app/css/styles.css. Added a matching "Status: Partially built" note under the SEC-7 finding itself. User-manual passage, printed device card, and the deferred rear-camera-LED idea remain outstanding/not built. |
| Conversant AAC Strategic Assessment.docx | ✓ current (point-in-time) | 2026-07-18 | `409ef3c` | Claude's independent strategic review answering Ken's four questions (problem importance, approach effectiveness, gaps/priorities, alpha-beta plan). **Point-in-time snapshot — does NOT participate in "sync docs"**; supersede with a new assessment rather than editing this one. Generator: `generate-strategic-assessment-doc.js` (project root, docx-js). Its two flagged Architecture Overview errors (free-tier claim, hardware discrepancy) were fixed in the 2026-07-27 sync. |
| Known Issues web page (`volksswitch.org/index.php/conversant-aac-known-issues/`) | live on WordPress | 2026-08-09 | n/a | **THE PAGE IS THE ONLY COPY. Nothing in this repo mirrors it, and nothing should** — a second copy is exactly what got the .docx retired. The app links to it from Settings -> Troubleshooting (`KNOWN_ISSUES_URL` in `app.js`); if the address ever changes, that constant and Beta Test Plan section 9 both change. A starter version of the content was handed to Ken on Aug 9 2026 and is disposable. **WHAT BELONGS ON IT (Ken, Aug 9 2026):** *"I don't think it should include plans for future releases even if those plans would mitigate or nullify the issue. I think the best source of known issues will be those that come from the beta testers and are waiting to be fixed or tested internally."* **No roadmap** — the retired document proved why: its issues aged fine and its "Status: ... is planned" lines all went wrong. **The one status that is NOT a plan and is kept:** each item says **Being worked on** or **Not changing for now**, because "wait" and "work around it" are different reactions and the reader cannot choose without it. No dates, no version numbers, no feature names. **Two parts, because tester reports alone leave the page empty on day one** and the standing limitations are precisely what five testers each report in week one: *what the app can't do well yet* (slow-moving) and *reported by testers, being dealt with* (turns over). A third short part, *things that look like faults but aren't*, carries the red tint, the refusal to answer general-knowledge questions, the empty Express Panel cells and the reload action. **An item comes off when its fix has been released** — that discipline is what keeps the page honest. **It is public**, so plain wording, no tester names, no internal ticket language. **Live as of Aug 9 2026** (page id `23303`). **TO DO: give Claude a way to edit it directly** so a session's findings reach it in the same pass — the mechanism, the credential handling and the trap in the API's base URL are recorded in CLAUDE.md under Open Questions. |
| ~~Conversant AAC Known Issues.docx~~ | ⛔ RETIRED 2026-08-09 | 2026-08-09 | `6b5b241` | **Retired by Ken (Aug 9 2026); replaced by the web page `https://volksswitch.org/index.php/conversant-aac-known-issues/`, which the app links to from Settings → Troubleshooting.** Moved to `Other/Previous Doc Version/`. **Does NOT participate in "sync docs" — do not edit it, do not regenerate it, and do not bring it back.** **Why a page and not a document, stated once so it is not re-argued:** a list bundled with a release is out of date the day the next release ships, and this one proved it — by the time it was retired, item 7 (pronunciation) had shipped in 0.7.0, item 9 (settings not travelling) had been superseded by the named-profile workflow, and item 10 said the app runs on a Windows tablet only, which stopped being true at 0.6.0. **What rotted was not the issues but the "Status:" lines, which were roadmap promises** — that is the origin of Ken's rule that the page carries no plans for future releases. *Prior state:* Checked all 11 items against commits since 96b5ba4 (Practice Mode, closed-set/choice-chip turns, API-key Test button, listening chime, conversation-pane layout fixes). #1 (placeholder/echo collision), #4 (multi-party), #8 (single-instance guard) confirmed still accurate/open — no edit. Added a manual-workaround sentence to #9 ("Settings don't travel between devices yet") pointing to the Settings → General "Settings profiles" save/load feature as today's cross-device copy option. No new items added. |
| Conversant AAC Platform Measurements.docx | ✓ current | 2026-08-01 | `e905113` | **New Aug 1 2026**, split out of the retired iPad Architecture document: the probe evidence record, kept separate from the architecture because measurements age differently from design — when a later iPadOS lands, this is the document that says what to re-run and what to distrust. Carries the marking scheme, the measured six-run matrix (Table A), the three follow-on findings (bookmarking does not rescue persistence; Home Screen and Safari tab are separate storage silos; all Home Screen runs report an identical Safari user-agent), and the tested-device record. **Added in this pass:** the July 31 voice measurement — 68 voices with no Ava at any tier after installing Enhanced/Premium, the roster's composition, why it is a fixed curated list rather than an enumeration of what is installed, that every voice reports itself as the default (so that flag is useless on iPadOS), and the iPadOS 26 rename of Spoken Content to Accessibility → Read & Speak. Built by copying the iPad document and cutting, so the house style is inherited exactly. **Residual:** no figures; one internal reference to the retired document's sources list. |
| ~~Conversant AAC iPad Architecture.docx~~ | **retired 2026-08-01** | — | — | **Merged into the Architecture Overview §5 and §17, and its evidence into Platform Measurements.** Moved to `Other/Previous Doc Version/Conversant AAC iPad Architecture (merged into Architecture Overview 2026-08-01).docx`. Generator `generate-ipad-architecture-doc.js` is now stale and should not be re-run — it would recreate the document this merge deliberately removed. *Prior pass, for history:* | **Rewritten July 30 2026 against measured probe results** (iPad 10th gen, iPadOS 26, Safari 26.6; six runs + two follow-ups). Claims now carry `[MEASURED]` (30), `[SOURCE]` (13), `[RESEARCH]` (18, only where untested), `[PROBE]` (2, still open). Three original `[RESEARCH]` claims were **overturned by measurement** and corrected in place: continuous recognition works, `getVoices()` returns 68 voices, and the `100vh` trap did not reproduce in a tab. New §7 "Two Modes" carries Ken's decision to ship both a Safari-tab mode and a Home Screen mode; new front-matter results matrix (Table A) and §8.0 device record (Table 6). **Residual:** no figures (no image toolchain); cloud-transcription pricing still deliberately unquoted; §11 lists 6 open questions, notably keyboard suppression (the probe's detection method was unsound). *Superseded description below:* **Originally created July 29 2026** — build-ready specification for running Conversant on iPadOS (Ken's request). Generator: `generate-ipad-architecture-doc.js` (project root, docx-js). Claims are marked `[SOURCE]` (read from the codebase at v0.5.99), `[RESEARCH]` (public documentation, cited in §12), or `[PROBE]` (unresolved — answered by `ipad-probe.html` on real hardware). **Not yet actionable:** the probe has not been run, so §7.1 (tab vs Home Screen) and the §4 speech tier are open. **Companion instrument:** `ipad-probe.html` at the project root. Was hosted at a separate throwaway Pages repo July 29–30 2026, **run on an iPad 10th generation, and the repo deleted by Ken on July 30 once results were in** (verified gone). The probe source remains in this repo and can be re-hosted if a later iPadOS release needs re-testing. The six raw reports are the primary evidence record and live on Ken's Desktop under `iPadOS probe/`. **Residual:** no figures (no image toolchain on this box); cloud-transcription pricing deliberately not quoted, to be confirmed at build time. |
| Conversant AAC Practice Mode.docx | ✓ current | 2026-07-27 | `09a51ae` | **Newly added to tracking.** Generated ~v0.5.97; corrected the launch/exit description for the v0.5.98 move of Practice Mode from the pre-start screen into a Settings → Practice tab: step 1 now reads "Open Settings and select the Practice tab" (was "tap Practice a conversation" on the opening screen); the ending step now says End conversation / the Practice tab's "End practice" return to the normal conversation screen, not an opening screen — verified against app/js/app.js (startPractice/endPractice/handleEndConversation) and the Settings tab list in app/index.html. **⚠ Process note:** this edit was made without the mandatory pre-edit backup (an agent process slip) — recovery, if ever needed, relies on OneDrive version history rather than a local `Doc Backups/` snapshot. |
| Conversant AAC Beta Test Plan.docx | ✓ current | 2026-08-15 | `c36a49e` | **Revised Aug 9 2026 after Ken's review, and the duplication warning below got WORSE than recorded: the prose lives in THREE places, not two — Ken edits the .docx directly.** **⚠ AND THE FIRST REGENERATION OF THIS PASS DID DELETE SIX OF THEM.** Ken had mentioned two hand edits (the MacBook line in §4, the keyguard sentence in §5.1); I backed the .docx up and then checked only for those two, so they survived and **six others did not** — the trailing clause on the device bullet, the added sentence on the internet bullet, the User Manual line becoming a §4 bullet, §9's heading shortened to "Privacy", the reworded week-1 and week-4 rows, and two glossary rewordings. All eight are now restored, verified by a **full paragraph-by-paragraph diff** of his backup against the rebuilt file, which is the check that should have been run first. **THE LESSON, now written into the head of both the markdown and the generator: never grep a hand-edited document for the changes you happen to know about — diff the whole thing. A rebuilt document looks freshly correct whether or not it dropped anything.** One deliberate departure from his text: "do a degree" corrected to "to a degree". **This pass:** §4 lists MacBook and says to use Chrome or Edge on a computer; "AI key" is now **API key** throughout, matching the app and what the AI companies call it; **§5.1 no longer duplicates the manual** — it points at manual §3, §5.2, §6.3 and §6.4 and keeps only the tick-list plus the two beta-only items (the key we supply, the tester ID); Deepgram-for-listening is correctly narrowed to **the iPad Home Screen app only**; §5.2 now names the new **Notice** Express Panel button that speaks the disclosure; §9 says plainly that error reports ride the weekly send, what the manual route is if a tester switches it off, and that the whole arrangement ends with the beta; the glossary's "Data folder" became **"Your data"**, because an iPad has no folder the user can see. **§8 (the weekly note) was deliberately left UNCHANGED pending Ken's decision** — he questioned whether the three questions earn their cost; the recommendation (drop Q1, which the report already counts; keep Q2/Q3 as a standing box rather than a weekly prompt; no pop-up) is written up in Appendix B.3. **Two decisions closed by Ken the same day.** **The weekly note is GONE** — §8 deleted along with the *Weekly* row in §3, on his call; his objections were decisive (no interface for it, it asked the people least able to afford effort to recall several days back, and its first question was already arriving in the weekly report automatically). Because the numbers say what happened and never whether the tester minded, **§7 now says the *Report a problem* box takes anything else they want to say, good or bad** — the same route, no obligation, nothing to build. **Known Issues is now a web page** — `https://volksswitch.org/index.php/conversant-aac-known-issues/`, named in §9 and opened by a new button at the top of Settings → Troubleshooting, so it is reachable at the moment something looks wrong. ⚠ **`Conversant AAC Known Issues.docx` is now a second copy of that content and will drift** — its own DOC-SYNC row still calls it a kit document; decide whether it is retired or becomes the source the page is written from. Sections renumbered 9/10/11 → 8/9/10 and all cross-references checked. **Two gaps unchanged and still blocking hand-over:** the printed card, and the controls-tour Practice scenario. **Where Known Issues should live is now an explicit open decision** (Appendix B.7; recommendation is a web page plus a button on the Troubleshooting tab, since a file in the kit is stale the day a release ships). **Restamp at the next release push** — the app changes this pass (the Notice button, the version on the opening screen) are in local unpushed commits. *Prior pass:* **⚠ THE PROSE LIVES IN TWO PLACES AND NEITHER GENERATES THE OTHER — this row previously said "source of truth is the markdown, edit it then regenerate", which is FALSE and nearly cost the §9 change.** `generate-beta-test-plan-doc.js` does not read `BETA-TEST-PLAN.md`; it carries its own hand-authored copy of every paragraph. So regenerating after editing only the markdown does not pick the edit up — **it overwrites the .docx from the generator's older text, silently reverting it**, with no error and a document that looks freshly built. **An edit must be made in BOTH files, in the same pass.** Caught before running it on Aug 8 2026; if the two ever need reconciling, the .docx is the one a tester sees and the markdown is the one Ken reads, so diff them rather than assuming either is ahead. *(The markdown keeps one thing the .docx must never have: Appendix B.)* **Regenerated Aug 8 2026** and verified: Appendix B absent, footer still "For beta testers", no engine names, no mojibake, all 11 sections + 5.1–5.3 + Appendix A, 5 callout tables. **NARROWED Aug 8 2026 — the reporting half is now real, so most of the warning below no longer applies.** The **Report a problem** button and its note, the pre-start affordance, the **Settings → Troubleshooting** tab and **automatic reporting with its off switch** are all built, opened in a browser and verified rendering; the weekly send was proven end to end against the live Apps Script endpoint (rows landed in the Sheet, and a wrong secret was rejected without writing). So **§7 and §9 now describe things a tester can actually do.** §9 was also rewritten this pass to say plainly that reports are **not anonymous** — they carry the tester name Ken assigns — and to point at where the contents and the send history can be read. **⚠ STILL DO NOT HAND IT OVER, for two reasons, neither of them code:** the printed **"this device listens and speaks for me" card** has not been produced, and the **controls-tour Practice scenario** does not exist (§5.3 still asks testers to improvise it). **⚠ The .docx is now BEHIND the markdown — regenerate before use.** *Original entry:* **New Aug 7 2026**, when the call for beta testers went out. Tester-facing: what the beta is for, the setup checklist, a six-week arc, how to report a problem, the weekly note, and the privacy disclosure. Generator `generate-beta-test-plan-doc.js`, regenerable. Companion markdown is **[BETA-TEST-PLAN.md](BETA-TEST-PLAN.md)** (see the duplication warning at the head of this row — it is *not* the generator's input). It was written for the target state, so at the time it described four reporting features that did not yet exist — all since built and verified; see the narrowing note at the head of this row for what remains. The build list is CLAUDE.md → *Beta instrumentation and error reporting*. **Two deliberate departures from house style, both because of the reader:** the footer says **"For beta testers"**, not the usual "For internal use" — handing someone a document stamped internal-use is a contradiction; and **Appendix B of the markdown is DROPPED from the .docx** (it lists unbuilt features and open decisions for Ken and must never reach a tester) — the generator does this by omission, so **re-check it stays dropped after any regeneration**. Reader-facing, so no engine names per the two-audiences rule (verified: no "Chromium"/"WebKit"). **Open decisions still in Appendix B:** whether this is one plan or two self-contained ones like the manuals, how the weekly note comes back, and how per-tester API keys are distributed and revoked. **Restamp at the next release commit** — `0ad748e` is the last pushed commit, but the decisions this document rests on are in local commits that have not been pushed.  **✓ BOTH BLOCKERS CLOSED AND §5.3 REWORDED (Aug 15 2026) — nothing now stands between this document and a tester.** §5.2 promised testers "a small printed card for the back of the device" and there was no such card; [Conversant AAC Partner Card.docx](Documents/Conversant%20AAC%20Partner%20Card.docx) is now that card, and its own row below records what is on it and why. **§5.2 needs no rewording** — it already describes the card as something we send, which is now true. **What remains: the controls-tour Practice scenario, which §5.3 still asks testers to improvise.** That was a build, not a document edit, and it is now built: **Settings → Practice → "A tour of the buttons"** walks every Command Bar control, one at a time, and **needs no API key**. **§5.3 NEEDS REWORDING and is the last thing standing between this document and a tester** — it currently says to "spend one practice session pressing every one of them at least once", which was the improvisation the tour replaces; it should name the tour and say it works before a key is set up. **§5.3 now points at the tour by name**, says it needs no API key, and tells the tester to do it FIRST — which also answers the §5.1 ordering question without moving anything, since the tour is the one thing that works before setup. The second paragraph now separates the conversation scenarios, which DO need the key. **⚠ ALL THREE COPIES WERE EDITED IN THE SAME PASS** — this .docx (in place, never regenerated), `BETA-TEST-PLAN.md`, and `generate-beta-test-plan-doc.js`, whose paragraphs are a hand-authored second copy that would otherwise silently revert this on the next rebuild. Verified by a full paragraph-and-table diff against the backup: 4 changed lines, nothing else moved, 97 lines before and after. Appendix B's gap table is updated for both the tour and the card. |
| Conversant AAC Partner Card.docx | ✓ current (new) | 2026-08-15 | `c36a49e` | **The printed partner-disclosure card (SEC-7), written Aug 15 2026 — the physical half of partner awareness, and the last thing the Beta Test Plan promised that did not exist.** The screen faces the USER, so an on-screen indicator reaches the partner only on a glance; the signals that actually reach them are audible (the chime, the amber Notice button) and PRINTED. **Ken chose the shape:** 3.5 x 2.5in, eight to a Letter sheet, disclosure plus `conversant.volksswitch.org` so a partner who is uneasy has somewhere to go. **The headline is word-for-word the Express Panel Notice button**, so the spoken and printed disclosures say the same thing. **⚠ EVERY CLAIM WAS CHECKED AGAINST SHIPPED BEHAVIOR and none may drift:** the words DO go to an online service (there is no on-device recognizer on any configuration), the TEXT is kept on the device, the AUDIO is not, and nothing is spoken unless the user picks it. **It makes no legal claim** — a card discloses, it cannot obtain consent, and recording law is for counsel. **⚠ NOT YET SEEN ON PAPER:** there is no renderer on this machine, so the geometry was verified by measuring the file (8.5 x 11in Letter, 8.5in wide used of 8.5, 10.8in tall of 11, eight cards, rows EXACT at 2.5in) rather than by looking at it. **Row heights are EXACT, which CLIPS silently** — the text measures about 1.9in in a 2.5in card, so it is safe today, but anyone lengthening it must open the file. Regenerate: `node scripts/doc-generators/generate-partner-card-doc.js`. |
| Conversant AAC Partner Privacy.docx | ✓ current (new) | 2026-08-17 | `03a3bf2` | **New, Aug 17 2026 (Ken: *“we've given a fair amount of thought to the issue of partner privacy — pull together our thinking and our decisions”*).** Consolidates what was spread across the security backlog (SEC-7 recording indicator, SEC-2 error-log redaction, SEC-6 cloud-synced folders), the chime-once-per-conversation rule, the amber Notice button, the printed card, the removal of the diagnostic trace, the weekly-report “counts never words” line, the Beta Test Plan's §5.2 and §8, and the relationship-graph privacy levels. **Nothing here is a new decision** — it is a reader for the ones already taken, so they stop being re-argued. Generator `scripts/doc-generators/generate-partner-privacy-doc.js`. **Every factual claim was checked against shipped behavior**, and the three that must never soften are stated in the generator's header: the audio does leave the device on every configuration, the audio is not stored but the text is, and nothing is spoken unless the user taps it. **§8 is a general orientation to recording law, added at Ken's request the same day, and it is fenced: background, not advice, not checked by a lawyer, and it changes nothing about the product's own silence on the question (§7.3 still stands).** It covers the one-party/all-party split and why the disputed state list is a signal to ask rather than an answer; why notice does the legal work in the stricter states, since carrying on talking after being told is widely treated as agreeing; that the laws generally cover the CONTENTS of a conversation, so discarding the audio is a real reduction and not an exemption; that participant recording protects the user only for conversations they are IN, which is the strongest argument for partner voice recognition; the UK/EU household exemption, Canada, Australia; and two things people wrongly expect to apply — medical privacy law binds the clinic and not the patient, and “it's a communication aid, not a recorder” is an argument rather than a rule to build on. **The conclusion is that the design already sits on the safe side of every regime described, having been reached on ethical grounds** — so if a later edit ever finds itself relying on the law to excuse a design choice, that is the signal something has gone wrong. **The product still makes no legal claim** and must not acquire one. Restamp when partner voice recognition, the diagnostic rebuild, or the deferred camera light changes any of it. |
| Conversant AAC Keeping Costs Down.docx | ✓ current (new) | 2026-08-08 | `6b5b241` | **Silence period — 0-second option added (Ken, Aug 9 2026).** This document was already correct about the default and the eager-preparation trade (it explains it better than anything else), so the only gap was the new floor: the cost-table row now says that **0 seconds** does not count off a time at all but prepares as soon as the speech recognition reports the other person has stopped, making it the setting that asks the AI most often. Content is current; **At commit restamps at the next release push.** *Prior pass:* **New, August 8 2026.** Ken: *"your explanation of the changes you made to the prompt design is mostly over my head. It would be useful to have a user focused (i.e., user level language) document that describes what the app is doing to limit AI usage costs as much as possible."* Covers what the two charges are and that writing back costs ~5x; the measures that cost nothing because they never reach the AI (Express Panel, "In my own words", keyboard word suggestions, holding phrases, say-it-again, your own starters and goodbyes); the single combined request; the August 2026 change that stops re-sending the standing briefing every time; the reply-length ceiling; the paid-service savings (speech-only upload, repeated-phrase voice reuse); the four settings that move the bill, with their names, tabs and options read out of the RUNNING app rather than inferred from markup; how to read Settings → About; and an honest closing section on the two things deliberately NOT optimized (the whole conversation is sent each turn so the AI can follow the thread; the app prepares eagerly rather than sparingly, with the silence period named as the lever). **NO engineering vocabulary — the generator carries a jargon check**, and finding that the ONLY violation was where the document quoted the app is what caused the About-tab readout to be reworded from "% of prompt cached" to "% reused rather than re-sent". Generator `generate-cost-doc.js`, regenerable. **Footer says "For internal use" per the standing convention — if this is to be handed to testers that one line changes**, with the Beta Test Plan as the precedent for doing so. |
| Conversant AAC Measurement Plan.docx | ✓ current (new) | 2026-08-16 | `03a3bf2` | **New, August 16 2026.** Ken, prompted by the Strategic Assessment's three measurement gaps: *"generate a document describing the kinds of user and partner metrics that we are capable of generating with the current product and which metrics might be important and which we don't currently have associated measurements/reporting."* The capability had never been written down in one place — it was scattered across the decision record, the Beta Test Plan appendix, and the code. **Written against the SHIPPED code (`usage-summary.js`, `weekly-send.js`, `diagnostics.js`, `storage.js`), not the design notes, so where the two disagree the code wins and the disagreement is named.** Four layers: what is reported today; what is already written to every saved conversation and read by nothing (eight measures, no new capture — the largest cheap win); what needs the unbuilt event capture and is therefore permanently lost if it is not built before the first tester's first week; and the partner and reading-load gaps. **Ten findings from checking the code against the plans, three of which change what an existing number means:** the weekly report is CUMULATIVE, so the week-over-week retention curve — the agreed headline number — cannot be seen even though every timestamp is present; a "don't save" conversation contributes NOTHING, not even counts, contrary to the recorded decision; and the system-information block is sent nearly every week rather than only on change, because the spending counters live inside the settings, which makes the tester-facing sentence "sent only when something changes" inaccurate. Also names the one-timestamp fix (record when the cards appear) that converts the existing wait figure into the reading-load measure the Strategic Assessment says has never been taken, and the partner-return proxy already sitting in the partner stamps. Generator `generate-measurement-doc.js`, regenerable. **REVISED AND REGENERATED the same day, after Ken directed that the first five items of §10 be built.** The revision marks §4, §5 and §7 as built, records Ken's correction that reading load is read **plus select** (so the clock stops at the user's first action of any kind, which is what catches the turns where the reading was so heavy they gave up and typed instead), marks findings 1 and 3 as fixed, adds the defect found while verifying (Start conversation recorded an ended conversation with no turns), and rewrites §10 as what was built versus what is left — the partner questionnaire now being the largest remaining gap, and not code. Restamped at the 0.7.6 release push. |
| Conversant AAC Weekly Report Handling.docx | ✓ current (new) | 2026-08-16 | `03a3bf2` | **New, August 16 2026.** Ken, having had the five measurement items built: *"put together a plan for processing and reporting on the weekly reports."* The receiving end — everything after a report leaves the device, which nothing had described. **Written for a fifteen-minute weekly routine over a cohort of about five, and that size governs the whole document**: with five testers none of this is statistics, so the stated output of the routine is *one question per tester for the next check-in*, never a chart. Covers where a report lands (the Sheet's `weeks` tab, `reports` tab, and the raw payload column); the six-step Monday read, ordered from what matters most; how to read each number and what to do when it moves, in three tables (headline / early-warning / setup-and-health); the three things that should reach Ken **without** anyone looking, with **"a tester has gone quiet" named as the highest-value alert in the system because it is the only one where acting early still changes the outcome**; what to report back (a per-tester page before each check-in, a cohort view monthly, and explicitly **no averages, no significance tests, and no ranking of testers** — the one with the fewest conversations is the most informative person in the study); six ways the data will otherwise be misread; the setup checklist; and the three-part cadence. **The honest limitations are stated as properties, not defects:** a private conversation contributes nothing at all, early weeks will be practice-heavy, the two speech configurations are different products, iPad history can be erased by the device, and deliberation time overstates reading. Generator `generate-report-handling-doc.js`, regenerable. Restamped at the 0.7.6 release push. |

*(`Conversant AAC Conversation Engine Design.bak.docx` and `Conversant AAC UI-Design-Helix2.docx` are backups/stray copies and are intentionally not tracked.)*
