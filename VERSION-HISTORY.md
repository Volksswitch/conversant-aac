# Conversant AAC — Version History

Per-release engineering detail, one row per released version. Split out of
`CLAUDE.md` on August 7 2026, where the table had grown to 20% of a file that is
loaded into context in full every session. Nothing was edited in the move — the
rows are byte-identical to what was there.

**This is the engineering record.** For the user-facing account of a release, see
`CHANGELOG.md`, which is deliberately coarser: not every patch earns a user-facing
entry, and one entry there can cover several patches here.

**Read this when you need to know why a specific version did what it did** — a
regression that bisects to a release, a decision whose reasoning is quoted by a
later note, or the history behind a subsystem. It is reference, not policy.

**The policy lives in `CLAUDE.md` → Versioning**: which digit to bump, the release
ritual, and the `bump Conversant AAC` trigger phrase. A release adds its row here
as step 4 of that ritual (see `RELEASING.md`).

**Rows are in insertion order, not version order.** The table accreted over time
and rows were sometimes added out of sequence; the order is preserved because it
records when a row was written, which version-sorting would destroy. Use your
editor's find rather than expecting a sorted list.

**Released versions only** — rows are added at bump, never during a dev cycle.

| Version | Phase | Notes |
|---------|-------|-------|
| 0.1     | 1     | Core conversation loop proof of concept |
| 0.2     | 1     | Continuous partner capture; persistent "Please repeat" control; auto-resume-listening setting |
| 0.2.8   | 1     | Settings panel usability pass: Close label, draggable modal, live-apply settings, point-release version in About, scrolling panel, General tab split into Speech & Input + Conversation tabs |
| 0.2.9   | 1     | Fix v0.2.8 regression: dialog flex layout scoped to [open] so the panel no longer shows at startup and Close works; touch-action:none on the drag handle for full-distance touch drags |
| 0.2.10  | 1     | Auto-update on launch/Start: SW revalidates (no-cache) to beat the GitHub Pages HTTP cache; controllerchange auto-reload rolls in a redeployed worker; update check on Start |
| 0.2.11  | 1     | App keyboard: 25%-taller keys, numbers/specials moved to a separate page, one-shot shift with double-tap caps lock; Settings panel fixed to a constant size per tab |
| 0.2.12  | 1     | App keyboard: dropped QWERTY for an Alphabetical layout (only layout for now); context-based docking — side dock for About Me/Settings, bottom dock for conversation |
| 0.2.13  | 1     | App keyboard: 20 selectable layouts (data-driven) + Settings (side/bottom layout selects, left/right slider); side dock fills its column, About Me fills width, conversation field stays above the keyboard, both docks window-aware |
| 0.2.14  | 1     | Settings panel uses the app keyboard (side-docked) instead of the Windows keyboard; keyboard reparents into the modal dialog's top layer so it stays interactive over the modal |
| 0.2.15  | 1     | Auto-nudge the Settings panel clear of the side keyboard (opposite side); layout-independent Cut/Copy/Paste toolbar above the keyboard |
| 0.2.16  | 1     | Removed the Settings Save button — every control applies AND persists immediately (live test bench, esp. for side-dock keyboard layouts); Close just dismisses |
| 0.2.17  | 1     | Settings keyboard-layout live preview: the keyboard stays shown on the Speech & Input tab (no focused field) and re-renders as you change layout/side, so layouts can be tried without it vanishing |
| 0.2.18  | 1     | App keyboard Hide button (toolbar) — dismisses the keyboard while leaving the Settings panel open; blurs the field so a later tap reopens it |
| 0.2.19  | 1     | Tapping a keyboard-layout setting reshows the keyboard if hidden — pointerdown trigger covers re-tapping an already-focused select (no focus event) |
| 0.2.20  | 1     | Fix Settings nudge with keyboard on left (CSS-driven off body classes; width-capped so it can't go off-screen; no stale nudge after a bottom-keyboard preview); About Me content is a readable centred column that clears the keyboard on either side |
| 0.2.21  | 1     | Settings+keyboard fixes: no API-field autofocus (no keyboard race on tab switch); bottom keyboard pushes the panel up; a dragged panel re-snaps clear when the keyboard dock/side changes |
| 0.2.22  | 1     | About Me questions fill the available width (removed the 40rem cap) — full window with no keyboard, and all the non-keyboard space beside a side keyboard |
| 0.2.23  | 1     | Gap between the About Me scrollbar and a right-side keyboard (scrollbar no longer hugs the keys); wide (22px) high-contrast scrollbars for limited-motor-control users |
| 0.2.24  | 1     | Fix: tapping keyboard Hide no longer closes About Me — swallow the ghost click that landed on the Done button after the keyboard's space was freed and Done slid under the pointer |
| 0.2.25  | 1     | Fix: a `worldview.json` copied into the data folder from another PC is now respected — the file in the connected folder is the source of truth (no cache-vs-disk timestamp contest); the cache is promoted only when no file exists on disk yet |
| 0.2.26  | 1     | About Me prompts for a data folder when none is assigned — a banner with a "Choose data folder" button (fresh user gesture for the FSA picker) so answers persist to the portable file instead of browser-only cache |
| 0.2.27  | 1     | Relationship graph: people/relationships moved to their own `relationships.json` model (nodes + edges) with a People editor in About Me, LLM injection (private people withheld), and a one-time migration of the former worldview "People" module (A3) into the graph |
| 0.2.28  | 1     | Removed the in-app A3→graph migration (single user, little data — re-enter via the People editor); old A3 answers stay orphaned/unused in worldview.json |
| 0.2.29  | 1     | People editor relationship field is now a standard grouped list + "Other…" (free text) instead of free text; single relationship per person for now |
| 0.2.30  | 1     | Destructive actions (Restart, Remove person) now use a distinct red "danger" confirmation modal (`confirmDanger`) instead of native `confirm()`, so they don't look like routine permission popups |
| 0.2.31  | 1     | Removed the "Suggested next" section from the About Me home (those questions are already on the first topic sub-page); gaps subsystem retained in the model |
| 0.2.32  | 1     | Start Listening gating: never auto-listens at startup; auto-resume fires only after the user manually starts listening at least once this session (`manualListenArmed`), and a manual Stop re-arms the requirement |
| 0.3.0   | 1     | Conversation Engine (Phase-1 full mode set: sequence stack, five modes, four-slot move palette, REPAIR-OF-SELF, persistent overrides, §6 filler ladder, §9 combined classify+generate contract) behind a degenerate diagnostic UI; no transcript-confirmation gate (generation fires on the silence period) |
| 0.3.4   | 1     | Test-suite Pass-1 fixes: filler ladder honors the user's Settings (first placeholder lands `initialDelay`s after the partner stops; re-fills every `subsequentDelay`s — was hardcoded 0.8s/1.7s); STT discards its own TTS echo by content match (mic stays on) so placeholder speech no longer renews the partner's turn, delays, or abandons the generated responses |
| 0.3.5   | 1     | Removed response-like acknowledgment tokens ("Okay.", "Right.", "I see.") from the rung-1 filler set — they read as the user's actual reply (e.g. to a greeting), not a stall, and are fragile for echo filtering |
| 0.5.36  | 1     | **On-screen keyboard now serves the Express Panel editor too (Ken, June 28 2026).** Same one-line scope fix as v0.5.34, applied to the other Settings editor: added `#expressEditor input` to `IN_SCOPE` (`keyboard.js`) so tapping a phrase/feeling text box on the Express Panel tab summons the app keyboard when "Keyboard for typing" = On-screen. The partner "Choose a person" control is a `<select>`, not an `<input>`, so it's correctly excluded. Verified in preview: a Express-editor input matches scope, opens the keyboard (hosted in the dialog), and gets `inputmode=none`; no console errors. |
| 0.5.37  | 1     | **Keyboard control row + header buttons sized to the Express Panel controls — Spatial Stability (Ken, June 28 2026).** The keyboard's top control strip (`.kbd-toolbar`) now lays out as FOUR equal ¼-width cells — **Cut / Copy / Paste / Hide** — each matching a `.persist-btn` override (Say again / Hold on / Pardon? / Wind down) hole-for-hole, so one keyguard overlays both. Was: Cut/Copy/Paste at flex-1 sharing the row with a flex-3 prediction reservation + a right-pushed auto-width Hide, so the three tools were ~⅙-width and unaligned. Changes: `.kbd-tool-hide` dropped `margin-left:auto`/`flex:0 0 auto`/extra padding → equal 4th cell; during the "In my own words" modal it's `visibility:hidden` (was `display:none`) so its cell stays RESERVED and Cut/Copy/Paste keep ¼ width + alignment. **Word prediction → interim overlay** (Ken explicitly deferred a dedicated prediction layout — "think further"): `.kbd-preds` is now `position:absolute; inset:0` over the control row, shown only while there's a prefix (`updatePredictions` toggles `.kbd-preds-active`) and `display:none` otherwise so taps fall through to the tools — it neither steals the tool cells' width nor adds a row that would offset the keys from the `#epGrid`. **Header buttons** `#aboutMeBtn`/`#settingsBtn` given `min-height:var(--btn-min-dim)` + `min-width:6rem` + persist-btn padding/font → same size as an Express Panel button. Verified in preview both docks: toolbar ↔ `#epControls` pixel-identical (bottom 1265×40, cells 312px at x7/325/643/961; side 364×40, cells ~87px) hole-for-hole; predictions overlay the same real-estate and clear back to the tools; header buttons 88×40 = an express button; no console errors. **Open (Ken):** whether Hide is still needed (each panel has its own close path) — if it goes, the 4th cell needs a new occupant to keep the 4-hole alignment; and the permanent word-prediction layout. *(Superseded by 0.5.38 — Hide removed, 3 buttons.)* |
| 0.5.38  | 1     | **Keyboard control row → THREE buttons in the left-three Express Panel cells; Hide removed (Ken, June 28 2026).** Ken: we don't need a 4th button as long as the three present (Cut / Copy / Paste) occupy the same real-estate as the **left-three** override buttons. `.kbd-toolbar` is now a `grid-template-columns: repeat(4, 1fr)` (still matching the four `#epControls` ¼-cells); the three tools auto-fill the **left three cells** (= Say again / Hold on / Pardon? holes) and the **4th cell stays empty**. The **Hide button is dropped** (every context the keyboard serves has its own dismissal — the "In my own words" modal: Speak/Reframe/Cancel; Settings: Close; About Me: Done), resolving the v0.5.36/v0.5.37 "is Hide still needed?" question; the `.kbd-tool-hide` / `kbd-no-hide` CSS was removed (the `dismiss()`/`suppressNextClick()` helpers + `handleTool('hide')` branch are left intact and unreferenced, so Hide is trivially re-addable). Word prediction stays the interim overlay (still TBD). Verified in preview both docks: 3 tools occupy the left-three cells hole-for-hole (bottom x7/325/643 ×312px, 4th cell at x961 empty; side x909/1001/1094 ≈87px) with `repeat(4,1fr)`; bottom-dock screenshot shows 3 icon buttons + empty 4th cell + no Hide; no console errors. |
| 0.5.39  | 1     | **Word-prediction DISPLAY dropped for now; infrastructure kept (Ken, June 28 2026).** Ken: drop the on-keyboard prediction buttons for now — several button-size questions need to be worked out first that will influence how word prediction should be brought into the UI. CSS-only change: `.kbd-preds` is now permanently `display:none` (removed the `.kbd-preds-active`/`:not(...)` show/hide toggle), so the prediction overlay never appears even while typing. **Everything else is untouched** — `prediction.js`, `updatePredictions` (still computes predictions + toggles `.kbd-preds-active`), `applyPrediction`, and word-learning on space/enter all keep running, so personalized frequency data still accrues and re-enabling is a small CSS restore (documented inline in both the CSS rule and the `keyboard.js` build comment). Verified in preview: typing "Th" still computes 3 predictions and sets `kbd-preds-active`, but `getComputedStyle(.kbd-preds).display === 'none'` and the toolbar shows only Cut/Copy/Paste (4th cell empty); no console errors. |
| 0.5.53  | 1     | **On-screen keyboard stays up the whole time About Me is open (Ken, June 30 2026).** Opening the About Me tab in Settings now displays the keyboard immediately and keeps it shown until Done — previously it only appeared on field focus, and `renderHome()` deliberately hid it on the topic list (which has no field). New `showDockKeyboard()` helper in `worldview-ui.js` (`keyboard.previewShow(loadKeyboardDock())`, no-op in physical mode) is called from `renderHome()` in place of the old `keyboard.hideKeyboard()`, so the keyboard persists across home↔module navigation; focusing a card field upgrades the preview to a typing keyboard via the existing `focusin` path; `close()` still takes it down and reopens Settings. The dock is reserved on `#worldviewScreen` at all times (v0.5.25), so the keyboard fills the blank dock band rather than covering the topic list. Also: `keyboard.previewShow()` now clears any leftover inline-prediction ghost when entering preview (so a ghost from a card you were typing in doesn't linger over the dock). Verified in preview (on-screen / bottom dock): About Me opens with the keyboard shown, it stays up drilling into a topic (field focused, typing keyboard) and back to "All topics", and Done hides it + reopens Settings; no console errors. |
| 0.5.95  | 1     | **Conversation-pane layout fixes (Ken, July 17 2026).** Three CSS fixes. (1) Selecting a response card flashed a scrollbar across the transcript + Command Bar + response palette, shrinking their width and jerking the buttons/cards left — caused by `#mainArea { overflow: auto }` transiently overflowing when a turn was appended; changed to `overflow: clip` so only the transcript (`.transcript-log`) scrolls. (2) Wide 22px high-contrast scrollbar on the transcript (the only conversation-screen scroll surface) for limited motor control, matching the About Me / Settings treatment. (3) User-statement bubbles hug their text — `.turn` gained `width: fit-content` (was a block at `max-width: 88%`, so a short user turn showed a wide blue band with the text hugging the right). Verified in preview: 40 overflowing turns → `#mainArea` scrollbar 0px, Command Bar/response area stay full width, transcript scrollbar 22px; short user bubble on the right, partner on the left. |
| 0.5.96  | 1     | **Recording indicator + pre-start screen sequencing (Ken, July 18 2026).** (1) **Recording indicator (SEC-7, partner awareness):** new `chime.js` plays a synthesized start-of-listening tone (Web Audio, offline-safe) fired once on the capture-start transition (the single choke point `ui.setListenButtonState` false→true edge, which never re-fires on the recognizer's mid-session restarts); Settings → Conversation toggle (`loadListenChime`, default on); the Listen button turns red and the whole button slowly brightens/dims (`filter: brightness(0.9→1.3)`, 2s) — no screen-wide frame. The geometry rationale: the tablet screen faces the user, so the audible chime is the reliable partner-reachable signal. (2) **Pre-start screens now run as a strict, non-overlapping sequence** Start → What's-new (Close) → API-key notice → conversation (`handleStart`→`afterWhatsNew`→`finishStart`); the API-key notice (`#apiKeyPrompt`) was previously shown independently by key-presence and could pile on top of the What's-new panel. Reworded to "To receive response suggestions from the AI, please provide an API key." with **Close** (proceed) / **Add API key to Settings** buttons. (3) `whatsNew.renderPanel` guards against an empty card (bails to the conversation instead of a contentless card that grays out the command bar). Verified all four start paths + the indicator in preview. |
| 0.5.97  | 1     | **Minimal Practice Mode (Ken, July 18 2026).** The "Practice Communication Partner" from Architecture Overview §8 — the AI plays the partner so the user can rehearse a conversation with no mic. Reuses the entire Phase-1 pipeline (`handleSilencePeriod` is mic-free), so it's a slot-in, not a new layer (patch, not minor). New `practice-scenarios.js` (5 bundled scenarios, one per §8 category); `llm.generatePartnerUtterance` authors the partner's line with roles inverted (partner=assistant); `tts.speak(text,{voiceURI})` per-utterance voice override + Settings "Practice partner voice" (Auto = a voice ≠ the user's). `app.js`: `practiceMode`/`practiceScenario`; pre-start "Practice a conversation" launcher + scenario picker (needs an API key — partner + options are both AI); `startPractice`/`advancePracticePartner`/`togglePracticeCue`/`practiceResumeOrIdle`; forks in `toggleListening` (Start Listening CUES the partner, honoring the real `manualListenArmed`+auto-resume gate), `resumeOrIdle`, `handleResponseSelected` (skip STT), `handleEndConversation` (exit→start screen), `partnerStamp` (flags turns `Practice: <scenario>`). **Response options grounded in the scenario** via `buildSituationBlock` (the response call otherwise didn't know the setting — suggested root beer at a coffee shop). Saved-and-flagged. Verified in preview through picker→select→cue→graceful keyless failure→End; successful generation needs a real key on-device. Doc: `Conversant AAC Practice Mode.docx`. |
| 0.5.98  | 1     | **Closed-set turns, choice chips, Practice Mode into Settings, and a decline-the-closing card (Ken, July 27 2026).** (1) **Practice Mode** moved off the pre-start screen onto its own **Settings → Practice** tab (scenario list + an "End practice" exit); ending practice now returns to the standard conversation screen instead of the Start screen. (2) **Closed-set (alternative-question) turns** — classification gains `offered_options`; when the partner offers a menu the four structural slots are abandoned and the palette becomes one `CHOICE` card per alternative, with every spare cell filled from a ranked filler set (`CHOICE_OTHER` ×2 / `CHOICE_ASK` / `CHOICE_REPAIR`) so a two-way question doesn't leave dead cells. The trigger covers both alternative questions AND the far commoner list-in-a-statement form ("we've got muffins, croissants…"), guarded against narrative lists; the partner's own alternatives always outrank fillers. (3) **Express Panel choice chips** — the offered alternatives appear as green chips taking only as many leading cells as there are choices (dynamic, no standing reservation); tapping one re-generates the four structural responses focused on that alternative via the Reframe seam. (4) **Steering now survives "New N"** within a turn (chip focus and typed Reframe text both), fixing a silent reset. (5) **Live partner speech stays in view** while they talk — `setTranscriptState`/`setNowPlaying` changed the pane's height after the scroll. (6) **`CLOSING` defined in the prompt** (empty discourse markers, warrants, arrangements, appreciations — with a topic-vs-conversation guard) plus a pinned **"Actually, before you go —"** card and `engine.reopenFromClosing()`. (7) **The listening chime is once per conversation** when auto-resume is on. 155 unit tests; live-model verified for the offered-set classification. |
| 0.6.0   | 1     | **iPad becomes a supported platform, and speech gets paid backends (Ken, Aug 1 2026).** The minor digit, because the cycle added genuinely new subsystems rather than features: two paid speech backends reached **browser-direct with no server** (Deepgram streaming transcription, gated by `vad.js` so silence is not billed; Deepgram Aura voice, with a repeated-phrase cache), a **second storage backend** (OPFS behind the same file-handle interfaces, so `storage.js` is unchanged above the handle), and the **`platform.js` layer** that lets one codebase serve both browser engines with every iOS-only value resolving to the previous Chromium value on desktop. Settings reorganized: a new **Speech** tab holding the Deepgram key + the two independent provider choices + both voice pickers, "Speech & Input" renamed **Buttons & Keyboard**, and the footer **Close** button replaced by an **X** in the title bar (~55px back to every tab). Storage wording made platform-neutral. Announced as one iPad summary rather than fifteen itemized fixes (Ken's call). Documents restructured the same day: Product Overview unified, User Manual split into two self-contained manuals, and the iPad Architecture document merged into the Architecture Overview with its probe evidence split into Platform Measurements. |
| 0.6.2   | 1     | **Spoken help in Settings, Express-derived control sizing, and a screen edge margin for keyguards (Ken, August 2 2026).** (1) **Spoken help** — a `?` in the Settings title bar arms "tap a control to hear what it does" (`help-mode.js`; 85 phrases authored for LISTENING in a root `settings-help.json` embedded at build time, like `CHANGELOG.md`). The affordance **Rule 14** was missing: a tooltip needs hover, which direct select cannot produce, and audio costs no panel space. An armed tap is intercepted in the capture phase so it can never change the control; any tap barges out, and tapping the SAME control also passes through — except on a slider, where the tap coordinate IS the value. One-shot, so the user is never trapped in help mode. Voice = the practice partner's, on both TTS backends: intelligibility over distinctness (Ken — on an iPad "any voice that is not theirs" can be a joke voice). Coverage, orphans, drift and phrase length are all enforced by `tests/settings-help.test.mjs`. (2) **Settings controls sized off the Express Panel button** (**Rule 15**) — 70% of a cell, floored at `--btn-min-dim`: "Copy" had been 35 px wide and every `<select>` 33 px tall, matched by none of the button rules. Extended to the title-bar `?`/X, reversing their exemption, because the likely mis-hit became Help → Close, which dismisses the panel. (3) **"Screen edge margin"** (**Rule 16**) — insets the whole app INCLUDING the dock, for a keyguard inside a tight case opening; the solver budgets against the reduced viewport so regions shrink into it rather than overflowing. (4) **Two false-alarm fixes**: the "storage warm-up did not finish" error logged on EVERY start (`Promise.race` does not cancel the loser and the timer was never cleared), and log text is now scrubbed to plain ASCII at the `logError` choke point (punctuation only — an error can carry a person's name). (5) The **"Voices offered to the app"** readout was removed from About, its diagnostic question having been settled. New `tests/styles.test.mjs` tripwire after a stray `*/` silently dropped a whole CSS rule and collapsed Rule 15 to its floor with no error anywhere. |
| 0.7.1   | 1     | **The beta instrumentation goes live, and the Known Issues document is retired in favor of a web page (Ken, August 9-10 2026).** (1) **The weekly report is no longer inert** — the Apps Script endpoint is wired, the receiving script reports which version is deployed, and the tester enters an assigned ID rather than an email, with the note separated from the report so a free-text problem description is not conflated with the automatic summary. A report is sent **on the first launch after the address changes** rather than waiting a week, so both ends can see reporting working before anyone relies on it; the Beta Test Plan now says plainly that reports carry an assigned name, and the shared secret stays out of the Sheet. Also fixed: the platform diagnostics were omitting whether the device's speech recognition is usable at all — the first thing worth knowing when the app cannot hear anyone. **⚠ Default-on is a BETA arrangement**: `weeklySendEnabled` must default false before public release (Ken, August 9 2026), or the app ships breaking a promise the Beta Test Plan makes in writing. (2) **Known Issues moves to `volksswitch.org` and the .docx is retired** — a document goes stale between releases and a page does not, and the page is now **the only copy**, reachable from a button in Settings → Troubleshooting. A to-do records the mechanism for editing it directly (WordPress REST, page id `23303`; **the base URL is `/index.php/wp-json/`, not `/wp-json/`, which 404s and reads as "the API is disabled"**), and the standing rule that it is **edited in place, never generated from a repo source** — Ken edits the page too, so a generator would silently overwrite him. (3) **A "This device listens and speaks for me" Express item in a new amber Notice color** — the SEC-7 partner-disclosure answer as one tap, seeded only on a panel the user has not edited. (4) **The version is back under Start**, which matters precisely when the app is too stuck to open Settings. (5) **Fixes:** the on-screen keyboard ignored the screen edge margin, so its keys stopped lining up with the Express Panel behind them and a keyguard cut for one no longer fitted the other; a tapped Express cell now stays marked while it is being edited; and "How I talk with them" in the People editor is a disclosure button with an arrow rather than a bare heading, which had made the per-partner starters and goodbyes look as though they did not exist. (6) A **0-second silence period** is now offered: ask the moment the other person stops, the quickest setting available, at the cost of more checkpoints per turn. |
| 0.7.5   | 1     | **The controls tour — the last thing the Beta Test Plan promised that did not exist (Ken, August 7 and 15 2026).** §5.3 asked testers to press every Command Bar button once and left them to improvise it; Ken: *"I often forget what specific buttons mean or when they should be used."* It is the predictable cost of **Rule 12** (icon-only controls) on the one surface where **Rule 17**'s spoken help was deliberately excluded, so the buttons are unlabelled by design and there was nowhere to ask. **A DIFFERENT KIND OF SCENARIO, not another scenario:** it carries `steps` where the five bundled ones carry a `partnerPersona`, because it instructs and then checks the right press, where they only converse. **⚠ NO AI, AND THAT IS THE PROPERTY TO PROTECT** — every step names a control that works from static phrases or local state (openers, wind-downs, Hold on, Ask them to repeat; "New N" pages a static palette rather than regenerating), so it runs with no key, for the person who needs it most: first day, key not working, facing a screen of unlabelled icons. The Practice tab's key gate is therefore **per-scenario** now. A test asserts no step targets an AI-dependent control, because that breaks silently — the tour would simply stall on a dead button. **A wrong press describes the right button** (Ken, Aug 15) — icon, position, and color only where one distinguishes anything (Listen turning red, the dark compose button); the rest of the row is uniform, so "the red one" would send someone hunting. Written from `icons.js` and MEASURED against the rendered bar, and guarded by a test requiring each description to name the icon, not just a position — a position is useless to whoever miscounted, which is why they mis-pressed. Only fires on a pressable target, with a 2.5s guard so repeat taps do not restart the sentence. **⚠ THE BUG THIS HIT: clearing the tour in `terminateConversation` looked right and was wrong.** That function is not "the conversation ended" but "wipe the conversation state", and **Start conversation calls it too** — which is step two. The tour died on its own second instruction, blank line, no error. It is cleared where the SESSION ends and nowhere else. Related: the observer runs on the **capture** phase, which looks wrong for an observer and is not — the last step is End conversation, whose handler clears the tour, so on bubble the final step would never be credited; that step is flagged `endsSession` and its closing message handed to the teardown rather than racing it. The instruction is a coach line inside the transcript, bottom-pinned like the live turn and like it **not** part of history — the app talking about itself is not something either party said, and in history it would reach the saved file and the model. Measured: it moves neither palette, Command Bar nor dock. Also this cycle: **the printed partner card** (`Documents/Conversant AAC Partner Card.docx`, 3.5 x 2.5in, eight to a Letter sheet), closing SEC-7's physical half — **the page size had to be stated because docx defaults to A4**, on which the right-hand column ran off the page, found by measuring the file. And **three generators missed in the August move** (beta-test-plan, ipad-architecture, strategic-assessment) still wrote to a bare filename, which after `doc-paths` chdirs lands BESIDE THE SCRIPT — running any would have left the real document untouched and a stray copy in `scripts/`, silently. |
| 0.7.6   | 1     | **Measuring what the reports could not show (Ken, August 16 2026).** He asked for a document describing which user and partner metrics the product can generate, which matter, and which have no measurement behind them; none existed, and writing it against the SHIPPED code rather than the design notes turned up three defects. **(1) The weekly report was CUMULATIVE**, so a tester whose use halved in week three still showed a healthy overall average — the retention curve, which is the agreed headline number, was the one thing the report could not show. It is now bucketed on the device, which is the only place it can be done: medians from separate reports cannot be subtracted from one another. Weeks run from the tester's own first day and an EMPTY WEEK IS STILL REPORTED, because a missing week is what quietly stopping looks like and closing the gap up would hide it. **(2) A tester-facing sentence claimed the system information was sent only when it changes**; the spending counters live among the settings and rise after every conversation, so something always had. The behavior is useful (spending figures arrive weekly) so the SENTENCE was corrected — with no raw payload view in the app, that description IS the disclosure, and a disclosure nobody re-reads must not drift into being untrue. **(3) Eight measures were already being written into every saved conversation and read by nothing** — where each turn came from, whether the same partner came back, place and feeling use, silent voice fallbacks, every figure split by which recognizer heard the partner, turn lengths, what was on offer, and personalization depth. Aggregation only: no new capture, no new consent, nothing new leaving the device. **THE EVENT CAPTURE (`metrics.js`) IS THE PART THAT COULD NOT BE DEFERRED** — about twenty moments (opened but never talked, suggestions shown then abandoned, regenerates, which Command Bar button, placeholders spoken, generation time, checkpoints per turn, generations billed and discarded, rate limits, recognizer delivery gaps) held as a per-day tally, with a short recent ring attached to a problem report as reproduction context. An event not recorded when it happens is gone, so this had to be in testers' hands before the first tester's first week. **The privacy rule is structural, not a call-site convention:** `redactFields` passes numbers and booleans, allows a string only under a few names that could not hold speech and truncates it even then, so a mistaken `event('x', { partner: whatTheySaid })` records NOTHING — guarded by a test, and wired to the same switch as the weekly send so turning reporting off also stops the counting. **READING LOAD IS NOW MEASURED, and Ken's correction improved the measure rather than merely fixing it.** The wait figure runs from the partner stopping and holds the recognizer, the AI, the reading and the choosing at once, so a slow model and a heavy reading task are indistinguishable in it and call for opposite fixes; recording when the cards appear splits it in two. Ken: it is read **plus select**, and the clock stops at the user's FIRST action of any kind — a card, an Express button, "In my own words", a Command Bar button, or asking for different options. **The wider rule catches what the narrow one would have missed: the turns where reading was so heavy the user gave up and typed instead**, so a card-tap-only clock would have reported reading as easier than it is in precisely the cases that matter. Each span is tagged with which action ended it, and is one-shot per palette. **⚠ THE DEFECT FOUND WHILE VERIFYING, and the reason to exercise instrumentation against the running app rather than reasoning about call sites: pressing Start conversation recorded an ended conversation with no turns, every time**, because Start wipes the previous conversation before opening a new one. Left alone it would have inflated the conversation count with conversations that never happened — quietly overstating adoption in the one number most likely to be quoted. It is now raised inside `metrics.conversationBoundary`, the only place that knows whether one had begun; an event about something ENDING has to be raised where that is known, which is not the call site. Also: the personalization block shows even for a tester with NO conversations, because someone who filled in their profile and then held none is the tester about to quit and would otherwise read as identical to someone who never touched the device. The Apps Script receiver gains a **`weeks` tab, UPSERTED not appended** — every report carries the whole history rebucketed, so appending would rewrite week 1 every week; keyed on install rather than tester name, so correcting a name later cannot silently start a second curve. Verified against a fake Sheet, because the app posts no-cors and a throw inside `doPost` is invisible at both ends. Two new documents: the **Measurement Plan** (inventory) and **Weekly Report Handling** (the receiving end — a fifteen-minute weekly read whose stated output is one question per tester, never a chart; no averages, no significance tests, no ranking, because with five testers the one with the fewest conversations is the most informative person in the study). 19 new tests, 458 total. **Still not measurable, and now the largest gap: the partner's own experience**, which no software can infer and which needs the questionnaire settled before the partner cards are printed. |
| 0.7.7   | 1     | **The AI's own short label for each response is now shown on the card (Ken, August 19 2026) — deliberately unconditional and temporary, so the team can judge it in real use before deciding whether it stays and what controls it.** The model has returned a glanceable few-word label naming what each response DOES alongside every response since the palette was built, and the card has never displayed it, so nobody could form a view. It sits under the full wording, smaller and unbolded, and is suppressed where it would only repeat the card's own words — a round-trip repair card has no response text yet and already shows its label AS the text — and hidden from assistive software, where the accessible name already carries category plus the full response and it would be read twice. **The card's geometry is untouched, which is the property that matters for a keyguard**: the label is a fixed-height line under a response text that already flexed, measured identical at 240px single and 120px stacked with no overflow. *Worth knowing before judging it: the prompt asks for a label that NAMES the move rather than truncating the sentence, so a label that reads like the first few words is the model not following the instruction, not the design.* Also this cycle: **the "What's new" notice now fills the whole transcript region.** It was not shortened — the version line and the "save a report" link were added to that same block on August 7 and 9, and the notice shares its height with them as a flex sibling, so every line they took was a line of release notes the reader had to scroll to reach (156px of a 240px region; visible notes 91px of a 321px list). Both are hidden for that one screen and restored on dismiss, because the API-key notice that may follow renders in the same block; neither belongs there anyway, since they exist for a tester whose app is too broken to drive and reaching this notice means it started, took a Start press and rendered. **⚠ THE TRAP: an author `display: block` on the report link beats the browser's own `[hidden]` rule**, so hiding it from script would have silently done nothing and the space would still have been eaten — guarded in CSS, and caught only because the fix was measured rather than assumed. The changelog bullet covers the card label only: the notice fix was found and built after Ken had verified the changelog, and nothing is authored at release time. |
| 0.7.8   | 1     | **The user can see when a new set of options is on its way, and can cut it short (Ken, August 20 2026, out of his Product Overview review).** Every AI round trip now marks the response palette — a stripe sweeping the top of the region plus a shallow breathe on the cards — because the swap previously happened with no warning at all: the partner carried on talking, a fresh request went out, and the cards were replaced under someone halfway through reading them. Applied to **every** round trip, not only the automatic refresh (Ken: *“even one requested by the user via the 4-more button”*), since the fact the user needs is identical either way. Look-only by construction: the stripe is absolutely positioned, so it is out of the grid flow and adds no fifth cell, and the cards change opacity alone — keyguard geometry verified identical in both docks with the cue on and off (Rule 1). The cards stay readable and hit-testable because selecting one is precisely how the user aborts, which is why this deliberately does NOT reuse `.working` (dims to 0.55 and drops the pointer); empty reserved cells do not breathe, and `prefers-reduced-motion` holds both cues still rather than dropping them. On the abort half `handleResponseSelected` and `speakAsUserTurn` already invalidated the in-flight token and now also clear the cue at the moment of commitment, but **`openComposer` did not abort at all** — a real defect: a completing refresh landed under the composer and could set a placeholder speaking over someone mid-sentence. Also `.cmd-btn` / `.cmd-btn-end` / `.persist-btn` / `.regenerate-btn` traded their slate-fill hover and focus for the response card's shadow plus a 1px nudge — more than cosmetic, because both latched states on that bar (`#listenBtn.listening`, `#privacyBtn.private-on`) are themselves fills, so an inverting hover made an ordinary button read as latched; a fill on the Command Bar now means only “this control is on” (Rule 6). |
| 0.7.9   | 1     | **Settings sections regrouped by the decision being made, and the response card's two forms became the user's choice.** (a) The Settings tabs had drifted into roughly one collapsible heading per control — Ken's example was the three placeholder timings sitting in three separate expanding sections, one dropdown apiece, for three numbers nobody would ever set apart from one another. Regrouped: Text Size 4 sections → 1; Buttons & Keyboard 5 → 2; Conversation 9 → 5 (Saving conversations, Response suggestions, Listening, Placeholders, Tapping to speak); Keyguard Design 4 → 2. Two of these fixed more than density: the **silence period** had been sitting alone several settings away from the two listening settings it governs, and the keyboard's position / side / layout were three sections **two of which carried the identical title**, only one ever visible. General, Speech and Troubleshooting were deliberately left alone — their sections each hold a genuinely separate idea, and merging those is the opposite mistake. **⚠ THE INNER BLOCKS ARE `.setting-item`, NOT `.setting-group`**: `sections.js` recurses into every descendant `.setting-group`, so a nested one becomes a sub-section — which is the same one-heading-per-setting shape being removed. Ids are unchanged, so `updateKeyboardPositionGroups()` still shows only the rows the chosen dock uses (verified: which-side and both layout pickers toggle correctly inside the merged section). **Each merged section carries its own `data-help`**, because `resolveTap` collects every control under a group and its “they all mean the same thing” fallback cannot fire once a group holds several ideas — without it the heading silently stops speaking under the “?”, which `tests/settings-help.test.mjs` catches. No setting changed its meaning, its stored value, or its default. (b) Card text mode — see the CLAUDE.md entry under Response Option Display for why there are four modes rather than three and why a card may never come up blank. Also this cycle, not user-visible: **the `Conversant AAC Settings Defaults Review` worksheet**, generated for the SLP team to propose the shipped defaults — every setting with its choices and current value, all 68 About Me questions, the 32-button Express Panel starting grid with its 17 filled buttons and a table asking how many Feeling / Partner / Place buttons there should be, and every fixed phrase the app speaks, each table with a shaded write-in column. Every value read out of source rather than recalled. |
| 0.7.10  | 1     | **The response cards stop disappearing, and reporting a problem becomes one tap.** (a) A tester wrote *“the choices kept disappearing when I went to pick one”*. Reading her report with the new evaluator: 32 sets of suggestions in twenty minutes, **15 of them (47%) replaced before she touched anything**, a set surviving a typical 11s against her own 7.8s of reading and choosing, and a final conversation of five sets with nothing chosen before she wrote in. **It was two defects wearing one coat, and the larger had not been diagnosed:** `generateOptions` called `ui.clearResponseOptions()` unconditionally at the top, so on every reprompt the suggestions vanished for the whole round trip (5.6s typical, 8.4s worst) — **and clearing also strips `palette-refreshing`**, so it switched OFF the provisional look 0.7.8 had just shipped, thirty lines after `setPaletteBusy(true)` switched it on. That look therefore worked when the user pressed New N (that path never clears) and was **dead on every reprompt the app started by itself**, which is the case it exists for. Removing the clear closes the gap and lets 0.7.8's cue run there for the first time. **Ken's rule, now the governing one:** *“In no case should the response options be cleared until something is spoken or the set there is replaced — but in all cases, not until they are replaced with a new set.”* Audited: every remaining clear site is either “something was spoken” or “the conversation ended”. **⚠ A first attempt HELD the newer set and lit the New N button to offer it, and Ken reversed it:** *“the 4-new button is an on-demand request for additional response options when the initially offered set aren't sufficient. This behavior is unrelated to continuous recording of the conversation partner.”* The damage was concrete rather than aesthetic — a user who read the set, decided none fitted and pressed New N would have been handed **the answers to a different question** instead of the alternatives they asked for, and the button would have meant two things depending on invisible state. Reverted in full; the two mechanisms stay separate. (b) **“In my own words” now keeps a reprompt's cards and shows them on CANCEL, discards them on SPEAK** (Ken). Opening the box used to abandon the request outright, which fixed the two things that go wrong — a set landing under the box, a placeholder speaking mid-sentence — by throwing away work already paid for and leaving a cancelling user on cards older than what the partner had said. The render waits instead. **The reframe race is decided by INTENT, never by which answer arrives last:** the reframe wins, because the user asked for it, because its context is a superset of the reprompt's, and because otherwise a slow reframe could be silently overwritten by an automatic refresh — indistinguishable, to the user, from Reframe not working. **⚠ Closed a hole that predates this and that the old code shared:** opening the box silences the ladder already running but **cannot silence one armed afterwards**, and one is armed whenever the partner speaks again while the user is still typing, because arming happens at the partner's pause and does not depend on the request. Measured: a placeholder spoke 3s into a composing session. Now carried by `setUserSpeakingGate`. **The general lesson: cancelling a REQUEST is not the same as silencing a BEHAVIOR the request does not drive.** (c) **Send on Report a problem** — reuses the weekly-report address, secret, queue and log, so a report written offline waits and goes at the next launch. **It is the one payload that can carry speech** (the report embeds transcripts of conversations an error happened in), permitted only as the carve-out for a report the tester reads and deliberately sends — hence the mandatory preview plus `confirmDanger`, and the rule that nothing automatic may call it. Deliberately not gated on the weekly-send switch, which governs what the app does *by itself*. The endpoint script gains a `problems` tab; until it is redeployed reports still arrive, in the `reports` tab's raw column. **Not user-visible this cycle:** `scripts/evaluate-reports.mjs` (trigger phrase **“evaluate reports”**), which reads tester problem reports and reports two things separately — what the app did, and **which figures in the report not to trust**. The second half exists because reading one real report turned up five statistics that were wrong or unreadable, including a median wait of **0.2s** where the honest figure was **22s**, wrong in the flattering direction on the one question the beta exists to answer. Those five are flagged, not fixed. Its output is asserted to contain no code identifiers, and a check that finds nothing says *“not visible here”* rather than staying silent — the trace carries no words by design, so the commonest class of complaint cannot be settled from a report at all. Also **section 4.7 of both User Manuals**, documenting the whole flow — cards, holding phrases and the composer — at Ken's direction. |
| 0.8.3   | 1     | **THE TIDY-UP PASS IS REMOVED AND REPLACED BY A MEASURE (Ken, August 27 2026).** The second AI request per exchange, which rewrote what the recognizer heard into readable prose, is gone. **Ken's argument is about WHEN it ran, so no accuracy would have rescued it:** it fired after the user had already chosen a response, so it could never improve the suggestions for the turn it tidied, and the user hears the partner directly and is looking at them rather than at the transcript. Two live findings behind it: where a garble leaves the sentence IMPLAUSIBLE the generation call already reads through it unaided (given "i went to the see side" it wrote "seaside" back in its own card), so the pass was redundant exactly where it worked; and where a garble leaves the sentence PLAUSIBLE ("i can make it" for "i can't") it had no more to go on than the responses did - the same net hung twice, not a safety net underneath. It could also **corrupt the record confidently and invisibly**, rewriting whatever it judged implausible with no way to know it was right, which for a beta whose records exist to show what the recognizer gets wrong destroys the evidence. **Roughly a third of requests, not half** - suggestions fire once per PAUSE, the tidy-up fired once per exchange (one real session: 32 sets across ~17 turns). **What replaced it:** `heard_uncertain` in the generation call names words suspected of being misheard, recorded on the turn, at no extra request (the instruction sits in the cached half). Reported in Settings -> Troubleshooting as **HOW WELL IT HEARD THEM**, broken down **by person, by place and by recognizer**, worst first, with settings under three turns omitted (1 of 1 reads as 100%). **⚠ IT IS A FLOOR AND SAYS SO ON SCREEN:** a mishearing that leaves an ordinary sentence is invisible to it - the same blind spot the tidy-up had - so it is for comparing settings, not for reading as an absolute. **⚠ THE FIRST INSTRUCTION WAS AMBIGUOUS AND TWO LIVE RUNS DISAGREED WITH EACH OTHER** on "see side": it said to flag only a mishearing that CHANGES THE MEANING, which is both ambiguous and aimed at the rarest case - the wrong target for something comparing rooms. Widened to any suspected mis-recognition, with slang, contractions, filler and false starts explicitly not errors; two full 8-case runs correct after, including four clean cases that must NOT flag. Doubted words are returned at the TOP LEVEL, never inside `classification`, because `ingestClassification` whitelists that object and drops unknown fields silently - the defect that shipped the number button dead in 0.7.14. Partner turns now also carry `place` (the situation was stamped only on the user's side, and the partner's side is the half that says how well they were heard); `cleanedTranscript` is kept and always equals the raw wording. The committed-turn "uncleaned" italic is gone - it distinguished nothing once nothing is tidied. **Also: adding an Express Panel phrase now brings the on-screen keyboard up and KEEPS it up** - the rebuild re-created the sections, whose restored open state fired `toggle` one tick later and put the panel straight back over the keyboard Add had just raised. Fixed with an explicit signal from the editor rather than a read of live focus (a heading tap must still restore the panel, and whether that tap has moved focus yet is a browser detail), and **not consumed on read** - one rebuild fires several toggles, so a one-shot was eaten by the first while the second still hid it. 587 tests (up from 578). |
| 0.8.4   | 1     | **TWO FIXED PHRASES BECOME LISTS, AND THE PAID EXTRAS GET THEIR OWN FIGURES (Ken, August 29 2026).** `pardon` and `declineClosing` were single strings in `control-phrases.js`; both are now lists of five, edited by the same `listSection` as the openers and goodbyes, watermarked by the same additive-merge rule, and `singleSection` is deleted since nothing used it any more. **Asking to repeat picks with the placeholder no-repeat rule** (`pickPhrase`), the same reasoning that moved "Hold on" onto the placeholder pool in 0.8.2: these are phrases the app says on the user’s behalf at moments that can come round twice in one conversation, and hearing the identical sentence again is what makes a device sound like a device. **The decline card is PINNED to a cell and so cannot page**; `nextPhrase` therefore advances its wording on "New N" instead, which is the only way every version is reachable without the palette changing shape. **⚠ THE MIGRATION KEEPS A STORED STRING AS THE FIRST ENTRY** rather than discarding it, so a phrase somebody had reworded survives and stays where it is reached soonest; `normalize` gained `listOrString`, and the list keys moved into one `LIST_KEYS` constant so `getPhrases` / `resetPhrases` / `mergeNewDefaults` cannot disagree about which keys are lists. `diagnostics` counts both as lists now. **The placeholder initial delay gained a 1-second option** (both manuals already documented 1 to 6). **Settings → About prices the two paid services separately**: they were always inside the total, which was exactly the problem — a user paying for both could not tell what they were paying for, so a bill that looked high said nothing about which part to reconsider. Heading "Estimated API Cost" → "Estimated Cost". **Documents:** the report-handling document gained section 6, the receiving end of a PROBLEM report, which nothing had described — and folding the drift back first was the real work, since the August 27 sync had edited that .docx directly, so running the generator would have discarded a table row and an entry. Both manuals now say what "Repeat what I said" is for as against the AI noticing the request by itself. 603 tests (up from 587). |
| 0.8.5   | 1     | **THE COST PANEL NAMES THE TWO COMPANIES THAT BILL (Ken, August 29 2026: "why can’t you estimate the AI and Deepgram costs separately?" then "they should also be totaled and identified as ‘Deepgram cost estimate’ and ‘Anthropic Claude’ cost estimate. Two values, by name.").** Both paid services had been inside the single total since they were built, which was the defect: a user paying for both could not tell what they were paying for, so a bill that looked high said nothing about which part to reconsider. `updateUsageDisplay` already computed all three figures; only the rendering was missing. **⚠ THE FIRST CUT BROKE OUT THE PAID EXTRAS AND LEFT THE AI'S SHARE TO SUBTRACTION** — the same failure one step along, and Ken's second message is what settled the shape: the two headline rows are named by COMPANY, because the user holds one account with each and those are the two figures the panel can be checked against; hearing and speaking stay, indented under the Deepgram total, since one bill covering two very different rates makes "which of them is carrying it" the second question. `#usageBreakdown` is hidden unless a paid service has been used (on the free setup the total IS the Anthropic figure). Money right-aligns into one column across all four rows — a first cut gave the amount `flex: 1` and pushed it to the panel edge, the one place it cannot be compared with the row above. **DOCUMENTATION: the Beta Test Plan was the last row not marked current, and its outstanding item had already fixed itself** (the five L9 findings were the checker misreading a pointer to the MANUAL's section; fixed Aug 27). Seven real L7 breaches in its place, now quoted. **A full sweep then found 20 errors across ten documents all marked current, and half were the checker's own fault:** S4 skipped a paragraph only when its AFTER spacing hit the threshold while `apply-doc-style.py` skips when EITHER does, so it reported errors on exactly the title lines the tool protects, in seven documents, none fixable by running it; and L6 let its trailing run swallow the sentence after a document name, so a name plus a prose aside read as a name with a comma. The discriminator is that a real parenthetical starts with a capital — **matched case-sensitively, since `_hits` searches with `re.I` and the first attempt changed nothing.** `fix-footer-page-numbers.py` now builds a whole "Page m of n" line where there is no PAGE field to extend (two documents had no footer at all). **The Product Overview is exempt from the Conversation Analysis category names (Ken's call) via a new per-term `exceptDocuments`, NOT by dropping it from `userFacingDocuments`** — the one-line version would also have switched off the retired-name, product-history, screen-name and glossary rules on it, silently. Zero errors across every tracked document. Report-handling gained section 6 (problem reports) and both manuals say what "Repeat what I said" is for. 603 tests. |
| 0.8.6   | 1     | **THE COMMAND BAR CAN SHOW WORDS, AND THE TAB THAT FEEDS IT IS NOW "Commands" (Ken, August 30 2026).** Rule 12s icon-only default is unchanged and still ships; the setting makes it a preference, because a button whose meaning you have to remember is one you hesitate over. Settings -> Commands -> "What the command buttons show" (storage.loadCommandLabels, default icon); ui.setCommandLabelMode re-draws the bar in place, so it changes behind the open Settings panel with no reload and the Listen button keeps its mic state. **The accessible name stays the long wording in BOTH modes** -- COMMAND_FACES is a table of faces, not of names -- so a screen reader and the spoken help are unaffected, and Listen keeps one word across both mic states because the red fill is what says it is on. **THE TRAP, MEASURED, and it generalizes to any future change to a Command Bar buttons box: the cells are flex items with flex-basis 0, so under border-box a cells used width is FLOORED AT ITS OWN PADDING PLUS BORDER** -- 0.15rem/0.25rem of padding on the worded face widened every button and pushed the eight after Listen 7px along, a keyguard hole moving because a label was switched on, with nothing on screen looking wrong. Shipped state measured identical to two decimal places in both modes, on both docks. **The tab rename is Kens** and the reason is the useful half: everything on that tab is the words behind a command bar button, so "Controls" named a category where "Commands" names the place to look. data-tab moved with it (controls -> commands, so the help key is tab:commands; the per-control help BUCKET is still called controls and is a different thing), **and the phrase editor needed its own section scope** -- with the tab now declaring a section in index.html the two would have shared the key controls#0 and opened each other, the same trap the Express tab hit. **Documents:** both User Manuals renamed in six places each (the verb "Controls whether..." in the Button Sizes table deliberately untouched) and both gained a paragraph on the new setting; zero errors, Word opens both. **A DOC-WORKFLOW LESSON worth more than the feature: Word refusing a document on a OneDrive path can be the SYNC rather than the document** -- a manual was refused reproducibly four times while every other check passed, which is exactly the corruption signature, and the file was perfect; the ten-second discriminator is to copy the same bytes to a different NAME. 603 tests. |
| 0.8.7   | 1     | **PROBLEM REPORTING BECOMES ONE BUTTON, AND THE APP LEARNS WHETHER A REPORT ARRIVED (Ken, August 31 2026).** Ken asked to drop Copy and Save-to-a-file, then made the objection that reshaped the answer: if a send fails he never hears, so the onus falls on the tester to notice nothing happened. The fix was not a fallback but removing the blindness. **MEASURED against the live endpoint rather than assumed: mode 'cors' with Content-Type text/plain returns 200, type cors, body readable** — the old `no-cors` belief was half right, it is the PREFLIGHT Apps Script cannot answer and text/plain avoids one. 'ok' means written; anything else, including an unrecognised body, is a refusal. **THE MIRROR-IMAGE BUG IN THE QUEUE: `flush` sliced the payload off BEFORE checking the outcome**, so a report the endpoint had not accepted was destroyed — harmless while everything claimed success, and the way reports would have gone missing the moment refusals became visible. Six delivery tests, confirmed to fail with the old behavior restored. **THE WEEKLY REPORT NOW HANGS OFF START, NOT PAGE LOAD.** The data folder is reconnected inside `handleStart` and nowhere else, so a three-second timer from load was a race the report usually lost: it counted conversations in a folder not yet reopened and reported "no data folder, no conversations" for anyone slower than three seconds. That was BOTH speech therapists — one had said 47 things across four days and was filed as having said nothing. **And 25 of the 29 rows in the Sheet were DEVELOPMENT LAUNCHES**, because `shouldSend` returned true unconditionally on a first launch and every preview-pane run starts with empty storage; ten reported a screen of 0x0, six arrived in one day. First-launch reporting is kept and qualified by `hasActivity`; later reports untouched, because a quiet tester must still report — silence is a finding. **THE READING TOOL WAS SILENTLY DROPPING EVERY COMPLAINT:** it required a JSON payload in every row and the problems tab has none (it is flat text), so four tester complaints — including the placeholder leak reported twice across two versions — read as "No problem reports". Now read by column name, with unreadable rows COUNTED and the file named. **Disclosure corrected against what is actually sent:** it said the text appeared above the button when it appeared below, said a "Don't save" conversation was not in the report when what is withheld is the TRANSCRIPT, and named four of six sections — omitting the usage summary, which prints THE NAMES OF PEOPLE THE USER HAS TALKED TO. One disclosure now serves both surfaces. The report moved into the confirmation card, **which is now confined to the Settings box** (Rule 11: centred in the viewport it could sit under the dock a keyguard covers) — measured 7px clear on a bottom dock, 258px on a side dock. **"Tester name" moved out of "Weekly reports"** into its own section: one name feeds both report kinds, and the placement implied otherwise, so 27 of 29 weekly reports and half the complaints arrived unattributed. The three data-folder subfolders are created on connect rather than at first use, so a profile or backup copied from another device has somewhere to land. **A NEAR-MISS WORTH KEEPING: removing an unused helper deleted `buildProblemReport`, which the app calls, and all 611 tests still passed** — `app.js` cannot be loaded by the suite, so only driving the real app caught it. 617 tests. |
| 0.8.8   | 1     | **THE REPORT CONFIRMATION IS AS WIDE AS THE SETTINGS CONTENT AREA (Ken, August 31 2026: it "should be as wide as the non-tab portion of the settings panel and be sensitive to the resizing of the app in real-time").** It spans from the inside edge of the tab column to the panel's outer edge, so it lines up with the panel it belongs to rather than floating in the middle of it. **Every term is a CSS variable the layout already maintains, so it follows a resize with NO JavaScript and nothing to re-run** — measured with the card still open, no reload: 1140/1140 flush at a 1280 viewport, 884/884 after resizing to 1024, and 577/577 on a LEFT side dock, which is the case the per-dock override exists for because the dock and the tab column stack on the same edge. **The tab column's width moved to `--settings-tabs-w`, declared once and read by both**; a hard-coded 140px in either place drifts silently the first time that column changes and leaves the card misaligned with the panel it sits in. **Scoped to cards carrying a preview (`has-preview`), not to every confirmation** — the width exists to make a long monospace report readable, and stretching "Delete this person?" across the whole panel would put its buttons a long way from the sentence they answer. **⚠ THE PER-DOCK RULES ARE MORE SPECIFIC THAN THE MODIFIER**, so the side-dock cases need their own has-preview rules or the dock edge wins on exactly the layout with least room. A first cut was 1px out: `#settingsTabs` is border-box, so its 1px border sits inside the 140px and the offset must not add it again. 617 tests. |
| 0.8.9   | 1     | **ANDROID: THE RECOGNIZER RE-SENDS THE WHOLE UTTERANCE, AND THE DATA FOLDER WAS BEING ERASED ON EVERY LAUNCH (Ken, measured on his own Android tablet and phone, August 31 2026).** Three faults, and the first two share a shape worth carrying: **a fault sat in the code for a year and appeared the moment a platform took the other branch.** **(1) Speech laddering.** Android Chrome hands over the whole utterance-so-far on every update, so one sentence arrived as growing prefixes and each rung was filed as its own statement - "this this is this is a this is a test" reached the partner's turn, the saved conversation, and the AI. `stt.js` now REPLACES a statement that the new settled text begins with, at both commit sites (two mechanisms fit the captured data and this neutralizes both). **Written unconditionally, not gated on Android**, per the standing rule that platform behavior goes behind something that is a no-op elsewhere and never a fork - a well-behaved recognizer sends only the new words. **(2) `restoreDataFolder` DELETED the user's folder on ANY failure.** Permission may only be requested while the browser still counts a tap as recent; the reconnect runs several awaits into Start, so on Android the request threw and the catch erased a folder that was sitting there intact. Windows never reached that line because permission is retained there. The handle is now KEPT on any failure - **forgetting is a one-way loss, failing to connect is not** - and a "Reconnect my folder" card in the pre-start chain supplies the fresh tap, reusing `restoreDataFolder` rather than adding a second entry point. **The general lesson: a catch covering ANY failure eventually covers one you did not mean, and where the recovery is DESTRUCTIVE the catch must name the failure it is for.** **(3) The settings-profile and backup lists were not redrawn** when a folder connected - the pick handler reconciled seven stores and omitted both - so profiles read "Choose a data folder first" until Settings was closed and reopened, and looked lost. All post-connect work is now one shared `adoptDataFolder`. **Verified in the running app BOTH ways** (fixed build, and old behavior restored): folder erased/kept, card absent/offered, profile list stale/correct; plus the unchanged no-remembered-folder path and both card exits. **Also recorded: Android is classified as a DESKTOP by `platform.js`**, so it inherits the Windows answers - which is why most things work untouched, and is the first thing to check when something else is wrong there. **Backgrounding is UNTESTED and is the likeliest next fault on a phone.** 622 tests. |
| 0.8.10  | 1     | **THE FOLDER PERMISSION RIDES THE START TAP, AND STORAGE PERSISTENCE IS ALWAYS ASKED FOR (Ken, August 31 2026).** **The measured fact that drove it: Ken's INSTALLED Android app is still asked for folder permission at every launch**, which settles the hypothesis that Chrome's documented persistent permissions for installed web apps would cover this - they do not, so the per-launch ask is permanent and 0.8.9's reconnect card was a permanent extra tap rather than a rare fallback. **The fix is one `await`:** a permission request is only granted while the browser still counts a tap as recent, and the HANDLE LOOKUP was the await that pushed it past the deadline. The handle is now read at page load (`primedHandle` in `storage.js` - lookup only, nothing requested) and `requestFolderPermissionNow()` is called at the TOP of `handleStart`, beside `requestAppFullscreen` and the audio unlocks, which sit there for exactly the same reason. Android now costs the same taps as Windows; the card becomes the fallback it was meant to be. **`restoreDataFolder` awaits that request and then re-checks `queryPermission` on the handle it is actually adopting**, rather than trusting the request's own return - so the request having been made against a separately-read handle cannot matter. **Storage persistence is now requested at every start on every platform**, fire and forget: it had been asked only by a Settings button shown where there is NO folder picker, so Ken could not reach it on Android. That gating was right while a folder meant Windows and wrong once Android turned out to have one - a phone runs out of space, and what eviction costs is the folder pointer plus every setting. **Chrome and Safari answer silently, which is what makes "always ask" the right shape and a button the wrong one.** **⚠ VERIFIED WITH A REAL TAP, which is the only kind that proves anything here** - a programmatic click carries no user activation: the request fires with `navigator.userActivation.isActive === true`, exactly once, the folder connects, no card appears; `persist()` called once per start; an unusable folder still keeps the handle and still offers the card. **A console error seen while testing was the local stale-service-worker trap** (old HTML, fresh JS), not a fault - a fresh tab was clean. 622 tests. |
| 0.8.11  | 1     | **THE FOLDER RECONNECT NOW REPORTS WHAT IT DID, AND A FAILURE STOPS LOOKING LIKE A SUCCESS (Ken, August 31 2026).** 0.8.10 moved the permission request onto the Start tap; on Ken's Android tablet it made no difference and **no permission prompt appears on Start at all - while the CARD's button does prompt, and connects.** So the shortcut is not being REFUSED, it is not being MADE, which rules out the too-late-after-the-tap explanation: the card's handler does strictly more work before asking and succeeds. **⚠ AND IT EXPOSED A HOLE IN THE VERIFICATION, which is the part worth carrying: the browser check used an OPFS handle, and OPFS grants permission INSTANTLY AND NEVER PROMPTS.** `activationLive: true` was real and is still true - the call does sit inside a live tap - but the test could not have shown whether a genuine prompt appears and is answered, which is exactly the failing part. **A stand-in that succeeds by construction verifies the call site and nothing about the outcome.** **Rather than a third hypothesis, the path is now instrumented**: `folderReconnectTrace()` records primed / not-primed, asked / did not ask, whether user activation was live at the moment of asking, what the browser answered, and what `queryPermission` said afterwards - surfaced in the STORAGE block of a problem report. Nothing had ever recorded which step did not happen, which is why two rounds went into theorizing. **Also fixes the defect that CAUSED the misreading: the card dismissed itself whether the reconnect worked or not**, so failure and success were indistinguishable (Ken: "it appears to connect") and a wrong conclusion stood for two rounds. A failure now stays on screen, says so, hides the retry that would repeat itself, and leaves "Not now" as the way on; wording and button reset on each pass so one failure cannot leave the card stuck. **A test-harness lesson too: the pre-start chain has several steps, and a check that does not dismiss "What's new" never reaches the screen it is testing** - which looked like the card being broken. 622 tests. |
| 0.8.12  | 1     | **THE RECONNECT CARD WAS NEVER ABOUT PERMISSION - THE APP STOPPED WAITING FOR THE ANSWER (Ken, August 31 2026).** The 0.8.11 instrumentation answered it on the FIRST report, and the cause was neither of the two chased across 0.8.9-0.8.11. **`warmUpStorage` is abandoned after `STORAGE_WARMUP_MS` (6s) so a hung storage layer can never strand anyone on the Start screen; on Android that deadline is reached while the folder is still OPENING - slow, not hung** - so the pre-start chain asked "do we have a folder?", got "not yet", and offered to reconnect one that was already connecting. Ken's trace shows it finishing moments later BEHIND the card, and his report shows two warm-up timeouts and two Start presses five seconds apart: Start looked dead, so he pressed it again. **The deadline is UNCHANGED (it guards a real failure); only the QUESTION is deferred** - `storage.settleRestore(ms)` waits for an in-flight restore, bounded, so a genuinely hung one still falls through to the card. **The lesson: "when to stop BLOCKING Start" and "when it is safe to ANSWER a question about the folder" are different decisions, and one timer was answering both.** **Separately, latent not causal: `requestFullscreen` CONSUMES user activation**, so asking for the folder afterwards found the tap spent - `tap:asking:activation-GONE` on the tablet (fullscreen ON) and not on the phone (OFF). *I had ruled this out by checking the PHONE's settings - the wrong device.* Chrome granted anyway, but a browser that enforced it would trade the user's data folder for a cosmetic fullscreen, so the folder is now asked for FIRST. **⚠ THE META-LESSON, worth more than the fix: three releases each fixed something that was not wrong, because nothing recorded WHICH STEP FAILED.** One instrumented report settled it in a line. Instrument before the second hypothesis, not after the third. Verified against the reported situation: a restore forced to take 7s, past the 6s deadline, connects with the card NEVER shown - precisely when it used to appear. 622 tests. |
| 0.8.13  | 1     | **A SLOW STORAGE LAYER IS NOT A BROKEN ONE - `withTimeout` WAS DOING TWO JOBS WITH ONE TIMER (Ken, August 31 2026).** With 0.8.12's fixes confirmed on device (trace: `activation-live > connected`, card gone), what remained was **a pink transcript at every launch** - the app's error wash - **with the storage warm-up timeout as the only error, on the same launch whose trace says the folder connected.** Reaching the deadline means the work is SLOW, which is why it stops BLOCKING Start; it does not mean the work FAILED, and on Android opening a real data folder simply takes longer than 6s. So every healthy launch logged a failure and painted the error signal. **⚠ A FALSE ALARM ON THAT SIGNAL IS WORSE THAN NO SIGNAL: it exists to tell the user AND THEIR PARTNER "expect a hiccup", and one that fires on every good start teaches them to ignore the one time it matters.** The deadline now resolves QUIETLY; a separate watch at `HANG_FACTOR` x the deadline reports work that has still not answered, which is the genuine hang. The permission-dialog exemption applies to both, since a user reading a prompt is neither slow nor hung. **⚠ THE IMPLEMENTATION TRAP: whether the WORK finished must be tracked separately from whether the RACE settled.** The race settles AT the deadline, so a flag set in its `finally` would mark the work "done" the moment we stopped waiting for it - silencing the hang watch in exactly the case it exists for. **Also confirmed permanent and NOT ours: Android asks for folder permission at every launch**, in a tab and as an installed app; the extra card and extra tap were removable, the platform's own prompt is not. Verified in the running app all three ways: slow-but-working (9s, past the 6s deadline) logs nothing, shows no tint, connects; a genuine hang still reports at 30s with Start not stranded; fast path unchanged. 622 tests. |
| 0.8.14  | 0     | **INSTRUMENTATION ONLY - what listening DID across being backgrounded (Ken, August 31 2026).** No behaviour changes anywhere, including the iPad's visibility guard; no user-visible change, so no "What's new" panel. **Backgrounding is the last untested Android behaviour and the only remaining one that could break a REAL CONVERSATION rather than annoy someone around it, and its failure is silent by construction:** if the recognizer stops when the page is hidden, the app goes on showing a lit microphone and hears nothing - the user finds out when the partner's words never appear, and cannot describe what happened. **Nothing watches visibility outside iOS** (`speechConfig().guardVisibility`), because nowhere else was it known to happen - and a phone is backgrounded constantly. `stt.listenActivity()` records counts and timings only: sessions started, times backgrounded while listening was intended, **restarts attempted WHILE hidden** (the tell for a restart loop, since `onend` restarts with `restartDelayMs: 0` on the non-iOS config), returns to foreground, **whether anything was heard after returning** (0 is the bad answer), and errors by kind - **counted BEFORE the `no-speech`/`aborted` allow-list, because one 'aborted' is normal and hundreds is the loop.** Surfaced in the SPEECH block of a problem report. Driven through the app's real code with a stand-in recognizer behaving the way Android is SUSPECTED to (ends when hidden, silent while hidden): `restartsWhileHidden=4, abortedx4` in two seconds, so the numbers do separate a loop from a healthy session - which is the property the instrumentation had to have. **⚠ That is a SIMULATION OF A SUSPICION, not a measurement of Android**; the device's own report is what settles it. This follows the 0.8.9-0.8.12 lesson directly: instrument before the second guess, not after the third. 622 tests. |
| 0.8.15  | 2     | **BACKGROUNDING IS GUARDED ON EVERY PLATFORM AND ON BOTH BACKENDS; THE PRE-ROLL IS WIDENED (Ken, August 31 2026).** His backgrounding test on the Android tablet: `sessions=18 backgrounded=1 restartsWhileHidden=1 returns=0 heardAfterReturn=0 errors=abortedx1,not-allowedx1,networkx1`. **Backgrounding stopped recognition, it came back `not-allowed`, and `handleSourceError` treats that as fatal - so listening was torn down entirely.** `returns=0` is not "he never came back"; by the time he did the app was no longer listening, and the trace shows him pressing Listen again by hand three minutes later. The one mercy: not silent - it errors and stops rather than sitting lit and deaf. **`guardVisibility` is now ON EVERYWHERE**, universal rather than an Android branch, which REMOVES a fork and means an untested platform defaults to the answer now known to be right on two of the three tried. **⚠ AND THE GUARD DID NOT COVER THE PAID BACKEND AT ALL**: it read `if (!recognition) return;`, and with Deepgram selected `recognition` is null - so the guard shipped an hour earlier did nothing in exactly the configuration Ken then chose to recommend for Android. Written when the browser recognizer was the only source. Now suspends whichever source is active via `suspendSource()`, **a stop that keeps the user's INTENT** so the guard can restore it, distinct from `stopListening()`. **`preRollMs` 400 -> 1200** (Ken: "it still loses about the first second of speech"): the gate can only open once the level has risen, so a quiet sentence-opening is already part-way in. **Raised the pre-roll rather than lowering `openLevel` deliberately - pre-roll sends only buffered audio and only when the gate genuinely opened, so it cannot fire on a noise, where a lower threshold would and every false open is billed.** **⚠ TEST FINDING WORTH KEEPING: the guard installs itself only where `document` exists, so it was silently absent from EVERY test - which is precisely why the paid-backend gap went unnoticed.** `tests/env.mjs` gains a minimal document; the guard itself needs a live socket and mic, so it is asserted at the source. **Writing that test hit the `indexOf` trap this file has recorded before** - anchoring on the first `visibilitychange` listener found the DIAGNOSTIC one, not the guard. 624 tests. |
| 0.9.0   | 2     | **ANDROID IS A SUPPORTED PLATFORM (Ken, August 31 2026), and the minor digit follows the iPad precedent** - 0.6.0 was what made iPad supported, and a new supported platform is the same class of announcement even though the CODE here was small: Android is Chromium, so it slotted into abstractions that already existed rather than needing a storage backend, a speech backend or a layout re-derivation. **`platform.js` gains `isAndroid()`.** Until now Android matched no test there and fell through to the DESKTOP answers, so every bug report from one said "desktop" and said nothing about the device. Plain substring test - Android browsers say "Android" and mean it, unlike iPadOS Safari, which claims to be a Mac. **Chrome OS is deliberately NOT caught** ("CrOS", and "Linux" appears in both): a looser test would hand every Chromebook the Android verdict and tell those users to pay for transcription they do not need. **Deepgram STT is REQUIRED on Android** - stated through `speechRecognitionSupport()`, the same place the iPad verdict lives, so the reason travels with a bug report rather than living only in a manual; `apiPresent` stays true so the warn-don't-block rule (July 30 2026) is unchanged, and unlike an INSTALLED iPad the built-in recognizer here genuinely works - badly - so someone evaluating the app before paying for a second service can still try it. **⚠ THE DEFAULT ALMOST SHIPPED BROKEN:** a first cut defaulted Android to deepgram unconditionally, on my own written claim that `stt.init` falls back to the built-in recognizer when the paid one cannot work. **IT DOES NOT** - that fallback covers a source that cannot be CONSTRUCTED, and an empty key constructs fine then fails at start with 'no-key', which `handleSourceError` treats as FATAL and switches listening off. Every Android user without a key would have been unable to listen at all, worse than the imperfect recognizer they had. The default is conditional on a key being present. **Also: the 0.8.9-0.8.16 release notes were CONSOLIDATED** - a 0.8.5 upgrader was being shown 24 notes, four versions of which were the folder-reconnect saga, fixing a card INTRODUCED in 0.8.9 and gone by 0.8.12. Per RELEASING.md they describe a problem that reader never had. Folded to the net outcome at 0.8.9, caveats REFRAMED rather than deleted, entries edited IN PLACE so nobody past 0.8.12 is re-announced to; 24 -> 18. **⚠ Test constraint worth knowing: `storage.js` imports `platform.js` WITHOUT a cache-buster, so one platform instance serves a whole test file and captures the first user agent** - right in a browser, and it means one process cannot answer for two platforms. 629 tests. |
| 0.9.1   | 0     | **AN ANDROID SCOPE FOR THE RELEASE NOTES, plus the document sync for Android support (Ken, August 31 2026).** No user-visible change, so nothing is announced. Android became supported in 0.9.0 while the notes still had only TWO scopes, so its notes went to everyone - a Windows tester read that Android needs a paid transcription service. **Worse, Android readers were classified by the FOLDER-PICKER capability, and Android HAS a picker: they were filed as computer users and shown notes about "your computer".** `currentPlatform()` now asks `isAndroid()` first - a deliberate exception to the capability-not-user-agent rule stated in that very function, and safe for the reason that rule exists: it guards against iPadOS Safari, which lies about being a Mac, and Android does not lie. **⚠ In the generator the Android test must come FIRST: "On an Android phone or tablet" contains "tablet", and a Windows tablet is the computer side, so a computer-first test would claim every Android heading.** `OTHER_PLATFORM_NOTE` is three entries, each naming the two it is not, written per platform rather than joined from a list - a missing entry would put `undefined` in front of a reader, so a test asserts every platform yields non-empty notes. **DOCUMENTS: a third User Manual (Android)**, Ken choosing that over folding Android into the Windows one so a reader never meets another device's instruction. **The measurement that made it tractable: only 12 of the Windows manual's 377 paragraphs are platform-specific - it is 97% shared** - so a third manual is far cheaper than its length suggests, at the price that a shared change now lands in THREE documents. Product Overview says three families of device; both existing manuals name all three; Beta Test Plan's device list includes Android. **⚠ Two self-inflicted checker errors worth remembering: replacing a paragraph's text COLLAPSES ITS RUNS, destroying the bold-label/italic-body format on Tip:/Note: paragraphs; and a document name was written with commas it does not have.** Every tracked document clean, Word opens all 36. Also fixed a stale DOC-SYNC row - "Security Issues.docx" was renamed to "Security Review.docx" and the tracker never caught it. 632 tests. |
| 0.9.2   | 1     | **DOCUMENTS AND THE DOCUMENTATION CHECKER ONLY - no app change; the deployed app is byte-identical to 0.9.1 apart from the version strings.** Ken asked why "Not supported: iPhone, Android phones and tablets." was in the ANDROID manual. **ONE CAUSE PRODUCED THREE OF THE FOUR FINDINGS: the scan used to decide which paragraphs were device-specific listed only paragraphs OUTSIDE TABLES** (379 of the manual’s 594), so it reported 12 device-specific paragraphs when the real number was higher and the whole "What You Need" table came across from the Windows manual - recommending a Microsoft Surface, naming a MacBook, giving Windows 10 as the operating system, describing the taskbar. The same blindness is why the Windows and iPad manuals went on saying Android was unsupported AFTER it became supported: the prose above that row was updated and the row, being a table cell, was not. **The standing tools were never wrong - the throwaway scan was**, using a view this file has warned about twice. **⚠ AND THE ANDROID MANUAL WAS NEVER ADDED TO `userFacingDocuments`, so every rule that matters most to a manual was skipped on it and it REPORTED CLEAN for as long as it existed** - a green run that checked almost nothing, now an ERROR rather than a silent downgrade. **FIGURE 2 HAD A CAPTION AND NO PICTURE in all three manuals**, lost in the Aug 30 OneDrive conflict: the recorded way of resolving one compares the TEXT of every paragraph, and a picture has no text, so the copies compared equal with a figure gone and the caption survived because a caption is text. Dated from the backups - two large images until Aug 30 20:54, one on both sides of the conflict. **Figure 1 also put a British idiom in the user’s voice** ("Do you fancy coming along sometime?"), invisible twice over: inside an image, and "fancy" is good American English in other senses. Regenerating changed 0.12% of pixels, confined to that line, at identical dimensions. **NEW RULES: L10** - a manual carries only its own device’s hardware words, plus a "Not supported:" list naming a platform we support; **severity splits on the CONTAINER**, because a device specification in a table row is always wrong while prose may be a deliberate contrast. **S12** - a figure caption has its figure; **both arrangements accepted** (manuals put the picture above, the Architecture Overview below, 9 of 9) and **a document embedding no pictures is skipped**, because "Sounds Like Me" captions a mockup typed as TEXT. Both shaped by surveying the real documents rather than by reasoning, which is what kept the false positives out. **A tooling warning now names any doc script reading `document.paragraphs`** (read via the syntax tree, so a docstring warning about the wrong view is not itself flagged) - it names `generate-product-overview.py` and, more seriously, `insert-manual-figures.py`; `docx_model.py` gained a scan CLI so the correct reader is the easy one to reach for. **⚠ TWO CLAIMS OF MINE WERE WRONG AND ARE RETRACTED IN PLACE: `insert-manual-figures.py` matches anchor TEXT, not indices**, so it cannot shift a figure; and **`apply-doc-style` reporting ~400 changes after any Word save is Word dropping a redundant `before="0"`** - measured: no style in these documents sets any spacing, so the two forms render identically and the manuals were never out of house style. **The sync found the SAME removed buttons three more times** - "Save to a file" and "Copy" for problem reports, in the Android manual and twice in the Beta Test Plan, which also claimed Troubleshooting still had both for the error log (it has Copy only, and the report has neither) - plus two Weekly Report Handling claims overtaken by the decision that weekly-report errors do NOT mail while problem reports always do. **Android added to Keeping Costs Down** (the one document about cost, silent about the platform where paid transcription is required) **and as Architecture Overview §17.4** (zero mentions a day after it became supported; Evidence renumbered 17.5). **Ken’s decision recorded: a "purification" pass before general release, driven from an extraction of what the CODE does** - because a sync works from a change window and therefore never re-reads a whole document. 632 tests; 36 documents open in Word. |
| 0.9.3   | 1     | **THE SPEECH AND LANGUAGE THERAPISTS' ALWAYS PHRASES, and the shipped panel configuration (Ken, September 2 2026).** The 26 phrases they authored replace the project's placeholder Always set wholesale (never merged - the additive-merge watermark is deliberately not used here; see `express-items.js`), and are what the band's Reset returns to. Context ships with six feelings, Silly replacing Stressed. `DEFAULT_SIZES` moves to whole ROWS, one Context row and no Flex; `contextMark` was already `shape`. Two house conventions applied to the wording and guarded by a test: no trailing period on a phrase (a button is a spoken utterance, not a sentence; question marks stay, they are part of the words) and no capital part-way through one. **The load-bearing half is the one-shot SEED REVISION** (`SEED_REVISION` on `express-items.js`, `seed` on the stored model): the defaults alone reach only a fresh install, and the therapists already have a stored version-2 panel, so the set that was actually commissioned would have reached nobody using the app. It replaces the ALWAYS band only - band sizes, Flex lists and any Context button the user has touched are untouched, so no partner or place button pointing at a real person is thrown away; bumping `MODEL_VERSION` instead would have discarded all of it. The Context feelings come with it only where the whole band is still ours, judged AFTER `ensureOrigin` (a pre-origin file carries none, so a bare `!origin` would have read a user's own partner button as ours). `getModel` carries `seed` out and back so a getModel/setModel round trip cannot look unseeded and re-replace the band on the user's next edit. Four tests cover replace / spare / fire-once; verified in the running app on B1 - all 26 phrases show, and the editor states plainly that the sixth feeling has no room (one Context row is 5 cells there, the compose key taking one). |
| 0.9.4   | 0     | **DOCUMENTS, plus the removal of a directive the redesign had already retired — the app behaves identically to 0.9.3.** Eight documents caught up with the therapists' Express Panel phrases; two long-standing errors surfaced on the way and were fixed: the Settings Defaults Review's Part 3 still described the PRE-BANDS panel (seventeen filled cells of thirty-two, feelings leading), wrong since 0.8.0, and two documents told a tester to look for an **amber Notice button** that has not existed since a phrase started taking its color from its band. **The half-populated Express Panel directive is DROPPED** (Ken: *"the Express Panel has undergone a fundamental redesign which eliminates this as an issue"*) — the blank half existed to teach that a cell is the user's to fill, and bands teach it structurally (an empty Flex band, an empty band per new partner and place, a Context band the user populates), so words they need on day one no longer have to be withheld to make the point. Gone with it: the dead version-1 flat `DEFAULT_ITEMS` list, unread by the app since bands shipped, and the test asserting the panel was less than half full. **The provenance half survives untouched**, because the rule was never really about blank cells — it was that a shipped phrase says nothing about the user, as true of a full panel as of a half-empty one. Also repaired: a python rewrite had converted `ui.js` wholesale from CRLF to LF, so a two-line comment change read as a thousand in the history. |
| 0.9.5   | 1     | **THREE FIELD-REPORTED FIXES, all from Ken on 0.9.4 (September 2 2026).** (1) **THE PAID VOICE DROPPED THE OPENING WORD** - "Are you wearing a tie tonight?" spoken as "You wearing a tie tonight?". `play()` in `tts-deepgram.js` called `resume()` on a suspended AudioContext WITHOUT AWAITING IT and started the buffer immediately, so the audio was scheduled against a context whose clock had not begun advancing and the browser discarded the part it judged already past. Intermittent by nature - it needs the context to have gone to sleep - which is why the COMPOSE PANE surfaced it: that is the longest the app stays quiet, so a minute of typing suspends the context and the statement just written is the first thing spoken after it. A short unstressed opening word is exactly what fits in the wake-up gap. Browser-voice users cannot see it (speechSynthesis owns its own audio). Now awaited, with a token so a cancel during the wait is honored - `cancel()` cannot stop a node that does not exist yet. **The test is the cross-layer one**: a fake socket plus a fake AudioContext that starts suspended, asserting the ORDER (resume settles before `start()`); verified to fail against the old code, which logs `start:suspended`. (2) **WORD COMPLETION IN THE RESPELLING BOXES** - a respelling is a deliberate misspelling, so accepting a suggestion silently restored the very spelling the voice already gets wrong. One `predictionOff()` in `keyboard.js` consulted by BOTH entry points - the visible ghost and the word-prefix reader, the second of which also stops respellings being LEARNED into the user's own word list. Marked at the field with `data-no-predict` rather than by id, because the five boxes are built at runtime by three editors and an id list would fall behind the next one silently; closes a standing gap in passing, the Deepgram key field having never been excluded though the Anthropic one was. Verified in the browser (the module cannot be loaded by a test): the same letters offer a completion in "What the button says" and none in "How to say it" - the control half being the point, since it proves the mechanism is live rather than untriggered. (3) **A MISPLACED INSTRUCTION ON THE PROBLEM REPORTER** - "Read it through, then confirm to send." was written to the status line and the dialog then covered the panel, so the one moment it applied was the one moment it could not be seen, and it remained afterwards telling the tester to do what they had already done. Removed; the two-step rule is untouched (the dialog still carries the disclosure and the full report) and the network wait it used to occupy now says "Sending...". Also: `cacheKey` used a RAW NUL byte as its separator, which made git and grep treat the whole file as binary and its diffs unreadable - written as an escape instead, same character, same keys. Guard test added for the two silent halves and verified to fail on each; tightening it exposed a hole in itself, `/predictionOff\(f\)/` matching the declaration so a count of 2 was met by the definition plus one caller. 641 tests pass. |
| 0.10.0  | 1     | **SEVEN COLOR SCHEMES ON A NAMED-COLOR LAYER, plus the outline pass beneath them - the minor digit rather than a patch because the LAYER is what later work builds on (Ken chose it when the release stopped and asked).** The naming pass converted 306 colour references and 8 CSS keywords and left zero literals below the palette; the six schemes after the first then cost tables of numbers and no stylesheet, which is the whole return and the argument for naming a colour before it is needed. Preceded by the boundary pass: **26 of the app’s 29 outlines failed the 3:1 a UI outline needs** - a response card stood out from the page by 1.14:1 - while the TEXT was never the problem at 15.8:1, which is the transferable lesson (readable and legible are different questions, and the first being answered hides the second). **Three kinds of colour hid from a hex sweep and all three shipped as real bugs that every test called clean**: a CSS keyword (`background: white`, so About Me was white-on-white in dark), a custom property declared outside the palette (every Express phrase invisible), and a token predating the palette; a fourth hid even from that - a button declaring NO background takes the BROWSER’s, fixed with `:where()` so the id could not outrank every class rule. **Whether a token needs a per-scheme value is decided by its VALUE, never by its NAME** - the first test carried a name list and excused six real colours. **Setting a border STYLE on something whose width is unset is a WIDTH change**: the colour-blind scheme moved every card 3px because `0px none` became `3px solid`, mentioned no width, and passed the test looking for one. "Colour-blind safe" is SIMULATED and scored rather than eyeballed (Vienot 1999 + CIE Lab), and the measurement CHANGED the design: four categories cannot be separated by hue alone under dichromacy while keeping colours that mean what they should, so the bar STYLE carries the category for tritanopia - shape coding was going to be skipped as redundant. Found in passing: the DISPREFERRED badge had been 4.24:1 white-on-amber for a year because nothing checked the badge. Measured across all seven: 518 text elements and ~600 outlines pass in each, and **47 rects identical across all seven, so one keyguard fits any scheme**. Also this release: **mip_opt_out=true on every Deepgram request** (hearing and speaking) after the provider survey found the per-request training opt-out - guarded by tests because its absence is completely invisible, the app behaving identically while the partner’s words become eligible for a training set. |
| 0.8.2   | 1     | **NO COMMAND BAR BUTTON IS A DEAD END, AND A SECOND CHECKER FOR THE DOCUMENTS (Ken, August 26 2026).** **Wrap up** (renamed from Wind down) and **Start conversation** are now toggles: one press puts their cards up, the next puts back exactly what was there, and nothing is spoken either way. Ken's report was that a stray tap on either was uncancellable — and in manual mode the mic is already off after you answered, so "wait for the partner to talk" was not an exit. **⚠ START CONVERSATION'S TEARDOWN IS DEFERRED TO OPENER SELECTION**, without which it could not honestly be a toggle: it used to stop listening, commit the partner's half-finished turn, discard the buffer, clear the screen, reset Partner/Feeling, close the conversation file and reset the engine on the press, so restoring the cards afterwards would have shown a palette belonging to a conversation that no longer existed. The deferred teardown **must run before the partner's pending speech is read**, or their words land in the new conversation where nobody said them; Ken accepted that risk explicitly, so the ordering carries a warning at the line. **ONE backout slot, not one per button** (from the openers you press Wrap up; a backout each would return you to the openers). Paging moved to New N — one button cannot enter a mode, cycle it and cancel it. `engine.resumeConversation()` is the shared "staying in this conversation" transition, deliberately not restoring the preceding floor. **Hold on became a placeholder the user fires themselves** (comment 76): same pool, same no-repeat rule, and **sharing `lastIndex` is the load-bearing half** — with its own state it could echo the phrase the app had just spoken, which is exactly when the button gets pressed. Its Controls-tab editor is gone; the fallback phrase stays in code so an emptied pool can never leave the button silent. **Two public faults fixed:** an error response was returned to the page as though it were the app (a sick host was worse than a dead one), now falling through to the cache with a 6s deadline that is dropped when nothing is cached; and the partner's choice buttons took the last cells of the WHOLE PANEL, which coincides with the Context band's reserved run only while Flex is empty — the shipped default, which is why it looked right. **Express Panel setup** reordered to the panel's own order, tab renamed **Express Panel and Keyboard**, situations list became a dropdown with an X (full-height buttons grew downward without limit), the feeling mark went diamond→square (a solid diamond is a rotated square and read as the partner's circle), and **Report a problem** gained a confirming Clear. **`scripts/doc-tests/check-docs.py` ("check docs")** is the documentation counterpart to the test suite, 20 rules, run as the last step of every "sync docs"; the spelling word lists moved to `writing-conventions.json` and are now read by both it and `american-spelling.test.mjs`, since two copies of that list would drift. Six .docx tools alongside it. **The Windows manual was rebuilt against Ken's 64 comments** and passes with zero errors. 578 tests (up from 575). |
| 0.8.1   | 1     | **THE HOLDING PHRASES BECOME USER-OWNED, AND STOP BEING SILENCED BY THE COMPOSER (Ken, August 25 2026).** `placeholder-phrases.js` is the model (data-folder file + cache, v0.2.25 file-in-folder-wins, a `seeded` watermark for additive merge) and `placeholder-editor.js` the editor, on a new **Placeholders** tab that also takes the three timings from Conversation, renamed **Timing and count**. `data/placeholders.json` is deleted: the defaults live in the model, so there is one source of truth rather than a bundled file and an inline fallback that could disagree. `placeholders.js` reads the pools **at the moment of speaking** rather than caching them at arm(), so an edit lands on the next phrase; blanks are dropped there and not in the model, because the editor needs an empty row to type into. Each row carries a speaker — the same argument that put one on the Express rows and the About Me answers. **⚠ KEN REVERSED HIS OWN AUGUST 20 INSTRUCTION: “In my own words” no longer silences the ladder.** His rule is that **composing is the equivalent of reading the offered cards**, so it follows the same rules; and the old behavior was worse than merely late, because the gate DEFERS rather than counting, so no phrase was ever spent and the silence had no end — which left the longest gaps in the app unfilled. Both mechanisms removed (`composerOpen` out of `setUserSpeakingGate`; `openComposer` no longer aborts). `openComposer` was the odd caller out: the other three aborts are Say again, Hold on and a choice chip, every one of them the user having ACTED. **Response cards: the full wording and the short label are sized apart** (`--hint-font-scale`), each scale following its form of words rather than the slot it occupies, so “the short version” is one size across all four card modes. **Three faults found on the way, two of them pre-existing.** (1) The square-icon-button rule names `#controlEditor` by name, so the new editor's four tools took the Express-derived ~100px width and **overflowed the row** — the `.ee-swatches` lesson again, and the stylesheet now says every one of those selectors must name both editors. (2) **The Settings tab column never scrolled**: About was already off the bottom of a 640px-tall screen at twelve tabs, and a thirteenth took Troubleshooting with it — no cut edge, no scrollbar, nothing to say the list continued. (3) The editor first shared the tab's section-memory scope, so “Timing and count” and the first pool were **one key** (`sections.js` remembers by position within a named scope); `expressBands` beside the `express` tab is the precedent it now follows. **Guards:** the model test drives editor storage through to the words actually spoken (the layers-crossing rule), a CSS test asserts each card scale is wired to its own form of words, and **two source-level tests** stop the composer silence returning — source-level because the decision lives in `app.js`, which no test can load, and because both old mechanisms were **silent**. 569 tests (up from 558). |
| 0.8.0   | 1     | **THE EXPRESS PANEL BECOMES THREE BANDS (Ken, August 23 2026)** — the design settled over the preceding days, then built. `express-bands.js` is the pure part (sizing arithmetic, fill order); `ui.js` still receives ONE ordered list plus a parallel list of band names, so the renderer never had to learn about bands to place a button. **SIZING: the user sets Context and Flex; ALWAYS takes the remainder**, so an untouched panel is almost exactly the old one. Always has **no floor** — a panel given over to steering the AI is a legitimate panel. **⚠ THE OVERFLOW DIRECTION MATTERS AND I HAD IT BACKWARDS UNTIL KEN CAUGHT IT:** an Always phrase with no room queues at the **END** of the Flex band, behind everything the situational lists supplied, and can **never** displace a situational phrase. **The Context floor of four is unconditional** (a menu must never have to drop one of the partner's own alternatives) and reverses the July 2026 "no standing reservation" decision — right when those cells had no resting job, wrong once they hold partner/place/feeling buttons. **Flex is filled by FOUR situational lists**, most specific first, all made by one mechanism: pick a partner and a place, each offering *Anyone*/*Anyplace*, so the general list is simply Anyone+Anyplace. **SETTINGS**: the Express tab now holds the whole job in order — grid, Always, Flex, Context, then sizes and look last; button size/spacing moved to a renamed **Button Sizes** tab. **The editor is a LIST again, which is not a regression:** the two things that made rows overflow are gone (color comes from the band, so the swatches are deleted; the tools left the rows for one fixed toolbar). Each phrase gained a **pronunciation box with a speaker** — a field the app has been able to use for months with nowhere to type it. A **temporary full Always set** ships (every phrase ever shipped as a default, minus feelings, which are Context buttons now); the therapists' set replaces it before beta. **A version-1 panel is discarded and reseeded once**, announced. **THE BUGS THIS CYCLE ARE MOSTLY MINE AND MOSTLY THE SAME SHAPE — a rule that was right for one surface applied to another where it was wrong.** (1) **The keyboard could not be dismissed on the Express tab**: `focusout` returns early whenever Settings is open, correct where a tap on Save must not dismiss it and fatal where the panel IS the work. Fixed with a **pointerdown** backstop rather than a focus rule, because whether a tap moves focus varies by browser and target. (2) **Side Layouts 2 and 8 end in a compose-only row**, so a one-row Context band got **zero** cells — and the floor rescue skipped it because it walked back from `indexOf(CONTEXT)`, which is `-1` exactly when the band is empty. Computed from the **boundary** now. (3) **A compose-only row was counted as one of the rows asked for**, so two rows gave one. **Rows of BUTTONS are what the user counts.** (4) **The floor is made up by WHOLE ROWS**, not borrowed cells, which left a row half Always and half Context — ragged, in the one mode chosen for its straight edge. Asserted across **all 21 layouts**, not a sample, because a sample is how S8 was missed twice. (5) **Two Express tab sections shared one memory key** (`sections.js` remembers by position within a named scope, and the tab and the editor both used `express`), so changing the unit of measure opened Flex and closed Band sizes — apparent randomness. (6) **The + / − steppers dispatched only `input`**; the band boxes listen for `change`, so they moved the number and committed nothing. (7) **12rem of dropdown scroll room on `#settingsContent` cost About Me 168px** — `height:100%` resolves against the content box — and moving it to the panel then had to out-specify the Express tab's own `padding:0`. Also: reserved choice cells now **name themselves** ("Choice button #1"), the compose icon **sizes to its cell**, Settings sections became an **accordion**, and the number button reads **123** with no dashed edge. 558 tests (up from 528). |
| 0.7.16  | 1     | **One compose button per layout, and the prediction toolbar removed (Ken, August 23 2026).** Ken asked whether layouts have different numbers of slots. **18 of 21 give exactly 32**; the exceptions are **B9 (42)** and **S10 (41)**, which carry a permanent row of digits, and **B6 (33)**, which has a second backspace. Checking that turned up two things. **(1) ⚠ BOTTOM LAYOUT 8 DREW TWO “IN MY OWN WORDS” BUTTONS.** It is a split keyboard with a space key per thumb, and the panel turned EACH space key into the compose button - verified in the shipped renderer, not inferred. Only the first is compose now; the second is an ordinary position, so **B8 goes 32 → 33 slots**. **⚠ THE CAUSE IS THE SAME SHAPE AS THE 0.7.14 WHITELIST BUG: THREE FILES EACH DECIDED INDEPENDENTLY** which cell was compose, which were spacers and which were usable - the renderer, `expressCellCount`, and the prototype - so any one could drift and nothing would say so. **`panelRoles()` in `keyboard-layouts.js` is now the single place that decides**, and the renderer, the counter, the tests and the prototype all ask it; the count and the drawing can no longer disagree. **(2) The word-prediction CELL TYPE was defined and never used by any layout**, while four places carefully handled a kind that could not occur - removed, with a test asserting no layout carries one. **Also removed: the prediction BUTTON OVERLAY in `keyboard.js`** - built, filled and hidden by CSS since June 2026, replaced by the inline ghost in v0.5.42, and still running an update pass on every keystroke. ~60 lines of JS and ~50 of CSS, including two selector exclusion lists naming a class that no longer exists. **Prediction itself is untouched and was checked rather than assumed:** typing “Th” in the compose box still offers the ghost “The”. **Prototype:** `prototypes/express-bands.html` gained the app's REAL grid (all 21 layouts by name, geometry generated by `prototypes/refresh-layouts.mjs`) and the real compose key, which belongs to no band and cannot be pushed off. **It briefly restated the compose rule itself and so kept drawing two buttons after the app stopped** - it now takes the roles from `panelRoles()` directly, removing the second opinion rather than correcting it. **The prototype's own findings:** the compose key eats positions from whichever band's row it lands in (B2 leaves the Context band **2** positions, below the floor of 4); **B3 cannot hold three bands at all** (3 rows, 2+1+1); and **a band size in ROWS does not travel** between layouts running 3 to 11 rows. 528 tests (up from 525). |
| 0.7.15  | 1     | **The number button 0.7.14 ANNOUNCED and never delivered (Ken tried the exact scenario from the release note and there was no button, August 23 2026).** **⚠ `engine.ingestClassification` REBUILDS `state.lastClassification` FIELD BY FIELD** rather than passing the model's object through, so `offered_range` was **thrown away between the parser and the panel, with no error and nothing to notice.** One line to fix; the finding is not the line. **THE MODEL WAS RIGHT THE WHOLE TIME** — checked live against all four cases including Ken's exact words: *“how many would you like?”* → a count with no top, *“one to ten”* → 1–10, *“mild, moderate, or severe?”* → a three-way CHOICE and not a range, *“how are you today?”* → neither. **⚠ THE VERIFICATION LESSON, WHICH GENERALIZES BEYOND THIS FEATURE: BOTH ENDS WERE TESTED AND THE MIDDLE WAS NOT.** The parser was tested by handing it JSON; the renderer by handing it a chip; the model was correct. Three of four links worked and the feature was dead. **Testing the ends of a chain is exactly what makes the untested link look proven.** When a feature spans layers, ONE test must cross all of them, or a browser check must exercise the real path. **The whitelist is now guarded** by a test that asserts every classification field survives the engine, verified to fail when the line is removed — and **every new classification field must be added there in the same change**. The same silent-drop shape exists anywhere a layer copies a known set of fields out of a richer object (the settings bundle, the weekly-report payload, the conversation record); those are guarded, this one was not. Also this cycle, not shipped to users: **`prototypes/express-bands.html`**, a tappable throwaway (outside `app/`, so it never deploys) for the Express Panel band design — pick a partner and place, resize the bands, watch the Flex band refill. **Its churn measure had to be corrected before it was useful:** counting “buttons outside the Flex band” LIES, because the Context band churns constantly by design and none of it is a cost — the honest count is **buttons that SPEAK**. It immediately surfaced that **the Context band overflows at three people** (3 partners + 3 places + 3 feelings fills a 9-wide row and pushes “In my own words” off) and that **a 3-column side dock cannot hold it in one row**. 524 tests (up from 522). |
| 0.7.14  | 1     | **Answering a NUMBER, and the Express Panel design reworked around Ken's corrections (August 22 2026).** **A SCALE IS NOT A CLOSED SET** — ten buttons reading 1 to 10 is not something anyone can scan mid-conversation — so classification gained **`offered_range`** alongside `offered_options`, with the prompt told explicitly not to enumerate one into the other and given the counter-example that goes the OTHER way (“poor, fair, good, excellent” IS a closed set). One **non-speaking** button opens “In my own words” with the keyboard already on its number page; **Enter speaks the number**, via a `setEnterAction` hook claimed for that one surface and cleared on close. **The display was already there** (the composer has a box and a Speak button, the keyboard has a number page), which is why this was small. **⚠ THE BUG WORTH REMEMBERING: `Number(null)` is 0, not NaN**, so “how many?” with no stated top parsed as a range of 1 to 0 until the missing end was rejected BEFORE the conversion — caught by a test, not by reading. Verified live: the button renders and taps through with no speaking behavior, the keyboard opens showing all ten digits, Enter fires the override once and reverts to a line break after close. **Design work, documents only:** Ken read the Express Panel design and corrected six things. **Bands renamed Always / Flex / Context**, organized by his test — **the Context band is the one whose buttons NEVER SPEAK** (which also explains why choice buttons already ignored the double-tap safeguard: it guards *speaking*). **Choices at the FRONT of that band in solid color** for prominence. **⚠ ROLES REMOVED — Ken killed the idea and was right**: a fixed role per position asks the user to think in someone else's categories and RESTRICTS them (four things to say, no fourth-role phrase, a hole). Replaced by ordering each list by likelihood and filling partner-then-place-then-general; the only promise is *the most likely thing is first*. **⚠ THE KEYGUARD ARGUMENT AGAINST FOLDERS WAS ALSO WRONG** (a keyguard is a barrier with no labels; paging changes what is behind a hole, never where the hole is) — **both dead ideas kept in the document as retractions rather than deleted, so neither gets re-derived.** Four figures added. **British VOCABULARY is now a rule and a test**, after “Mum” and “the surgery” reached a document: every entry in the existing spelling list is a MISSPELLING, so a correctly spelled wrong word had nothing checking it — and the new list is necessarily SHORT, because *surgery*, *tablets*, *torch*, *boot*, *flat* and *lift* are all real American words. 522 tests (up from 519). |
| 0.7.13  | 1     | **The partner's offered choices moved to the BOTTOM of the Express Panel (Ken, August 22 2026: *“transient buttons… should appear and disappear from the bottom of the express panel so they don't push other buttons around - at most, they push the last buttons temporarily off the panel”*).** A shipped fault, not a new feature. Chips claimed the LEADING cells and pushed every item along, and **the panel ships with six feelings first**, so a three-way menu **moved every phrase three places for the whole turn and dropped the last few off the end** — currently including the device-disclosure button. For that turn nothing was where the user had learned it, which is spatial stability broken across the WHOLE panel to make room for something lasting one turn. **`chipStartIndex` (`express-items.js`, 4 tests) puts them at the last cells, and the renderer now walks item cells by a FIXED ORDINAL** — the property that matters is that an item's cell can no longer depend on how many chips are showing. **Measured in the running app with three alternatives: ZERO changes to any cell's position, size or shape, and exactly three cells changed what they held** (29, 31, 32 — 30 is the compose key, correctly skipped). **They flow AROUND the compose key like any phrase**: a three-way set cannot be contiguous at the end of every layout, and pushing them further up to force contiguity would start moving phrases again, which is the thing being fixed. **How it was found is the transferable part** — not from a report (nobody has reported it; most likely no tester has yet had a closed-set turn with a full panel) but by writing the Express Panel design document, where it surfaced as a consequence of an UNBUILT feature. **Filing a shipped fault inside an unbuilt feature's design is how it stays shipped**, so it was pulled out and fixed on its own. Also this cycle, documents only: **`Conversant AAC Express Panel Design.docx`** (new) — the no-folders decision with its seven grounds, and the three-band / layering / standing-role design for panel content that varies by partner and place. 519 tests (up from 515). |
| 0.7.12  | 1     | **The five misleading usage figures fixed, and a way to read the beta Sheet (Ken, August 21 2026).** Reading one real report turned up five statistics that were wrong or unreadable. **Four were ONE defect wearing different clothes: a figure counted over a smaller slice of history than the heading above it, with nothing saying so** — which made the fix far cheaper than recomputing anything: print what each figure was measured over. **(1) "Per week" is off the page.** It scaled the days actually used up to a whole week, so five days of light use read as 29 conversations a week — flattering exactly the tester who is drifting away, on the FIRST question the beta asks. The week-by-week table below it was already the honest version, making the line both wrong and redundant. Still computed (one test uses it to show what a cumulative figure hides) and **must not go back on the page**. **(2) The wait figure gets a 500ms floor and reports what it discarded.** Pre-July-2026 records wrote both halves of an exchange at the same moment, so the interval is an artifact; they cluster near zero and dragged the median to **0.2s against 22.0s** on turns recorded properly. **⚠ IT EXCLUDES BY TIME, NEVER BY WHERE THE REPLY CAME FROM** — restricting to card turns would have removed the artifacts too and quietly changed the number MEANS from "how long did they wait" to "how long did we take". The other person waits whether the reply was chosen, typed or tapped, and typing is slowest. A test guards it. **(3)(4) The two splits print their own base.** Where the words came from and which kind of reply have only been recorded since August, so against a total covering all history they read as a contradiction (**172 against 17** in the real report) with nothing to say which was wrong. Neither was. **(5) The error line names its period**, because the weekly report carries a DIFFERENT count under the same word. **Mostly self-cleaning, and worth knowing before judging a similar finding: all four era problems exist because ONE device has months of old records.** A tester starting on the current version has correct figures from day one; the contaminated reader was Ken’s own. **NEW — "evaluate beta"** (`scripts/evaluate-beta.mjs` + three pure modules, 13 tests) reads a CSV export of the reports Sheet. **⚠ THE ARITHMETIC RULE SHAPING ALL OF IT: MEDIANS CANNOT BE POOLED** — a report carries each tester’s median, and the middle of five middles is not the middle of everyone’s replies. So every pooled figure is a sum or a **ratio of two sums**, which improved the answer: the founding question is not "what is the typical wait" but **"how often did they wait longer than they will tolerate"**, and that pools exactly right. **⚠ AND WHAT MAKES AGGREGATION HONEST AT N≈5: pooling across TESTERS and across TURNS are different acts.** Five people cannot be averaged as people; those five produce thousands of turns, and a CONFIGURATION question is a turns question — so it groups by device, recognizer, voice and version, with no "average tester" anywhere. **Scrubbed by construction**: the renderer never reads partner or place names, so they cannot be printed. **Ken’s own devices set aside; unnamed reports LISTED for chasing**, since one quietly binned turns that person into someone who appears to have stopped. **⚠ Reports are never added together** (each re-counts the whole history) **except errors, which must be** — and those are deduplicated because a pre-0.7.11 report carried everything and the first report after upgrading still carries the backlog. **It reads the RAW column, not the flat ones**, so a number added to the app appears there next week with no Apps Script redeploy. **Error alerting live:** threshold 5 → **1** (with a handful of testers the interesting event is a KIND of error appearing for the first time, which arrives as a one), mailing ken@volksswitch.org, which also receives problem reports on arrival. 515 tests (up from 497). |
| 0.7.11  | 1     | **Error reporting audited against what the documents promise; three faults fixed, and the transcript learns to keep its own history.** Ken asked for the error path to be checked against expectation. Three of six held; three did not. **⚠ THE ROOT OF TWO OF THEM: an error lives in TWO STORES WITH DIFFERENT LIFETIMES** — a browser-held ring buffer and the conversation's own file on disk. The browser copy is origin-scoped, and **the August 1 2026 address move emptied it for every tester at once while the disk copy survived**, which is why a real report read *“Errors recorded 7”* in the summary and *“(no errors recorded)”* three lines below. **(1) The weekly report re-sent the entire ring buffer every time.** Nothing cleared it, so a week with ONE new error reported THREE (measured). Beyond noise: the Sheet's error column climbs on its own, **a tester whose problems have STOPPED still looks like they are accumulating**, and an alert threshold fires once and then every week after. A `weeklyErrorMark` now records the newest error already sent. **A timestamp, not a count** — the buffer drops its oldest at 200, so an index would slide underneath itself; **advanced on ENQUEUE**, matching `weeklySendLastAt`; **an entry with no timestamp counts as NEW**, because a duplicate is noise while a dropped error loses the only record it happened; and **a mark, not a purge** — the full list stays on the device for problem reports. The disclosure text was updated with it, since that text IS the disclosure. **(2) A problem report now falls back to the conversations on disk** when the browser list is empty, and says it did. **The contradiction was never the damage: transcripts hang off that list**, so an empty list attached NONE — losing the one thing that could explain the complaint, exactly when it was needed. **(3) The privacy wording was overstated in three documents.** Both manuals said a private conversation *“leaves no record behind”*; the Beta Test Plan said *“nothing from it is written down”*. The speech IS stripped (verified with real speech in the payload), but an error entry naming the context and conversation id is still written. Ken chose to correct the wording rather than the behaviour — knowing an error happened is worth keeping and carries no content. **Verified as promised and now stated so they stay true:** speech stripped from a private conversation; **no speech reaches the weekly report under ANY setting**, because `redactErrors` REBUILDS each entry from four fields rather than filtering one out — the safer construction; the red wash fires on every logged error. **Also: a partner turn keeps its own history.** Ken asked whether the transcript records the partner's speech as it grows. It did not — each pause overwrote the raw line and the FIRST pause's timestamp was kept, so a turn spanning four pauses was one line with one time. **That left the two records unable to answer even together:** the event trace has the pause and reprompt structure but **no words** (deliberate, Aug 5 2026), the transcript had words but no structure. A `revisions` array now holds one timestamped entry per pause; `rawTranscript`/`cleanedTranscript` keep their meaning so **every existing reader is untouched**. A pause that heard nothing new is not recorded; the finalize path records the last state, which can be text no pause ever saw; **trimming at the 20 cap drops the SECOND entry, never the first**, because the first is what the app acted on when it asked the AI initially. **⚠ AND A CAVEAT RETIRED THAT WAS NEVER TRUE.** Every conversation-logging change here had been signed off with *“the on-disk write needs a granted data folder, which the preview cannot produce.”* Ken asked why one could not simply be created. **The folder was never the obstacle:** the File System Access API hands one to the browser only through a native dialog no automation can click — but that is not the only root `storage.js` accepts. **With no folder picker present it adopts the browser's own private filesystem, which needs no gesture**, and that is the iPad path through identical code. `tests/storage-transcript.test.mjs` now drives the REAL storage layer and asserts **the bytes written**: the pending turn tracked across calls, the flush producing valid JSON, a later turn appending rather than overwriting, an error interleaved in time order, the revisions surviving the round trip, and **“Don't save this conversation” proven by an unchanged file**. **The lesson: a caveat repeated often enough stops being examined — when a limitation is stated the same way twice, check it was ever true.** 497 tests (up from 478). |
| 0.7.4   | 1     | **The Express Panel editor stops hiding the panel it is editing (Ken, August 15 2026, from the same SLP call).** (1) **Selecting a button focused its first field**, which on any defined button is a text box — so the on-screen keyboard came up at once and, sharing the dock band, covered the panel being edited. Measured: keyboard 384x720 sitting exactly on the panel's 384x720. Add before / Add after / Move up / Move down are all judged against the panel, so they became guesswork. Nothing focuses a text box now. **An undefined slot still focuses its four type buttons** (they raise no keyboard), and the selector is `.ee-fields .ee-add` specifically rather than "the first thing that is not an input" — on a phrase that would have landed on the first color swatch, which wears a focus ring and would read as a color having been chosen. (2) **Enter in any editor field blurs and hides the keyboard.** The text commits on every keystroke, so this changes only what can be SEEN; it serves both keyboards, since the on-screen Enter key dispatches the same keydown. (3) **The fixed color on a Partner / Feeling / Place card was a `<span>` drawn at the full 56px touch-target size with the same border and radius as the seven tappable swatches on a phrase card** — pixel-identical to a control, inert when pressed, with `cursor: default` the only difference, which cannot be seen on a touch screen. Now 15px, deliberately below `--btn-min-dim`, so on a surface where every control is at least that big it reads as a sample. **⚠ Ken could not recall the state when he saw it**, so this is the reproduced candidate rather than a confirmed diagnosis; a phrase card's seven swatches were checked and are neither clipped nor overflowing in either dock. |
| 0.7.3   | 1     | **Express Panel buttons are defined in Settings and nowhere else, from an SLP problem report (Ken, August 15 2026).** (1) **Phrase buttons looked dead in the Settings Express tab while feeling buttons worked** — her tap mode was double, and **only phrases honor that setting**, so a single click merely armed the cell and expired 400ms later. The safeguard guards SPEAKING, and a tap in Settings does not speak, so it was protecting against nothing there while blocking the only thing she was there to do. The panel is now single-tap while hosted in Settings; the dock is untouched. **`hostExpressPanel` re-renders unconditionally** — tap behavior is decided when the handler is BOUND, so the panel had kept whichever handlers it was last drawn with. (2) **`revealSetting` did not exist anywhere** and was called by the pre-start "Add an API key" button, which therefore threw and left the user on a list of closed headings — the exact failure Rule 18 warned all-closed sections would cause. Implemented: switch to the control's tab (which also builds that tab's sections), open every section above it, do NOT focus it (a focused text field raises the on-screen keyboard over the panel). (3) **Tap-to-define is Settings-only now (Ken), superseding the August 7 rule** that allowed it from the dock between conversations: a filled cell SPEAKS on tap, this population taps imprecisely, and a neighbour that throws open a full-screen editor is the wrong thing to have on the conversation surface at all, not merely at the wrong moment. Outside Settings the cell carries **no handler and no promise in its label**; measured identical in both hosts (142.23 x 53.79, same coordinates), so no keyguard hole moves. `conversationInProgress()` existed only for the old gate and is removed. **Found while verifying it:** a turn reaches the history only after its speech finishes, so an opening Express phrase left a window as long as the utterance in which a conversation had visibly begun and the predicate was still false. |
| 0.7.2   | 1     | **The Settings panel is reorganized: one column, collapsible sections, and a one-button-at-a-time Express editor (Ken, August 11 2026).** (1) **Multi-column is gone** — it moved controls around as the panel resized and had a failure mode with no symptom (an item too wide for its column is painted OVER the next column's controls, with nothing reporting it). `applyColumnFallback` and the `.single-column` class go with it. (2) **Every group with a heading is a disclosure**, all closed on arrival, opening one leaves the others alone, and open state is kept for the session. Sections **nest**, and a section may hold sub-sections AND loose controls — "Button sizes and gaps" holds the three sliders plus the Reset button *at the same indent*, which is what says it acts on all of them (Ken); same shape for "Screen openings". Every control now sits in a section. Shared `sections.js` also sections the **Controls** tab. **⚠ The heading must be a `<span>`, not the group's `<label>`: a click whose target is a label inside a `<summary>` does NOT toggle the details**, so the arrow and the background opened a section and its words did not. (3) **Express Panel editor reworked** — the live panel beside Settings *is* the list, so the tab shows an invitation and then one button's properties plus add before / add after / move up / move down / delete / done. The card is a column, which is what fixes the narrow-tab overlap Ken photographed; `flex-wrap` on the swatch row had never engaged because a `flex: 0 0 auto` item sizes to max-content. Adding to a full panel warns and names the button that will be deleted, and **removes it BEFORE the insert** — trimming after would discard the new slot on "add after the last button". Delete is confirmed and disabled on an empty slot. **⚠ `.ee-row`/`.ee-tools` are shared with the Controls editor**, so the card styling is scoped to `#expressEditor`. (4) **Spoken-help fixes:** all-closed silently broke every "take me to that setting" jump (`revealSetting`); `resolveTap` no longer invents a key for an id-less control, which had made all six Controls headings resolve to nothing; and the coverage test now also scans the sections `control-phrases-editor` builds at runtime — it read only `index.html`, which is how they shipped silent. (5) Tab intro paragraphs get their own style (1.05rem #333) instead of the 0.8rem #777 status style. |
| 0.7.0   | 1     | **The voice layer and the beta instrumentation, the two subsystems that make this a minor (Ken, August 8 2026).** **SOUNDS LIKE ME, Phases 0-2** — the app had no voice-modelling layer at all. Phase 0 put a voice block behind the prompt's long-standing "you speak AS the user" claim; Phase 1 is **Sound Check**, a forced-choice module whose wording Ken settled over two rejected drafts ("happy to have said" admitted a threshold reading and stopped discriminating; "would you rather say" is faintly nonsensical about a situation that never happened) landing on *"sounds most like something you would say in response"* — comparative, item-level, and answerable about a hypothetical; Phase 2 harvests voice from real logs, with **selections promoted to the preference authority and barred as style exemplars**. The plan itself was rewritten after Ken challenged its central claim: writing style is not speaking style (Biber 1988), and the cost argument was backwards, since a writing sample is distorted by the effort of producing it. **BETA INSTRUMENTATION** — the Troubleshooting tab, conversation-log aggregation, problem reports, system info and an automatic weekly send, inert until an endpoint is set. **Also:** prompt caching (the org's hit rate was zero — one line of ordering, and the cost display had to change or it would have under-reported the bill by the hit rate); Tier B completed (humor, beliefs, register by group, conflict style) after finding that a DESCRIPTION of humor reliably fails to be funny where three chosen exemplars succeed on the first try; the per-partner profile on the me-to-person edge; the app no longer answering general-knowledge questions in the user's voice, with declared expertise as the only exception; placeholders decoupled from the AI round-trip (they had been gated on it since June, so lowering the delay alone would have changed nothing) and the silence period cut to 0.5s; undefined Express Panel cells rendered and tappable to define a button in place; and a **pronunciation layer** — measured against a real Aura-2 voice first, which overturned two documented pause claims — applied at the synthesiser and nowhere else, with the spoken form, the voice used and the recogniser now recorded per turn so a replay can reproduce what was actually heard and said. |
| 0.6.7   | 1     | **Where you are is the SETTING, not the subject (Ken, August 5 2026).** Found in the demo script, which tapped the comic shop and then treated the resulting comics talk as the feature working: the place button stands in for the GPS we do not have, and it had drifted into a way to frame the topic. The prompt never said otherwise, and the fact list leaked in that direction on its own — details with no stated purpose read as material to work in, the same defect found in the privacy blocks in August. **The correction was then over-corrected and fixed again**, which is the part worth keeping: the first cut gated the facts behind "only when the partner raises it", a clause borrowed from the privacy blocks where it is right and here is wrong. Those facts are not private, they are **ambient**, and withholding them costs exactly the competence the feature exists to give. Ken’s formulation is now the prompt nearly verbatim — *the purpose of the place should inform the suggestions, but it should not become the topic of discussion* — with his own failure example quoted in it ("what did you find here last Saturday?", asked while standing in the shop), since a concrete failure instructs better than a rule. His "a recorded fact is meaningless when you are standing in it" also exposed a **second bug**: the current place was being sent **twice** under two contradicting framings, and the standing "Places I go — When: Saturdays" entry is precisely what generates the last-Saturday question, so `buildBlock` now omits the place the user is in. The partner button carries the matching line (the person being spoken TO, not a topic to raise). Framing keeps its own channel: it arrives through Reframe today, and through context-scoped Express Panel buttons later. |
| 0.6.6   | 1     | **The Listen button stops losing the partner's words (Ken, August 5 2026).** Two fixes to the same symptom, found from opposite ends. (1) **The `capturing` fix** — `handleSttStatus` treated every unrecognized status as *not listening*, silently; the paid backend emits `'capturing'` when the voice gate opens, so the indicator went dark exactly when it was most true, and the next tap took the START branch. It is now a whitelist: only an explicit `stopped`/`error` turns listening off, and an unknown status leaves the state alone and is logged. (2) **Stop/start mid-turn is a PAUSE, not a turn boundary** — `stt.js` splits the mic-opening into `openSource()`, shared by `startListening()` (which resets the transcript) and a new `resumeListening()` (which does not); `app.js` takes the resume path via `resumePartnerCapture()` when `heardPartnerText()` is non-empty. **The invariant: a partner turn ends at a FLOOR CHANGE — a response picked, a pardon, or end of conversation — never at a mic toggle.** Diagnosed from Ken's scripted `LISTEN-TEST.md` run: the saved conversation file kept the *first* checkpoint's timestamp while carrying the *second* checkpoint's text, proving the record was reused and overwritten rather than re-created, so the loss was both a cleared buffer and a shortened line. Pinned by `tests/stt.test.mjs` ("LISTEN-TEST Test 3 == Test 4"). (3) **The diagnostic trace was added AND removed inside this cycle** and never reached a release — Ken's call, to be revisited pre-beta as a Troubleshooting tab against whatever the engine has become by then; see Open Questions. |
| 0.6.5   | 1     | **Settings stays in front of fullscreen; "Use the whole screen" withdrawn on iPad; and the model is told its words are SPOKEN and must not be coarse (Ken, August 3 2026).** (1) Toggling fullscreen from inside Settings drew the whole conversation screen OVER the panel — not z-index but the **top layer**, which paints in the order things were added to it: `showModal()` promoted Settings, the toggle then promoted `document.documentElement` above it. Ken's own "closing and reopening resets it" was the tell, and the fix re-promotes the dialog in place on `fullscreenchange`. Applied immediately rather than deferred, because the Keyguard Design tab reads `isReallyFullscreen()` and Generate Screen Openings measures the LIVE layout, both from inside Settings. (2) **"Use the whole screen" is no longer offered on WebKit** — measured on Ken's iPad (iPadOS 26.6): nothing to hide in a Home Screen app (fullscreen does not displace the iOS status bar) and it breaks Settings and the Listen button in a Safari tab, where the only control that could turn it off is the one that stops working. The *request* is gated, not just the checkbox, so a stored `true` goes inert. **Rule 19's "ONE path for every platform" ambition did not survive the device: the capability check PASSED and the feature was still useless in one mode and destructive in the other.** (3) The generation prompt never said its output would be **SPOKEN ALOUD** — the Practice-partner prompt had always said so, which is the wrong way round — so a PREFERRED card read "I fw it" (texting shorthand, unsayable by a synthesizer). Speakability rule added, deliberately **not** a register rule. (4) Separately, **no vulgarity**, absolute for now; the eventual control is **per-identified-partner** and is blocked on guardian approval. The load-bearing half is the refusal to *infer* permission — the offending turn had `partner: null` and an age in the profile, and an age with no instruction attached is worse than no age at all. (5) Deepgram: the Listen button no longer reports "listening" before the socket opens (violating a contract `stt.js` documents), `ws.onclose` now carries the close code and reason, and the **Test button opens the socket the app actually opens** — it had been connecting with `?model=` alone, so it could report a working key while listening failed, sending the diagnosis in the wrong direction. |
| 0.6.4   | 1     | **"Use the whole screen" reversed to default OFF, and the keyguard title-bar field made self-managing (Ken, August 3 2026).** (1) The setting shipped ON one release earlier; Ken lived with it and reversed it, and the reasoning is the durable part: **fullscreen cannot keep a title bar** (the API is all-or-nothing), **maximising the window is the better deal for most people** — it costs only the title bar's height, keeps the close button, and with the taskbar auto-hidden already reaches the screen edges — and **starting OFF teaches the control as an expand/contract toggle** rather than a state the user arrives in and must find their way out of. Explicit 0.6.3 choices are respected either way; only "never touched it" changes meaning. (2) **`generateScreenOpenings` now takes the title-bar offset from `isReallyFullscreen()`, not from the input field**, which is disabled with a live status while fullscreen is in effect. The real reason is not a stale value (the field has no storage accessor and resets to 0 every load — an overstatement I made and corrected): it is that **the openings are measured LIVE**, so a file is only ever valid for the mode the app is in, and a title-bar height entered while fullscreen would emit fullscreen positions plus a phantom bar. The predicate reads `document.fullscreenElement`, never the setting, since the two come apart when a request is refused or Esc is pressed. (3) **Docs**: all four ⚠ documents synced for 0.6.2–0.6.3 (My Places through both overviews and both manuals; a `<add a description>` placeholder in the Express Panel table filled; the circular privacy phrasing corrected in both overviews), the Windows manual gained the **taskbar auto-hide** step that makes a full-screen capture match the size the openings file quotes, the **Marc Delgado persona** gained a My Places section with deliberately heterogeneous fact keys, and the **demo script** now uses Marc as its user throughout. |
| 0.6.3   | 1     | **My Places, plus a fullscreen setting and three layout fixes (Ken, August 3 2026).** (1) **My Places** — a new `places.js` model (own `places.json` + cache, file-in-folder-wins), where a place is a name plus **arbitrary named facts**: no fixed field set, because a coffee shop, a clinic and a cousin's house share almost nothing, so "Location" is itself just a fact. Facts are an ordered array, so the user controls their order and duplicate keys survive. Two blocks split by what changes when — `buildBlock()` lists every place as standing context, `buildHereBlock()` names the tapped place and repeats its facts. A new Express Panel **place** toggle (olive) makes this **Phase-2 situational awareness without GPS**: the user taps where they are. Turns are stamped with the place; unlike Partner and Feeling it **persists across End conversation** (Ken — ending a conversation does not move you). (2) **"Use the whole screen"** (Settings → General, default **on**) — Fullscreen API on the Start tap, capability-gated so one path serves every platform, with a `display_override` manifest hint. Measured: it moves every keyguard hole, which is why it is a setting. (3) **Privacy wording corrected in all three models** — the rule ended "only include them if the user's chosen response requires it", which names nothing that exists when the options are written; it now names the two real prompts (the partner asks, or the user steers via Reframe), guarded by tests. A stale A5 module note describing pre-v0.3.12 behavior was fixed too, and both User Manuals corrected in place. (4) **Express Panel settings tab no longer scrolls horizontally** — the cause was Rule 15 reaching the editor's swatches and icon buttons (99.6px each where the design says 1.7rem/2rem), not the phrase field, which could already shrink to zero. (5) **A Settings tab whose controls would overlap drops to a single column** (Rule 18) — CSS multi-column paints a too-wide item over the next column and reports nothing. (6) **Hover no longer sticks on a touch screen** (Rule 20) — 44 `:hover` rules had no `@media (hover: hover)` guard, so a tapped button kept its dark state; the thirteen conversation-surface rules are now gated, `:focus-visible` left global. (7) **A tapped choice chip shows as chosen** (Rule 21) — the steering was already re-sent by "New N", but nothing on screen said so. |
| 0.6.1   | 1     | **Settings decluttered, and a mojibake bug repaired (Ken, August 1 2026).** (1) **All per-control explanatory text removed from the Settings panel** — 38 hint paragraphs from `index.html` plus the per-control help generated in `app.js`, `control-phrases-editor.js` and `express-editor.js`. Ken: *"way too much explanatory (hint) text… remove all text associated with individual controls."* A tooltip is not the alternative — it needs hover and this app targets direct select — so the text moved to the manuals, staged verbatim in `SETTINGS-HELP-TEXT.md`. Tab-level intros, live status regions, count spans and empty-state messages were kept, because none of them is explanation. Recorded as **UI Layout Rule 14**. (2) **Mojibake repaired in `app.js` and `sw.js`** — em dashes, curly quotes, bullets and box-drawing characters were rendering as double-encoded sequences in Settings on both platforms, including the redacted API key. Cause: a past version bump rewrote exactly those two files through PowerShell `Get-Content`/`Set-Content`, which on PS 5.1 decodes UTF-8 as Windows-1252 and adds a BOM. Cosmetic only — `keyFieldValue()` guards on a dataset flag, not the placeholder string, so no key could ever be overwritten. `RELEASING.md` now warns both bump steps off that path with a copy-pasteable check. |
| 0.5.94  | 1     | **API-key feedback: missing-key prompt, format warning, live Test button (Ken, July 17 2026).** Ken (testing the 0.5.93 deploy) had no visible cue for a missing key and no error for an incomplete paste; the User Manual §3.2 promised a red invalid-key warning that was never built, and the "No API key set" message went to the hidden status bar (v0.5.2). Built three surfaces: (1) a pre-start **"no API key" prompt** (`#apiKeyPrompt` over the transcript region, amber, with an Open Settings button; shows when no key is saved, informational — the user can still Start and use manual AAC); (2) a **format warning under the API Key field** (`llm.validateKeyFormat`, no network — flags missing `sk-ant-` prefix / spaces / too-short as you type); (3) a **"Test" button** (`llm.testApiKey` → `GET /v1/models`, bills no tokens) that reports "working" / "rejected" — the only surface that catches a subtly-truncated key. Verified in preview incl. a live 401 for a bogus key via browser CORS; 119/119 tests. Docs follow-up deferred: User Manual §3.2 wording + Test-button/prompt mention. |
| 0.5.93  | 1     | **Wind-down and closing split into two card categories; static palettes paginate (Ken, July 17 2026).** Wind-down statements ("I should get going.") and closings ("Bye!") are now distinct steps of the CA closing sequence. The **Wind down** button shows wind-down statements; selecting one auto-offers the goodbyes (`engine.showClosings()`); a partner-initiated close (AI `CLOSING` or the `looksLikeClosing` fast-path) also shows the goodbyes. Re-pressing **Wind down** re-offers wind-downs — no escalation, just "a different 4": openers / wind-downs / closings now **paginate**, and both the **"New N"** button and a Wind-down re-press dip to the next page of the defined set (wrapping, no LLM). New `SLOT.WIND_DOWN`; `control-phrases.js` `closers` → `windDowns` + `closings` (defaults + seeded-watermark; a legacy single-`closers` file reseeds both from defaults — single-user pre-beta, re-edit via Controls). Controls editor gains two sections (Wind-down statements / Closings). `app.js`: `renderStaticPalette`/`pageWindow`/`staticOffsets` + `offerClosings`; the Wind down / Initiate / Regenerate / CLOSING paths route through the pager, cleared to regenerate when AI cards show. 119/119 unit tests; static flows + Controls-tab editing verified in preview. |
| 0.5.92  | 1     | **Pardon re-speak becomes a separate turn; space between speech segments (Ken, July 13 2026).** Two issues from a pardon-flow transcript. (1) After **Ask them to repeat**, the partner's re-speak was appended to their earlier turn (and rendered before the pardon): "…great weekend.Yeah, I had a great weekend too." Refines v0.5.87 ("repeat appends to it") — the re-speak should be a NEW turn AFTER the pardon, without discarding the original. Fix (`handlePardon`): after `logSpokenUserTurn` commits the kept turn + the pardon, `finalizePendingPartnerTurn()` (detach + finalize storage, cleaned=raw) + `stt.resetTranscript()` + clear `currentPartnerText`/`pendingPartnerHistoryIdx`, so the next partner speech is a fresh turn; engine repair resolution unaffected. (2) The recognizer returns segments with no separating space, so "Good morning." + "How was your weekend?" concatenated into "…weekend.How…". Fix (`stt.js`): a `joinParts()` helper joins finalized segments (and the interim) with single spaces everywhere `accumulatedText` / `getCurrentTranscript` / the checkpoint text / `dropLastStatement` are built. Also recorded that transcript/pane fidelity is foundational for the future relive-and-critique (Phase-3 conversation-review) feature, not just test hygiene. Unit-tested (spaced concat + checkpoint); 116/116. Boots clean; the live pardon flow is the on-device retest. |
| 0.5.91  | 1     | **Abort placeholders the instant a speaking button is pressed (Ken, July 13 2026).** On-device: **Repeat what I said** was cut off partway by a placeholder. Cause: Say again / Hold on did NOT invalidate an in-flight `generateOptions` (a response selection does), so when the partner's already-running LLM round-trip completed it called `placeholders.start()` again; with the initial-delay clock measured from the partner's pause (already elapsed) the placeholder fired immediately and its `tts.speak` cancelled the button's speech. Fix, two layers: (1) a `placeholderEpoch` (app.js) bumped by `abortPlaceholders()` — Say again / Hold on call it, and `generateOptions` captures the epoch and skips `placeholders.start()` if it changed mid-generation, suppressing the placeholder WITHOUT discarding the response options (separate from `generationToken`, which a response selection uses to cancel generation outright); Wind down uses `generationToken++`. (2) A hard backstop in `placeholders.js` — `setUserSpeakingGate(() => speakingUserStatement)` so `speakNext` defers (never barges) while the app speaks the user's own statement. Buttons that already `generationToken++` (response / Ask-them-to-repeat / Express / composer) were already covered. Unit-tested (the gate defers then speaks once the user is done); 115/115. Boots clean; acoustic timing is the on-device retest. |
| 0.5.90  | 1     | **Harden TTS-echo excision — partial / mis-heard / lagged echo (Ken, July 13 2026).** On-device: after **Repeat what I said**, the mic caught only the TAIL of the re-spoken response ("had some time to relax") and appended it to the partner's turn; a placeholder "Still mulling that over" came back mis-heard as "Steel mulling that over." Ken's directive: keep the mic hot (talk-over stays) — excise the app's own phrases from the STT stream more robustly instead. Fix entirely in `stt.js` `isEcho`: (1) `fuzzySlice()` matches an arbitrary CONTIGUOUS token slice of what we said (either direction), catching a suffix/middle fragment the old exact/prefix/embedded matcher missed; (2) `tokensSimilar()` allows a small per-token edit distance (2 for len≥4, 1 for len 3; short words exact) so "still"→"steel" still matches; (3) decoupled windows — `ECHO_TAIL_MS`=1500 still gates the checkpoint (a genuine reply isn't delayed) but a new `ECHO_MATCH_MS`=4000 keeps a spoken phrase matchable for excision long enough to catch the cloud recognizer's late echo (past the tail). Accepted tradeoff: within the match window a partner utterance that genuinely repeats our phrase is treated as echo. Unit-tested (suffix slice, still→steel, late echo past the tail, no-over-drop); 114/114. Boots clean; the acoustic loop + cloud timing is the on-device retest boundary. Robust long-term answer remains Phase-2 partner voice recognition. |
| 0.5.89  | 1     | **Fix conversation-pane ordering — spoken-command turn jumped above the partner (Ken, July 13 2026).** On-device follow-up to 0.5.88: after the partner replied, tapping **Repeat what I said** (or Hold on / Ask them to repeat) rendered the user's line ABOVE the partner's reply in the pane, though the transcript had it correctly after. Cause: the pane showed the partner's in-progress turn as a bottom-pinned live line (`ui.setLiveTranscript`) while a `logSpokenUserTurn` command turn was appended into the committed history above it. Fix (app.js, pane-only — storage/transcript were already correct): `flushLivePartnerToHistory()` promotes the live partner turn into `conversationHistory` (tracked by `pendingPartnerHistoryIdx`) before appending the command turn; `commitExchange`/repair use `placePartnerTurn` (update the promoted entry in place, keeping it before the command, else append); `updatePartnerLive` keeps a promoted turn updating in place instead of re-showing the live line; `logSpokenUserTurn` also writes the partner's heard text to the transcript first (pane+transcript identical even before the partner's first pause). Traced through partner1→user1→partner2→say-again→response (both order partner2 before the say-again); boots clean; live-mic ordering is the on-device retest boundary. 110/110 tests. |
| 0.5.88  | 1     | **Transcript mirrors the conversation pane — incremental partner writes (Ken, July 13 2026).** The recorded transcript (`conversations/<id>.json`) now mirrors the on-screen conversation pane at all times, so it's a reliable record while the app misbehaves during testing (kept until/unless it causes performance issues). Reverses the prior commit-at-response model. Terminology fixed: "the transcript" = the JSON file, "the conversation pane" = the UI element. Behavior: the file is **created on Listen/Start** (`startConversationLog` made idempotent), the partner's **raw line is written at each pause** (`logPartnerInterim`) and a **continuation overwrites it + clears the cleaned line** (no duplicate turn), and on a response the **user turn is written immediately** (no longer waiting on the AI cleanup round-trip) while the partner entry is **finalized in place** (cleaned line filled) in the background — partner-then-user order preserved; interruption/repair/End-Start finalize the pending entry instead of appending a duplicate. New pure module `transcript-log.js` (`upsertPartnerInterim`/`finalizePartner`, the array-shaping rules) unit-tested (`tests/transcript-log.test.mjs`, 5 cases); `storage.js` tracks `pendingPartnerTurn` and exposes `logPartnerInterim`/`detachPendingPartnerTurn`/`finalizePartnerTurn` (old `logPartnerSpeech` removed). Verified end-to-end through the real `storage.js` against an in-memory FSA folder mock (file-on-start, raw→overwrite-clearing-cleaned, user-immediate, finalize-in-place); 110/110 tests; on-device confirmation is the usual granted-folder boundary. Manual test plan §8 rewritten + terminology swept. |
| 0.5.87  | 1     | **STT interim preservation + settings-profile picker fix + "Ask them to repeat" keeps the transcript (Ken, July 12 2026).** (1) Browser recognition ends continuous capture periodically and the app restarts it; the restart cleared `currentInterim`, and the fresh session doesn't re-hear already-spoken audio, so not-yet-finalized interim words shown live were lost — interrupting the partner mid-utterance recorded only the finalized segment ("I was"). Fix: `stt.js` `onend` restart branch flushes `currentInterim` into `accumulatedText`/`segments` before clearing (regression test added; no duplication — the new session only transcribes forward). (2) Settings-profile picker showed the wrong name after save/load/restart (the data always applied — proven via an in-memory-folder round-trip test): added a machine-local `activeSettingsProfile` pointer (in `PROFILE_EXCLUDE`), set on save/apply and cleared on delete-of-active; `renderSettingsProfiles` selects it + shows "In use: …". (3) Enter in the profile-name box saves the current settings (`profileNameInput.onkeydown`; the on-screen keyboard's Enter dispatches the same keydown). (4) "Ask them to repeat" (`handlePardon`) no longer discards the partner's transcript — removed `stt.dropLastStatement()`, keeps `heardPartnerText()` so the re-speak appends (reverses v0.5.32). (5) Controls-tab label "'Pardon?' phrase" → "'Ask them to repeat' phrase" (`control-phrases-editor.js`). Verified: 105/105 unit tests, clean boot, label + Enter handler confirmed in the running app. |
| 0.5.86  | 1     | **End/Start now commit the partner's pending turn instead of dropping it (Ken, July 12 2026).** Diagnosed from two attached conversation logs: a partner spoke but the user ended before replying, so (a) the uncommitted partner turn was dropped, and (b) it leaked into the NEXT conversation — `terminateConversation()` cleared `currentPartnerText` but not the STT buffer (`stopListening()` keeps `accumulatedText`), so `heardPartnerText()` read the stale text when the user picked the next opener and it was committed at the top of the new conversation though nobody spoke. Fix: `terminateConversation()` (shared by End + Start) captures the pending partner turn + situation stamp up front, `stt.resetTranscript()`s the buffer, does the instant UI teardown, then — before the id reset and before re-seeding the next conversation's privacy — writes the pending turn RAW (no AI cleanup round-trip, so the flush can't race the id reset) to the CURRENT `<id>.json` via `logPartnerSpeech`, then `resetConversationId()`. Made `terminateConversation` async and `await`ed in both callers (`handleInitiate`, `handleEndConversation`). Verified in preview: boots clean, Start renders openers / End clears them through the async paths, no console errors; the disk-write flush needs a live mic + granted folder (tablet). |
| 0.5.85  | 1     | **Settings keyboard-navigation + Express per-item Save + layout rename (Ken, July 11 2026).** Feedback batch on the on-screen keyboard / tab order. (1) **Settings tabs are keyboard-navigable** via a roving-tabindex ARIA tablist (`aria-orientation="vertical"`): the tab column is a single Tab stop, and **↑/↓** (plus Home/End) move between tabs while Tab continues into the panel — so a physical-keyboard user reaches About Me from General without tabbing through nine tabs' content (`initSettingsTabs`/new `activateSettingsTab` in `app.js`; `.settings-tab:focus-visible` ring; `openSettings` keeps the roving state in sync). ←/→ are inert (vertical strip). (2) **On-screen keyboard keys removed from the Tab order** unconditionally — every `.kbd-key` (and the hidden prediction buttons) gets `tabIndex = -1` in `keyboard.js` `renderRows`/`build`, since keys are pointer targets, never Tab targets, in both typing modes. (3) **Per-item "Save" button on the Express Panel editor** (`express-editor.js` `buildRow`): commits the item and calls `keyboard.hideKeyboard()` so an on-screen-keyboard user can put the keyboard away and see the panel/list again (the Hide button having been removed in v0.5.38); green text button, `.ee-save` CSS overrides the 2rem icon sizing. (4) **Layout names "Keyboard" → "Layout"** in `keyboard-layouts.js` (Side Layout 1–10, Bottom Layout 1–11) — they set both the keyboard and the Express Panel grid. (5) Profile-load confirm button relabeled **"Load & reload" → "Load"** (`app.js`). Item 3 from the feedback (a "template" caption for physical mode) was dropped — physical mode already shows only the Express Panel, no keyboard. Verified in the running app (DOM assertions + reload): roving tabindex, ↑/↓ nav with ←/→ inert, all 33 keys `tabindex=-1`, 32 Express rows each with a Save button, layout selects renamed; no console errors. |
| 0.5.84  | 1     | **Wind-down card count fix + composer Tab trap + keyguard-tab separation settings + fast-path goodbyes (Ken, July 11 2026).** (1) A partner's reply to a wind-down came back as the FULL closer list and rendered UNCAPPED (`ui.showResponses` non-responding branch keyed `perCell` off item count), so 8 closings showed even in 1-card mode; now the branch caps/stacks by the `responses per category` setting — ≤4 items are one-per-cell (repair-of-self stays 3 single cards), >4 caps to 4 (1-card) or stacks to 8 (8-card). (2) The "In my own words" composer is a soft modal (base UI intentionally live), so a physical-keyboard Tab leaked focus into the base controls behind it; added a Tab/Shift+Tab focus trap (`composerTabTrap` in `ui.js`) cycling textarea → Speak → Reframe → Cancel. (3) Moved "Keyboard separation" from Speech & Input to the **Keyguard Design** tab and added a new **"Transcript separation"** slider there (shortens the transcript to open a gap above the command bar; command bar + dock holes stay put) — new `transcriptSepPos` storage + `--transcript-sep` var, applied in both docks. (4) **Fast-path closing:** while winding down (PRE_CLOSING), a plain-farewell partner reply re-offers the goodbyes IMMEDIATELY without an AI round-trip (so the user can speak another closing sooner) — `conversation-logic.looksLikeClosing()` (precision-biased keyword+length heuristic, documented field-feedback note) gates `generateOptions`' early path, which feeds the engine a synthetic CLOSING classification; a non-match falls through to the normal AI path. Verified in preview (both docks, closing-count matrix, tab-trap wrap, transcript-sep in side+bottom, synthetic-CLOSING integration); 104/104 unit tests (+3 `looksLikeClosing`). |
| 0.5.83  | 1     | **Named settings profiles + About Me becomes an ordinary Settings tab (Ken, July 10 2026).** (1) **Settings profiles** (Settings → General, below Data Folder): save the whole `aac_settings` bundle to `settings/<name>.json` in the data folder and re-apply/delete it in one tap — a repeatable on-device test baseline and the seed of cross-device transfer / the PRE-BETA data-folder-settings work. New `storage.js` `saveSettingsProfile`/`listSettingsProfiles`/`settingsProfileExists`/`applySettingsProfile`/`deleteSettingsProfile`; Load writes localStorage then `location.reload()` so every setting re-applies exactly as at startup. **Excludes** the API key (SEC-6 — never written to a possibly cloud-synced folder), the token-usage counters, and `lastSeenVersion`; applying a profile preserves those machine-local keys. Danger-confirmed on save-over/load/delete; the name field is in the on-screen-keyboard scope. Verified end-to-end against an in-memory folder mock (round-trip, key-exclusion, key-preservation, case-insensitive overwrite, delete). (2) **About Me** converted from a full-screen `#worldviewScreen` overlay (dark title bar + "Done" button, launched by closing Settings) to an **ordinary inline Settings tab** rendered into `.tab-panel[data-tab="aboutme"]` — no title bar, no Done, dismissed by the shared **Close** button like every other tab; sub-pages (modules / People) show an in-content `.wv-page-title` heading instead. Removed the overlay lifecycle (show/hide/`setBackgroundInert`/`close`) from `worldview-ui.js` (the Settings modal already inerts the background and hosts the keyboard); the tab handler calls `worldviewUI.open()` to render, and `handleSettingsTab` hides the keyboard when leaving any non-keyboard tab. Removed the `#worldviewScreen`/`.worldview-header` styles + its dock-reservation rules (the Settings dialog already reserves the dock). Also carries the pending "goodbye no longer repeats the partner's last words" fix. |
| 0.5.82  | 1     | **Stop gating generation on turn_status — the user judges turn completion (Ken, July 10 2026; reverses design §8).** After the 0.5.81 fix, a *later* short/disfluent go-ahead ("Go ahead. Uh, my — my ears are wide open.") could still stall silently, because the false-TRP guard has no recovery when the model misreads a complete turn as incomplete. Ken's call: stop trying to detect when the partner is "done" (fragile from garbled STT, hard even for humans). New model: every silence checkpoint generates AND shows a palette, refined on each later pause; the partner's turn is complete only when the USER responds or ends the conversation. Removed design §8's false-TRP suppression guard (and the interim pause-to-complete fallback). CA-checked: doesn't violate CA — turn-completion is a participant judgment, and the device only *prepares* options (never auto-acts), so there's no false-TRP action. Kept `partner_action` + `is_repair_initiator`; `turn_status` is informational only. Engine now updates the same ongoing partner FPP in place per pause (no duplicate). Placeholders unchanged (fire after the pause, abort on partner resume). Known limitation accepted: resumption *during* an active placeholder isn't detected until it finishes (echo guard) — Phase-2 partner voice ID is the fix. Also created the tester-facing `Conversant AAC Known Issues.docx`. |
| 0.5.81  | 1     | **Fix silent stall in user-started conversations (Ken, July 10 2026).** When the user opened with a starter and the partner replied with a short go-ahead ("Yeah sure, any time you want, just let me know"), the app produced no placeholder and no response options — a silent stall, nothing logged. Root cause: in the user-led case the model misclassified the short go-ahead as a non-COMPLETE turn, and the engine's false-TRP guard then suppressed the whole lead palette. Fix: the "User is leading" generation prompt now requires `turn_status: COMPLETE` + the four lead responses for a go-ahead, and the engine skips the false-TRP suppression when the partner is replying to a user-opened sequence (normalizing the status to COMPLETE). Added silent-dead-end instrumentation — any "complete turn but no options" / "non-COMPLETE while user leading" outcome now logs (transcript red-wash + errors.log) so this class of failure can never be invisible again; the loop-decision logic + tripwires were extracted to `conversation-logic.js`. Also added a three-tier `node --test` harness (`tests/`, ~95 deterministic tests + live-API scenarios) and an on-device manual test plan with seed fixtures. Confirmed against the live model. |
| 0.5.80  | 1     | **Fix the composer's leftover clipped-looking border (Ken, July 9 2026).** The "In my own words" container drew a 2px inset framing border whose rounded corner clipped at the response-footprint edges, reading as cut off. Removed (`box-shadow: none`) — a flush, borderless container also matches the Response Palette it overlays. |
| 0.5.79  | 1     | **Graceful behavior when the AI is unreachable + composer polish (Ken, July 9 2026).** When generation fails (API/network down), the partner's raw words stay visible in the transcript styled blue/italic ("uncleaned" — the AI can't tidy them), the red-wash heads-up stays, and the user replies via the Express Panel / "In my own words" (committed + saved as before) — nothing is committed on failure, so words aren't duplicated or lost if the partner keeps talking. A partner turn recorded without the AI cleanup pass (this case or an interruption) is flagged `uncleaned` and rendered blue/italic. STT errors are now logged (tripping the red wash) and the recognition restart-loop is stopped on a surfaced (e.g. `network`) error, since browser STT is cloud-based and can't transcribe with no internet. Composer: removed the "In my own words" input's border AND its focus ring (edgeless in every state — the whole box is the predicted-word tap target); Cancel now has the same dark border as Reframe. Docs: recorded the AI-is-an-enhancement-layer-not-a-hard-dependency property (basic functionality survives with no LLM) + the iPad "browser DB" storage path and the cloud-based-STT / "won't work as expected on iOS" caveat. |
| 0.5.78  | 1     | **Security review Phase 1 + conversation-screen fixes (Ken, July 8 2026).** Security: SEC-2 — "Don't save this conversation" now also keeps partner speech out of the error log / bug report (`storage.logError` redacts `extra.partner` + `buildErrorReport` withholds a private live transcript); SEC-4 — stale `-Helix2` app copy moved out of `app/` (+ `.gitignore` guard); SEC-8 — removed the dead `innerHTML` branch in the worldview `el()` helper; SEC-10 — `.gitignore` hygiene. Conversation: **8 reserved response slots** shown at launch/rest in 8-card mode (`ui.setCardsPerCategory` + split empty cells); **8 built-in closings** so Wind down fills all eight; **new default openers/closings additively merge** into the user's list (seeded-watermark in `control-phrases.js` — new defaults append, deletions stick); **placeholders play after ANY complete partner turn** including greetings (question-flavored vs. neutral acknowledgment sub-pools), reversing the questions-only gate; **repair-of-self pre-generates rephrase + expand** in one call so all three repair cards show real, speakable text (`llm.repairOptions` + `engine.setRepairOptions`). Also added the doc-currency tooling (`DOC-SYNC.md`, "sync docs" process, mandatory pre-edit backups) and recorded the security backlog + settings-persistence design (not built). |
| 0.5.77  | 1     | **Interrupting the partner captures their partial speech (Ken, July 8 2026).** Cutting in with an instant statement (Express phrase / composer Speak) no longer drops the partner's in-progress speech: new `stt.getCurrentTranscript()` exposes the live accumulated transcript, `app.heardPartnerText()` captures it before stopping the mic, and it's committed (transcript + JSON) interleaved before the interrupting statement. `commitExchange` gained `opts.cleanup`; the interruption path passes `cleanup:false` so the fragment is recorded verbatim (no AI cleanup). Confirmed (no change needed): auto-listen resumes capture after the interruption, and the entire conversation is sent to the AI each turn. Verified in preview with a fake recognizer; live mic needs the tablet. |
| 0.5.76  | 1     | **Conversation-screen feedback batch + log persistence (Ken, July 8 2026).** (1) User statements now append to the transcript AFTER being spoken; the `♪` now-playing line is reserved for system placeholders (`speakUserStatement` flag suppresses it for user speech). (2) **Reframe is floor-aware**: with a partner turn it reworks the responses around the typed steer; with the user holding the floor it generates steering **STATEMENTS** (`llm.generateStatements`, new `SLOT.STATEMENT`, one-per-cell). (3) Selecting a wind-down/closing **re-offers the closings** (incl. "Bye!") so the user can sign off without waiting. (4) **5 new conversation starters** (9 total in `engine.DEFAULT_OPENERS` / `control-phrases.DEFAULTS.openers`); opener/closer palettes pack **2-per-cell when >4** so 8-card mode fills all 8 (`showConversationPalette` caps to `conversationPaletteCap()`). (5) Say again / Hold on / Ask them to repeat **write to the transcript** (`logSpokenUserTurn`). (6) **"In my own words" → compose icon + tooltip** (new UI Layout **Rule 12**: constant buttons are icon-only). (7) **Errors interleave into the conversation JSON** in time order (`storage.logErrorToConversation`, flushed immediately). (8) Bug fix — `resetConversationId()` now clears the log target, so consecutive conversations no longer merge into the first conversation's `<id>.json`. Verified in preview (Python static server + stubbed API/TTS); on-disk JSON writes need a granted FSA folder to confirm on the tablet. |
| 0.5.75  | 1     | **Transcript error indicator — faint-red wash on any logged error (Ken, July 8 2026).** A non-verbal heads-up that the app hit an error mid-conversation (a hiccup the user/partner should expect and pass to support) — no worded message on screen; details still go to the error log. Wired at the single choke point `storage.logError`, so it trips on **thrown** API/parse failures AND the **silent** no-usable-responses paths (empty/malformed `responses[]`, INCOMPLETE classification, superseded generation token) that never throw. `logError` dispatches an `aac-error-logged` event (keeps storage decoupled from the UI); `app.js` listens → `ui.setTranscriptError(true)`; `ui.setTranscriptError` toggles `has-error` on `#transcript`; `showResponses` clears it (sticky-until-next-success), as does conversation reset (Start/End). `.transcript-log.has-error` = faint red bg `rgba(220,53,69,0.08)` + red border, 0.25s fade. The old worded "amber box" (`showResponseError`) left in place for now; CLAUDE.md TODO tracks deciding its disposition before public release. Verified in preview: boots clean; the event trips `has-error` on `#transcript` and the rule computes to the faint-red bg + red border; removing the class restores transparent/gray. |
| 0.5.73  | 1     | **Error log grouped by conversation + Copy bundles the transcript (Ken, July 7 2026).** Follow-up to v0.5.72. `renderErrorLog()` now groups entries by `conversation` (newest conversation first, `(none)` last) instead of a flat chronological list, via `groupErrorsByConversation()`. The Copy button no longer copies the raw textarea — `buildErrorReport()` assembles a full report: for each conversation with errors, its transcript followed by that conversation's errors. The transcript comes from new `storage.readConversationLog(id)` (reads `conversations/<id>.json` back), or, when the id is the still-open conversation (`storage.getConversationId()`), from the live in-memory `conversationHistory` (covers an error that fired before the turn was committed to disk); otherwise "[not available — no data folder / not saved]". So a pasted bug report carries the conversation, not just the error line. Verified in preview: grouping (2 conversations newest-first), the report structure (header + per-conversation transcript/errors), Copy → "Copied ✓"; on-disk transcript needs a granted FSA folder to exercise. |
| 0.5.72  | 1     | **Persistent error log correlated to the conversation (Ken, July 7 2026).** Errors (API 429/529 failures, JSON parse failures, transcript cleanup, repair/reframe/regenerate) previously went only to the now-hidden status bar + console, so an intermittent live-demo failure — most importantly "no response options" — left no trace. New `storage.logError(context, message, extra)` writes each error to a capped (200) localStorage ring buffer AND appends it to `errors.log` in the data folder (FSA `createWritable({keepExistingData:true})` + seek-to-end append), stamped with the app version and a per-conversation id. That id comes from `ensureConversationId()` and is SHARED with the conversation log filename (`<id>.json`) — reset in `terminateConversation` via `storage.resetConversationId()` — so an error matches its conversation (both derive from the same id; second-resolution, same as the existing log filename). Viewer in Settings → About (`#errorLogView` textarea + count + Copy + Clear-with-`confirmDanger`); `renderErrorLog()` populated on Settings open. Catch sites wired: `generateOptions`/`regenerate`/`reframe`/`repairSelf`/`cleanupTranscript`. Generation failures also now surface VISIBLY in the response region via new `ui.showResponseError(msg, onRetry)` (amber dashed box + "Try again"), replacing the blank look. Verified in preview: viewer renders seeded entries with conversation id; logError stamps version + shared/stable-within-conversation id + resets; showResponseError renders + retry fires; Clear opens the danger dialog; no console errors (the data-folder append needs a granted FSA folder to exercise, same path as conversation logging). **Root-cause still open:** the log is the instrument to confirm whether the demo "no options" is API 429/529 (no retry yet) — automatic retry-with-backoff is the likely follow-up fix. |
| 0.5.71  | 1     | **Word-prediction acceptance: tap the field, not a separator (Ken, July 7 2026).** The inline ghost completion is no longer accepted by a word-separator — space / comma / period / Enter now insert literally and dismiss the ghost (`handleKey` in keyboard.js: `learnCurrentWord()` + `clearGhost()` instead of `acceptGhost()`). This fixes the silent, invisible swap where "Yes" + space became "Yesterday" (the user may not even be looking at the box). Acceptance is now an explicit **tap anywhere in the active field** — a new document `pointerdown` handler: when `ghostWord` is showing and the tap's `closest(IN_SCOPE)` is the `activeField`, it `preventDefault()`s (keeps focus, caret stays at the end) and `acceptGhost()`s. The whole field is one big FIXED acceptance target, so there's no moving inline tap target to chase (keyguard-safe) and no silent separator acceptance; the `#predGhost` overlay is `pointer-events:none` so the tap lands on the field. Applies to every predicted field (composer + About Me), so prediction stays acceptable everywhere now that separators don't accept; the only trade-off (Ken's call — "this region won't be used as a typical composition space") is that a tap while a ghost is showing no longer positions the caret. Verified in preview via dispatched `pointerdown` (accepts, `defaultPrevented`) + all separator cases (space/comma/period/Enter insert literally); real trusted-tap needs the device (the preview click tool doesn't drive pointer events through the overlay). No console errors. |
| 0.5.70  | 1     | **Composer keyboard keep-open fix + single-row composer buttons + "Reload the app" button (Ken, July 7 2026).** (1) The real device fix for "keyboard doesn't display for In my own words": `servingPanelOpen()` (keyboard.js) now also treats an open `#composerOverlay` as a keyboard-serving surface, so a stray `focusout` while the composer is open (a reflow, or the auto-resumed mic re-rendering just after the field gains focus) no longer hides the keyboard. This never reproduced in the headless preview because it has no window focus, so no focus events fire — the earlier v0.5.69 `showFor()` summon was necessary but insufficient without this. (2) `#composerResponses` is now a single-row 2-column grid in BOTH docks (dropped the side-dock stacking), so Speak/Reframe/Cancel sit in one horizontal row in side mode like bottom; still mirrors `#responseArea`'s flex split so Cancel = "New 4" width. (3) New Settings → About **"Reload the app"** button (`#reloadAppBtn`) — a keyboard-free Ctrl+Shift+R for a tablet: `reg.update()` + clears all `caches`, then `location.reload()` (network-first SW re-fetches fresh code). Non-destructive (committed exchanges already logged). Verified in preview: single-row both docks, keyboard stays up on stray focusout while composer open (and Cancel still dismisses both), reload button present/wired and clears caches; no console errors. *(Note: the original report that prompted the keyboard digging turned out to be operator error — "Keyboard for typing" was set to Physical — but the keep-open fix is a real robustness improvement kept regardless.)* |
| 0.5.69  | 1     | **Self-started-conversation logging + composer keyboard fixes; composer-button alignment (Ken, July 7 2026).** Three items. (1) `storage.logUserResponse` now lazily calls `startConversationLog()` when no log exists yet (mirroring `logPartnerSpeech`) — a conversation opened by the user (an opener or an Express Panel phrase) had its first turn dropped, and a conversation that never reached a committed partner turn was never written at all; both are now recorded from the first user turn. (2) New `keyboard.showFor(field)` summons the on-screen keyboard deterministically; `openComposer` calls it instead of relying on the `#composerInput` `focusin` side effect, which can be swallowed (field already focused → `.focus()` is a no-op; focus race after an Express phrase auto-resumes listening) — so "In my own words" no longer opens with no keyboard. (3) Composer buttons mirror the response footprint for Spatial Stability in BOTH docks: Speak + Reframe are wrapped in `#composerResponses` (flex:9, reusing `.palette-grid`'s grid + gaps) so they tile the card region (side 2×2 → stacked full-rows; bottom 1×4 → two side-by-side pairs), and Cancel mirrors `#regenerateBtn` exactly (flex:1 1 0, `--btn-min-dim` floor, no padding) = the "New 4" width at every viewport; the overlay's horizontal padding was dropped and its border redrawn as an inset box-shadow so buttons reach the footprint edges. Verified pixel-exact in preview (side + bottom): Speak/Reframe = card region, Cancel = New 4; no console errors. |
| 0.5.68  | 1     | **"What's new" notice layout — fill the transcript, tighter header (Ken, July 4 2026).** The card now fills the ENTIRE transcript control (`#whatsNewPanel` = `flex:1 1 auto; align-self:stretch`; `#startBlock` padding 0). Dropped the "The app updated itself to version X…" intro line and the ✨ graphic. Moved the dismiss button into the header row on the right, relabeled **"Close"**, and exempted it from the global `--btn-min-dim` floor (`#whatsNewPanel .whatsnew-ok { min-height:0 }`, id-qualified to beat the 0,4,1 global rule) so it's short. Net: the scrolling notes list went ~45%→~72% of the card height. Verified in preview: card fills the section (0px delta both axes), header one row (title left / Close right), Close 32px, list 72%, Close dismisses + records seen, no console errors. |
| 0.5.67  | 1     | **"What's new" notice rendered IN the transcript area, not a centered modal (Ken, July 4 2026) — keyguard/Spatial Stability.** The centered top-layer modal could put parts of the dialog (incl. "Got it") under keyguard bars. Now the notice is an inline card in `#whatsNewPanel` inside the pre-start block, which overlays the **Transcript control's footprint** (a keyguard opening), so the whole card + "Got it" stay reachable. Flow: app re-renders post-update (knows the transcript's location) → user presses Start → `whatsNew.pending()` returns notes → `renderPanel()` shows the card + hides `#startBtn`, main stays dimmed → "Got it" → `markSeen` + `finishStart()` (hide start block, enter conversation). No-notice path calls `finishStart()` directly. Replaced the `.whatsnew-dialog` modal with `#whatsNewPanel` card CSS. Verified in preview: panel + "Got it" measured within the transcript bounds, seen recorded only on "Got it", no re-show, no console errors. |
| 0.5.66  | 1     | **"What's new" notice moved to AFTER Start, as a persist-until-"Got it" modal (Ken, July 4 2026).** Was an inline panel in the pre-start block (where the SW auto-update reload could make it flash); now `maybeShowWhatsNew` is called from `handleStart` (after Start, when the page is stable) and renders a top-layer `<dialog>` (`showWhatsNewModal`) that only closes on "Got it" — Escape (`cancel` preventDefault) and backdrop clicks are blocked; `lastSeenVersion` is recorded only on that acknowledgment. Removed the unused `#whatsNewPanel` element + its inline-card CSS (back to `.whatsnew-dialog`). Verified in preview: no modal pre-Start, modal appears on Start (`:modal`), Escape blocked, "Got it" closes + records seen, no re-show after. |
| 0.5.65  | 1     | **Rollback leg of the dry run + "What's new" persists until dismissed (Ken, July 4 2026).** Removed the 0.5.64 test caption (the rollback release). Fixed the announcement disappearing too fast to read: it was marked seen at *render* time, so the service-worker `controllerchange` auto-update reload wiped it (a flash). Now `lastSeenVersion` is recorded only on acknowledgment — "Got it" (`markSeen` in the button handler) or pressing Start (`whatsNew.markSeen(APP_VERSION)` in `handleStart`) — so the panel re-renders across the update reload and stays until dismissed. Verified in preview: panel survives a reload, clears only on Got it/Start, doesn't reappear after. |
| 0.5.64  | 1     | **Release-process dry run + a welcome line under Start (Ken, July 4 2026).** Added a "Ready when you are." caption beneath the Start button on the opening screen — a trivial user-visible change used to exercise the newly-formalized dev→release→"What's new"→rollback loop end to end before testers come online. First release cut via the "bump Conversant" ritual (finalize digit → rename `## Unreleased` → regenerate bundled notes → table row → commit → push). |
| 0.5.63  | 1     | **What's-new notice moved into the transcript space; About "See what's new" button removed (Ken, July 4 2026).** The post-update announcement no longer pops as a full-screen modal — it renders **inline in the pre-start block that overlays the (empty) transcript region**, above the **Start button, which is now centered in that space** (replacing the old full-screen `#startOverlay` scrim). `main.disabled` dims only the sibling regions now (not `main` itself, so the Start block stays crisp/tappable). "Got it" collapses the panel, leaving Start centered. Removed the Settings → About "See what's new in this version" button (and `showWhatsNewForVersion`). Verified in preview: Start centers in the transcript both axes (no update) / stays centered-X with the card above (update), Got-it recenters, Start hides the block + enables main, no console errors. |
| 0.5.62  | 1     | **"What's new" post-update notice (Ken, July 4 2026) — modeled on the Keyguard Designer web app.** After the app auto-updates itself, the freshly-loaded build shows a short, plain-language summary of what changed since the version the user last saw. Four parts: a user-facing `CHANGELOG.md` (single source of truth), a generator `scripts/apply-release-notes.mjs` that bundles it into `app/js/whats-new.js` (BUNDLED not fetched — works offline), the modal + semver logic in `whats-new.js` (flat combined list, newest first), and `storage.lastSeenVersion` gating. Shows once per update; the version that introduces it can't announce itself (null→silent baseline). Also on-demand at Settings → About → "See what's new in this version". Blue-accent top-layer `<dialog>` sibling of `confirmDanger`. Switched `.claude/launch.json` to autoPort. Verified in preview: baseline-silent first load, modal on simulated update with correct flat/newest-first notes, show-once, on-demand button, no console errors. |
| 0.5.60  | 1     | **User-Manual review batch 2 — About Me fixes, UI/manual terminology, in-document comments (Ken, July 3 2026).** Four field requests + the eight Word comments in the User Manual. **Code:** (1) **Decline is now reversible — "Prefer not to say" no longer destroys a saved answer.** `worldview.declineField` stashes the prior value in `prevValue` (never sent to the AI — `getField`/`buildBlock` still see null); new `undeclineField`/`hasStashedAnswer`; the declined card's Undo restores the answer ("Undo — bring my answer back") or re-opens the question if there was none. Fixes the data loss Ken hit (decline → Done-without-Save → answer gone). (2) **Two "Done" buttons disambiguated.** The in-module / People bottom button "Done" → **"‹ Back to topics"** (it returns to the topic list, same as the top "‹ All topics"); the header **"Done"** (closes About Me → Settings) is now the only "Done". (3) **UI↔manual terminology aligned** (request 3 + comment 23): "Minimum gap" → **"Minimum spacing"**, "Optional Responses silence period" → **"Silence period"**, "Initial/Subsequent Placeholder Statement Delay" → **"Initial/Subsequent placeholder delay"**. **Manual (`Conversant-AAC-User-Manual.docx`, OneDrive not git):** comment 25 — `filler`→`placeholder` scrubbed from the **tables** the v0.5.59 pass missed; comment 31 — removed "backchannel" from §7.1 and corrected the phrase-color list to the real defaults (affirm teal / social indigo / pace orange / repair purple / needs pink / continuer gray); comment 20 — §6 intro now enumerates all nine tabs, added a **6.5 Keyguard Design Tab** subsection (About → 6.6), and fixed the §6.3 Speech table (dropped the stale "Text size" row, added "Keyboard separation"); comment 36 — rebuilt the §8 Persistent Buttons table with an **Icon column + all nine Command Bar buttons** (icons rendered from `icons.js` to 88px PNGs via a browser→disk bridge and embedded); comment 38 — filled the empty "Tip" with the family/friends opener-closer tip; "Eight buttons" → "Nine buttons". Comment 26 (placeholder timers reset on partner resume) was already shipped in v0.5.59. **Verified in preview:** boots clean at 0.5.60, no console errors; the four renamed labels present; decline→undecline round-trips (value withheld while declined, restored on undo); About Me shows one "Done" (header) + "‹ Back to topics" (bottom). Manual re-verified: no "filler" anywhere, nine-tab intro, six §6 subsections, §8 table = 3 cols × 9 rows with 9 embedded icons. |
| 0.5.59  | 1     | **User-Manual review batch — code changes (Ken, July 3 2026).** Six program changes from a mixed manual-review list (the remaining items are recorded as To Dos below). (1) **Per-conversation privacy — "Don't save this conversation."** New Command Bar toggle `#privacyBtn` (shield-with-slash `noSave` icon; amber `.private-on` selected state, Rule 6) + a Settings → Conversation default checkbox `#noSaveDefaultInput`. When active the conversation is NOT written to the data folder: `storage.setConversationSaving(false)` makes `startConversationLog`/`logPartnerSpeech`/`logUserResponse` no-op entirely (no file, no turns). Runtime flag `conversationPrivate` seeds from the Settings default (`loadNoSaveDefault`) and re-seeds at every Start/End conversation (`terminateConversation`) so a per-conversation choice never carries over; the button toggles it live. (2) **About Me tab moved below General** in the Settings nav (order: General · About Me · Text Size · Speech & Input · …). (3) **Response-card text size** setting (`responseFontSelect` → `--response-font-scale`, applied to `.response-text`), joining the existing Transcript/Composer/Express scales. (4) **New "Text Size" tab** — all four size selects (Response cards, Transcript, "In my own words", Express Panel) moved off the over-long Speech & Input tab onto their own short tab (Ken chose this over reorganizing in place). (5) **Placeholder timers reset when the partner resumes** after a pause — `stt.init` gained an `onPartnerSpeech` callback fired alongside `resetSilenceTimer` (genuine, non-echo partner content only); `app.handlePartnerResumed` calls `placeholders.stop()` so a scheduled/playing placeholder can't speak over the partner. (6) **"Reset buttons and gaps to default" moved above "Keyboard separation"** on the Speech tab (it doesn't reset that value). Also: **`filler`/`fillers` → `placeholder`/`placeholders`** throughout the app code (comments + the two identifiers `FILLER_WORTHY_ACTIONS`→`PLACEHOLDER_WORTHY_ACTIONS`, `shouldPlayFiller`→`shouldPlayPlaceholder`) and the User Manual — grid-spacer "filler" (keyboard/CSS) is a different sense and kept; and the Express Panel **"Backchannel" category → "Continuer"** (`express-items.js` key `back`→`cont`, label; manual bullet). **Verified in preview** (1280×800): boots clean, no console errors; tab order + Text Size tab + response-font select + privacy button/icon + selected-state color (#b9770e) + Speech-tab reset-above-separation all confirmed; Express Panel renders with the Continuer tint intact. *Live mic behavior (placeholder-reset on resume) and the on-disk no-save gate need the tablet.* |
| 0.5.52  | 1     | **"Keyboard separation" setting (Ken, June 30 2026).** New Settings → Speech & Input slider (`dockSepSlider`, storage `dockSepPos`, default 0) controls the gap BETWEEN the dock (keyboard / Express Panel) and the rest of the UI, independent of the inter-button `--grid-gap`. New `--dock-sep` CSS var (set in `applyButtonSizing()`, 0–4rem); it pushes only the MAIN content away from the dock — the dock keeps its edge footprint, so the keyguard holes don't move. Applied on every surface that reserves the dock: conversation main (`#conversationMain` padding side / `#dockArea` margin-top bottom), `#settingsDialog`, and `#worldviewScreen`, both side and bottom docks. **Deliberately excluded from the "Reset buttons and gaps to default" button** (Ken — it's an independent layout preference, not button sizing). Verified in preview: gap applies on bottom (dock margin-top) + side (content padding) + Settings + About Me; persists + restores on init; reset leaves it untouched; no console errors. |
| 0.5.51  | 1     | **Bug-list batch (Ken, June 29 2026).** Eleven fixes/features from a field-report list. (1) Backspace no longer leaves an inline word-prediction ghost that reads as a "selected" suffix — backspace now `clearGhost()`s and doesn't re-predict until typing forward (`keyboard.js`). (2) Composer **Speak / Reframe** now fold a showing prediction ghost into the text first (`keyboard.acceptPendingGhost()` called in `handleSpeakComposed`/`handleReframe`) — "What is your n" + ghost "name" → speaks "…name". (3) **Settings gear** redrawn as a crisp filled Material cog (the stroked Feather gear read as a blob). (4) **"Reset buttons and gaps to default"** button (Settings → Speech & Input) → `storage.resetButtonSizing()`. (5) **− / + stepper buttons** flank the three size sliders for precise control (the slider "grew uncontrollably" when dragged right); slider remains for coarse moves. (6) Removed the prediction strip from bottom layout **B10** (display dropped in v0.5.39 anyway). (7) **Keyboard layouts renamed** to "Side Keyboard 1…10" / "Bottom Keyboard 1…11" (the old descriptive names no longer matched the grids). (8) "New 4" regenerate **tooltip → "New 8"** when Suggestions-per-category = 2 (`ui.setRegenerateLabel`). (9) **Done from About Me returns to the Settings panel** (`worldview-ui` `onClose` hook → `openSettings`). (10) **Settings tab padding** restored (`.tab-panel.active { padding: max(--grid-gap, 0.6rem) }`; Express + About excluded) — it was 0 at the default gap. (11) **4 default openers** to match the 4 closings + 4 reserved Response-Palette cells (editor already redefines all). (12) **Selected response now appears in the transcript immediately** — `commitExchange` renders the user turn at once and cleans the partner transcript in the background (was awaiting the cleanup round-trip first, lagging the user's words several seconds). (13) **Per-surface text-size settings** for the Transcript, the "In my own words" composer, and the Express Panel buttons (CSS `*-font-scale` vars; Settings selects). *Not done (answered, not built):* generating 32 responses to cache the unshown ones — LLM output is serial, so 32 ≈ 8× the response-generation latency, which would delay the first 4; keep generating 4/8 and regenerate on demand. Verified in preview: boots clean, no console errors; steppers/reset/font-scales/regenerate-label/layout-names/B10/gear all confirmed. *Live on-screen-keyboard ghost behavior needs the tablet (focus events don't fire in the headless preview).* |
| 0.5.50  | 1     | **Conversation layout solver — bottom dock (Ken, June 30 2026).** Adds the bottom-dock branch to the v0.5.49 solver (same model, axes swapped): the dock's expandable axis is VERTICAL, so **button-size grow makes the dock taller** (`--conv-dock-h`), the **transcript yields vertically** to its 3rem floor, and **command (10vh) + response (30vh) stay fixed** (they have no free direction to grow). Bottom CSS: transcript `flex:1` with a min-height floor; command/response fixed `flex:0 0 10vh/30vh`. Shrink (button-size left) grows the gap, same as side. Verified at 1280×800 bottom: default 30/10/30/30 unchanged; button-size max → dock 30%→55% (436px), transcript →44px (3rem floor), command/response unchanged; keyboard congruent (reads the same `--dock-h`); no console errors. Same first-cut approximations as side (calibration, gap-grows-the-dock, aspect-ratio, min-gap dock inset) remain to tune. |
| 0.5.49  | 1     | **Conversation layout solver — side dock, FIRST CUT (Ken, June 30 2026).** Wires the three sliders into the side-dock layout per Ken's change-rules (priority min-gap › button-size › gap-size). New **"Minimum gap" slider** (Settings → Speech & Input; storage `minGapPos`, default 0). `applyButtonSizing()` is now a JS solver (runs on init / settings change / window resize) that computes region sizes + the effective gap into CSS vars (`--conv-dock-w`, `--conv-transcript-v`, `--grid-gap`, `--gap-min`); the side-dock CSS consumes them (dock width = `--conv-dock-w`; transcript height = `--conv-transcript-v`; command:response keep 1:6 of the rest). **Button-size slider default = MIDDLE** (= the % budget); **right grows** (dock widens → main narrows; transcript shrinks to a 3rem floor → command/response grow vertically), **left shrinks** (buttons fill less, gap grows). **Gap** floored at min-gap; **min-gap** raises the gap and the gap slider follows (one-way/sticky — lowering min-gap leaves the gap). Verified at 1280×800 side: default 30/30/10/60 unchanged; button-size max → dock 30%→82%W, transcript →44px (3rem floor), command/response grow V; min-gap 40 → gap+slider follow to 8px, then lower min-gap → gap stays (sticky); no console errors. **FIRST CUT / pending (Ken to react + next steps):** the BOTTOM-dock solver (still fixed % default; button-size inert there); calibrating the slider's right end to the exact last-yielder-at-floor point; the precise shrink curve, freed-space split (currently command:response 1:6), per-button aspect-ratio restoration, and insetting the fixed dock by min-gap — all reasonable approximations now, to tune against what Ken sees. |
| 0.5.48  | 1     | **Bottom-dock default percentage layout + outer-padding spec (Ken, June 30 2026).** Mirrors v0.5.46 for the bottom dock per Ken's spec: dock = **30% HEIGHT, full width** at the bottom (`body.conv-bottom { --dock-h: 30vh }`, replacing the button-size-driven height); the remaining 70% height splits **transcript 30 / command bar 10 / response palette 30** of the viewport (3:1:3). Command bar 8 buttons ⅛ width; response palette **1×4** cards (each 90%/4 width, full height) + "New 4" 10% (9:1). The side block was generalized to cover both docks (shared per-component fill rules; dock-specific budget + dock size). Also wired the **outer padding around the cluster to `--gap-min`** (the main-area side; insetting the fixed dock still deferred with the min-gap slider). gap/min-gap still default 0 (flush); button-size/gap CHANGE behavior still deferred (sliders inert for the conversation layout). Verified at 1280×800 bottom: transcript/command/response/dock = 30/10/30/30 (y 0/240/320/560, full width), 8 command buttons ⅛ width, response 1×4 (cards 90% + New-4 10%), keyboard congruent with the dock (1280×240 @ y560), no console errors. |
| 0.5.47  | 1     | **Dock surround padding → ½·gap (Ken, June 29 2026).** The dock's inner panel (`#expressPanel` + the keyboard `#appKeyboard`) had a hardcoded `padding: 0.5rem` around the button grid (leftover from before the gap model); per spec the dock's internal surround is **½·gap**, so at the gap=0 default the buttons sit flush to the dock edges. Keyboard kept congruent (bottom keeps its safe-area inset). |
| 0.5.46  | 1     | **Side-dock default percentage layout (Ken, June 30 2026) — first step of the layout-budget approach.** Implements Ken's explicit side-dock spec as the static default (at gap = 0, min-gap = 0; the button-size/gap/min-gap CHANGE rules are still deferred, so those sliders don't drive the side dock yet). **Regions:** Dock = **30% width, full height** (`body.conv-side { --dock-w: 30vw }`, replacing the v0.5.43 button-size-driven width); the remaining 70% width splits vertically **transcript 30% / command bar 10% / response palette 60%** via `flex: 3/1/6` (auto-absorbs gaps). **Command bar** is now its OWN region (moved out of `#transcriptSection` in `index.html` so it has an independent vertical budget) — 8 buttons each ⅛ width × full region height. **Response palette:** cards take 90% width (the 2×2 grid, each card 50% height) + the "New 4" button 10% width × full height (`flex: 9/1`). Components surrounded by ½·gap, separated by 1·gap. **Default gap → 0** (flush; `GAP_MIN_REM` 0, `DEFAULT_BTN_GAP_POS` 0) — borders still separate buttons. **Bottom dock untouched** (the command-bar HTML move is mode-agnostic; verified bottom still renders: full-width command bar region + bottom dock strip). Verified at 1280×800 side-right: dockArea 384×800 (30%), transcript 896×240, command 896×80, response 896×480 (30/10/60), 8 command buttons ⅛ width, palette 89%/New-4 11%, keyboard congruent at 30% width, no console errors. *Known/expected: the button-size & gap sliders are inert for the side dock until the change-rules land; at gap = 0 the Settings/About-Me surface paddings (which share `--grid-gap`) are also 0.* |
| 0.5.45  | 1     | **Fix: Express Panel buttons overflowed the dock and fell off (Ken, June 29 2026).** Regression from v0.5.44: the global `button { min-height: var(--btn-min-dim) }` floor excluded the keyboard keys (`.kbd-key`) but NOT the Express Panel buttons (`.ep-btn`). So Express buttons were pinned to the button-size minimum while the keys shrank to fit — and when `rows × button-size` exceeded the constrained dock extent (vertical for a side dock; worst at large button sizes), the Express grid overflowed and the last row ("In my own words") fell off the bottom, offsetting every button above it. The keyboard fit because its keys shrink. **Fix:** treat `.ep-btn` exactly like `.kbd-key` — excluded from the global floor (`:not(.ep-btn)`) and given `min-height: 0` so the row stretch drives the height and the grid always fits the dock (text clips via the existing `overflow:hidden`); removed the stale `body.conv-bottom .ep-btn` clamp. Principle (Ken): side docks are vertically constrained / bottom docks horizontally constrained; the Express Panel maps 1:1 onto the keyboard with no location properties of its own, and the keyboard's keys can never fall off — so the Express buttons can't either; button size reaches the dock grid through the **dock size** (which grows with `--btn-min-dim`), never a per-button min. Verified in preview at MAX button size: side dock 470×850 — Express ↔ keyboard pixel-congruent (9 rows), "In my own words" fully visible (bottom 843 ≤ 850 vh, was overflowing); bottom dock congruent (4 rows); no console errors. |
| 0.5.44  | 1     | **Button size + gap apply to EVERY surface (Ken, June 29 2026).** Ken's definition: a "button" is anything tappable with an action (so the response cards, the "New 4" button, composer buttons, and the Settings / About Me controls are all buttons), and the gap is also the *padding* around non-button elements (a keyguard rail surrounds the transcript too), so **gap applies to any UI element**. Implemented: (1) a global rule — `button:not(.kbd-key):not(.kbd-tool):not(.kbd-pred-btn) { min-height: var(--btn-min-dim) }` plus `.icon-btn { min-width: var(--btn-min-dim) }` — so every button on every surface is at least the button size (icon buttons square-ish); the **"New 4"** regenerate button width → `var(--btn-min-dim)`. (2) `--grid-gap` extended beyond the dock to the conversation main area (region gaps + outer padding), the response area / palette / stacked-card gaps, the Command Bar, and the overlay **surface paddings** (`#settingsContent`, `.dialog-actions`, `#worldviewContent`). **One technical exception (Ken acknowledged the risk):** the on-screen-keyboard **keys** are sized BY the dock band (itself derived from `--btn-min-dim`), so a hard min there would overflow the fixed dock — they scale through the dock, not a min. **Boundary I drew (flag to Ken):** fine typographic spacing inside Settings/About Me (e.g. `.setting-group` margins, chip-row gaps) was left as-is — squeezing those to the gap value hurts readability with no keyguard benefit (those overlays are the keyguard's "open window", not under it). At the default gap (0.4rem) the overlay paddings read tight; the gap slider tunes it. Verified in preview: command bar / Settings Close / Paste / regenerate all = button size (58px at ~4rem); surface paddings = the gap; boots clean, no console errors. |
| 0.5.43  | 1     | **Button sizing reworked — button size is authoritative, gap expands the dock (Ken, June 29 2026).** Fixes how the v0.5.41 sliders behaved. The dock was `max(floor, count×size+gaps)`, and the `min(26rem,46vw)` floor masked the setting on side layouts (few columns → never exceeded the floor), so raising button size did nothing visible and raising the gap stole width from the buttons. **Revised model (Ken's):** the dock's scarce axis is now sized DIRECTLY as `count × --btn-min-dim + gaps + pad` (floor removed, still capped at 70vw/72vh). So **button size is authoritative** (sets the actual key width in a side dock / key height in a bottom dock) and raising it grows the dock along its expandable axis — side dock **widens** (Settings/main area shrinks + reflows), bottom dock grows **taller** (transcript shrinks). Raising the **gap** now also grows the dock the same direction and **never shrinks the buttons** (verified: side cell width 95px→95px as gap went max). Setting **renamed "Minimum button size" → "Button size"**; default recalibrated (range 2.75–6.5rem, default ≈4rem so a 5-wide side dock ≈ the old width). **Command Bar gap** now tracks the spacing setting too (`#listenControls` → `var(--grid-gap)`; verified 5.8px→20.4px). *Known caveat: a side dock is full-height (Spatial Stability), so its WIDTH responds fully but vertical gap still comes out of key height (no room to grow vertically); the dominant axis per dock behaves correctly.* Verified in preview: side dock 299→512px (button size max), →571px + Settings 768→709px (gap max, button width preserved); bottom dock 241→412px (button size); no console errors. |
| 0.5.42  | 1     | **Inline word prediction — ghost completion, accept on any separator (Ken, June 29 2026).** Word prediction returns to the UI as an **inline ghost** instead of a button row (which had no keyguard-safe home): as the user types on the on-screen keyboard, the single best completion appears **inline, right after their text**, drawn **bold** in a tunable color (`--pred-ghost-color`, default a high-contrast blue) on a subtle tint (`--pred-ghost-bg`) — bold + tint mark it provisional WITHOUT relying on low contrast (legible for low vision; bold also relaxes WCAG to 3:1). **Accept by typing any word-separator** (space / comma / period / `? ! ; :` / Enter / etc. — anything outside the predictor's `[A-Za-z']` word set): the separator completes the ghost word, then is itself inserted (space→space, comma→`,`), so there's **no moving tap target** (keyguard-safe, no contenteditable). Implemented in `keyboard.js` as a **non-interactive mirror overlay** (`#predGhost`): it copies the field's font/padding/border and renders the typed text transparent (reserving exact width) + the styled suffix, positioned over the field so the real text shows through as one line; shown only when the **caret is at the end** of the value (true appending) so nothing follows the ghost. Scope: all on-screen-keyboard fields except the API key; capitalization matches the typed prefix; the old hidden prediction-button infrastructure (`prediction.js`, learning on space/enter) is reused. Verified in preview: "Th"→bold "e" (case-matched "The"); space accepts → "The "; "ca"+comma → "The can,"; ghost box aligns to the field exactly; mirrored text transparent; clears on blur/field-end; no console errors. *Single-line inputs that overflow horizontally can clip the ghost past the right edge (accepted limitation); the composer textarea wraps so it's fine. Physical-keyboard typists don't get the ghost (the on-screen keyboard drives accept-on-separator) — a possible later extension.* |
| 0.5.41  | 1     | **"Minimum button size" + "Button spacing" settings (Ken, June 29 2026).** Two **unitless sliders** (Settings → Speech & Input) let the user scale touch targets for motor control / finger size and set the inter-button gap (= the bar width on a 3D-printed keyguard). Both drive the existing `--btn-min-dim` token + a new `--grid-gap` token; slider positions (0–100) map to rem in `app.js applyButtonSizing()` so the user picks by visual "feel" (the web can't read physical screen size, so abstract is the honest unit). Default positions reproduce the historical 2.75rem / 0.4rem exactly. **Dock grows from the grid (pure CSS):** `--dock-w`/`--dock-h` are now `min(cap, max(floor, calc(rows/cols × --btn-min-dim + gaps + pad)))` — bottom dock grows **taller**, side dock grows **wider**, exactly enough that every key / Express cell clears the minimum; JS sets `--kbd-rows`/`--kbd-cols` for the active layout. Since every consumer (keyboard, Express Panel, Settings & About Me dock reservations) reads those tokens, all stay aligned under one keyguard, and the Express Panel (shared grid) is covered by the same calc. **The transcript yields** the squeeze (a 3rem floor; shrinks in height for a bottom dock, width for a side dock) so buttons win; caps (72vh / 70vw) keep the main area usable at extreme sizes. The rendered dock gaps (`#epGrid`/`.ep-row`/`#appKeyboard`/`.kbd-row`) now reference `var(--grid-gap)` so the gap is real, not just in the formula. Setup-tier (changing either moves the keyguard holes). Verified in preview (1280×800, rem=14.6px): defaults unchanged; bottom dock floor 384→438 (mid)→576 (72vh cap); side dock floor→653 (large)→capped; command buttons grow to 4.5rem and the transcript shrinks to absorb it; gap token reaches rendered cells (1.4rem→20.4px); both sliders populate from storage, apply live, and persist; no console errors. |
| 0.5.40  | 1     | **Title bar removed; Command Bar consolidates the control surface (Ken, June 29 2026).** Pre-work for the button-size / word-prediction questions. (1) **Title bar deleted** — the `<header>` (app title + About Me + Settings buttons) is gone; the conversation main area fills from the top. (2) **About Me** moved off the title bar into the **Settings panel** as its own tab (first tab); clicking it closes Settings and launches the existing full-screen questionnaire overlay (no panel of its own — special-cased in `initSettingsTabs`; `ui.onAboutMeClick`/`aboutMeBtn` removed). (3) **Command Bar** = the row where Start Listening sat (`#listenControls`). It now holds, as equal-width **icon-only** buttons (Rule 4): Start Listening, Start conversation, End conversation (red), the two repeat controls, Hold on, Wind down, and **Settings** (new gear icon). (4) The two repeat controls were **renamed speaker-specifically** (Ken — disliked "Pardon", "Say again" is speaker-non-specific): self-repeat → **"Repeat what I said"** (replay icon, re-speaks the user's last words), partner-repeat → **"Ask them to repeat"** (the old Pardon?). (5) The four override buttons were **removed from the Express Panel** (`#epControls` gone — the panel is now just `#epGrid`). (6) **Cut/Copy/Paste toolbar removed from the keyboard** — its key rows now start at the dock top, matching the Express Panel which also lost its top row, so one keyguard still overlays both; word-prediction infrastructure (`predWrap`, hidden) is preserved. (7) A **Paste button** was added beside the **API Key** field (relabeled from "Claude API Key") in Settings to replace the keyboard's removed clipboard paste for the long `sk-ant-…` key. Verified in preview (1280×800): boots clean, no console errors; 8 icon buttons in the Command Bar with the correct labels; About Me tab launches the overlay and closes Settings; Express Panel has no override row; keyboard has no tools; v0.5.40. |
| 0.5.35  | 1     | **Dock position/layout settings reframed around the Express Panel (Ken, June 28 2026).** The "on-screen keyboard position" and "keyboard layout" settings also drive the always-visible Express Panel's location and grid (one keyguard overlays both per Spatial Stability), so physical-keyboard users still need them — but the keyboard-centric labels read as a non-sequitur (Ken). Chosen fix (AskUserQuestion): **relabel + rehint in place**, not decouple (decoupling would break the one-keyguard principle). `index.html` Speech & Input tab: "On-screen keyboard position" → **"Express Panel & keyboard position"**, both layout selects → **"Express Panel & keyboard layout"**, hints rewritten to say the grid is shared so one keyguard fits both and the setting applies **even if you type on a physical keyboard**; the which-side hint names the Express Panel too. "Keyboard for typing" (physical vs on-screen) left as the only genuinely keyboard-only control. No behavior change (`expressLayoutRows()` already reads `loadKeyboardDock()`/`loadSideLayout()`/`loadBottomLayout()`). User Manual §6.2 entries + version updated to match. |
| 0.5.34  | 1     | **On-screen keyboard now serves the Settings → Controls editor (Ken, June 28 2026).** Tapping a text box on the Controls tab (the Hold on / Pardon? phrases and the opener/closer cards) had no effect because the editor's inputs weren't in the keyboard's `IN_SCOPE` — so with "Keyboard for typing" = On-screen keyboard, nothing appeared. Added `#controlEditor input` to `IN_SCOPE` (`keyboard.js`); the existing `focusin` path then suppresses the OS keyboard (`inputmode="none"`, applied just-in-time) and shows the app keyboard, reparented into the Settings dialog's top layer like the API-key field. Behavior keys off the user's keyboard-mode setting only — physical-keyboard attachment is never detected, so it works regardless of whether one is attached. Verified in preview: a real `focusin` on a Controls input opens the keyboard (docked, hosted in the dialog) and sets `inputmode=none`; no console errors. |
| 0.5.33  | 1     | **Editable control phrases — User-Manual review item #3 (Ken, June 28 2026).** The persistent override controls' spoken text and the opener/closer cards are now user-editable (Settings → **Controls** tab), so each can be reworded to sound like the user. New portable model `control-phrases.js` (`control-phrases.json` in the data folder + `aac_control_phrases` cache, the v0.2.25 "file in folder wins" reconciliation) holds `holdOn` / `pardon` (single phrases) and `openers` / `closers` (ordered card lists). New `control-phrases-editor.js` editor (reuses the Express-editor `.ee-*` styles): single-field rows for Hold on / Pardon?, add/reorder/delete lists for Openers and Closings, a read-only note that **Say again** has no editable phrase (it re-speaks the user's own last words), and a danger-confirmed Reset. **Openers are templates with a `{name}` token** — `engine.applyName()` substitutes the active Partner's name and, when no Partner is active, drops the token *and* an adjacent comma with punctuation repair so it still reads cleanly ("Hi {name}, got a minute?" → "Hi, got a minute?"). `engine.js` OPENERS/CLOSERS became injectable via `setConversationPhrases()` (inline defaults mirror the model so the engine still works standalone); `handleHoldOn`/`handlePardon` read their text from the model at tap time; app loads/syncs the model at init, in `handleStart`, and on Choose-Folder, and re-injects the engine on every edit via the editor's `onChange`. Blank rows are kept in storage (so the editor can hold a transient empty row to type into) but filtered out before reaching the palette. Verified in preview: boots clean; Controls tab renders all four sections + note + Reset; editing Hold on persists; editing an opener flows through to the engine palette; Add grows the list, focuses the new row, and the typed card reaches the engine; Reset restores defaults; named vs. no-name opener forms both read cleanly. *Live TTS of an edited phrase + the on-disk `control-phrases.json` write need the tablet (FSA + speaker).* |
| 0.5.32  | 1     | **User-Manual review fixes, four of five (Ken, June 28 2026).** (1) **"Maximum placeholders per turn" = 0 now means NONE** for users who find fillers artificial/robotic — `0` was taken by "No limit"; that option moved to value `-1`, a literal `0 (none)` was added, and `placeholders.start()` schedules nothing when the setting is 0. (2) **Initial-placeholder-delay clock starts at the partner's PAUSE, not at options-arrival** (reverses the v0.3.9 basis — a slow AI round-trip was leaving dead air; Ken: "responses can be very long in coming"). New `placeholders.arm()` is called at the silence checkpoint (`app.js handleSilencePeriod`) to start the initial-delay clock; `start()` (still gated on the classification — questions only) then waits only the *remainder* of `initialDelay`, firing immediately if generation already consumed it. Holding the utterance until classification keeps fillers off statements/closings. (3) **Pardon? now discards only the partner's LAST statement**, not the whole accumulated turn — `stt.js` tracks finalized segments and `dropLastStatement()` pops just the last one (keeping earlier good sentences for the re-speak to append to); `handlePardon` sets `currentPartnerText` to the remaining text. (4) **Restored a UI surface for the gaps log** (removed in v0.2.31, so progressive-profiling gaps were being *recorded* but never shown — answering Ken's "I haven't seen new questions appear"). About Me home now shows a **"Questions worth answering"** section listing only fields the AI actually needed in real conversations (`worldview.listGaps()`, most-asked first), each deep-linking to its field card (`renderModule(moduleId, focusKey)` scrolls+focuses it); renders nothing when there are no open gaps, so it never duplicates the topic pages. *Note: gaps only accrue from live Claude-API turns that return `missing_facts`, so testing #4 needs Ken's key on the tablet.* Item #3 from the review (user-editable spoken text for the persistent controls + Wind-down/opener/closer cards) is deferred to its own editor pass. Verified in preview: boots clean; dropdown shows `0 (none)`/`No limit`; a seeded gap renders the section and deep-links to the Passions card (focused); `dropLastStatement`/`arm`/`start` load and run without error. |
| 0.3.6   | 1     | Three keyboard/Settings bug fixes (Ken): (1) API key now persists on Settings Close/Escape even if a paste didn't fire an `input` event (belt-and-suspenders save from the field value); (2)+(3) the on-screen keyboard no longer hides when focus moves to an in-panel button (Save in About Me, controls in Settings) — fixing both "keyboard disappears on Save" and the "must press Save twice" bug (hiding-on-focusout reflowed the layout and stole the first click). Panels' own close paths (`worldview close()`, Settings Close/Escape, `renderHome`) dismiss the keyboard explicitly via new `keyboard.hideKeyboard()`. |
| 0.3.7   | 1     | Fix v0.3.6 keyboard-retention regression (Ken: Save still hid the keyboard). The v0.3.6 focusout check kept the keyboard only when `e.relatedTarget` was a button inside the panel — but tapping a `<button>`, especially via touch, often leaves `relatedTarget` null, so the check missed and the keyboard hid. Now `focusout` keeps the keyboard whenever a panel it serves is still open (`servingPanelOpen()` — `#settingsDialog[open]` or visible `#worldviewScreen`), independent of `relatedTarget`. Composer (no panel open) still hides on blur as before; explicit close paths + Hide button still dismiss it. |
| 0.3.8   | 1     | On-screen keyboard starts each field in one-shot Shift (Ken) so the first letter is capitalized — proper nouns in About Me (Carl, Chicago, Mom) and sentence starts in the composer; reverts to lowercase after that one character. Set in `keyboard.js show()`. Exception: the Settings API-key field (`#apiKeyInput`) starts Shift-off — its value is case-sensitive and lowercase (`sk-ant-…`). |
| 0.3.9   | 1     | Conversation Engine observations Pass 1 (Ken): placeholders fire on response-arrival and only for questions (not statements/closings), covering the *choosing* window not the AI-latency gap (a quick pick plays none); neutral non-imperative filler set ("Good question." / "Hmm, interesting." …) — imperatives read as curt through the flat voices; response options no longer lead with empty interjections ("Ah/Well/So"); TTS-echo loop fixed at the trigger (no generation checkpoint fires while the app is speaking + echo tail), killing the runaway stack-growth/option-flicker; new "End conversation" hard-terminate control → STANDBY |
| 0.3.10  | 1     | Placeholders are role-differentiated by position to stop sequential fillers sounding repetitive: first filler acknowledges ("Good question."), later ones say you're still thinking ("Still thinking it through."); `placeholders.json` is now `{acknowledgment, thinking}`; new Settings "Maximum placeholders per turn" (default 2) caps how many play so you never hear two same-category fillers back to back |
| 0.3.11  | 1     | About Me improvements: address_terms removed from About You (per-person nicknames replace it); People editor adds nickname + Lives with me fields; Private badge made visually distinct from Prefer not to say; Contact & Logistics shows 🔒 in Topics list and per-field privacy notes; Close friend added to relationship dropdown; keyboard input scrolls to cursor when text fills the narrowed field |
| 0.3.12  | 1     | Fix privacy model: Private now sends the value to the AI with a "don't volunteer" instruction rather than withholding it entirely (which made Private equivalent to Prefer not to say and useless). Prefer not to say remains the control for information the user does not want the AI to know at all. UI labels and CLAUDE.md updated to reflect the correct three-level model. |
| 0.3.13  | 1     | Inline follow-up questions: a field with `followups[]` shows its follow-ups as indented child cards below the parent once answered, each with its own input/Save/Speak; answers inject into the LLM profile block under the parent. (Superseded by 0.3.14.) |
| 0.3.14  | 1     | Reverted 0.3.13: the only `followups` host was `passions` (a `multi` field), so one shared follow-up answer for "music, hiking" was meaningless — depth is per-item, and the follow-up question is often a function of the answer (Music→instruments, Sports→teams), which a static registry can't scale to (no coverage for typed passions). Passion-tailored follow-ups deferred to the LLM-driven Conversational Worldview Collection (Architecture Overview §6); `passions` stays a clean list and the follow-up infrastructure was removed rather than left as dead code. |
| 0.3.15  | 1     | UI + CE fixes (Ken, June 19 2026): removed the redundant "About You" hint block from Settings → General; "In your own words" Speak no longer needs two presses (keyboard kept up when a composer control is tapped, so the reflow doesn't steal the first click); People editor "Edit" no longer jumps the page to the top when editing someone below the fold (scrolls their form into view instead); "Hold on" speaks "Let me think about that." (dropped the unintelligible leading "Hmm,"). CE Pass 6: repeated Pardon? no longer stacks multiple `REPAIR*` (dedup — one open repair at a time), the partner's re-speak pops it AND clarifies the existing question in place rather than pushing a duplicate, so the stack again holds a single question. |
| 0.5.12  | 1     | Move cards: **dropped the hint**, the response text is now the large, easy-to-read primary element. **Suggestions per category setting (1 or 2, max 2; Ken)** — choosing 2 yields **8 cards total** (2 stacked in each of the four category cells, same footprint as 4). Renderer groups the palette into 4 fixed category cells (each 1–2 stacked options); `llm.generateResponses` `perCategory` opt requests 2 distinct options/slot when set. |
| 0.5.26  | 1     | Express Panel editor: Partner entry uses a "Choose a person" dropdown (shows name + nickname in parentheses) replacing the separate name and nickname text inputs; panel button label unchanged (nickname if set, name if not). |
| 0.5.27  | 1     | **Keyboard toolbar ↔ Express Panel control-row alignment** (Ken). The keyboard's Cut/Copy/Paste/Hide toolbar (`.kbd-toolbar`, was `min-height: 2.4rem`) was shorter than the Express Panel's four override buttons (`#epControls`/`.persist-btn`, ~44px from `0.7rem` vertical padding), so the keyboard's keys sat higher than the phrase grid and the two dock layouts didn't overlay hole-for-hole. Both control rows are now pinned to ONE shared height and the grids beneath start at the same y in **both** docks (verified bottom: rows at y=420; side: y=51). Introduces the **`--btn-min-dim`** token (root, default 2.75rem) = "minimum button dimension," the single seam a future Settings control will raise to enlarge targets fleet-wide; here its first job is alignment. Both `#epControls` and `.kbd-toolbar` use `flex: 0 0 var(--btn-min-dim)`; `.kbd-tool`/`.kbd-pred-btn`/`.persist-btn` floor their `min-height` to it; `.persist-btn` vertical padding removed (height now comes from row stretch, not padding, so it can't grow past the keyboard's toolbar). Content buttons (keys/phrase cards) deliberately NOT floored to the token yet — the bottom dock is a fixed `48vh` band of ~12 stretched rows, so a hard floor there would overflow; growing content buttons is the future Settings job that must reconcile with the fixed dock. |
| 0.5.28  | 1     | **Symbols page now matches the active letters layout's geometry** (Ken — Spatial Stability). The 123 (symbols/numbers) page was ONE fixed grid per dock (5-wide side, 10-wide bottom), so when the selected letters layout was a different shape (e.g. the 3-wide `S6`), tapping 123 changed the whole grid and a single static keyguard's holes no longer lined up. Replaced the static `SYMBOLS` object with `buildSymbolsPage(letterRows)` in `keyboard-layouts.js`: it GENERATES the symbols page from whichever letters layout is active — identical rows, identical spans, action/space/blank/pred cells left exactly in place, only each LETTER cell swapped for a symbol (from an ordered `SYMBOL_POOL`: digits → common specials → rarer, surplus letter cells go blank if the pool runs out) and the 123 key relabeled to ABC. `keyboard.js` `currentRows()` calls it for the active layout. Geometry is therefore guaranteed congruent between the two pages for every layout — verified all 21 (S1–S10, B1–B11) congruent letters↔symbols. |
| 0.5.29  | 1     | Keyboard-layout Settings menus drop the internal `S1`/`B1` ids from the option labels (they meant nothing to the user) — show only the human-readable name; the id stays as the `<option>` value for storage (`fillLayoutSelect` in app.js). |
| 0.5.31  | 1     | **End conversation clears the active Partner & Feeling toggles** (Ken) — the next person/mood shouldn't inherit this conversation's selections. New `clearInfluencers()` (nulls both + re-renders the panel so the selected rings drop) called from `handleEndConversation`. Deliberately NOT in the shared `terminateConversation()` — Start conversation reuses that and still needs the active Partner to personalize its openers ("Hi Tim, …"). Verified: a feeling ring active → End conversation → 0 rings. |
| 0.5.30  | 1     | **Stamp every turn with the active Partner / Feeling in the conversation log** (Ken). The Partner and Feeling toggles (v0.5.14) were session-runtime only — they shaped generation (`buildSituationBlock`) but were never persisted. Now each logged turn carries a **situation stamp** for Phase-3 review: `partner` (`{ id: personId\|null, label }` — keeps the stable relationship-graph `personId` when the Partner is a known person, so the log can join back to the person, plus the display label) and, on the user turn, `feeling` (`{ id, text }`); each is `null` when its toggle is off. Stamp-every-turn (not a change-event) so the state at any turn is self-contained and survives the toggles changing mid-conversation. New `partnerStamp()`/`feelingStamp()` helpers in app.js feed `storage.logUserResponse` (partner + feeling) and `storage.logPartnerSpeech` (partner = who spoke; the seam for future multi-party). NOT pushed into the in-memory `conversationHistory` that feeds the LLM (the situation block already injects it each turn — would be redundant). Boots clean; the on-disk write needs a granted FSA folder + live conversation to exercise (tablet). |
| 0.5.25  | 1     | **Spatial Stability across all UIs — two fundamental layouts, one keyguard** (Ken). Every screen now adheres to the base conversation layout = **MAIN AREA + DOCK AREA**, with the **dock region constant across Conversation, About Me, and Settings** so a single physical keyguard overlays them all. Two layouts only: **SIDE** (keyboard/Express-Panel a fixed-width full-height column on the chosen edge) and **BOTTOM** (a fixed-height full-width strip). Decisions (Ken): the keyguard need only hold the **dock** constant (the main area is its open window); the dock is **empty/reserved** on About Me & Settings; **Settings becomes a fixed main-area panel** (drag removed). Changes: new `--dock-w`/`--dock-h` tokens (single source of truth); **bottom dock is a fixed `--dock-h` (48vh)** that the Express Panel AND the keyboard both fill (rows stretch) so they occupy the identical band; **side dock is a full-height fixed column** (so the app header no longer pushes the Express Panel below the keyboard — they now share y0→bottom; main area + header confined beside it); **About Me reserves the dock at all times** (content in the main-area rect, dock blank, keyboard fills it on focus); **Settings** fills the main-area rect (`inset:0` minus the dock), dock reserved, drag/nudge code deleted. Verified both docks: keyboard ↔ Express Panel ↔ Settings/About Me dock regions are pixel-identical (bottom: 0,416 1280×384; side: full-height `--dock-w` column); danger-confirm dialog still centers. Realizes UI Layout **Rule 11**'s goal. |
| 0.5.24  | 1     | **Express Panel editor: "Choose a name" dropdown, wider; editor uses full width** (Ken). The partner-row people dropdown's placeholder is now **"— Choose a name —"** (was "— Type a name —") and is **~2× wider** (`.ee-name-select` flex `2 1 16rem`). To give it room, the **Express Panel settings tab is exempted from the Settings two/three-column layout** (`column-count: auto`) — the editor is a vertical list of rows that needs full width, so rows no longer wrap (select 395px ≈ 2× the 198px Name box at Surface width). |
| 0.5.23  | 1     | **Express Panel editor: insert in place** (Ken — the v0.5.22 select-then-scroll-to-toolbar flow was convoluted). Each row now has a **＋** button that opens an inline **Phrase / Partner / Feeling** picker right above that row; choosing a type inserts the new item there and **focuses its input** (and scrolls it into view) so the user can type immediately — no scrolling up to the toolbar and back down. The toolbar's add buttons now simply append to the end (and seed an empty list), also focusing the new row. Replaces the v0.5.22 ↧ marker + `insertBeforeId` + status line with `pickerForId`/`pendingFocusId` and `buildInsertBar()`. |
| 0.5.22  | 1     | *(superseded by 0.5.23)* **Express Panel editor: insert a new item before a selected one** (Ken — replaces the v0.5.21 auto-household feature, now **reverted**). Each editor row has a **↧ marker**: tap it to choose a target row, then **+ Phrase / + Partner / + Feeling** inserts the new item *above* that row (tap again or "Add at the end instead" to clear → append, the default). A status line shows where the next item lands; the target row is highlighted; consecutive adds stack above the same target (tracked by id, so it survives the re-render). Solves "new items land at the bottom and moving them up is many taps" — and, per Ken, household members stay as real editable items (so you can remove people you live with but rarely talk to, and add people you don't live with but talk to often), so the automatic household injection isn't needed. Reverted from 0.5.21: the `expressAutoHousehold` setting + toggle, `expressItemsForPanel()`, and the `worldview-ui` onClose hook. |
| 0.5.21  | 1     | *(reverted in 0.5.22)* **Auto household Partner buttons in the Express Panel** (Ken). With a new Settings toggle (Express Panel tab, **on by default**) the panel auto-adds a Partner button for each person marked **"lives with me"** in About Me → People, injected right **after the feelings** so they form a row or two just below them. Generated from the relationship graph at render time (`expressItemsForPanel()` in app.js — id `live:<personId>`), so they track adds/edits/removes and aren't stored in the editable list (and aren't shown in the editor; manage via About Me). Anyone already added manually as a Partner (same personId) isn't duplicated. The panel refreshes when About Me closes (new `worldview-ui.init({onClose})` hook). The option exists because the panel's default layout predates any people; it no-ops until household people are added. `storage.loadExpressAutoHousehold` (default true). |
| 0.5.20  | 1     | **Express Panel editor: pick button COLOR, not category** (Ken). A phrase item's `cat` only sets its button color, and the category names ("Affirm / deny"…) mean nothing to the user — so the editor's category `<select>` is replaced by a row of **color swatches** (the colors available for that type); clicking one sets `cat` and live-recolors the panel button. Partner/Feeling have one fixed color per type, shown as a single non-editable swatch. `cat` is still the stored value (rendering unchanged); only the editor UX changed. New `colorControl()` in express-editor.js + `.ee-swatch*` styles. |
| 0.5.19  | 1     | **Rename "move" → "response" in code too** (Ken — reverses the earlier "keep `move` as the internal CA term" decision). Identifiers `buildMoveCard`→`buildResponseCard`, `showMoves`→`showResponses`, `handleMoveSelected`→`handleResponseSelected`, `selectMove`→`selectResponse`, `paletteFromMoves`→`paletteFromResponses`; the LLM JSON contract key `moves`→`responses` (prompt + `parseGeneration` + `engine.ingestClassification`/`refreshPalette` + app); CSS `.move-card`→`.response-card`, `.move-cell`→`.response-cell`, `.move-response`→`.response-text`, `.move-badge`/`.move-latency`→`.response-*`; and all "move"/"Move" comments + prompt prose → "response". **Deliberately preserved** (these are genuine *motion*, not the response concept): `cursor: move`, "drag to move", "the caret never moves", "geometry never moves", "move the dock area", "move between devices". The **design docs were also scrubbed** `move`→`response` (Configuration-Model, Conversation-Engine-Design/Overview/Test-Script, UI-Design, Product & Architecture Overviews) — same motion-preserving rules ("never move", "cursor moves through", "move it off", etc. kept). Net: "move" no longer denotes the response concept anywhere in code, product UI, or our docs; it remains only in the external CA literature. Verified end-to-end in preview (llm→engine→ui data path + live opener flow) and by re-scanning every doc (only motion uses remain). |
| 0.5.18  | 1     | **Complete the Express Panel rename** (Ken: "there are no existing user settings — I am the only user — so rename all references"). Renamed everything the v0.5.17 pass had kept for back-compat/churn-avoidance: persisted Settings key `fastPhraseTapMode`→`expressTapMode`; DOM ids `#fastPhrases`→`#expressPanel`, `#fpGrid`→`#epGrid`, `#fpControls`→`#epControls`, `#phraseEditor`→`#expressEditor`; CSS classes `.fast-phrases-panel`→`.express-panel`, `.fp-*`→`.ep-*`, `--fp-*`→`--ep-*`, editor `.pe-*`→`.ee-*`; Settings tab id `phrases`→`express`; and all "fast/quick phrase" comment + user-visible strings ("Speaking an Express Panel phrase", "Reset the Express Panel?"). Removed the now-pointless localStorage migration shim in express-panel.js. Only the CA term `move` is intentionally kept. Verified: boots clean, panel + editor + toggles work under the new selectors. |
| 0.5.17  | 1     | **Express Panel goes portable + module rename** (Ken, June 26 2026 — the two "yes to both" follow-ups). (1) **Data-folder portability:** new `express-panel.js` model persists the item list to `<data folder>/express-panel.json` (source of truth) + localStorage cache `aac_express_items`, with the v0.2.25 "file in folder wins" reconciliation (`load`/`getItems`/`setItems`/`resetItems`/`syncToFolder`); loaded at init + adopted/promoted in `handleStart` and the Choose-Folder handler, like worldview/relationships. One-time migration reads the old `aac_settings.fastItems` location so existing edits carry over. (2) **Rename for consistency:** `fast-phrases.js`→`express-items.js`, `fastphrase-editor.js`→`express-editor.js`; JS identifiers/comments `fast*`→`express*` (`renderExpressPanel`, `handleSpeakExpressItem`, `expressLayoutRows`, `loadExpressTapMode`, etc.). **Kept on purpose:** the CA term `move` (move card/`showMoves`/`SLOT`), and the internal DOM ids / CSS selectors (`#fastPhrases`, `#fpGrid`, `.fp-*`) — renaming those is churn/risk with no user benefit; the persisted Settings keys (`fastPhraseTapMode`, radio `name`) also kept so user settings survive. |
| 0.5.16  | 1     | **Naming decisions + Product Overview review pass** (Ken, June 26 2026). Settled product-facing names: the fast/quick-phrase panel → **Express Panel**; the four AI suggestion cards / their area → **Response Palette** / **response cards** (replacing "Move Palette"/"move"; "move" stays only as the internal CA term); the fixed-layout principle (nothing on screen moves → motor planning + one keyguard) → **Spatial Stability**. App: Settings tab + panel aria-label renamed to "Express Panel". Silence trigger range changed to **0.5–3.0s in 0.5s steps** (default 2.0) in v0.5.15. The `Conversant AAC Product Overview.docx` was revised per Ken's review — corrections: removed the never-built "low-confidence words underlined" claim and the "three clearly marked transcript states" / "confirm transcript" step (generation fires on the pause; the require-confirmation-first gate is *planned, not in Settings*); cards are color-coded by category, no badge; "four or eight cards" (1 or 2 per category); relative card positions (upper-left etc.); nicknames always used for people; "Private" means the SAME for facts and people (known to AI, never volunteered) vs "prefer not to say" (withheld); device can be Windows tablet / Chromebook / Mac; on-screen keyboard also serves fold-back touchscreen laptops; local models are NOT a Phase-2 target; added a Spatial Stability section, an Express Panel Phase-1 item, and a "Further Out — Future Directions" roadmap (speaker-aware capture, multi-party, conversation review, social-skills practice, neural/ElevenLabs voices, streaming generation, messaging/social connectivity, symbol support). Docs sync via OneDrive (not git). |
| 0.5.14  | 1     | **Editable fast phrases + Partner & Feeling influencer toggles** (Ken, June 26 2026). The fast-phrase panel is now a user-editable, ORDERED, TYPED item list (Settings → **Phrases** tab). Three item types: **phrase** (speaks on tap, category-colored), **partner** (a TOGGLE — who you're talking with; brown), **feeling** (a TOGGLE — current mood; cyan). Item order = panel position (editor ↑↓ re-maps; extra items beyond the layout's cells overflow/hide). Editor: add/edit/reorder/delete + "Reset to default" (danger-confirm); a partner can be **picked from People I Know** (carries personId + nickname) or typed free-form. One active partner + one active feeling at a time (tap again = off, tap another = switch; shown as a selected ring + ✓, Rule 6). **Effects:** active Partner personalizes the openers ("Hi Tim, got a minute?", via `engine.initiate({partnerName})`) and injects "you're talking with X, address them as X" into generation; active Feeling injects a mood-steer line. Both via the new `llm.setSituationBlock()` (appended in `buildProfileBlock`), set each turn alongside worldview/relationships. Data: `storage.loadFastItems/saveFastItems` (localStorage; stable ids on defaults so toggle-state tracks by id; future: move to the data folder for portability). New module `fastphrase-editor.js`. The old single hardcoded `PHRASES` list is replaced by `DEFAULT_ITEMS`. |
| 0.5.13  | 1     | **Start-conversation bug fix + screen-real-estate cleanup + nicknames + QWERTY** (Ken, June 26 2026). (1) **Bug:** opening with a pre-question ("Can I ask you something?") then the partner's "sure" produced options as if the *partner* had asked the opener. Fix in `engine.js`: selecting an OPENER now pushes a **USER-opened** sequence (floor → partner); when the partner's COMPLETE reply lands and the innermost open sequence is USER-opened (non-repair), it's an **SPP from the partner** — pop it, push no partner FPP, floor → user to LEAD. `buildRequestContext` exposes `user_holds_floor_to_lead`; `llm.js` prompt tells the model to generate *lead/continue* moves (treat "sure"/"go ahead" as a go-ahead, not a question). (2) **UI cleanup:** removed the "Conversation" label, the engine **mode chip**, and the **Engine diagnostics** `<details>` (kept the guarded JS — a new launch path TBD if needed later); **Start/End conversation** widened to flex-fill their row like the Say-again/Hold-on persist controls (room for more buttons later); move cards **dropped the category badge + latency dot** (color + position carry category; all cards spoken instantly). (3) **Nicknames:** `relationships.buildBlock()` now instructs the model to address people by their preferred term ("mom", not "Mary") when a nickname is set. (4) **QWERTY:** new bottom layout **B11** "QWERTY (touch-typist, 3-row)" — letters in QWERTY order, grid-aligned (q/a/z share column 1), space on the bottom row, specials on the shared symbols page; selectable in Settings. Field-tabbing (a listed item) was already shipped. |
| 0.5.7–0.5.11 | 1 | Conversation-screen polish: border on the conversation box (0.5.7); Settings panel bigger + two-column no-scroll + fixed broken tab nesting (0.5.8); Settings fixed-dimension per dock, never overlaps the keyboard (0.5.9, UI Rule 11); End/Start conversation clear the window + cards, empty move cards show slot colors at rest (0.5.10); auto-start recording after the opening statement (0.5.11). |
| 0.5.6   | 1     | Transcript box → **scrolling conversation log at FIXED height** (Ken): it scrolls internally and the layout never grows (static keyguard) — bound the app to the viewport (`body height:100vh; overflow:hidden`) with a min-height:0 flex chain. Both parties shown with **different fonts** (partner serif/left, user sans-bold/right); renamed to "Conversation"; **removed the light/input-like background** (not an input). Fast-phrase buttons **word-wrap** (3-line clamp) instead of ellipsis. Word-prediction dictionary expanded ~220→~560 words so 4–7 char prefixes keep matching. |
| 0.5.0   | 1     | **Conversation relayout — fast-phrase panel on the keyboard's real-estate + "In my own words" modal** (Ken, June 21 2026). Screen splits into a main area + a dock area (the keyboard's exact footprint, side or bottom per the dock choice). Dock shows the fast-phrase panel at rest, the keyboard during the modal → one static keyguard overlays both. Fast-phrase grid mirrors the selected keyboard layout cell-for-cell; space cell → "In my own words" (distinct dark); top row → the four persistent overrides; phrases fill remaining cells from the set's pool (leftovers blank). Response region = reserved fixed footprint: 4 cards 2×2 (side) / 1×4 (bottom) + a full-height "New 4" regenerate icon. "In my own words" opens a modal (input box Speak/Reframe/Cancel overlaying the response footprint, base UI not blurred; keyboard over the dock); any of the 3 dismisses both. Composer Region D removed. Fast phrases + composer Speak commit to history and take the floor (shared `speakAsUserTurn`). |
| 0.4.6   | 1     | Keyboard dock is now ONE user choice (Ken, June 21 2026), replacing context-based docking: Settings → on-screen keyboard position = Bottom or Side (then Left/Right); `dockFor()` returns that choice for every typing context (conversation composer, About Me, Settings), so a single keyboard shows wherever typing is appropriate. Settings reveals only the relevant controls per choice; preview follows the chosen dock. Default bottom. (Conversation+side spacing deferred to the panel-location step.) |
| 0.4.5   | 1     | Local word-prediction engine (`prediction.js` + `data/words.json`): prefix completion over a bundled frequency list + personalized local learning (the user's own word counts boosted ahead of the dictionary), fully offline, no AI per keystroke. The keyboard toolbar hosts the prediction buttons in the space reclaimed by the icon conversion; tapping one inserts it (matching the typed prefix's capitalization) + a space and learns it; word boundaries also learn. LLM stays for explicit phrase expansion only. |
| 0.4.4   | 1     | Icon pass (Rule 4): new `icons.js` inline-SVG vocabulary (currentColor, em-sized); conversation control buttons (Listen, Say again, Hold on, Pardon?, Wind down, Start/End, regenerate, Speak, Reframe, Clear) + keyboard toolbar (Cut/Copy/Paste/Hide) are now icon-only, each keeping its accessible name; "Show me different options" is a compact icon button. Content buttons (move cards, fast phrases) keep text. |
| 0.4.3   | 1     | Fast-phrases panel (Rule 9): category-colored base-UI quick-speak grid (`fast-phrases.js` CORE/MINIMAL/SOCIAL/REGULATE/BACKCHANNEL starter sets), content buttons (text, ellipsis-truncated) that speak directly; six functional categories each a distinct color (secondary cue). Single-tap or confirming double-tap with a configurable interval (Rule 10). Settings: set selector, tap-mode, interval. Placement provisional (final home = keyboard's side, keyguard-overlay). |
| 0.4.2   | 1     | Two-axis proportional layout (Ken, June 21 2026): the conversation UI is sized as a predictable fraction of the screen, with horizontal dimensions scaling off width (`vw`) and vertical off height (`vh`) independently, and font-size off `vmin`; all `clamp()`-bounded outside the device fleet so it scales linearly within it. Replaced the fixed `max-width:1100px` content cap (identical pixels on every device) with a width-proportional `min(96vw,1600px)`. |
| 0.4.1   | 1     | Viewport diagnostics (`viewport.js`): logs the full platform-knowable display metric set — layout viewport, full screen, available area, devicePixelRatio (the Windows scaling factor), recovered native resolution, visual viewport (reflects the OS keyboard overlay) — at startup and on every resize/orientation/keyboard change, so beta testers can report their real pixel box. Physical size/true PPI deliberately absent — no web API exposes them. |
| 0.4.0   | 1     | **Presentation Layer** — the full four-region triple-coded UI from `UI-Design.docx`, replacing the degenerate diagnostic renderer (Ken, June 21 2026). Region A transcript + mode chip + three-state display (informational, no gate) + now-playing line; Region B move palette as triple-coded cards (slot badge + color + position; glanceable hint; full text; format tag; filled/hollow latency dot; ≤200ms crossfade); Region C four persistent CA overrides + session Start/End; Region D composer (Speak/Reframe/Clear). Engine diagnostics demoted to a collapsed `<details>`. Renderer-only — engine untouched; descriptor→renderer boundary kept clean for future scanning/eye-gaze renderers. Minor bump: the defining UI layer the engine scaffolded toward. |
| 0.3.20  | 1     | "Reframe" — a second verb on the "In your own words" composer (Ken, June 21 2026): instead of speaking the box text, hand it to the AI as steering/context and regenerate the suggested responses around it (a guided regenerate reusing the v0.3.16 `refreshPalette` seam + a new `steer` opt in `llm.generateResponses`). One-shot — the box is cleared on success so it never reads as a persistent steer; the sticky/conversation-goal version is deferred to the Goals subsystem. The steer is framed as user-authored ground truth, so it's the channel for supplying the specifics the v0.3.18 anti-fabrication rule won't let the AI invent. |
| 0.3.19  | 1     | "In your own words" composer Speak now takes the floor like a selected response (Ken, Pass 14): it terminates the partner's open turn (`engine.selectMove` pops the partner FPP), stops recording, commits the exchange to history, and resumes listening iff auto-resume is armed (shared `resumeOrIdle` gate). Resolves the long-deferred "should composed utterances push to history?" question — yes. |
| 0.3.18  | 1     | Anti-fabrication guardrail in the generation prompt (Ken, June 20 2026): the AI was inventing specific autobiographical events — e.g. "I played a game with Tyler last night and beat him!" — because the prompt said "speak as the user / the most likely thing THIS user would say" with no constraint against making up episodes. Added a top-priority "speak only to what is real" rule: never invent specific events/outcomes/scores/dates/places/names not given; MAY use standing profile facts (habitual activities, interests, people) and general/non-committal replies; keep answers GENERAL when a specific detail is unknown ("Been playing online games with friends lately") rather than fabricating particulars. Also constrained the DISPREFERRED slot's "account/reason" so it can't invent a concrete excuse (no fake "dentist appointment at 3"). First concrete implementation of the conversational-honesty principle (Further Design Thoughts #2). |
| 0.3.17  | 1     | People editor scroll: editing a person below the fold no longer moves the page on Save/Cancel either — the list scroll position is captured when Edit is pressed and restored when the edit form closes, so the page returns exactly where it was (heights match: the form becomes a card again). Fresh entry from the home screen still lands at the top. |
| 0.3.16  | 1     | "Show me different options" regenerate control (Ken, June 19 2026): when the offered palette isn't quite right, re-runs generation for the SAME partner turn with the rejected options passed as `avoid` (llm.js), refreshing ONLY the palette via `engine.refreshPalette` (stack/mode/floor untouched — no duplicate FPP). Whole-palette regenerate; per-move regenerate + multiple-options-per-slot recorded as a to-do. Also merged the two misheard-partner controls into one: the old "Pardon?" (managed the stack but kept the garbled capture) and "Please repeat what you said." (discarded the capture but ignored the stack) each did half the job; the single **Pardon?** now does both — speaks "Sorry, I didn't catch that. Could you say it again?", discards the captured transcript, AND pushes the (deduped) repair sequence. The standalone "Please repeat" persistent control is gone; its slot now holds the regenerate control. |
